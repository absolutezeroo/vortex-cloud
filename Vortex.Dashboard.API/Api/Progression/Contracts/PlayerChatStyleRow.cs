namespace Vortex.Dashboard.API.Api.Progression.Contracts;

/// <summary>One chat style a player owns, by the styles table's key.</summary>
public sealed record PlayerChatStyleRow(int Id, int ChatStyleId);
