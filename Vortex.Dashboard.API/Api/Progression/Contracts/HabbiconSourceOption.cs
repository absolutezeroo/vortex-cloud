namespace Vortex.Dashboard.API.Api.Progression.Contracts;

/// <summary>One acquisition source, by its enum name and its stored number.</summary>
public sealed record HabbiconSourceOption(string Name, int Value);
