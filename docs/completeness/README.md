# Vortex Functional Completeness

This is **Phase 2**, after the Runtime V4 and Dashboard/API/Security architecture work.

Use three systems together:

```text
/document-vortex      -> what the current code actually does
/habbo-spec           -> what is known about the target client / protocol
/vortex-completeness  -> what is still missing and what to implement next
```

Start with `COMPLETENESS-V1.md`.

The generated matrix belongs in `generated/` and must never be hand-edited.

The first implementation slice is `FIRST-SLICE-FC1.md`.
