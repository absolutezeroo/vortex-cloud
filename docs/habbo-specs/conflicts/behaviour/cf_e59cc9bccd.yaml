# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: conflict
spec_version: 1
generated_digest: "sha256:06736f33d490dfe43c13947034d040670584e1804ac5a9476f5a5ce44d67eb40"
generated:
  id: cf_e59cc9bccd
  kind: behaviour
  subject: "SSOTicket: packets emitted in response"
  packet: SSOTicket
  positions:
  - origin: habbo-arcturus-daybreak
    authority: reference_emulator
    claim: BadgePointLimits then SanctionStatus then MOTDNotification then HabboBroadcast then NavigatorSavedSearches
    evidence: ev_1111fd915cdf
  - origin: vortex
    authority: vortex_emulator
    claim: UserBanned then AvatarEffects then NavigatorSettings then Favourites then FigureSetIds then VortexFurniEditorRights then ClubGiftNotification then CreditBalance then ActivityPoints then SilverBalance then EmeraldBalance then AvailabilityStatus then InfoFeedEnable then AchievementsScore then MysteryBoxKeys then BuildersClubSubscriptionStatus then PerkAllowances
    evidence: ev_a189cc3925be
  evidence:
  - id: ev_1111fd915cdf
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/handshake/SSOTicketMessageEvent.java
    symbol: SSOTicketMessageEvent
    line: 50
    note: third-party server implementation; evidence, not authority
  - id: ev_a189cc3925be
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Handshake/SSOTicketMessageHandler.cs
    symbol: SSOTicketMessageHandler
    line: 45
  official:
    status: unknown
verified: {}
manual: {}
