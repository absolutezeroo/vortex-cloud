# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: conflict
spec_version: 1
generated_digest: "sha256:66922bb4868187d33e4fe2af0ccb75b08130aa80a473c62c0b4531e83fe4a322"
generated:
  id: cf_0bcd51a55d
  kind: behaviour
  subject: "PurchaseFromCatalogAsGift: packets emitted in response"
  packet: PurchaseFromCatalogAsGift
  positions:
  - origin: habbo-arcturus-daybreak
    authority: reference_emulator
    claim: HotelWillCloseInMinutes then PurchaseError then PurchaseError then PurchaseError then PurchaseError then PurchaseError then PurchaseError then GiftReceiverNotFound then PurchaseError then PurchaseNotAllowed then PurchaseError then PurchaseNotAllowed then PurchaseNotAllowed then LimitedEditionSoldOut then PurchaseNotAllowed then LimitedEditionSoldOut then PurchaseError then PurchaseError then PurchaseError then PurchaseError then PurchaseError then PurchaseError then PurchaseError then HabboBroadcast then PurchaseError then PurchaseOK then PurchaseError then PurchaseError
    evidence: ev_24d5bc76ecea
  - origin: vortex
    authority: vortex_emulator
    claim: GiftReceiverNotFound then PurchaseOK then PurchaseNotAllowed then unresolved
    evidence: ev_75da769fdeac
  evidence:
  - id: ev_24d5bc76ecea
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/catalog/PurchaseFromCatalogAsGiftEvent.java
    symbol: PurchaseFromCatalogAsGiftEvent
    line: 41
    note: third-party server implementation; evidence, not authority
  - id: ev_75da769fdeac
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Catalog/PurchaseFromCatalogAsGiftMessageHandler.cs
    symbol: PurchaseFromCatalogAsGiftMessageHandler
    line: 15
  official:
    status: unknown
verified: {}
manual: {}
