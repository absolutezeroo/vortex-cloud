# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: unknown
spec_version: 1
generated_digest: "sha256:120d42c6a6ff3746fb8d7ec38176ca52fc451c5e5afb13a8c413838107f982ee"
generated:
  id: uk_e60d33052a
  subject: headers this emulator maps that the target client never sends
  severity: critical
  question: 14 incoming headers are registered here but appear in no composer of the target client build. Each is a handler that can never fire (for example CompostPlant=1989, GetRoomEntryData=1250, HarvestPet=1210, RentableSpaceConfigure=4601, RentableSpaceGetConfig=4600, VortexApplyFurniDefinition=8007, VortexApplyFurniEdit=8003, VortexFishingJoinDerby=8105).
  resolved_by: "check each id against the client's message registry; a header above the client's highest real id was invented and must be corrected or removed"
  known_evidence:
  - id: ev_1ed0aead370e
    kind: emulator_header
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Revisions/Revision20260701/Headers.cs
    symbol: MessageEvent / MessageComposer
    note: the header table this emulator answers on; ids are revision-specific
verified: {}
manual: {}
