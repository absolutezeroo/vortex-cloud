# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: unknown
spec_version: 1
generated_digest: "sha256:a05c8e54ecaf5c0197a847c2e1f3e2de2a35d2f99df361bb6b766cb3a2a75f23"
generated:
  id: uk_e60d33052a
  subject: headers this emulator maps that the target client never sends
  severity: critical
  question: 27 incoming headers are registered here but appear in no composer of the target client build. Each is a handler that can never fire (for example AppealCfh=1077, ChangeUserNameInRoom=3652, ClickCharacter=785, CompostPlant=1989, CustomizeAvatarWithFurni=9012, Game2LeaveGame=2698, Game2QuickJoinGame=2000, GetBadgePointLimits=981).
  resolved_by: "check each id against the client's message registry; a header above the client's highest real id was invented and must be corrected or removed"
  known_evidence:
  - id: ev_1ed0aead370e
    kind: emulator_header
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Revisions/Revision20260701/Headers.cs
    symbol: MessageEvent / MessageComposer
    note: the header table this emulator answers on; ids are revision-specific
verified: {}
manual: {}
