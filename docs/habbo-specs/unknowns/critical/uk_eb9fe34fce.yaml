# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: unknown
spec_version: 1
generated_digest: "sha256:c86250a203280521585d4e579012b387ec46d7cbf5ae28df7cd6b6e72e6f95a4"
generated:
  id: uk_eb9fe34fce
  subject: tracking.latency_ping_request
  severity: critical
  question: The handler for LatencyPingRequest reaches no domain operation and sends nothing. What is the message supposed to do?
  resolved_by: a capture of the official server answering this message, or the client code that consumes the reply
  feature: tracking.latency_ping_request
  known_evidence:
  - id: ev_dc405f0ecf0a
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Tracking/LatencyPingRequestMessageHandler.cs
    symbol: LatencyPingRequestMessageHandler
    line: 9
verified: {}
manual: {}
