# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: unknown
spec_version: 1
generated_digest: "sha256:e2a2d0a40d248e3b04456ed45fadce73abfe92e46cf800820f5c1d2fda6999fe"
generated:
  id: uk_c641019bb0
  subject: quest.get_concurrent_users_goal_progress
  severity: critical
  question: The handler for GetConcurrentUsersGoalProgress reaches no domain operation and sends nothing. What is the message supposed to do?
  resolved_by: a capture of the official server answering this message, or the client code that consumes the reply
  feature: quest.get_concurrent_users_goal_progress
  known_evidence:
  - id: ev_51419bb75e94
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Quest/GetConcurrentUsersGoalProgressMessageHandler.cs
    symbol: GetConcurrentUsersGoalProgressMessageHandler
    line: 27
  - id: ev_cf6f4823503c
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Quest/GetConcurrentUsersGoalProgressMessageHandler.cs
    symbol: GetConcurrentUsersGoalProgressMessageHandler
    line: 16
verified: {}
manual: {}
