# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: feature
spec_version: 1
generated_digest: "sha256:28b46fdeb49f73e5958d2481a26f6d2e564e4f6ac6192e1f1f2a243d84788b2a"
generated:
  id: users.get_motd
  domain: users
  title: get motd
  trigger_packets:
  - GetMOTD
  observed_in:
    vortex: true
    references:
    - arcturus
  official_behavior:
    status: unknown
  flow:
  - order: 0
    layer: handler
    symbol: GetMOTDMessageHandler.HandleAsync
    evidence: ev_e36a644bdb7c
  - order: 1
    layer: grain
    symbol: ServerConfigGrain.GetMotdAsync
    evidence: ev_86f81f4f919c
  checks:
  - expression: ctx.PlayerId <= 0
    on_fail: return
    observed_in: vortex
    evidence: ev_c140f572d164
  - expression: "!_cache.TryGetValue(MotdKey, out string? json) || string.IsNullOrWhiteSpace(json)"
    on_fail: return
    observed_in: vortex
    evidence: ev_86f81f4f919c
  state_changes: []
  outgoing:
    ordering: unknown
    packets:
    - packet: MOTDNotificationEvent
      recipient: actor
      recipient_confidence: implementation_observed
      evidence: ev_c140f572d164
  reaches_persistence: false
  evidence:
  - id: ev_86f81f4f919c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/ServerConfigGrain.cs
    symbol: ServerConfigGrain
    line: 129
  - id: ev_c140f572d164
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Users/GetMOTDMessageHandler.cs
    symbol: GetMOTDMessageHandler
    line: 23
  - id: ev_e36a644bdb7c
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Users/GetMOTDMessageHandler.cs
    symbol: GetMOTDMessageHandler
    line: 13
  scenarios:
  - users.get_motd.guard_0
  - users.get_motd.guard_1
  - users.get_motd.success
verified: {}
manual: {}
