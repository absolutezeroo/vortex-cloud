# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: feature
spec_version: 1
generated_digest: "sha256:fc7603789ab761c02c1484b14c94008a393256bf8d63c4d543d892f0e0efb8a2"
generated:
  id: room.get_item_data
  domain: room
  title: get item data
  trigger_packets:
  - GetItemData
  observed_in:
    vortex: true
    references:
    - habbo-arcturus-daybreak
  official_behavior:
    status: unknown
  flow:
  - order: 0
    layer: handler
    symbol: GetItemDataMessageHandler.HandleAsync
    evidence: ev_961bf28d8436
  - order: 1
    layer: grain
    symbol: RoomGrain.GetItemDataAsync
    evidence: ev_5e51a330c801
  checks:
  - expression: ctx.RoomId <= 0
    on_fail: return
    observed_in: vortex
    evidence: ev_c3e9abc54f38
  - expression: data is null
    on_fail: return
    observed_in: vortex
    evidence: ev_c3e9abc54f38
  state_changes: []
  outgoing:
    ordering: unknown
    packets:
    - packet: ItemDataUpdate
      recipient: actor
      recipient_confidence: implementation_observed
      evidence: ev_c3e9abc54f38
  reaches_persistence: false
  evidence:
  - id: ev_5e51a330c801
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Furni.Interactive.cs
    symbol: RoomGrain
    line: 630
  - id: ev_961bf28d8436
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/GetItemDataMessageHandler.cs
    symbol: GetItemDataMessageHandler
    line: 19
  - id: ev_c3e9abc54f38
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/GetItemDataMessageHandler.cs
    symbol: GetItemDataMessageHandler
    line: 28
  scenarios:
  - room.get_item_data.guard_0
  - room.get_item_data.guard_1
  - room.get_item_data.success
verified:
  answered_to_the_asker_only:
    confidence: assumed
    claim: The answer is an ItemDataUpdate sent to the requesting client alone, not broadcast to the room.
    reason: "Everyone in the room already received the item's data with the room's contents; this request is raised by one client opening the note. Broadcasting would be a second copy of what the others already hold. What the official server does is unknown, but the client's own handler is a per-widget request, not a room event."
    evidence:
    - kind: client_code
      source: com/sulake/habbo/room/_SafeCls_1821.as
      symbol: ROFCAE_STICKIE
manual:
  blank_vs_absent: An item that exists but carries nothing answers with an empty string; an item that is gone, or that the caller may not touch, is not answered at all. The client draws the first as a blank note and leaves the dialog closed on the second.
