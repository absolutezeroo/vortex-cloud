# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: feature
spec_version: 1
generated_digest: "sha256:fecb056404828f8830880cc65b42e46fe04e80967047b21f728941a65098802b"
generated:
  id: room.place_bot
  domain: room
  title: place bot
  trigger_packets:
  - PlaceBot
  observed_in:
    vortex: false
    references:
    - habbo-arcturus-daybreak
  official_behavior:
    status: unknown
  flow:
  - order: 0
    layer: handler
    symbol: PlaceBotMessageHandler.HandleAsync
    evidence: ev_e225e41a9ba0
  checks:
  - expression: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.BotId <= 0
    on_fail: return
    observed_in: vortex
    evidence: ev_dfc4de7ba812
  state_changes:
  - target: grainFactory .GetRoomBots(ctx.RoomId)
    value: grainFactory .GetRoomBots(ctx.RoomId) .PlaceBotAsync(ctx.AsActionContext(), message.BotId, message.X, message.Y, ct)
    observed_in: vortex
    evidence: ev_dfc4de7ba812
  outgoing:
    ordering: unknown
    packets: []
  reaches_persistence: false
  evidence:
  - id: ev_dfc4de7ba812
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/PlaceBotMessageHandler.cs
    symbol: PlaceBotMessageHandler
    line: 18
  - id: ev_e225e41a9ba0
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/PlaceBotMessageHandler.cs
    symbol: PlaceBotMessageHandler
    line: 10
  scenarios:
  - room.place_bot.guard_0
  - room.place_bot.success
  unknowns:
  - uk_bca5df33b3
  - uk_ed59de3b66
verified: {}
manual: {}
