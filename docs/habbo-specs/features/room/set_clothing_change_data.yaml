# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: feature
spec_version: 1
generated_digest: "sha256:de779463ca7baa64bce18ff2b4d4b75e771d979ba03fa5c4d7e5f9af7e24104d"
generated:
  id: room.set_clothing_change_data
  domain: room
  title: set clothing change data
  trigger_packets:
  - SetClothingChangeData
  observed_in:
    vortex: false
    references:
    - habbo-arcturus-daybreak
  official_behavior:
    status: unknown
  flow:
  - order: 0
    layer: handler
    symbol: SetClothingChangeDataMessageHandler.HandleAsync
    evidence: ev_a42d2d83d3e7
  checks: []
  state_changes: []
  outgoing:
    ordering: unknown
    packets: []
  reaches_persistence: false
  evidence:
  - id: ev_a42d2d83d3e7
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/SetClothingChangeDataMessageHandler.cs
    symbol: SetClothingChangeDataMessageHandler
    line: 8
  scenarios:
  - room.set_clothing_change_data.success
  unknowns:
  - uk_fb3d246a9f
verified:
  one_gender_per_message_merged_into_two:
    confidence: assumed
    claim: The booth stores "<boyLook>,<girlLook>" and a message carrying one gender merges into that string rather than replacing it.
    reason: "FurnitureClothingChangeLogic splits the item's data on a comma and takes index 0 as furniture_clothing_boy and index 1 as furniture_clothing_girl, while the packet carries a single gender and a single look. Overwriting would clear the outfit nobody was editing, which is invisible until someone of the other gender uses the booth. What the official server stores is unknown; the two-part string is what the client reads."
    evidence:
    - kind: client_code
      source: com/sulake/habbo/room/object/logic/furniture/FurnitureClothingChangeLogic.as
      symbol: updateClothingData
  unknown_gender_writes_the_boys_half:
    confidence: assumed
    claim: A gender that is neither M nor F is written to the boys' half.
    reason: "The client sends M or F. Accepting the packet and changing nothing would be indistinguishable from a booth that refuses to save, so an unexpected value writes somewhere rather than nowhere."
manual:
  not_built: "Applying the stored outfit to an avatar is the client's: the booth raises ROWRE_CLOTHING_CHANGE and its widget offers the look, the same shape as purchasable clothing. This feature stores the two looks and nothing else."
