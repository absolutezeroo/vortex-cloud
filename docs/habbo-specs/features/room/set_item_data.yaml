# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: feature
spec_version: 1
generated_digest: "sha256:41db78fe4c7e7cac13d43caa97ca99ce67bbe31a34d535d7adc0e7cbcec4a150"
generated:
  id: room.set_item_data
  domain: room
  title: set item data
  trigger_packets:
  - SetItemData
  observed_in:
    vortex: true
    references:
    - habbo-arcturus-daybreak
  official_behavior:
    status: unknown
  flow:
  - order: 0
    layer: handler
    symbol: SetItemDataMessageHandler.HandleAsync
    evidence: ev_2c6f62ada260
  - order: 1
    layer: grain
    symbol: RoomGrain.SetPostItAsync
    evidence: ev_5e51a330c801
  checks:
  - expression: ctx.PlayerId <= 0 || ctx.RoomId <= 0
    on_fail: return
    observed_in: vortex
    evidence: ev_05ee5133fcd7
  - expression: item?.Logic.StuffData is not ILegacyStuffData legacy
    on_fail: return
    observed_in: vortex
    evidence: ev_5e51a330c801
  state_changes:
  - target: grainFactory .GetRoomFurni(ctx.RoomId)
    value: grainFactory .GetRoomFurni(ctx.RoomId) .SetPostItAsync( ctx.AsActionContext(), message.ItemId, message.ColorHex, message.Text, ct )
    observed_in: vortex
    evidence: ev_05ee5133fcd7
  - target: legacy
    value: "legacy.SetState($\"{colorHex} {text}\")"
    observed_in: vortex
    evidence: ev_5e51a330c801
  outgoing:
    ordering: unknown
    packets: []
  reaches_persistence: false
  evidence:
  - id: ev_05ee5133fcd7
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/SetItemDataMessageHandler.cs
    symbol: SetItemDataMessageHandler
    line: 23
  - id: ev_2c6f62ada260
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/SetItemDataMessageHandler.cs
    symbol: SetItemDataMessageHandler
    line: 14
  - id: ev_5e51a330c801
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Furni.Interactive.cs
    symbol: RoomGrain
    line: 695
  scenarios:
  - room.set_item_data.guard_0
  - room.set_item_data.guard_1
  - room.set_item_data.success
  unknowns:
  - uk_0f353a874d
verified:
  colour_and_text_are_one_string:
    confidence: assumed
    claim: The colour and the text are stored as a single value, "<colorHex> <text>", split on the FIRST space.
    reason: "The client is unambiguous about reading it that way and there is no capture of what the official server stores. FurnitureStickieWidgetHandler takes the item's data, and if it contains a space uses everything before the first one as the colour and everything after it as the body -- newlines included, so the text may itself contain spaces and only the first one separates. A note shorter than six characters is refused by the widget before it draws, which is why an empty note is written as the colour alone rather than as an empty string."
    evidence:
    - kind: client_code
      source: com/sulake/habbo/ui/handler/FurnitureStickieWidgetHandler.as
      symbol: processWidgetMessage
    - kind: client_code
      source: com/sulake/habbo/room/object/logic/furniture/FurnitureStickieLogic.as
      symbol: setColorIndexFromItemData
  colour_is_not_validated:
    confidence: assumed
    claim: Any colour string is stored as sent; the server does not restrict it to the client's eight papers.
    reason: What the official server accepts is unknown. The client already fails safe -- FurnitureStickieLogic matches the value against its own eight hex codes and falls back to yellow on anything else -- so refusing here would only replace a wrong-coloured note with a note that cannot be saved, and the value is never interpreted as anything but a colour.
    evidence:
    - kind: client_code
      source: com/sulake/habbo/room/object/logic/furniture/FurnitureStickieLogic.as
      symbol: setColorIndexFromItemData
manual:
  rights: Room rights, not ownership -- the same rule the bin button already uses. A room owner expects to be able to rewrite a sticky a visitor left on their wall.
