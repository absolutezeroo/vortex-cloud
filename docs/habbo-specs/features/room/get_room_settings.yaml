# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: feature
spec_version: 1
generated_digest: "sha256:170f526f326cb8e5f6a13d0560f686bddb3b7351042a206e3c4c105f8188cb52"
generated:
  id: room.get_room_settings
  domain: room
  title: get room settings
  trigger_packets:
  - GetRoomSettings
  observed_in:
    vortex: true
    references:
    - habbo-arcturus-daybreak
  official_behavior:
    status: unknown
  flow:
  - order: 0
    layer: handler
    symbol: GetRoomSettingsMessageHandler.HandleAsync
    evidence: ev_41f50501910b
  - order: 1
    layer: grain
    symbol: RoomGrain.GetRoomSettingsAsync
    evidence: ev_329537530b13
  checks:
  - expression: ctx.PlayerId <= 0 || message.RoomId <= 0
    on_fail: return
    observed_in: vortex
    evidence: ev_2f074d4ea877
  - expression: snapshot is null
    on_fail: return
    observed_in: vortex
    evidence: ev_2f074d4ea877
  - expression: "!await IsRoomOwnerAsync(actor).ConfigureAwait(true)"
    on_fail: return
    observed_in: vortex
    evidence: ev_329537530b13
  state_changes: []
  outgoing:
    ordering: unknown
    packets:
    - packet: RoomSettingsDataEvent
      recipient: actor
      recipient_confidence: implementation_observed
      evidence: ev_2f074d4ea877
  reaches_persistence: false
  evidence:
  - id: ev_2f074d4ea877
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/RoomSettings/GetRoomSettingsMessageHandler.cs
    symbol: GetRoomSettingsMessageHandler
    line: 22
  - id: ev_329537530b13
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Settings.cs
    symbol: RoomGrain
    line: 39
  - id: ev_41f50501910b
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/RoomSettings/GetRoomSettingsMessageHandler.cs
    symbol: GetRoomSettingsMessageHandler
    line: 13
  scenarios:
  - room.get_room_settings.guard_0
  - room.get_room_settings.guard_1
  - room.get_room_settings.guard_2
  - room.get_room_settings.success
  conflicts:
  - cf_a53bc597e6
verified: {}
manual: {}
