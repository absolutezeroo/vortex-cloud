# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: feature
spec_version: 1
generated_digest: "sha256:92f1977ea9caadf8dc77313e85d2f108fe1531e352bc37dbf07d0c5ee2bc9497"
generated:
  id: room.get_raid_protection_settings
  domain: room
  title: get raid protection settings
  trigger_packets:
  - GetRaidProtectionSettings
  observed_in:
    vortex: true
    references: []
  official_behavior:
    status: unknown
  flow:
  - order: 0
    layer: handler
    symbol: GetRaidProtectionSettingsMessageHandler.HandleAsync
    evidence: ev_da0760dcf748
  checks:
  - expression: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.RoomId != ctx.RoomId
    on_fail: return
    observed_in: vortex
    evidence: ev_c7f37b1df18c
  - expression: settings is null
    on_fail: return
    observed_in: vortex
    evidence: ev_c7f37b1df18c
  state_changes: []
  outgoing:
    ordering: unknown
    packets:
    - packet: RaidProtectionSettings
      recipient: unknown
      recipient_confidence: unknown
      evidence: ev_c7f37b1df18c
  reaches_persistence: false
  evidence:
  - id: ev_c7f37b1df18c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/RaidProtection/GetRaidProtectionSettingsMessageHandler.cs
    symbol: GetRaidProtectionSettingsMessageHandler
    line: 35
  - id: ev_da0760dcf748
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/RaidProtection/GetRaidProtectionSettingsMessageHandler.cs
    symbol: GetRaidProtectionSettingsMessageHandler
    line: 21
  scenarios:
  - room.get_raid_protection_settings.guard_0
  - room.get_raid_protection_settings.guard_1
  - room.get_raid_protection_settings.success
  unknowns:
  - uk_c2fff921b1
verified: {}
manual: {}
