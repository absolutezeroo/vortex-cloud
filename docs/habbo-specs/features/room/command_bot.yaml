# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: feature
spec_version: 1
generated_digest: "sha256:a15834592ab818265d1c3c24af2f70e2c98b8c99b5c0d6916879a41912464267"
generated:
  id: room.command_bot
  domain: room
  title: command bot
  trigger_packets:
  - CommandBot
  observed_in:
    vortex: false
    references:
    - arcturus
  official_behavior:
    status: unknown
  flow:
  - order: 0
    layer: handler
    symbol: CommandBotMessageHandler.HandleAsync
    evidence: ev_9774ac9b669b
  checks:
  - expression: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.BotId <= 0
    on_fail: return
    observed_in: vortex
    evidence: ev_27143b751641
  state_changes:
  - target: grainFactory .GetRoomBots(ctx.RoomId)
    value: grainFactory .GetRoomBots(ctx.RoomId) .SetBotSkillAsync( ctx.AsActionContext(), message.BotId, message.CommandId, message.Data, ct )
    observed_in: vortex
    evidence: ev_27143b751641
  outgoing:
    ordering: unknown
    packets: []
  reaches_persistence: false
  evidence:
  - id: ev_27143b751641
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Bots/CommandBotMessageHandler.cs
    symbol: CommandBotMessageHandler
    line: 19
  - id: ev_9774ac9b669b
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Bots/CommandBotMessageHandler.cs
    symbol: CommandBotMessageHandler
    line: 10
  scenarios:
  - room.command_bot.guard_0
  - room.command_bot.success
  conflicts:
  - cf_4826788e32
  unknowns:
  - uk_6ac19b643a
  - uk_d306785cd7
verified: {}
manual: {}
