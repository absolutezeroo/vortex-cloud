# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: feature
spec_version: 1
generated_digest: "sha256:6560d6850e26e003c823bd5b48495ca9fff7ded7c54383ad89f4c0ff6e692f99"
generated:
  id: quest.get_concurrent_users_goal_progress
  domain: quest
  title: get concurrent users goal progress
  trigger_packets:
  - GetConcurrentUsersGoalProgress
  observed_in:
    vortex: false
    references: []
  official_behavior:
    status: unknown
  flow:
  - order: 0
    layer: handler
    symbol: GetConcurrentUsersGoalProgressMessageHandler.HandleAsync
    evidence: ev_cf6f4823503c
  checks:
  - expression: ctx.PlayerId <= 0
    on_fail: return
    observed_in: vortex
    evidence: ev_51419bb75e94
  state_changes: []
  outgoing:
    ordering: unknown
    packets: []
  reaches_persistence: false
  evidence:
  - id: ev_51419bb75e94
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Quest/GetConcurrentUsersGoalProgressMessageHandler.cs
    symbol: GetConcurrentUsersGoalProgressMessageHandler
    line: 27
  - id: ev_cf6f4823503c
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Quest/GetConcurrentUsersGoalProgressMessageHandler.cs
    symbol: GetConcurrentUsersGoalProgressMessageHandler
    line: 16
  scenarios:
  - quest.get_concurrent_users_goal_progress.guard_0
  - quest.get_concurrent_users_goal_progress.success
  unknowns:
  - uk_c641019bb0
verified: {}
manual: {}
