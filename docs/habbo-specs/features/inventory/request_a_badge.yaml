# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: feature
spec_version: 1
generated_digest: "sha256:cd04fab58e9a16eaf94e9d37aedaa8dde19280fe10e2d140c03e9989c7f914cf"
generated:
  id: inventory.request_a_badge
  domain: inventory
  title: request a badge
  trigger_packets:
  - RequestABadge
  observed_in:
    vortex: true
    references: []
  official_behavior:
    status: unknown
  flow:
  - order: 0
    layer: handler
    symbol: RequestABadgeMessageHandler.HandleAsync
    evidence: ev_cb07d3019d09
  checks:
  - expression: ctx.PlayerId <= 0 || string.IsNullOrEmpty(message.RequestCode)
    on_fail: return
    observed_in: vortex
    evidence: ev_f4e9b4a63c7f
  state_changes: []
  outgoing:
    ordering: unknown
    packets:
    - packet: IsBadgeRequestFulfilledEvent
      recipient: unknown
      recipient_confidence: unknown
      evidence: ev_f4e9b4a63c7f
  reaches_persistence: false
  evidence:
  - id: ev_cb07d3019d09
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Inventory/Badges/RequestABadgeMessageHandler.cs
    symbol: RequestABadgeMessageHandler
    line: 16
  - id: ev_f4e9b4a63c7f
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Inventory/Badges/RequestABadgeMessageHandler.cs
    symbol: RequestABadgeMessageHandler
    line: 25
  scenarios:
  - inventory.request_a_badge.guard_0
  - inventory.request_a_badge.success
  unknowns:
  - uk_aaa2e110a3
verified: {}
manual: {}
