# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: feature
spec_version: 1
generated_digest: "sha256:b3e17ca39c7da40d61f710db52a37bd806e7ab9b4803b946efe97bf5bfe292c0"
generated:
  id: preferences.get_discord_preferences
  domain: preferences
  title: get discord preferences
  trigger_packets:
  - GetDiscordPreferences
  observed_in:
    vortex: true
    references: []
  official_behavior:
    status: unknown
  flow:
  - order: 0
    layer: handler
    symbol: GetDiscordPreferencesMessageHandler.HandleAsync
    evidence: ev_e279b3487948
  - order: 1
    layer: grain
    symbol: PlayerGrain.GetAccountPreferencesAsync
    evidence: ev_630a59578b9c
  checks:
  - expression: ctx.PlayerId <= 0
    on_fail: return
    observed_in: vortex
    evidence: ev_36fbf55f14c1
  state_changes: []
  outgoing:
    ordering: unknown
    packets:
    - packet: DiscordPreferences
      recipient: actor
      recipient_confidence: inferred
      note: one of several composers the call can return, depending on the item type
      evidence: ev_36fbf55f14c1
    - packet: VortexFurniDefinition
      recipient: actor
      recipient_confidence: inferred
      note: one of several composers the call can return, depending on the item type
      evidence: ev_36fbf55f14c1
  reaches_persistence: false
  evidence:
  - id: ev_36fbf55f14c1
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Preferences/DiscordPreferencesHandlers.cs
    symbol: GetDiscordPreferencesMessageHandler
    line: 36
  - id: ev_630a59578b9c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerGrain.AccountPreferences.cs
    symbol: PlayerGrain
    line: 26
  - id: ev_e279b3487948
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Preferences/DiscordPreferencesHandlers.cs
    symbol: GetDiscordPreferencesMessageHandler
    line: 27
  scenarios:
  - preferences.get_discord_preferences.guard_0
  - preferences.get_discord_preferences.success
verified: {}
manual: {}
