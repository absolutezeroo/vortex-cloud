# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: feature
spec_version: 1
generated_digest: "sha256:188a1c9bbbd7b5eb7a0c5f95eccb0e71df388e910d9048721cfcdb4f7cbb21f0"
generated:
  id: sound.add_jukebox_disk
  domain: sound
  title: add jukebox disk
  trigger_packets:
  - AddJukeboxDisk
  observed_in:
    vortex: true
    references:
    - habbo-arcturus-daybreak
  official_behavior:
    status: unknown
  flow:
  - order: 0
    layer: handler
    symbol: AddJukeboxDiskMessageHandler.HandleAsync
    evidence: ev_2a13c19722fd
  checks:
  - expression: ctx.PlayerId <= 0 || ctx.RoomId <= 0
    on_fail: return
    observed_in: vortex
    evidence: ev_9bb5203f8d83
  state_changes:
  - target: _grainFactory .GetRoomJukebox(ctx.RoomId)
    value: _grainFactory .GetRoomJukebox(ctx.RoomId) .AddDiskAsync(ctx.AsActionContext(), message.DiskItemId, ct)
    observed_in: vortex
    evidence: ev_9bb5203f8d83
  outgoing:
    ordering: unknown
    packets:
    - packet: JukeboxPlayListFull
      recipient: actor
      recipient_confidence: implementation_observed
      evidence: ev_9bb5203f8d83
  reaches_persistence: false
  evidence:
  - id: ev_2a13c19722fd
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Sound/AddJukeboxDiskMessageHandler.cs
    symbol: AddJukeboxDiskMessageHandler
    line: 21
  - id: ev_9bb5203f8d83
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Sound/AddJukeboxDiskMessageHandler.cs
    symbol: AddJukeboxDiskMessageHandler
    line: 32
  scenarios:
  - sound.add_jukebox_disk.guard_0
  - sound.add_jukebox_disk.success
  unknowns:
  - uk_e67e59520d
verified:
  slot_number_is_not_honoured:
    confidence: assumed
    claim: The slot number the client sends is read and ignored; the disk is appended to the end of the playlist.
    reason: "What the official server does with the slot is unknown - no capture observes it. The client's own editor offers no way to drop a disk into a chosen position, and its playlist has no gaps for one to land in: PlayListEditorWidgetHandler sends the slot as the index the widget happened to be showing. Honouring it would let a client claim a position that does not exist, and the only ordering the playlist has is insertion order, which is what the read returns. The reference emulator reads the field and discards it in the same way."
    evidence:
    - kind: client_code
      source: com/sulake/habbo/ui/handler/PlayListEditorWidgetHandler.as
      symbol: processWidgetMessage
    - kind: reference_emulator
      source: src/main/java/com/eu/habbo/messages/incoming/rooms/items/jukebox/AddJukeboxDiskEvent.java
      symbol: AddJukeboxDiskEvent
  capacity_refusal:
    confidence: assumed
    claim: A disk offered to a full jukebox is refused with JukeboxPlayListFull to the sender alone, and nothing moves.
    reason: The client has a dialog for exactly this message and reads no fields from it, so the refusal is the whole answer. Whether the official server also re-sends the playlist afterwards is unknown; it is not sent here, because nothing changed.
    evidence:
    - kind: client_code
      source: com/sulake/habbo/sound/music/JukeboxPlayListController.as
      symbol: onJukeboxPlayListFullMessage
manual:
  ownership: "A loaded disk stays its owner's the whole time it is in the jukebox. Vortex stores that as furniture.jukebox_id, the same shape a wired chest uses, so the disk is one row in one place and can never be listed in an inventory and a playlist at once."
