# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: feature
spec_version: 1
generated_digest: "sha256:9114c8a36982aafa7333e4b8a969571681f2a822ad30e12b2667559382447cb2"
generated:
  id: sound.get_jukebox_play_list
  domain: sound
  title: get jukebox play list
  trigger_packets:
  - GetJukeboxPlayList
  observed_in:
    vortex: true
    references:
    - habbo-arcturus-daybreak
  official_behavior:
    status: unknown
  flow:
  - order: 0
    layer: handler
    symbol: GetJukeboxPlayListMessageHandler.HandleAsync
    evidence: ev_a452a691fccf
  checks:
  - expression: ctx.RoomId <= 0
    on_fail: return
    observed_in: vortex
    evidence: ev_003d070777b7
  state_changes: []
  outgoing:
    ordering: unknown
    packets:
    - packet: JukeboxSongDisks
      recipient: actor
      recipient_confidence: implementation_observed
      evidence: ev_003d070777b7
  reaches_persistence: false
  evidence:
  - id: ev_003d070777b7
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Sound/GetJukeboxPlayListMessageHandler.cs
    symbol: GetJukeboxPlayListMessageHandler
    line: 31
  - id: ev_a452a691fccf
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Sound/GetJukeboxPlayListMessageHandler.cs
    symbol: GetJukeboxPlayListMessageHandler
    line: 20
  scenarios:
  - sound.get_jukebox_play_list.guard_0
  - sound.get_jukebox_play_list.success
  conflicts:
  - cf_a47144a65a
verified: {}
manual: {}
