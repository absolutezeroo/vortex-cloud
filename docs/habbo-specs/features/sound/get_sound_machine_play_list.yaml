# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: feature
spec_version: 1
generated_digest: "sha256:3b9f014b12ae35918c9256e92af726d5f1f15c761e82b5c246880e800198d508"
generated:
  id: sound.get_sound_machine_play_list
  domain: sound
  title: get sound machine play list
  trigger_packets:
  - GetSoundMachinePlayList
  observed_in:
    vortex: true
    references:
    - habbo-arcturus-daybreak
  official_behavior:
    status: unknown
  flow:
  - order: 0
    layer: handler
    symbol: GetSoundMachinePlayListMessageHandler.HandleAsync
    evidence: ev_b95f1041c033
  checks: []
  state_changes: []
  outgoing:
    ordering: unknown
    packets:
    - packet: PlayList
      recipient: actor
      recipient_confidence: implementation_observed
      evidence: ev_027e768d5a36
  reaches_persistence: false
  evidence:
  - id: ev_027e768d5a36
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Sound/GetSoundMachinePlayListMessageHandler.cs
    symbol: GetSoundMachinePlayListMessageHandler
    line: 27
  - id: ev_b95f1041c033
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Sound/GetSoundMachinePlayListMessageHandler.cs
    symbol: GetSoundMachinePlayListMessageHandler
    line: 19
  scenarios:
  - sound.get_sound_machine_play_list.success
verified:
  answered_empty:
    confidence: assumed
    claim: Vortex answers with an empty PlayList. Loading a sound machine is not implemented.
    reason: "Silence is worse than an empty list here: SoundMachinePlayListController waits for this message before it draws anything, so an unanswered request leaves the composer dialog blank with no explanation. An empty list is both true - nothing is loaded, because nothing can be - and something the client already renders. What the official server sends for a machine with no disks is unknown."
    evidence:
    - kind: client_code
      source: com/sulake/habbo/sound/music/SoundMachinePlayListController.as
      symbol: requestPlayList
manual:
  not_implemented: "The sound machine's own loading path (disks into the machine, PlayListSongAdded) is not built. The packet is answered; the feature behind it is not."
