# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: feature
spec_version: 1
generated_digest: "sha256:de8994dcf6c9b26a0616c5f60a289451f04ff214ed82b6a004a46a5597073a5d"
generated:
  id: sound.remove_jukebox_disk
  domain: sound
  title: remove jukebox disk
  trigger_packets:
  - RemoveJukeboxDisk
  observed_in:
    vortex: true
    references:
    - habbo-arcturus-daybreak
  official_behavior:
    status: unknown
  flow:
  - order: 0
    layer: handler
    symbol: RemoveJukeboxDiskMessageHandler.HandleAsync
    evidence: ev_02cdf81d88ef
  checks:
  - expression: ctx.PlayerId <= 0 || ctx.RoomId <= 0
    on_fail: return
    observed_in: vortex
    evidence: ev_38f1244eb4b0
  state_changes:
  - target: _grainFactory .GetRoomJukebox(ctx.RoomId)
    value: _grainFactory .GetRoomJukebox(ctx.RoomId) .RemoveDiskAsync(ctx.AsActionContext(), message.Index, ct)
    observed_in: vortex
    evidence: ev_38f1244eb4b0
  outgoing:
    ordering: unknown
    packets: []
  reaches_persistence: false
  evidence:
  - id: ev_02cdf81d88ef
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Sound/RemoveJukeboxDiskMessageHandler.cs
    symbol: RemoveJukeboxDiskMessageHandler
    line: 13
  - id: ev_38f1244eb4b0
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Sound/RemoveJukeboxDiskMessageHandler.cs
    symbol: RemoveJukeboxDiskMessageHandler
    line: 24
  scenarios:
  - sound.remove_jukebox_disk.guard_0
  - sound.remove_jukebox_disk.success
  unknowns:
  - uk_4d89bb47a2
verified:
  stale_index_is_a_no_op:
    confidence: assumed
    claim: The index names a position in the playlist the client was last sent. An index outside the current playlist removes nothing.
    reason: The client has no disk id to send - JukeboxPlayListController keeps entries by position - so the index is the only handle it has, and it can be stale by the time it arrives. What the official server does with a stale one is unknown; guessing at what was meant would hand back a disk nobody asked for.
    evidence:
    - kind: client_code
      source: com/sulake/habbo/sound/music/JukeboxPlayListController.as
      symbol: getEntry
manual:
  ownership: "The disk goes back to whoever owns it, which is not necessarily whoever emptied the jukebox: otherwise clearing someone else's jukebox would be a way to collect their disks."
