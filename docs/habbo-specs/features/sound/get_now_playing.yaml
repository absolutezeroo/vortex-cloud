# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: feature
spec_version: 1
generated_digest: "sha256:0569a8b3e96c59787b01b6b11db135a3a51a9c56d5e6a406f1d657b010a10c92"
generated:
  id: sound.get_now_playing
  domain: sound
  title: get now playing
  trigger_packets:
  - GetNowPlaying
  observed_in:
    vortex: false
    references:
    - habbo-arcturus-daybreak
  official_behavior:
    status: unknown
  flow:
  - order: 0
    layer: handler
    symbol: GetNowPlayingMessageHandler.HandleAsync
    evidence: ev_c65d069ade81
  checks: []
  state_changes: []
  outgoing:
    ordering: unknown
    packets: []
  reaches_persistence: false
  evidence:
  - id: ev_c65d069ade81
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Sound/GetNowPlayingMessageHandler.cs
    symbol: GetNowPlayingMessageHandler
    line: 8
  scenarios:
  - sound.get_now_playing.success
  unknowns:
  - uk_cc22d643e4
verified:
  playback_is_a_looping_clock:
    confidence: assumed
    claim: The playlist loops, and the position in it is derived from when the current song started rather than stored. The server pushes NowPlaying again at every song boundary.
    reason: "Official playback behaviour is unknown - no capture observes a jukebox running. The push is not a choice: JukeboxPlayListController.onSongFinishedPlayingEvent is empty, so a client is never told to advance by itself and a server that only answers GetNowPlaying plays one song and goes quiet. Deriving the offset from a room-anchored start is what makes two clients in one room hear the same bar, which is the only observable the message has. Looping rather than stopping matches the reference emulator."
    evidence:
    - kind: client_code
      source: com/sulake/habbo/sound/music/JukeboxPlayListController.as
      symbol: onSongFinishedPlayingEvent
    - kind: client_code
      source: com/sulake/habbo/sound/music/JukeboxPlayListController.as
      symbol: onNowPlayingMessage
manual:
  field_meanings: "currentPosition and nextPosition are positions in the playlist, not in the track - the client exposes the first as playPosition. Time is carried by syncCount alone, in milliseconds, which the client divides by 1000."
