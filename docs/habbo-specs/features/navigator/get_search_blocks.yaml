# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: feature
spec_version: 1
generated_digest: "sha256:70163513af3c81211bec17ddbbb465bfef8281448ecaa68292dcc895b0fc5329"
generated:
  id: navigator.get_search_blocks
  domain: navigator
  title: get search blocks
  trigger_packets:
  - NewNavigatorSearch
  observed_in:
    vortex: true
    references:
    - habbo-arcturus-daybreak
  official_behavior:
    status: unknown
  flow:
  - order: 0
    layer: handler
    symbol: NewNavigatorSearchMessageHandler.HandleAsync
    evidence: ev_f1fc6069c2a8
  - order: 1
    layer: service
    symbol: NavigatorService.GetSearchBlocksAsync
    evidence: ev_ca6db2d966f5
  checks:
  - expression: searchCode == NavigatorSearchCodes.Categories
    on_fail: return
    observed_in: vortex
    evidence: ev_ca6db2d966f5
  - expression: quickLinks.Length > 0
    on_fail: return
    observed_in: vortex
    evidence: ev_ca6db2d966f5
  state_changes: []
  outgoing:
    ordering: unknown
    packets:
    - packet: NavigatorSearchResultBlocks
      recipient: actor
      recipient_confidence: implementation_observed
      evidence: ev_b253bcf1dfeb
  reaches_persistence: false
  evidence:
  - id: ev_b253bcf1dfeb
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/NewNavigator/NewNavigatorSearchMessageHandler.cs
    symbol: NewNavigatorSearchMessageHandler
    line: 39
  - id: ev_ca6db2d966f5
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Navigator/NavigatorService.cs
    symbol: NavigatorService
    line: 291
  - id: ev_f1fc6069c2a8
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/NewNavigator/NewNavigatorSearchMessageHandler.cs
    symbol: NewNavigatorSearchMessageHandler
    line: 13
  scenarios:
  - navigator.get_search_blocks.guard_0
  - navigator.get_search_blocks.guard_1
  - navigator.get_search_blocks.success
  conflicts:
  - cf_0f8972c794
verified: {}
manual: {}
