# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: feature
spec_version: 1
generated_digest: "sha256:d2544e724e8317bfc36dbfc5c6cce0f158d6a081dfb786b9d9e4e3fabdcd69a0"
generated:
  id: navigator.navigator_add_saved_search
  domain: navigator
  title: navigator add saved search
  trigger_packets:
  - NavigatorAddSavedSearch
  observed_in:
    vortex: true
    references:
    - habbo-arcturus-daybreak
  official_behavior:
    status: unknown
  flow:
  - order: 0
    layer: handler
    symbol: NavigatorAddSavedSearchMessageHandler.HandleAsync
    evidence: ev_cacff3ed3e74
  - order: 1
    layer: grain
    symbol: PlayerNavigatorGrain.AddSavedSearchAsync
    evidence: ev_d429657b2673
  checks:
  - expression: string.IsNullOrWhiteSpace(message.SearchCode)
    on_fail: return
    observed_in: vortex
    evidence: ev_bc4740ce9db2
  state_changes:
  - target: _grainFactory .GetPlayerNavigatorGrain(ctx.PlayerId)
    value: _grainFactory .GetPlayerNavigatorGrain(ctx.PlayerId) .AddSavedSearchAsync(message.SearchCode, message.Filter ?? string.Empty, ct)
    observed_in: vortex
    evidence: ev_bc4740ce9db2
  outgoing:
    ordering: unknown
    packets: []
  reaches_persistence: true
  evidence:
  - id: ev_bc4740ce9db2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/NewNavigator/NavigatorAddSavedSearchMessageHandler.cs
    symbol: NavigatorAddSavedSearchMessageHandler
    line: 21
  - id: ev_cacff3ed3e74
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/NewNavigator/NavigatorAddSavedSearchMessageHandler.cs
    symbol: NavigatorAddSavedSearchMessageHandler
    line: 10
  - id: ev_d429657b2673
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerNavigatorGrain.cs
    symbol: PlayerNavigatorGrain
    line: 101
  scenarios:
  - navigator.navigator_add_saved_search.guard_0
  - navigator.navigator_add_saved_search.success
  unknowns:
  - uk_68b4d3b451
verified: {}
manual: {}
