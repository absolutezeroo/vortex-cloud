# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: feature
spec_version: 1
generated_digest: "sha256:ce3a47fe0272b7735b97e4bcaa07cc046ff0945dd69623a3ab65e023063ca80e"
generated:
  id: handshake.disconnect
  domain: handshake
  title: disconnect
  trigger_packets:
  - Disconnect
  observed_in:
    vortex: false
    references: []
  official_behavior:
    status: unknown
  flow:
  - order: 0
    layer: handler
    symbol: DisconnectMessageHandler.HandleAsync
    evidence: ev_838c976fc94c
  checks: []
  state_changes: []
  outgoing:
    ordering: unknown
    packets: []
  reaches_persistence: false
  evidence:
  - id: ev_838c976fc94c
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Handshake/DisconnectMessageHandler.cs
    symbol: DisconnectMessageHandler
    line: 8
  scenarios:
  - handshake.disconnect.success
  unknowns:
  - uk_98cf050c8e
verified: {}
manual: {}
