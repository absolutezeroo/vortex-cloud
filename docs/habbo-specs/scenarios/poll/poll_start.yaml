# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:2e6a3102dea95500db8646e3b10d00981d96e7df664794046a98b963a9913e66"
generated:
  feature: poll.poll_start
  scenarios:
  - scenario: poll.poll_start.guard_0
    title: "poll start: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: PollStart
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of PollStart sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_155cd073a847
  - scenario: poll.poll_start.success
    title: "poll start: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: PollStart
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of PollStart succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0c062418ea27
  evidence:
  - id: ev_0c062418ea27
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Poll/PollStartMessageHandler.cs
    symbol: PollStartMessageHandler
    line: 10
  - id: ev_155cd073a847
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Poll/PollStartMessageHandler.cs
    symbol: PollStartMessageHandler
    line: 18
verified: {}
manual: {}
