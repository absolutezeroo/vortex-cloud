# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:a6ddf808fc9cc18901ad63297051497dedb2ae1cfd8e43756b5a9f17a490fc4f"
generated:
  feature: poll.poll_answer
  scenarios:
  - scenario: poll.poll_answer.guard_0
    title: "poll answer: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: PollAnswer
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of PollAnswer sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_96038fbdfd53
  - scenario: poll.poll_answer.guard_1
    title: "poll answer: the guard 'message.Answers.IsDefaultOrEmpty' holds"
    given:
      guard: message.Answers.IsDefaultOrEmpty
      guard_holds: "true"
    when:
      packet: PollAnswer
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of PollAnswer sent while 'message.Answers.IsDefaultOrEmpty' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_96038fbdfd53
  - scenario: poll.poll_answer.success
    title: "poll answer: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: PollAnswer
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of PollAnswer succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_96038fbdfd53
  evidence:
  - id: ev_96038fbdfd53
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Poll/PollAnswerMessageHandler.cs
    symbol: PollAnswerMessageHandler
    line: 19
verified: {}
manual: {}
