# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:cf2f845b771ed60fbb8e229d9388dbc696146f96f70e3dcbfec84a866152d88e"
generated:
  feature: poll.poll_reject
  scenarios:
  - scenario: poll.poll_reject.guard_0
    title: "poll reject: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: PollReject
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of PollReject sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_c536f0bc6176
  - scenario: poll.poll_reject.success
    title: "poll reject: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: PollReject
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of PollReject succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_391b62eabeb8
  evidence:
  - id: ev_391b62eabeb8
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Poll/PollRejectMessageHandler.cs
    symbol: PollRejectMessageHandler
    line: 10
  - id: ev_c536f0bc6176
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Poll/PollRejectMessageHandler.cs
    symbol: PollRejectMessageHandler
    line: 19
verified: {}
manual: {}
