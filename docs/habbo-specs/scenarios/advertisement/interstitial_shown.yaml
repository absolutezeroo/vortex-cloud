# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:70ef070ee6864fef4db6d5fba56ecf931dca2ddbc01bdbcd52f08a42a5381a34"
generated:
  feature: advertisement.interstitial_shown
  scenarios:
  - scenario: advertisement.interstitial_shown.success
    title: "interstitial shown: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: InterstitialShown
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of InterstitialShown succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7b03b0bd59dc
  evidence:
  - id: ev_7b03b0bd59dc
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Advertisement/InterstitialShownMessageHandler.cs
    symbol: InterstitialShownMessageHandler
    line: 8
verified: {}
manual: {}
