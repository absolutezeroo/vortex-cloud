# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:ce841cc9f0b65f3590508a0a3e38ebe496f782891649c0fac22e8cd48b8ab4ea"
generated:
  feature: advertisement.get_interstitial
  scenarios:
  - scenario: advertisement.get_interstitial.success
    title: "get interstitial: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetInterstitial
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetInterstitial succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_be806705584a
  evidence:
  - id: ev_be806705584a
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Advertisement/GetInterstitialMessageHandler.cs
    symbol: GetInterstitialMessageHandler
    line: 8
verified: {}
manual: {}
