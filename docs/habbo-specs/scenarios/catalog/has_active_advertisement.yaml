# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:5a1f2967c9e8c10fc9d11e453456a7770328177097fa90f3dfa151d2379c1780"
generated:
  feature: catalog.has_active_advertisement
  scenarios:
  - scenario: catalog.has_active_advertisement.guard_0
    title: "has active advertisement: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetRoomAdPurchaseInfo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetRoomAdPurchaseInfo sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_614cdf3234fd
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - RoomAdPurchaseInfo
      evidence: ev_9d0184fc3bfa
  - scenario: catalog.has_active_advertisement.success
    title: "has active advertisement: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetRoomAdPurchaseInfo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetRoomAdPurchaseInfo succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - RoomAdPurchaseInfoEvent
      evidence: ev_1f83e61934ca
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - RoomAdPurchaseInfo
      evidence: ev_9d0184fc3bfa
  evidence:
  - id: ev_9d0184fc3bfa
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/promotions/GetRoomAdPurchaseInfoEvent.java
    symbol: GetRoomAdPurchaseInfoEvent
    line: 12
    note: third-party server implementation; evidence, not authority
  - id: ev_1f83e61934ca
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerGrain.cs
    symbol: PlayerGrain
    line: 444
  - id: ev_614cdf3234fd
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Catalog/GetRoomAdPurchaseInfoMessageHandler.cs
    symbol: GetRoomAdPurchaseInfoMessageHandler
    line: 32
verified: {}
manual: {}
