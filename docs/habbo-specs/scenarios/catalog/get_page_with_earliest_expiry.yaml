# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:21af5be7b8bb2520348ffc2ffb7f140dd65113d4614ffe5f73a31ee83f30a55b"
generated:
  feature: catalog.get_page_with_earliest_expiry
  scenarios:
  - scenario: catalog.get_page_with_earliest_expiry.guard_0
    title: "get page with earliest expiry: the guard 'soonestExpiring is null || string.IsNullOrEmpty(soonestEx...' holds"
    given:
      guard: soonestExpiring is null || string.IsNullOrEmpty(soonestExpiring.Name)
      guard_holds: "true"
    when:
      packet: GetCatalogPageWithEarliestExpiry
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetCatalogPageWithEarliestExpiry sent while 'soonestExpiring is null || string.IsNullOrEmpty(soonestExpiring.Name)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e0224e8793f0
  - scenario: catalog.get_page_with_earliest_expiry.success
    title: "get page with earliest expiry: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetCatalogPageWithEarliestExpiry
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetCatalogPageWithEarliestExpiry succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - CatalogPageWithEarliestExpiry
      evidence: ev_af6fd34bbdc8
  evidence:
  - id: ev_af6fd34bbdc8
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Catalog/GetCatalogPageWithEarliestExpiryMessageHandler.cs
    symbol: GetCatalogPageWithEarliestExpiryMessageHandler
    line: 10
  - id: ev_e0224e8793f0
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Catalog/LtdScheduleService.cs
    symbol: LtdScheduleService
    line: 97
verified: {}
manual: {}
