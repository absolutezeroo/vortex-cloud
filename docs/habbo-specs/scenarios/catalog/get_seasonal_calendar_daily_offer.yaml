# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:ad32ad6397558d348c1645cf150b31cbd54b1392bff82a874d370ba186fd9d51"
generated:
  feature: catalog.get_seasonal_calendar_daily_offer
  scenarios:
  - scenario: catalog.get_seasonal_calendar_daily_offer.success
    title: "get seasonal calendar daily offer: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetSeasonalCalendarDaily
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetSeasonalCalendarDaily succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1b5a52cb4d49
  evidence:
  - id: ev_1b5a52cb4d49
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Catalog/GetSeasonalCalendarDailyOfferMessageHandler.cs
    symbol: GetSeasonalCalendarDailyOfferMessageHandler
    line: 8
verified: {}
manual: {}
