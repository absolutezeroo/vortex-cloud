# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:a433dc14fd4143888eef9b62d0ae1d236728d15a39759deaba06234a1bcd925e"
generated:
  feature: catalog.redeem_voucher
  scenarios:
  - scenario: catalog.redeem_voucher.guard_0
    title: "redeem voucher: the guard 'string.IsNullOrWhiteSpace(code)' holds"
    given:
      guard: string.IsNullOrWhiteSpace(code)
      guard_holds: "true"
    when:
      packet: RedeemVoucher
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RedeemVoucher sent while 'string.IsNullOrWhiteSpace(code)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_539488477073
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - HotelWillCloseInMinutes
      - VoucherRedeemError
      evidence: ev_039823ee340b
  - scenario: catalog.redeem_voucher.success
    title: "redeem voucher: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RedeemVoucher
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RedeemVoucher succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - VoucherRedeemError
      - VoucherRedeemOk
      evidence: ev_373e76aa8a80
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - HotelWillCloseInMinutes
      - VoucherRedeemError
      evidence: ev_039823ee340b
  evidence:
  - id: ev_039823ee340b
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/catalog/RedeemVoucherMessageEvent.java
    symbol: RedeemVoucherMessageEvent
    line: 11
    note: third-party server implementation; evidence, not authority
  - id: ev_373e76aa8a80
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerPresenceGrain.cs
    symbol: PlayerPresenceGrain
    line: 112
  - id: ev_539488477073
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Catalog/RedeemVoucherMessageHandler.cs
    symbol: RedeemVoucherMessageHandler
    line: 22
verified: {}
manual: {}
