# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:bd56b22d6df3fee26a7f5a0efe03b7ed4789f69438d5f6479a275b62a9241624"
generated:
  feature: catalog.shop_targeted_offer_viewed
  scenarios:
  - scenario: catalog.shop_targeted_offer_viewed.guard_0
    title: "shop targeted offer viewed: the guard 'ctx.PlayerId <= 0 || message.TargetedOfferId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.TargetedOfferId <= 0
      guard_holds: "true"
    when:
      packet: ShopTargetedOfferViewed
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ShopTargetedOfferViewed sent while 'ctx.PlayerId <= 0 || message.TargetedOfferId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_9b2f9a0d02cd
  - scenario: catalog.shop_targeted_offer_viewed.success
    title: "shop targeted offer viewed: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ShopTargetedOfferViewed
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ShopTargetedOfferViewed succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0ae61cafde43
  evidence:
  - id: ev_0ae61cafde43
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Catalog/ShopTargetedOfferViewedMessageHandler.cs
    symbol: ShopTargetedOfferViewedMessageHandler
    line: 10
  - id: ev_9b2f9a0d02cd
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Catalog/ShopTargetedOfferViewedMessageHandler.cs
    symbol: ShopTargetedOfferViewedMessageHandler
    line: 19
verified: {}
manual: {}
