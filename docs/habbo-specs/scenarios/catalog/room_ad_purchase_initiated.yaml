# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:213d63ec1d8d79e670d41f2d7ca95495b08167e4f3d898fecd1f6664875c9b48"
generated:
  feature: catalog.room_ad_purchase_initiated
  scenarios:
  - scenario: catalog.room_ad_purchase_initiated.success
    title: "room ad purchase initiated: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RoomAdPurchaseInitiated
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RoomAdPurchaseInitiated succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3d2967e04b18
  evidence:
  - id: ev_3d2967e04b18
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Catalog/RoomAdPurchaseInitiatedMessageHandler.cs
    symbol: RoomAdPurchaseInitiatedMessageHandler
    line: 11
verified: {}
manual: {}
