# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:cae7f3a64dc85b5d6190a74cfb279a2b613e23b0e2770d6af818b556dd99a721"
generated:
  feature: catalog.use_item_in_room
  scenarios:
  - scenario: catalog.use_item_in_room.guard_0
    title: "use item in room: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || itemId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || itemId <= 0
      guard_holds: "true"
    when:
      packet: ChargeFirework
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ChargeFirework sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || itemId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7c6cea3bc17b
  - scenario: catalog.use_item_in_room.guard_1
    title: "use item in room: the guard '!await ActionModule.UseItemByIdAsync(ctx, itemId, ct, param)' holds"
    given:
      guard: "!await ActionModule.UseItemByIdAsync(ctx, itemId, ct, param)"
      guard_holds: "true"
    when:
      packet: ChargeFirework
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ChargeFirework sent while '!await ActionModule.UseItemByIdAsync(ctx, itemId, ct, param)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_2298dc2dabc2
  - scenario: catalog.use_item_in_room.guard_2
    title: "use item in room: the guard '!_roomGrain._state.ItemsById.TryGetValue(itemId, out IRoo...' holds"
    given:
      guard: "!_roomGrain._state.ItemsById.TryGetValue(itemId, out IRoomItem? item)"
      guard_holds: "true"
    when:
      packet: ChargeFirework
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ChargeFirework sent while '!_roomGrain._state.ItemsById.TryGetValue(itemId, out IRoomItem? item)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: rejected
      emits: []
      evidence: ev_8a097ef973ee
  - scenario: catalog.use_item_in_room.guard_3
    title: "use item in room: the guard '!canUse || !await _roomGrain.FurniModule.UseItemByIdAsync...' holds"
    given:
      guard: "!canUse || !await _roomGrain.FurniModule.UseItemByIdAsync(ctx, itemId, ct, param)"
      guard_holds: "true"
    when:
      packet: ChargeFirework
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ChargeFirework sent while '!canUse || !await _roomGrain.FurniModule.UseItemByIdAsync(ctx, itemId, ct, param)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_8a097ef973ee
  - scenario: catalog.use_item_in_room.guard_4
    title: "use item in room: the guard 'usageType == FurnitureUsageType.Nobody' holds"
    given:
      guard: usageType == FurnitureUsageType.Nobody
      guard_holds: "true"
    when:
      packet: ChargeFirework
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ChargeFirework sent while 'usageType == FurnitureUsageType.Nobody' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_82c2d2746361
  - scenario: catalog.use_item_in_room.guard_5
    title: "use item in room: the guard 'controllerLevel < RoomControllerType.Rights' holds"
    given:
      guard: controllerLevel < RoomControllerType.Rights
      guard_holds: "true"
    when:
      packet: ChargeFirework
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ChargeFirework sent while 'controllerLevel < RoomControllerType.Rights' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_82c2d2746361
  - scenario: catalog.use_item_in_room.guard_6
    title: "use item in room: the guard 'item.OwnerId.Value != ctx.PlayerId.Value' holds"
    given:
      guard: item.OwnerId.Value != ctx.PlayerId.Value
      guard_holds: "true"
    when:
      packet: ChargeFirework
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ChargeFirework sent while 'item.OwnerId.Value != ctx.PlayerId.Value' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_82c2d2746361
  - scenario: catalog.use_item_in_room.guard_7
    title: "use item in room: the guard 'spaceItem is null' holds"
    given:
      guard: spaceItem is null
      guard_holds: "true"
    when:
      packet: ChargeFirework
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ChargeFirework sent while 'spaceItem is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_82c2d2746361
  - scenario: catalog.use_item_in_room.guard_8
    title: "use item in room: the guard '!_roomGrain.FurniModule.GetTileIdForFloorItem(spaceItem, ...' holds"
    given:
      guard: "!_roomGrain.FurniModule.GetTileIdForFloorItem(spaceItem, out List<int> spaceTileIds)"
      guard_holds: "true"
    when:
      packet: ChargeFirework
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ChargeFirework sent while '!_roomGrain.FurniModule.GetTileIdForFloorItem(spaceItem, out List<int> spaceTileIds)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_82c2d2746361
  - scenario: catalog.use_item_in_room.guard_9
    title: "use item in room: the guard '!InBounds(minX, minY)' holds"
    given:
      guard: "!InBounds(minX, minY)"
      guard_holds: "true"
    when:
      packet: ChargeFirework
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ChargeFirework sent while '!InBounds(minX, minY)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: rejected
      emits: []
      evidence: ev_c26714967e6b
  - scenario: catalog.use_item_in_room.success
    title: "use item in room: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ChargeFirework
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ChargeFirework succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_2298dc2dabc2
  evidence:
  - id: ev_2298dc2dabc2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Furni.cs
    symbol: RoomGrain
    line: 80
  - id: ev_7c6cea3bc17b
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/RoomService.cs
    symbol: RoomService
    line: 353
  - id: ev_82c2d2746361
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomSecurityModule.cs
    symbol: RoomSecurityModule
    line: 40
  - id: ev_8a097ef973ee
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomActionModule.cs
    symbol: RoomActionModule
    line: 85
  - id: ev_c26714967e6b
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomMapModule.cs
    symbol: RoomMapModule
    line: 255
verified: {}
manual: {}
