# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:8f75d25461d25617733c0836aa4d07fcfa3ea87a629236e546226e9e57ef96c2"
generated:
  feature: catalog.get_next_targeted_offer
  scenarios:
  - scenario: catalog.get_next_targeted_offer.guard_0
    title: "get next targeted offer: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetNextTargetedOffer
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetNextTargetedOffer sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_69f77077d5dd
  - scenario: catalog.get_next_targeted_offer.success
    title: "get next targeted offer: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetNextTargetedOffer
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetNextTargetedOffer succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0f24ea6318f8
  evidence:
  - id: ev_0f24ea6318f8
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Catalog/Grains/PlayerTargetedOfferGrain.cs
    symbol: PlayerTargetedOfferGrain
    line: 46
  - id: ev_69f77077d5dd
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Catalog/GetNextTargetedOfferMessageHandler.cs
    symbol: GetNextTargetedOfferMessageHandler
    line: 20
verified: {}
manual: {}
