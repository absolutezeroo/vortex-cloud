# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:55d1ba08869094e2c3dcdbf1140d4e39fc6f79259c04950c25e0ce760efad38c"
generated:
  feature: catalog.get_bundle_discount_ruleset
  scenarios:
  - scenario: catalog.get_bundle_discount_ruleset.success
    title: "get bundle discount ruleset: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetBundleDiscountRuleset
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetBundleDiscountRuleset succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - BundleDiscountRuleset
      evidence: ev_0128461dab48
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - BundleDiscountRuleset
      evidence: ev_11dcf3354114
  evidence:
  - id: ev_11dcf3354114
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/catalog/GetBundleDiscountRulesetEvent.java
    symbol: GetBundleDiscountRulesetEvent
    line: 8
    note: third-party server implementation; evidence, not authority
  - id: ev_0128461dab48
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Catalog/GetBundleDiscountRulesetMessageHandler.cs
    symbol: GetBundleDiscountRulesetMessageHandler
    line: 10
verified: {}
manual: {}
