# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:efa3f7bf239f0d68670e354ae7143b90f1dcd78210291cdb42f156824bda8525"
generated:
  feature: catalog.mark_catalog_new_additions_page_opened
  scenarios:
  - scenario: catalog.mark_catalog_new_additions_page_opened.success
    title: "mark catalog new additions page opened: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: MarkCatalogNewAdditionsPageOpened
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of MarkCatalogNewAdditionsPageOpened succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_953c9dcfc913
  evidence:
  - id: ev_953c9dcfc913
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Catalog/MarkCatalogNewAdditionsPageOpenedMessageHandler.cs
    symbol: MarkCatalogNewAdditionsPageOpenedMessageHandler
    line: 14
verified: {}
manual: {}
