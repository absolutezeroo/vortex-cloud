# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:98d6ad57e689db2086f015000ef1e6ccb29043d250e76319298a68f7c93f4f37"
generated:
  feature: catalog.set_targeted_offer_state
  scenarios:
  - scenario: catalog.set_targeted_offer_state.guard_0
    title: "set targeted offer state: the guard 'ctx.PlayerId <= 0 || message.TargetedOfferId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.TargetedOfferId <= 0
      guard_holds: "true"
    when:
      packet: SetTargetedOfferState
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SetTargetedOfferState sent while 'ctx.PlayerId <= 0 || message.TargetedOfferId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_164aaa724c27
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_6100c3828c70
  - scenario: catalog.set_targeted_offer_state.success
    title: "set targeted offer state: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: SetTargetedOfferState
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of SetTargetedOfferState succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0f24ea6318f8
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_6100c3828c70
  evidence:
  - id: ev_6100c3828c70
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/catalog/SetTargetedOfferStateEvent.java
    symbol: SetTargetedOfferStateEvent
    line: 8
    note: third-party server implementation; evidence, not authority
  - id: ev_0f24ea6318f8
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Catalog/Grains/PlayerTargetedOfferGrain.cs
    symbol: PlayerTargetedOfferGrain
    line: 207
  - id: ev_164aaa724c27
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Catalog/SetTargetedOfferStateMessageHandler.cs
    symbol: SetTargetedOfferStateMessageHandler
    line: 19
verified: {}
manual: {}
