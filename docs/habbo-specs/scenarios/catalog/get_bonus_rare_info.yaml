# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:3893746ebeee55f3d80ceba7bbaab9485ff11b76e3dff1a574f740e2b7dbaf6c"
generated:
  feature: catalog.get_bonus_rare_info
  scenarios:
  - scenario: catalog.get_bonus_rare_info.success
    title: "get bonus rare info: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetBonusRareInfo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetBonusRareInfo succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - BonusRareInfo
      evidence: ev_9d16c1bdc535
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - BonusRareInfo
      evidence: ev_f05a8473dc69
  evidence:
  - id: ev_f05a8473dc69
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/hotelview/GetBonusRareInfoMessageEvent.java
    symbol: GetBonusRareInfoMessageEvent
    line: 8
    note: third-party server implementation; evidence, not authority
  - id: ev_9d16c1bdc535
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Catalog/GetBonusRareInfoMessageHandler.cs
    symbol: GetBonusRareInfoMessageHandler
    line: 24
verified: {}
manual: {}
