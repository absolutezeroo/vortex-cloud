# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:1e1572ece4e482f65e6397177e7b99cfdd8ab6151b118140e9c8ed49a7115071"
generated:
  feature: catalog.get_all
  scenarios:
  - scenario: catalog.get_all.guard_0
    title: "get all: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetClubGiftInfoRequest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetClubGiftInfoRequest sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1c38d76a1be8
  - scenario: catalog.get_all.guard_1
    title: "get all: the guard 'baseOffer is null' holds"
    given:
      guard: baseOffer is null
      guard_holds: "true"
    when:
      packet: GetClubGiftInfoRequest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetClubGiftInfoRequest sent while 'baseOffer is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f6230c04a715
  - scenario: catalog.get_all.success
    title: "get all: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetClubGiftInfoRequest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetClubGiftInfoRequest succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - ClubGiftInfoEvent
      - HabboClubExtendOffer
      - HabboClubOffers
      evidence: ev_0c89a4c1bdef
  evidence:
  - id: ev_0c89a4c1bdef
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Catalog/GetClubOffersMessageHandler.cs
    symbol: GetClubOffersMessageHandler
    line: 30
  - id: ev_1c38d76a1be8
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Catalog/GetClubGiftInfoMessageHandler.cs
    symbol: GetClubGiftInfoMessageHandler
    line: 32
  - id: ev_f6230c04a715
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Catalog/GetHabboClubExtendOfferMessageHandler.cs
    symbol: GetHabboClubExtendOfferMessageHandler
    line: 51
verified: {}
manual: {}
