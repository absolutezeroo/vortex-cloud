# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:baee1aab9575c4b420dc77388406420c997a31f47d05cd0273f10e81afe1d4d2"
generated:
  feature: catalog.get_palettes_for_type
  scenarios:
  - scenario: catalog.get_palettes_for_type.guard_0
    title: "get palettes for type: the guard '_byType.TryGetValue(petType, out ImmutableArray<PetPalett...' holds"
    given:
      guard: _byType.TryGetValue(petType, out ImmutableArray<PetPaletteEntry> entries)
      guard_holds: "true"
    when:
      packet: GetSellablePetPalettes
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetSellablePetPalettes sent while '_byType.TryGetValue(petType, out ImmutableArray<PetPaletteEntry> entries)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_09434fe70e5d
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - SellablePetPalettes
      evidence: ev_2d77f3b37bc3
  - scenario: catalog.get_palettes_for_type.success
    title: "get palettes for type: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetSellablePetPalettes
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetSellablePetPalettes succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - SellablePetPalettes
      evidence: ev_09434fe70e5d
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - SellablePetPalettes
      evidence: ev_2d77f3b37bc3
  evidence:
  - id: ev_2d77f3b37bc3
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/catalog/GetSellablePetPalettesEvent.java
    symbol: GetSellablePetPalettesEvent
    line: 14
    note: third-party server implementation; evidence, not authority
  - id: ev_09434fe70e5d
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Providers/PetPaletteProvider.cs
    symbol: PetPaletteProvider
    line: 32
verified: {}
manual: {}
