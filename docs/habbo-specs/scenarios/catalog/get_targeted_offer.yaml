# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:7751a1607cf229e8398de72abcd9263e31073f76307a99d272ef3426ca6de80e"
generated:
  feature: catalog.get_targeted_offer
  scenarios:
  - scenario: catalog.get_targeted_offer.guard_0
    title: "get targeted offer: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetTargetedOffer
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetTargetedOffer sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a9978a478dcb
  - scenario: catalog.get_targeted_offer.success
    title: "get targeted offer: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetTargetedOffer
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetTargetedOffer succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0f24ea6318f8
  evidence:
  - id: ev_0f24ea6318f8
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Catalog/Grains/PlayerTargetedOfferGrain.cs
    symbol: PlayerTargetedOfferGrain
    line: 43
  - id: ev_a9978a478dcb
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Catalog/GetTargetedOfferMessageHandler.cs
    symbol: GetTargetedOfferMessageHandler
    line: 21
verified: {}
manual: {}
