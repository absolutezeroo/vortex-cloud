# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:dcf477b9b3625541193d3912893989ed51fd1ff904f7befd302df1333bc516e0"
generated:
  feature: moderation.mod_kick
  scenarios:
  - scenario: moderation.mod_kick.guard_0
    title: "mod kick: the guard 'ctx.PlayerId <= 0 || message.UserId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.UserId <= 0
      guard_holds: "true"
    when:
      packet: ModKick
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ModKick sent while 'ctx.PlayerId <= 0 || message.UserId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1d5fb3a1b3a4
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_907a939ef104
  - scenario: moderation.mod_kick.guard_1
    title: "mod kick: the guard 'ModerationPolicy.IsAllowed(actorPermissions, targetPermis...' holds"
    given:
      guard: ModerationPolicy.IsAllowed(actorPermissions, targetPermissions, action)
      guard_holds: "true"
    when:
      packet: ModKick
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ModKick sent while 'ModerationPolicy.IsAllowed(actorPermissions, targetPermissions, action)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_dd4e77d6bf65
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_907a939ef104
  - scenario: moderation.mod_kick.guard_2
    title: "mod kick: the guard 'playerId <= 0' holds"
    given:
      guard: playerId <= 0
      guard_holds: "true"
    when:
      packet: ModKick
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ModKick sent while 'playerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_743940bb69f7
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_907a939ef104
  - scenario: moderation.mod_kick.guard_3
    title: "mod kick: the guard '_registry is null' holds"
    given:
      guard: _registry is null
      guard_holds: "true"
    when:
      packet: ModKick
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ModKick sent while '_registry is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_34bf3ac088e2
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_907a939ef104
  - scenario: moderation.mod_kick.guard_4
    title: "mod kick: the guard 'actorCtx.PlayerId <= 0 || actorCtx.RoomId != _roomGrain._...' holds"
    given:
      guard: actorCtx.PlayerId <= 0 || actorCtx.RoomId != _roomGrain._state.RoomId || actorCtx.PlayerId == targetPlayerId
      guard_holds: "true"
    when:
      packet: ModKick
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ModKick sent while 'actorCtx.PlayerId <= 0 || actorCtx.RoomId != _roomGrain._state.RoomId || actorCtx.PlayerId == targetPlayerId' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_9eb84a2a2088
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_907a939ef104
  - scenario: moderation.mod_kick.guard_5
    title: "mod kick: the guard 'string.IsNullOrWhiteSpace(message)' holds"
    given:
      guard: string.IsNullOrWhiteSpace(message)
      guard_holds: "true"
    when:
      packet: ModKick
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ModKick sent while 'string.IsNullOrWhiteSpace(message)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_dd4e77d6bf65
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_907a939ef104
  - scenario: moderation.mod_kick.success
    title: "mod kick: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ModKick
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ModKick succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - ModeratorActionResult
      - ModeratorCautionEvent
      evidence: ev_1d5fb3a1b3a4
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_907a939ef104
  evidence:
  - id: ev_907a939ef104
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/modtool/ModKickMessageEvent.java
    symbol: ModKickMessageEvent
    line: 8
    note: third-party server implementation; evidence, not authority
  - id: ev_1d5fb3a1b3a4
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Moderator/ModKickMessageHandler.cs
    symbol: ModKickMessageHandler
    line: 27
  - id: ev_34bf3ac088e2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Events/EventSystem.cs
    symbol: EventSystem
    line: 16
  - id: ev_743940bb69f7
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Authentication/Permissions/PermissionService.cs
    symbol: PermissionService
    line: 62
  - id: ev_9eb84a2a2088
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomModerationSystem.cs
    symbol: RoomModerationSystem
    line: 33
  - id: ev_dd4e77d6bf65
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Moderator/ModToolActionHelper.cs
    symbol: ModToolActionHelper
    line: 52
verified: {}
manual: {}
