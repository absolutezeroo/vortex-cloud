# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:6e0978fd8bdd950d22f824bdd0a099bcdec9dcc71435fcd45e8a959e1f2d10aa"
generated:
  feature: moderation.guide_session_guide_decides
  scenarios:
  - scenario: moderation.guide_session_guide_decides.guard_0
    title: "guide session guide decides: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GuideSessionGuideDecides
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GuideSessionGuideDecides sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a96cf2a06b36
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_a458dc8242dc
  - scenario: moderation.guide_session_guide_decides.guard_1
    title: "guide session guide decides: the guard '!_requesterByOfferedGuide.Remove(guideId, out int request...' holds"
    given:
      guard: "!_requesterByOfferedGuide.Remove(guideId, out int requesterId)"
      guard_holds: "true"
    when:
      packet: GuideSessionGuideDecides
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GuideSessionGuideDecides sent while '!_requesterByOfferedGuide.Remove(guideId, out int requesterId)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_9c06d4d48950
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_a458dc8242dc
  - scenario: moderation.guide_session_guide_decides.guard_2
    title: "guide session guide decides: the guard '!_pendingByRequester.TryGetValue(requesterId, out Pending...' holds"
    given:
      guard: "!_pendingByRequester.TryGetValue(requesterId, out PendingRequest? request)"
      guard_holds: "true"
    when:
      packet: GuideSessionGuideDecides
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GuideSessionGuideDecides sent while '!_pendingByRequester.TryGetValue(requesterId, out PendingRequest? request)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_9c06d4d48950
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_a458dc8242dc
  - scenario: moderation.guide_session_guide_decides.guard_3
    title: "guide session guide decides: the guard 'accepted' holds"
    given:
      guard: accepted
      guard_holds: "true"
    when:
      packet: GuideSessionGuideDecides
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GuideSessionGuideDecides sent while 'accepted' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_9c06d4d48950
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_a458dc8242dc
  - scenario: moderation.guide_session_guide_decides.guard_4
    title: "guide session guide decides: the guard 'nextGuideId == 0' holds"
    given:
      guard: nextGuideId == 0
      guard_holds: "true"
    when:
      packet: GuideSessionGuideDecides
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GuideSessionGuideDecides sent while 'nextGuideId == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_9c06d4d48950
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_a458dc8242dc
  - scenario: moderation.guide_session_guide_decides.success
    title: "guide session guide decides: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GuideSessionGuideDecides
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GuideSessionGuideDecides succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_180137861fc1
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_a458dc8242dc
  evidence:
  - id: ev_a458dc8242dc
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/guides/GuideSessionGuideDecidesMessageEvent.java
    symbol: GuideSessionGuideDecidesMessageEvent
    line: 9
    note: third-party server implementation; evidence, not authority
  - id: ev_180137861fc1
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Help/GuideSessionGuideDecidesMessageHandler.cs
    symbol: GuideSessionGuideDecidesMessageHandler
    line: 16
  - id: ev_9c06d4d48950
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/GuideDirectoryGrain.cs
    symbol: GuideDirectoryGrain
    line: 159
  - id: ev_a96cf2a06b36
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Help/GuideSessionGuideDecidesMessageHandler.cs
    symbol: GuideSessionGuideDecidesMessageHandler
    line: 25
verified: {}
manual: {}
