# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:984228f1f3bb810268cc3e2864637c203e60a8cdd5a2b080bff6eabe29282b64"
generated:
  feature: moderation.get_pending_for_reporter
  scenarios:
  - scenario: moderation.get_pending_for_reporter.guard_0
    title: "get pending for reporter: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetPendingCallsForHelp
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetPendingCallsForHelp sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_d452905700d2
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - CallForHelpPendingCalls
      evidence: ev_0cdbbfd70d88
  - scenario: moderation.get_pending_for_reporter.guard_1
    title: "get pending for reporter: the guard 'reporterPlayerId <= 0' holds"
    given:
      guard: reporterPlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetPendingCallsForHelp
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetPendingCallsForHelp sent while 'reporterPlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_055f4068589c
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - CallForHelpPendingCalls
      evidence: ev_0cdbbfd70d88
  - scenario: moderation.get_pending_for_reporter.success
    title: "get pending for reporter: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetPendingCallsForHelp
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetPendingCallsForHelp succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - CallForHelpPendingCalls
      evidence: ev_055f4068589c
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - CallForHelpPendingCalls
      evidence: ev_0cdbbfd70d88
  evidence:
  - id: ev_0cdbbfd70d88
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/modtool/GetPendingCallsForHelpMessageEvent.java
    symbol: GetPendingCallsForHelpMessageEvent
    line: 9
    note: third-party server implementation; evidence, not authority
  - id: ev_055f4068589c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/CfhTicketService.cs
    symbol: CfhTicketService
    line: 449
  - id: ev_d452905700d2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Help/GetPendingCallsForHelpMessageHandler.cs
    symbol: GetPendingCallsForHelpMessageHandler
    line: 29
verified: {}
manual: {}
