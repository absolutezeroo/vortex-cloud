# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:233b6b6226d58f76cd837972d1b1f4ddb4eca54e5e3d2efd9d1cd9807d5acdd1"
generated:
  feature: moderation.get_quiz_questions
  scenarios:
  - scenario: moderation.get_quiz_questions.guard_0
    title: "get quiz questions: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetQuizQuestions
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetQuizQuestions sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_39dcba53e19d
  - scenario: moderation.get_quiz_questions.guard_1
    title: "get quiz questions: the guard 'quiz is null' holds"
    given:
      guard: quiz is null
      guard_holds: "true"
    when:
      packet: GetQuizQuestions
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetQuizQuestions sent while 'quiz is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_fd80c081bc3a
  - scenario: moderation.get_quiz_questions.guard_2
    title: "get quiz questions: the guard 'questions.Count == 0' holds"
    given:
      guard: questions.Count == 0
      guard_holds: "true"
    when:
      packet: GetQuizQuestions
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetQuizQuestions sent while 'questions.Count == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_fd80c081bc3a
  - scenario: moderation.get_quiz_questions.success
    title: "get quiz questions: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetQuizQuestions
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetQuizQuestions succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - QuizData
      evidence: ev_373e76aa8a80
  evidence:
  - id: ev_373e76aa8a80
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerPresenceGrain.cs
    symbol: PlayerPresenceGrain
    line: 112
  - id: ev_39dcba53e19d
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Help/GetQuizQuestionsMessageHandler.cs
    symbol: GetQuizQuestionsMessageHandler
    line: 21
  - id: ev_fd80c081bc3a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Social/Grains/PlayerQuizGrain.cs
    symbol: PlayerQuizGrain
    line: 51
verified: {}
manual: {}
