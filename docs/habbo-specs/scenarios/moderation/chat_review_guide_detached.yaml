# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:ee9657fb33a9866b12dd8c3c8a11175c3280af65e7c90799f53575782ca4ef08"
generated:
  feature: moderation.chat_review_guide_detached
  scenarios:
  - scenario: moderation.chat_review_guide_detached.guard_0
    title: "chat review guide detached: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: ChatReviewGuideDetached
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ChatReviewGuideDetached sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0efa7dcf5f29
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_5f86a6e707e1
  - scenario: moderation.chat_review_guide_detached.guard_1
    title: "chat review guide detached: the guard '!TryFindReview(guardianId, out ChatReview? review)' holds"
    given:
      guard: "!TryFindReview(guardianId, out ChatReview? review)"
      guard_holds: "true"
    when:
      packet: ChatReviewGuideDetached
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ChatReviewGuideDetached sent while '!TryFindReview(guardianId, out ChatReview? review)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_5ea072b947f5
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_5f86a6e707e1
  - scenario: moderation.chat_review_guide_detached.success
    title: "chat review guide detached: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ChatReviewGuideDetached
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ChatReviewGuideDetached succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0efa7dcf5f29
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_5f86a6e707e1
  evidence:
  - id: ev_5f86a6e707e1
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/guardians/ChatReviewGuideDetachedMessageEvent.java
    symbol: ChatReviewGuideDetachedMessageEvent
    line: 7
    note: third-party server implementation; evidence, not authority
  - id: ev_0efa7dcf5f29
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Help/ChatReviewGuideDetachedMessageHandler.cs
    symbol: ChatReviewGuideDetachedMessageHandler
    line: 24
  - id: ev_5ea072b947f5
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Social/Grains/GuideDirectoryGrain.cs
    symbol: GuideDirectoryGrain
    line: 362
verified: {}
manual: {}
