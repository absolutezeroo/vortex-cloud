# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:bab6f52ce7232e6f241f183c90c9aa3810d7839e96b48fe69a9eced47e5bc976"
generated:
  feature: moderation.guide_session_message
  scenarios:
  - scenario: moderation.guide_session_message.guard_0
    title: "guide session message: the guard 'ctx.PlayerId <= 0 || string.IsNullOrEmpty(message.Message)' holds"
    given:
      guard: ctx.PlayerId <= 0 || string.IsNullOrEmpty(message.Message)
      guard_holds: "true"
    when:
      packet: GuideSessionMessage
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GuideSessionMessage sent while 'ctx.PlayerId <= 0 || string.IsNullOrEmpty(message.Message)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_c2d3b5fb2daf
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_aca47f31c629
  - scenario: moderation.guide_session_message.guard_1
    title: "guide session message: the guard 'partnerId <= 0' holds"
    given:
      guard: partnerId <= 0
      guard_holds: "true"
    when:
      packet: GuideSessionMessage
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GuideSessionMessage sent while 'partnerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_c2d3b5fb2daf
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_aca47f31c629
  - scenario: moderation.guide_session_message.success
    title: "guide session message: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GuideSessionMessage
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GuideSessionMessage succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - GuideSessionMessage
      evidence: ev_5e4aba593bd7
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_aca47f31c629
  evidence:
  - id: ev_aca47f31c629
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/guides/GuideSessionMessageMessageEvent.java
    symbol: GuideSessionMessageMessageEvent
    line: 12
    note: third-party server implementation; evidence, not authority
  - id: ev_5e4aba593bd7
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Help/GuideSessionMessageMessageHandler.cs
    symbol: GuideSessionMessageMessageHandler
    line: 20
  - id: ev_c2d3b5fb2daf
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Help/GuideSessionMessageMessageHandler.cs
    symbol: GuideSessionMessageMessageHandler
    line: 29
verified: {}
manual: {}
