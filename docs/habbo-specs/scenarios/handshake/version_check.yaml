# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:0c503664a2503697630ebe60289cbc2ffda4d05b46cfcfa486691b2aac9a75bf"
generated:
  feature: handshake.version_check
  scenarios:
  - scenario: handshake.version_check.success
    title: "version check: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: VersionCheck
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of VersionCheck succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e07c4b8a9e7b
  evidence:
  - id: ev_e07c4b8a9e7b
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Handshake/VersionCheckMessageHandler.cs
    symbol: VersionCheckMessageHandler
    line: 8
verified: {}
manual: {}
