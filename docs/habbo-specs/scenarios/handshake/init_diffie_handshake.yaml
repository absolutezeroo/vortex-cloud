# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:c62e6091647f2015385792f08007ece42759cf9de69b1d8c1df3db39e8f4847b"
generated:
  feature: handshake.init_diffie_handshake
  scenarios:
  - scenario: handshake.init_diffie_handshake.success
    title: "init diffie handshake: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: InitDiffieHandshake
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of InitDiffieHandshake succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - InitDiffieHandshake
      evidence: ev_71a5159e2d62
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - InitDiffieHandshake
      evidence: ev_177d9358f957
  evidence:
  - id: ev_177d9358f957
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/handshake/InitDiffieHandshakeEvent.java
    symbol: InitDiffieHandshakeEvent
    line: 12
    note: third-party server implementation; evidence, not authority
  - id: ev_71a5159e2d62
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Handshake/InitDiffieHandshakeMessageHandler.cs
    symbol: InitDiffieHandshakeMessageHandler
    line: 25
verified: {}
manual: {}
