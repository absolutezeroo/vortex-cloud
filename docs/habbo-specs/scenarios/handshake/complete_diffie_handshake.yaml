# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:601a2c409ec0781e258f200632910a42cae60755b4a1133c9e156eb7be272601"
generated:
  feature: handshake.complete_diffie_handshake
  scenarios:
  - scenario: handshake.complete_diffie_handshake.success
    title: "complete diffie handshake: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: CompleteDiffieHandshake
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of CompleteDiffieHandshake succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - CompleteDiffieHandshake
      evidence: ev_4ee288142db5
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - CompleteDiffieHandshake
      evidence: ev_b65f089b022b
  evidence:
  - id: ev_b65f089b022b
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/handshake/CompleteDiffieHandshakeEvent.java
    symbol: CompleteDiffieHandshakeEvent
    line: 16
    note: third-party server implementation; evidence, not authority
  - id: ev_4ee288142db5
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Handshake/CompleteDiffieHandshakeMessageHandler.cs
    symbol: CompleteDiffieHandshakeMessageHandler
    line: 12
verified: {}
manual: {}
