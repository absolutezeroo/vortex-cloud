# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:515783b09e8806c1bbe692dcb2e30843e5c52dde817ca64bd353889049ac4855"
generated:
  feature: handshake.client_hello
  scenarios:
  - scenario: handshake.client_hello.guard_0
    title: "client hello: the guard 'message.Production is null' holds"
    given:
      guard: message.Production is null
      guard_holds: "true"
    when:
      packet: ClientHello
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ClientHello sent while 'message.Production is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_bff19b686037
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_101638482dcc
  - scenario: handshake.client_hello.success
    title: "client hello: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ClientHello
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ClientHello succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_4ef51b628e13
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_101638482dcc
  evidence:
  - id: ev_101638482dcc
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/handshake/ClientHelloMessageEvent.java
    symbol: ClientHelloMessageEvent
    line: 10
    note: third-party server implementation; evidence, not authority
  - id: ev_4ef51b628e13
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Handshake/ClientHelloMessageHandler.cs
    symbol: ClientHelloMessageHandler
    line: 8
  - id: ev_bff19b686037
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Handshake/ClientHelloMessageHandler.cs
    symbol: ClientHelloMessageHandler
    line: 16
verified: {}
manual: {}
