# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:a91cbd60d56ec3523769de4970a2555b1d2ad390932c0170d3ea66f1104582a3"
generated:
  feature: handshake.pong
  scenarios:
  - scenario: handshake.pong.success
    title: "pong: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: Pong
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of Pong succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0db040bfafc5
  evidence:
  - id: ev_0db040bfafc5
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Handshake/PongMessageHandler.cs
    symbol: PongMessageHandler
    line: 8
verified: {}
manual: {}
