# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:258e5205f2d738937544fb5d9f2ce22dd0b44cae88603432ee54315d145a6ec3"
generated:
  feature: preferences.remove_from_custom_filter
  scenarios:
  - scenario: preferences.remove_from_custom_filter.guard_0
    title: "remove from custom filter: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: RemoveFromCustomFilter
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RemoveFromCustomFilter sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_2f274b6520f2
  - scenario: preferences.remove_from_custom_filter.guard_1
    title: "remove from custom filter: the guard 'normalized.Length == 0' holds"
    given:
      guard: normalized.Length == 0
      guard_holds: "true"
    when:
      packet: RemoveFromCustomFilter
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RemoveFromCustomFilter sent while 'normalized.Length == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a67717eb79da
  - scenario: preferences.remove_from_custom_filter.guard_2
    title: "remove from custom filter: the guard 'entity is null' holds"
    given:
      guard: entity is null
      guard_holds: "true"
    when:
      packet: RemoveFromCustomFilter
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RemoveFromCustomFilter sent while 'entity is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a67717eb79da
  - scenario: preferences.remove_from_custom_filter.success
    title: "remove from custom filter: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RemoveFromCustomFilter
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RemoveFromCustomFilter succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - ModifyCustomFilterResult
      evidence: ev_2f274b6520f2
  evidence:
  - id: ev_2f274b6520f2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Preferences/RemoveFromCustomFilterMessageHandler.cs
    symbol: RemoveFromCustomFilterMessageHandler
    line: 23
  - id: ev_a67717eb79da
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerGrain.WordFilter.cs
    symbol: PlayerGrain
    line: 92
verified: {}
manual: {}
