# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:0481d58ee743d54ae1bed666543850ed166e2143f34af229274a5627b2497e10"
generated:
  feature: preferences.get_custom_filter
  scenarios:
  - scenario: preferences.get_custom_filter.guard_0
    title: "get custom filter: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetCustomFilter
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetCustomFilter sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_b1e742a54bcb
  - scenario: preferences.get_custom_filter.success
    title: "get custom filter: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetCustomFilter
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetCustomFilter succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - GetCustomFilterResult
      evidence: ev_a67717eb79da
  evidence:
  - id: ev_a67717eb79da
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerGrain.WordFilter.cs
    symbol: PlayerGrain
    line: 25
  - id: ev_b1e742a54bcb
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Preferences/GetCustomFilterMessageHandler.cs
    symbol: GetCustomFilterMessageHandler
    line: 23
verified: {}
manual: {}
