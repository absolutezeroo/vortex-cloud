# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:9c824a56bee7fbd1d225d4434270531b64f928001e8e9b7b3aa7b8a2a2003239"
generated:
  feature: preferences.set_ui_flags
  scenarios:
  - scenario: preferences.set_ui_flags.guard_0
    title: "set ui flags: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: SetUIFlags
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SetUIFlags sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_d8e926fbfb76
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - PerkAllowances
      evidence: ev_adf6c8637823
  - scenario: preferences.set_ui_flags.success
    title: "set ui flags: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: SetUIFlags
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of SetUIFlags succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1b14f4bad519
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - PerkAllowances
      evidence: ev_adf6c8637823
  evidence:
  - id: ev_adf6c8637823
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/users/SetUIFlagsMessageEvent.java
    symbol: SetUIFlagsMessageEvent
    line: 9
    note: third-party server implementation; evidence, not authority
  - id: ev_1b14f4bad519
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Preferences/SetUIFlagsMessageHandler.cs
    symbol: SetUIFlagsMessageHandler
    line: 11
  - id: ev_d8e926fbfb76
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Preferences/SetUIFlagsMessageHandler.cs
    symbol: SetUIFlagsMessageHandler
    line: 22
verified: {}
manual: {}
