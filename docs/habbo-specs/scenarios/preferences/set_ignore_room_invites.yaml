# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:4d41154878b2bd96bf09e5aeeb8f662609f34fce6ef1b09727e24c036b5031c3"
generated:
  feature: preferences.set_ignore_room_invites
  scenarios:
  - scenario: preferences.set_ignore_room_invites.guard_0
    title: "set ignore room invites: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: SetIgnoreRoomInvites
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SetIgnoreRoomInvites sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_04ce85b231e9
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_eeeee4bcba72
  - scenario: preferences.set_ignore_room_invites.success
    title: "set ignore room invites: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: SetIgnoreRoomInvites
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of SetIgnoreRoomInvites succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_04ce85b231e9
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_eeeee4bcba72
  evidence:
  - id: ev_eeeee4bcba72
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/users/SetIgnoreRoomInvitesMessageEvent.java
    symbol: SetIgnoreRoomInvitesMessageEvent
    line: 9
    note: third-party server implementation; evidence, not authority
  - id: ev_04ce85b231e9
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Preferences/SetIgnoreRoomInvitesMessageHandler.cs
    symbol: SetIgnoreRoomInvitesMessageHandler
    line: 22
verified: {}
manual: {}
