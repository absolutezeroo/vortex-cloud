# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:84b267fd1fd8faa7e8cdfad2e99430658946a306044acd2be35c516b1351867f"
generated:
  feature: preferences.set_discord_preferences
  scenarios:
  - scenario: preferences.set_discord_preferences.guard_0
    title: "set discord preferences: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: SetDiscordPreferences
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SetDiscordPreferences sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_dc5e70e97795
  - scenario: preferences.set_discord_preferences.success
    title: "set discord preferences: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: SetDiscordPreferences
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of SetDiscordPreferences succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - DiscordPreferences
      evidence: ev_5d7cdeac355a
  evidence:
  - id: ev_5d7cdeac355a
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Preferences/DiscordPreferencesHandlers.cs
    symbol: SetDiscordPreferencesMessageHandler
    line: 54
  - id: ev_dc5e70e97795
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Preferences/DiscordPreferencesHandlers.cs
    symbol: SetDiscordPreferencesMessageHandler
    line: 63
verified: {}
manual: {}
