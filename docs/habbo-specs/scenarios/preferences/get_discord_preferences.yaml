# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:d507606501b3a30f393b356aa2f508ca8243749c71d8fdfca912fdbe8cc1f299"
generated:
  feature: preferences.get_discord_preferences
  scenarios:
  - scenario: preferences.get_discord_preferences.guard_0
    title: "get discord preferences: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetDiscordPreferences
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetDiscordPreferences sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_36fbf55f14c1
  - scenario: preferences.get_discord_preferences.success
    title: "get discord preferences: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetDiscordPreferences
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetDiscordPreferences succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - DiscordPreferences
      - VortexFurniDefinition
      evidence: ev_36fbf55f14c1
  evidence:
  - id: ev_36fbf55f14c1
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Preferences/DiscordPreferencesHandlers.cs
    symbol: GetDiscordPreferencesMessageHandler
    line: 36
verified: {}
manual: {}
