# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:bdd2e839dab0c93410e5710d64a2032f2f4961ce6b975ab7595c4e5adc36fca1"
generated:
  feature: sound.get_jukebox_play_list
  scenarios:
  - scenario: sound.get_jukebox_play_list.success
    title: "get jukebox play list: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetJukeboxPlayList
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetJukeboxPlayList succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a452a691fccf
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - JukeboxSongDisks
      - UserSongDisksInventory
      evidence: ev_3d8adc6239e7
  evidence:
  - id: ev_3d8adc6239e7
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/items/jukebox/GetJukeboxPlayListMessageEvent.java
    symbol: GetJukeboxPlayListMessageEvent
    line: 10
    note: third-party server implementation; evidence, not authority
  - id: ev_a452a691fccf
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Sound/GetJukeboxPlayListMessageHandler.cs
    symbol: GetJukeboxPlayListMessageHandler
    line: 8
verified: {}
manual: {}
