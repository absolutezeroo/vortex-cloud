# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:202e3e12ad0270ecbc1253f950d67b825c694119fd39f08c2c0674a1a674a916"
generated:
  feature: sound.get_song_info
  scenarios:
  - scenario: sound.get_song_info.success
    title: "get song info: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetSongInfo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetSongInfo succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_b3c548c2a9ea
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - TraxSongInfo
      evidence: ev_39a4301b86e3
  evidence:
  - id: ev_39a4301b86e3
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/catalog/GetSongInfoMessageEvent.java
    symbol: GetSongInfoMessageEvent
    line: 13
    note: third-party server implementation; evidence, not authority
  - id: ev_b3c548c2a9ea
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Sound/GetSongInfoMessageHandler.cs
    symbol: GetSongInfoMessageHandler
    line: 8
verified: {}
manual: {}
