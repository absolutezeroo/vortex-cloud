# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:34d8ccc4246fda8b77ae8bcd991debd3bbb7c58365367469084dc1b226539f53"
generated:
  feature: sound.get_sound_settings
  scenarios:
  - scenario: sound.get_sound_settings.guard_0
    title: "get sound settings: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetSoundSettings
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetSoundSettings sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e547d106cfb0
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - AccountPreferences
      evidence: ev_35e0c7c1c5ac
  - scenario: sound.get_sound_settings.success
    title: "get sound settings: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetSoundSettings
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetSoundSettings succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - AccountPreferencesEvent
      - unresolved
      evidence: ev_1f83e61934ca
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - AccountPreferences
      evidence: ev_35e0c7c1c5ac
  evidence:
  - id: ev_35e0c7c1c5ac
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/users/GetSoundSettingsEvent.java
    symbol: GetSoundSettingsEvent
    line: 8
    note: third-party server implementation; evidence, not authority
  - id: ev_1f83e61934ca
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerGrain.cs
    symbol: PlayerGrain
    line: 216
  - id: ev_e547d106cfb0
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Sound/GetSoundSettingsMessageHandler.cs
    symbol: GetSoundSettingsMessageHandler
    line: 29
verified: {}
manual: {}
