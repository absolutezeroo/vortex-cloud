# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:7f8fc45301324344c6ac3be90a156ecdba0a39e9aef475bc4a56ee6ca2c90b37"
generated:
  feature: sound.get_now_playing
  scenarios:
  - scenario: sound.get_now_playing.guard_0
    title: "get now playing: the guard 'ctx.RoomId <= 0' holds"
    given:
      guard: ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: GetNowPlaying
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetNowPlaying sent while 'ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_25999e608f14
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - JukeboxSongDisks
      - UserSongDisksInventory
      evidence: ev_0c29e0399130
  - scenario: sound.get_now_playing.success
    title: "get now playing: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetNowPlaying
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetNowPlaying succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - NowPlaying
      evidence: ev_25999e608f14
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - JukeboxSongDisks
      - UserSongDisksInventory
      evidence: ev_0c29e0399130
  evidence:
  - id: ev_0c29e0399130
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/items/jukebox/GetNowPlayingMessageEvent.java
    symbol: GetNowPlayingMessageEvent
    line: 10
    note: third-party server implementation; evidence, not authority
  - id: ev_25999e608f14
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Sound/GetNowPlayingMessageHandler.cs
    symbol: GetNowPlayingMessageHandler
    line: 31
verified: {}
manual: {}
