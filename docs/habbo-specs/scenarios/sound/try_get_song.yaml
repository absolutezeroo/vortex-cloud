# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:bb8e5d24e58d2ad03f799dbbb18db4e9e7321d74eecf5336e7ac1c88f75da74d"
generated:
  feature: sound.try_get_song
  scenarios:
  - scenario: sound.try_get_song.guard_0
    title: "try get song: the guard 'message.SongIds.IsDefaultOrEmpty' holds"
    given:
      guard: message.SongIds.IsDefaultOrEmpty
      guard_holds: "true"
    when:
      packet: GetSongInfo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetSongInfo sent while 'message.SongIds.IsDefaultOrEmpty' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_498f9235254e
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - TraxSongInfo
      evidence: ev_9469cbc113c0
  - scenario: sound.try_get_song.guard_1
    title: "try get song: the guard 'found.Count == 0' holds"
    given:
      guard: found.Count == 0
      guard_holds: "true"
    when:
      packet: GetSongInfo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetSongInfo sent while 'found.Count == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_498f9235254e
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - TraxSongInfo
      evidence: ev_9469cbc113c0
  - scenario: sound.try_get_song.success
    title: "try get song: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetSongInfo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetSongInfo succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - TraxSongInfo
      evidence: ev_498f9235254e
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - TraxSongInfo
      evidence: ev_9469cbc113c0
  evidence:
  - id: ev_9469cbc113c0
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/catalog/GetSongInfoMessageEvent.java
    symbol: GetSongInfoMessageEvent
    line: 13
    note: third-party server implementation; evidence, not authority
  - id: ev_498f9235254e
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Sound/GetSongInfoMessageHandler.cs
    symbol: GetSongInfoMessageHandler
    line: 34
verified: {}
manual: {}
