# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:0351ffef394cc5053d4230cf2139c42ab62ad65564a2a551e207f993c918f136"
generated:
  feature: sound.get_user_song_disks
  scenarios:
  - scenario: sound.get_user_song_disks.guard_0
    title: "get user song disks: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetUserSongDisks
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetUserSongDisks sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_4a53aaff4f3b
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - JukeboxSongDisks
      - UserSongDisksInventory
      evidence: ev_5c2fb0a0e101
  - scenario: sound.get_user_song_disks.success
    title: "get user song disks: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetUserSongDisks
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetUserSongDisks succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - UserSongDisksInventory
      evidence: ev_22a4c945253e
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - JukeboxSongDisks
      - UserSongDisksInventory
      evidence: ev_5c2fb0a0e101
  evidence:
  - id: ev_5c2fb0a0e101
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/items/jukebox/GetUserSongDisksMessageEvent.java
    symbol: GetUserSongDisksMessageEvent
    line: 10
    note: third-party server implementation; evidence, not authority
  - id: ev_22a4c945253e
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Primitives/Sound/SongDiskExtraData.cs
    symbol: SongDiskExtraData
    line: 15
  - id: ev_4a53aaff4f3b
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Sound/GetUserSongDisksMessageHandler.cs
    symbol: GetUserSongDisksMessageHandler
    line: 25
verified: {}
manual: {}
