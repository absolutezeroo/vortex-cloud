# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:d1754dfb0b2182723b029bb7cc266d0d7b4a557adfd5100abc1b5c19bc296221"
generated:
  feature: sound.add_jukebox_disk
  scenarios:
  - scenario: sound.add_jukebox_disk.guard_0
    title: "add jukebox disk: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: AddJukeboxDisk
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AddJukeboxDisk sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_9bb5203f8d83
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_8ca52059ecf8
  - scenario: sound.add_jukebox_disk.success
    title: "add jukebox disk: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: AddJukeboxDisk
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of AddJukeboxDisk succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - JukeboxPlayListFull
      evidence: ev_2a13c19722fd
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_8ca52059ecf8
  evidence:
  - id: ev_8ca52059ecf8
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/items/jukebox/AddJukeboxDiskEvent.java
    symbol: AddJukeboxDiskEvent
    line: 10
    note: third-party server implementation; evidence, not authority
  - id: ev_2a13c19722fd
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Sound/AddJukeboxDiskMessageHandler.cs
    symbol: AddJukeboxDiskMessageHandler
    line: 21
  - id: ev_9bb5203f8d83
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Sound/AddJukeboxDiskMessageHandler.cs
    symbol: AddJukeboxDiskMessageHandler
    line: 32
verified: {}
manual: {}
