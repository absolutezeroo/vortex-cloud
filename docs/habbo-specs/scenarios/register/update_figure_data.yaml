# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:acfe63e025ef0a52a4c2e8fd8d3e864659b195e5cf4d91f5a88db46ddc95601a"
generated:
  feature: register.update_figure_data
  scenarios:
  - scenario: register.update_figure_data.guard_0
    title: "update figure data: the guard 'ctx.PlayerId < 0' holds"
    given:
      guard: ctx.PlayerId < 0
      guard_holds: "true"
    when:
      packet: UpdateFigureData
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateFigureData sent while 'ctx.PlayerId < 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_c1a848c3d73c
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - FigureUpdate
      evidence: ev_ff862bc62256
  - scenario: register.update_figure_data.guard_1
    title: "update figure data: the guard 'message.Figure.Length > FigureString.MaxLength' holds"
    given:
      guard: message.Figure.Length > FigureString.MaxLength
      guard_holds: "true"
    when:
      packet: UpdateFigureData
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateFigureData sent while 'message.Figure.Length > FigureString.MaxLength' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_c1a848c3d73c
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - FigureUpdate
      evidence: ev_ff862bc62256
  - scenario: register.update_figure_data.guard_2
    title: "update figure data: the guard '!await MayWearAsync(message.Figure, ctx.PlayerId, ct).Con...' holds"
    given:
      guard: "!await MayWearAsync(message.Figure, ctx.PlayerId, ct).ConfigureAwait(false)"
      guard_holds: "true"
    when:
      packet: UpdateFigureData
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateFigureData sent while '!await MayWearAsync(message.Figure, ctx.PlayerId, ct).ConfigureAwait(false)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_c1a848c3d73c
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - FigureUpdate
      evidence: ev_ff862bc62256
  - scenario: register.update_figure_data.guard_3
    title: "update figure data: the guard '_registry is null' holds"
    given:
      guard: _registry is null
      guard_holds: "true"
    when:
      packet: UpdateFigureData
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateFigureData sent while '_registry is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_34bf3ac088e2
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - FigureUpdate
      evidence: ev_ff862bc62256
  - scenario: register.update_figure_data.success
    title: "update figure data: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: UpdateFigureData
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of UpdateFigureData succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - FigureUpdateEvent
      evidence: ev_1f83e61934ca
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - FigureUpdate
      evidence: ev_ff862bc62256
  evidence:
  - id: ev_ff862bc62256
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/users/UpdateFigureDataMessageEvent.java
    symbol: UpdateFigureDataMessageEvent
    line: 19
    note: third-party server implementation; evidence, not authority
  - id: ev_1f83e61934ca
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerGrain.cs
    symbol: PlayerGrain
    line: 65
  - id: ev_34bf3ac088e2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Events/EventSystem.cs
    symbol: EventSystem
    line: 16
  - id: ev_c1a848c3d73c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Register/UpdateFigureDataMessageHandler.cs
    symbol: UpdateFigureDataMessageHandler
    line: 57
verified: {}
manual: {}
