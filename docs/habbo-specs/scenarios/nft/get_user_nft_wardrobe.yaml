# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:9c0935a94c2bdb7c2a883d78556b8d5b9f6ba92e7bf09bbfa92485a0579f131e"
generated:
  feature: nft.get_user_nft_wardrobe
  scenarios:
  - scenario: nft.get_user_nft_wardrobe.guard_0
    title: "get user nft wardrobe: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetUserNftWardrobe
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetUserNftWardrobe sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_2839622f7a45
  - scenario: nft.get_user_nft_wardrobe.success
    title: "get user nft wardrobe: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetUserNftWardrobe
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetUserNftWardrobe succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - UserNftWardrobe
      evidence: ev_2839622f7a45
  evidence:
  - id: ev_2839622f7a45
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Nft/GetUserNftWardrobeMessageHandler.cs
    symbol: GetUserNftWardrobeMessageHandler
    line: 32
verified: {}
manual: {}
