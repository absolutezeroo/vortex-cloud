# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:a310a7541c9a8530d5c232c6917a5553e4606a231ade0e7fd6ceaed9b57df299"
generated:
  feature: nft.get_silver
  scenarios:
  - scenario: nft.get_silver.guard_0
    title: "get silver: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetSilver
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetSilver sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f5568474a4eb
  - scenario: nft.get_silver.success
    title: "get silver: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetSilver
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetSilver succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - SilverBalance
      evidence: ev_8f169b1609b2
  evidence:
  - id: ev_8f169b1609b2
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Nft/GetSilverMessageHandler.cs
    symbol: GetSilverMessageHandler
    line: 14
  - id: ev_f5568474a4eb
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Nft/GetSilverMessageHandler.cs
    symbol: GetSilverMessageHandler
    line: 24
verified: {}
manual: {}
