# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:9acc4df3bcbae555d1faa5c45c80b4598249b7447a601e8b5903997023f831a6"
generated:
  feature: nft.get_nft_credits
  scenarios:
  - scenario: nft.get_nft_credits.guard_0
    title: "get nft credits: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetNftCredits
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetNftCredits sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_bf6145799d88
  - scenario: nft.get_nft_credits.success
    title: "get nft credits: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetNftCredits
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetNftCredits succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - EmeraldBalance
      - SilverBalance
      evidence: ev_8a9033c422d1
  evidence:
  - id: ev_8a9033c422d1
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Nft/GetNftCreditsMessageHandler.cs
    symbol: GetNftCreditsMessageHandler
    line: 14
  - id: ev_bf6145799d88
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Nft/GetNftCreditsMessageHandler.cs
    symbol: GetNftCreditsMessageHandler
    line: 25
verified: {}
manual: {}
