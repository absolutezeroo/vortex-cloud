# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:9dc7908f62791afcbb7f6ae6711c7487488b2971972664e91e55d6d12d0fa711"
generated:
  feature: room.avatar_expression
  scenarios:
  - scenario: room.avatar_expression.guard_0
    title: "avatar expression: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || me...' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.ExpressionId < 0
      guard_holds: "true"
    when:
      packet: AvatarExpression
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AvatarExpression sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.ExpressionId < 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_31d0be6a25b4
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - Expression
      evidence: ev_009baa8cac5c
  - scenario: room.avatar_expression.guard_1
    title: "avatar expression: the guard '!_state.AvatarsByPlayerId.TryGetValue(ctx.PlayerId, out R...' holds"
    given:
      guard: "!_state.AvatarsByPlayerId.TryGetValue(ctx.PlayerId, out RoomObjectId objectId) || !await AvatarModule.SetAvatarExpressionAsync(objectId, expressionType, ct)"
      guard_holds: "true"
    when:
      packet: AvatarExpression
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AvatarExpression sent while '!_state.AvatarsByPlayerId.TryGetValue(ctx.PlayerId, out RoomObjectId objectId) || !await AvatarModule.SetAvatarExpressionAsync(objectId, expressionType, ct)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ac641f7283b9
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - Expression
      evidence: ev_009baa8cac5c
  - scenario: room.avatar_expression.guard_2
    title: "avatar expression: the guard 'objectId <= 0 || !_roomGrain._state.AvatarsByObjectId.Try...' holds"
    given:
      guard: objectId <= 0 || !_roomGrain._state.AvatarsByObjectId.TryGetValue( objectId.Value, out IRoomAvatar? avatar )
      guard_holds: "true"
    when:
      packet: AvatarExpression
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AvatarExpression sent while 'objectId <= 0 || !_roomGrain._state.AvatarsByObjectId.TryGetValue( objectId.Value, out IRoomAvatar? avatar )' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e6fef545a42a
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - Expression
      evidence: ev_009baa8cac5c
  - scenario: room.avatar_expression.guard_3
    title: "avatar expression: the guard '_registry is null' holds"
    given:
      guard: _registry is null
      guard_holds: "true"
    when:
      packet: AvatarExpression
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AvatarExpression sent while '_registry is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_34bf3ac088e2
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - Expression
      evidence: ev_009baa8cac5c
  - scenario: room.avatar_expression.success
    title: "avatar expression: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: AvatarExpression
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of AvatarExpression succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - Expression
      evidence: ev_31d0be6a25b4
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - Expression
      evidence: ev_009baa8cac5c
  evidence:
  - id: ev_009baa8cac5c
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/users/AvatarExpressionMessageEvent.java
    symbol: AvatarExpressionMessageEvent
    line: 14
    note: third-party server implementation; evidence, not authority
  - id: ev_31d0be6a25b4
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Avatar/AvatarExpressionMessageHandler.cs
    symbol: AvatarExpressionMessageHandler
    line: 24
  - id: ev_34bf3ac088e2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Events/EventSystem.cs
    symbol: EventSystem
    line: 16
  - id: ev_ac641f7283b9
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Avatar.cs
    symbol: RoomGrain
    line: 283
  - id: ev_e6fef545a42a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomAvatarModule.cs
    symbol: RoomAvatarModule
    line: 658
verified: {}
manual: {}
