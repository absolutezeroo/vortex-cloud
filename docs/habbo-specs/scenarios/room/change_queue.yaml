# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:c4ec91c2b24039ba314b67bd9e1c439e56adfb69e9d06fa2e417f869625766c4"
generated:
  feature: room.change_queue
  scenarios:
  - scenario: room.change_queue.success
    title: "change queue: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ChangeQueue
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ChangeQueue succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_9dfbca6b7317
  evidence:
  - id: ev_9dfbca6b7317
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Session/ChangeQueueMessageHandler.cs
    symbol: ChangeQueueMessageHandler
    line: 8
verified: {}
manual: {}
