# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:7e3cf549d5180d70327ae80233a06457a29d502b0629455e4e723c19e25fa1dd"
generated:
  feature: room.extend_rent_or_buyout_strip_item
  scenarios:
  - scenario: room.extend_rent_or_buyout_strip_item.success
    title: "extend rent or buyout strip item: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ExtendRentOrBuyoutStripItem
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ExtendRentOrBuyoutStripItem succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_54f27bdd77d5
  evidence:
  - id: ev_54f27bdd77d5
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Furniture/ExtendRentOrBuyoutStripItemMessageHandler.cs
    symbol: ExtendRentOrBuyoutStripItemMessageHandler
    line: 8
verified: {}
manual: {}
