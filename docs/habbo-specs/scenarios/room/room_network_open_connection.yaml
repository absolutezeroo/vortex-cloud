# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:a7f183a402788df476da9b0e35582d7b35600b07f8fcb8b72db69d824b9da84e"
generated:
  feature: room.room_network_open_connection
  scenarios:
  - scenario: room.room_network_open_connection.success
    title: "room network open connection: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RoomNetworkOpenConnection
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RoomNetworkOpenConnection succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a878d839653b
  evidence:
  - id: ev_a878d839653b
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/RoomDirectory/RoomNetworkOpenConnectionMessageHandler.cs
    symbol: RoomNetworkOpenConnectionMessageHandler
    line: 8
verified: {}
manual: {}
