# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:310a6024c4c48bcdad71a9469ad06cb870b066915ee035933626331242a03da8"
generated:
  feature: room.room_dimmer_get_presets
  scenarios:
  - scenario: room.room_dimmer_get_presets.guard_0
    title: "room dimmer get presets: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: RoomDimmerGetPresets
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RoomDimmerGetPresets sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_75b51ff43f71
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - RoomDimmerPresets
      evidence: ev_2ecbb4c06165
  - scenario: room.room_dimmer_get_presets.guard_1
    title: "room dimmer get presets: the guard 'state is null' holds"
    given:
      guard: state is null
      guard_holds: "true"
    when:
      packet: RoomDimmerGetPresets
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RoomDimmerGetPresets sent while 'state is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_75b51ff43f71
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - RoomDimmerPresets
      evidence: ev_2ecbb4c06165
  - scenario: room.room_dimmer_get_presets.success
    title: "room dimmer get presets: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RoomDimmerGetPresets
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RoomDimmerGetPresets succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - RoomDimmerPresets
      evidence: ev_40274c7704f4
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - RoomDimmerPresets
      evidence: ev_2ecbb4c06165
  evidence:
  - id: ev_2ecbb4c06165
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/items/RoomDimmerGetPresetsMessageEvent.java
    symbol: RoomDimmerGetPresetsMessageEvent
    line: 8
    note: third-party server implementation; evidence, not authority
  - id: ev_40274c7704f4
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Furniture/RoomDimmerGetPresetsMessageHandler.cs
    symbol: RoomDimmerGetPresetsMessageHandler
    line: 17
  - id: ev_75b51ff43f71
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Furniture/RoomDimmerGetPresetsMessageHandler.cs
    symbol: RoomDimmerGetPresetsMessageHandler
    line: 26
verified: {}
manual: {}
