# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:72ee1d3ebf195b775b188c73052619131b393cf23a2a55c29d1113a652f5707b"
generated:
  feature: room.get_enabled_command_ids
  scenarios:
  - scenario: room.get_enabled_command_ids.guard_0
    title: "get enabled command ids: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0
      guard_holds: "true"
    when:
      packet: GetPetCommands
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetPetCommands sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f5497704ca87
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - PetTrainingPanel
      evidence: ev_1958d0c42caa
  - scenario: room.get_enabled_command_ids.guard_1
    title: "get enabled command ids: the guard 'pet is null' holds"
    given:
      guard: pet is null
      guard_holds: "true"
    when:
      packet: GetPetCommands
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetPetCommands sent while 'pet is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f5497704ca87
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - PetTrainingPanel
      evidence: ev_1958d0c42caa
  - scenario: room.get_enabled_command_ids.success
    title: "get enabled command ids: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetPetCommands
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetPetCommands succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - PetCommands
      evidence: ev_1c352a1c81b4
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - PetTrainingPanel
      evidence: ev_1958d0c42caa
  evidence:
  - id: ev_1958d0c42caa
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/pets/GetPetCommandsMessageEvent.java
    symbol: GetPetCommandsMessageEvent
    line: 9
    note: third-party server implementation; evidence, not authority
  - id: ev_1c352a1c81b4
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Pets/PetSelectedMessageHandler.cs
    symbol: PetSelectedMessageHandler
    line: 29
  - id: ev_f5497704ca87
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/GetPetCommandsMessageHandler.cs
    symbol: GetPetCommandsMessageHandler
    line: 29
verified: {}
manual: {}
