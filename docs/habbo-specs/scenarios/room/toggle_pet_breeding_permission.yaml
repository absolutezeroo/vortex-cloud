# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:dbb7b81dda3b35d1b8849ee4583de23099d734a3ac6dfa3f20cdc64248a5a3f4"
generated:
  feature: room.toggle_pet_breeding_permission
  scenarios:
  - scenario: room.toggle_pet_breeding_permission.guard_0
    title: "toggle pet breeding permission: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0
      guard_holds: "true"
    when:
      packet: TogglePetBreedingPermission
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of TogglePetBreedingPermission sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_2a21931a8cea
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - PetStatusUpdate
      evidence: ev_a5a4b706a468
  - scenario: room.toggle_pet_breeding_permission.guard_1
    title: "toggle pet breeding permission: the guard '!_roomGrain._state.PetsById.TryGetValue(petId, out PetSna...' holds"
    given:
      guard: "!_roomGrain._state.PetsById.TryGetValue(petId, out PetSnapshot? pet)"
      guard_holds: "true"
    when:
      packet: TogglePetBreedingPermission
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of TogglePetBreedingPermission sent while '!_roomGrain._state.PetsById.TryGetValue(petId, out PetSnapshot? pet)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e3aeea05728f
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - PetStatusUpdate
      evidence: ev_a5a4b706a468
  - scenario: room.toggle_pet_breeding_permission.guard_2
    title: "toggle pet breeding permission: the guard 'pet.OwnerId != ctx.PlayerId' holds"
    given:
      guard: pet.OwnerId != ctx.PlayerId
      guard_holds: "true"
    when:
      packet: TogglePetBreedingPermission
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of TogglePetBreedingPermission sent while 'pet.OwnerId != ctx.PlayerId' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e3aeea05728f
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - PetStatusUpdate
      evidence: ev_a5a4b706a468
  - scenario: room.toggle_pet_breeding_permission.success
    title: "toggle pet breeding permission: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: TogglePetBreedingPermission
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of TogglePetBreedingPermission succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_06768524ec0c
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - PetStatusUpdate
      evidence: ev_a5a4b706a468
  evidence:
  - id: ev_a5a4b706a468
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/pets/TogglePetBreedingPermissionMessageEvent.java
    symbol: TogglePetBreedingPermissionMessageEvent
    line: 12
    note: third-party server implementation; evidence, not authority
  - id: ev_06768524ec0c
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/TogglePetBreedingPermissionMessageHandler.cs
    symbol: TogglePetBreedingPermissionMessageHandler
    line: 11
  - id: ev_2a21931a8cea
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/TogglePetBreedingPermissionMessageHandler.cs
    symbol: TogglePetBreedingPermissionMessageHandler
    line: 22
  - id: ev_e3aeea05728f
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomPetSystem.Breeding.cs
    symbol: RoomPetSystem
    line: 53
verified: {}
manual: {}
