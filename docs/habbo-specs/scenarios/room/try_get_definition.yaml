# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:cc50b2171b14dff5546716ea8a5a9533ead4dc8775a4a0e9109277ed4074820f"
generated:
  feature: room.try_get_definition
  scenarios:
  - scenario: room.try_get_definition.guard_0
    title: "try get definition: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: VortexApplyFurniDefinition
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of VortexApplyFurniDefinition sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f0a89813bc2a
  - scenario: room.try_get_definition.guard_1
    title: "try get definition: the guard '!permissions.Has(Capabilities.Room.FurniEdit)' holds"
    given:
      guard: "!permissions.Has(Capabilities.Room.FurniEdit)"
      guard_holds: "true"
    when:
      packet: VortexApplyFurniDefinition
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of VortexApplyFurniDefinition sent while '!permissions.Has(Capabilities.Room.FurniEdit)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f0a89813bc2a
  - scenario: room.try_get_definition.guard_10
    title: "try get definition: the guard 'transformError.Length > 0' holds"
    given:
      guard: transformError.Length > 0
      guard_holds: "true"
    when:
      packet: VortexApplyFurniDefinition
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of VortexApplyFurniDefinition sent while 'transformError.Length > 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1b4c602bc4f6
  - scenario: room.try_get_definition.guard_11
    title: "try get definition: the guard 'swapError.Length > 0' holds"
    given:
      guard: swapError.Length > 0
      guard_holds: "true"
    when:
      packet: VortexApplyFurniDefinition
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of VortexApplyFurniDefinition sent while 'swapError.Length > 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1b4c602bc4f6
  - scenario: room.try_get_definition.guard_2
    title: "try get definition: the guard 'newOwnerId is null || newOwnerId.Value == PlayerId.Invalid' holds"
    given:
      guard: newOwnerId is null || newOwnerId.Value == PlayerId.Invalid
      guard_holds: "true"
    when:
      packet: VortexApplyFurniDefinition
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of VortexApplyFurniDefinition sent while 'newOwnerId is null || newOwnerId.Value == PlayerId.Invalid' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f0a89813bc2a
  - scenario: room.try_get_definition.guard_3
    title: "try get definition: the guard 'newDefinition is null' holds"
    given:
      guard: newDefinition is null
      guard_holds: "true"
    when:
      packet: VortexApplyFurniDefinition
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of VortexApplyFurniDefinition sent while 'newDefinition is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f0a89813bc2a
  - scenario: room.try_get_definition.guard_4
    title: "try get definition: the guard 'playerId <= 0' holds"
    given:
      guard: playerId <= 0
      guard_holds: "true"
    when:
      packet: VortexApplyFurniDefinition
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of VortexApplyFurniDefinition sent while 'playerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_743940bb69f7
  - scenario: room.try_get_definition.guard_5
    title: "try get definition: the guard 'string.IsNullOrWhiteSpace(name)' holds"
    given:
      guard: string.IsNullOrWhiteSpace(name)
      guard_holds: "true"
    when:
      packet: VortexApplyFurniDefinition
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of VortexApplyFurniDefinition sent while 'string.IsNullOrWhiteSpace(name)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7f41f2bdde62
  - scenario: room.try_get_definition.guard_6
    title: "try get definition: the guard '_nameToId.TryGetValue(name, out PlayerId playerId)' holds"
    given:
      guard: _nameToId.TryGetValue(name, out PlayerId playerId)
      guard_holds: "true"
    when:
      packet: VortexApplyFurniDefinition
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of VortexApplyFurniDefinition sent while '_nameToId.TryGetValue(name, out PlayerId playerId)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7f41f2bdde62
  - scenario: room.try_get_definition.guard_7
    title: "try get definition: the guard 'foundId is null' holds"
    given:
      guard: foundId is null
      guard_holds: "true"
    when:
      packet: VortexApplyFurniDefinition
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of VortexApplyFurniDefinition sent while 'foundId is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7f41f2bdde62
  - scenario: room.try_get_definition.guard_8
    title: "try get definition: the guard '!_state.ItemsById.TryGetValue(edit.ObjectId, out IRoomIte...' holds"
    given:
      guard: "!_state.ItemsById.TryGetValue(edit.ObjectId, out IRoomItem? item)"
      guard_holds: "true"
    when:
      packet: VortexApplyFurniDefinition
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of VortexApplyFurniDefinition sent while '!_state.ItemsById.TryGetValue(edit.ObjectId, out IRoomItem? item)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1b4c602bc4f6
  - scenario: room.try_get_definition.guard_9
    title: "try get definition: the guard 'fields == FurniEditField.None' holds"
    given:
      guard: fields == FurniEditField.None
      guard_holds: "true"
    when:
      packet: VortexApplyFurniDefinition
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of VortexApplyFurniDefinition sent while 'fields == FurniEditField.None' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1b4c602bc4f6
  - scenario: room.try_get_definition.success
    title: "try get definition: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: VortexApplyFurniDefinition
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of VortexApplyFurniDefinition succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - VortexFurniDefinition
      evidence: ev_0b98da256559
  evidence:
  - id: ev_0b98da256559
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/VortexApplyFurniEditMessageHandler.cs
    symbol: VortexApplyFurniEditMessageHandler
    line: 24
  - id: ev_1b4c602bc4f6
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Furni.Edit.cs
    symbol: RoomGrain
    line: 90
  - id: ev_743940bb69f7
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Authentication/Permissions/PermissionService.cs
    symbol: PermissionService
    line: 62
  - id: ev_7f41f2bdde62
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerDirectoryGrain.cs
    symbol: PlayerDirectoryGrain
    line: 146
  - id: ev_f0a89813bc2a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/VortexApplyFurniEditMessageHandler.cs
    symbol: VortexApplyFurniEditMessageHandler
    line: 36
verified: {}
manual: {}
