# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:ac7ebbb33354e48d2b39724e26ef96380755af20b6be33d3731dfb0aa477e4c4"
generated:
  feature: room.chat
  scenarios:
  - scenario: room.chat.guard_0
    title: "chat: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: Chat
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Chat sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_740a128ba258
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_c47dc92cdeb9
  - scenario: room.chat.guard_1
    title: "chat: the guard 'targetPlayerId is not null && !_roomGrain._state.AvatarsB...' holds"
    given:
      guard: targetPlayerId is not null && !_roomGrain._state.AvatarsByPlayerId.ContainsKey(targetPlayerId.Value)
      guard_holds: "true"
    when:
      packet: Chat
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Chat sent while 'targetPlayerId is not null && !_roomGrain._state.AvatarsByPlayerId.ContainsKey(targetPlayerId.Value)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f0fbc7d68cc3
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_c47dc92cdeb9
  - scenario: room.chat.guard_2
    title: "chat: the guard '!_roomGrain._state.AvatarsByPlayerId.TryGetValue(playerId...' holds"
    given:
      guard: "!_roomGrain._state.AvatarsByPlayerId.TryGetValue(playerId, out RoomObjectId objectId) || !_roomGrain._state.AvatarsByObjectId.TryGetValue(objectId, out IRoomAvatar? avatar)"
      guard_holds: "true"
    when:
      packet: Chat
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Chat sent while '!_roomGrain._state.AvatarsByPlayerId.TryGetValue(playerId, out RoomObjectId objectId) || !_roomGrain._state.AvatarsByObjectId.TryGetValue(objectId, out IRoomAvatar? avatar)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f0fbc7d68cc3
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_c47dc92cdeb9
  - scenario: room.chat.guard_3
    title: "chat: the guard 'IsUserMuted(playerId, out int secondsRemaining)' holds"
    given:
      guard: IsUserMuted(playerId, out int secondsRemaining)
      guard_holds: "true"
    when:
      packet: Chat
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Chat sent while 'IsUserMuted(playerId, out int secondsRemaining)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: rejected
      emits:
      - FloodControl
      - RemainingMutePeriod
      evidence: ev_f0fbc7d68cc3
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_c47dc92cdeb9
  - scenario: room.chat.guard_4
    title: "chat: the guard 'IsFloodGated(playerId, avatar, out int floodSecondsRemain...' holds"
    given:
      guard: IsFloodGated(playerId, avatar, out int floodSecondsRemaining)
      guard_holds: "true"
    when:
      packet: Chat
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Chat sent while 'IsFloodGated(playerId, avatar, out int floodSecondsRemaining)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: rejected
      emits:
      - FloodControl
      - RemainingMutePeriod
      evidence: ev_f0fbc7d68cc3
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_c47dc92cdeb9
  - scenario: room.chat.success
    title: "chat: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: Chat
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of Chat succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - FloodControl
      - RemainingMutePeriod
      evidence: ev_3dfe9ba800df
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_c47dc92cdeb9
  evidence:
  - id: ev_c47dc92cdeb9
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/users/ChatMessageEvent.java
    symbol: ChatMessageEvent
    line: 18
    note: third-party server implementation; evidence, not authority
  - id: ev_3dfe9ba800df
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomEventModule.cs
    symbol: RoomEventModule
    line: 27
  - id: ev_740a128ba258
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Chat/ChatMessageHandler.cs
    symbol: ChatMessageHandler
    line: 22
  - id: ev_f0fbc7d68cc3
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomChatSystem.cs
    symbol: RoomChatSystem
    line: 42
verified: {}
manual: {}
