# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:adcf82f8ab4bc9a5f76e22c1329908cb2ccd3d679af21cbc8abe4dca8c905115"
generated:
  feature: room.set_custom_stacking_height
  scenarios:
  - scenario: room.set_custom_stacking_height.guard_0
    title: "set custom stacking height: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: SetCustomStackingHeight
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SetCustomStackingHeight sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_2bcdafed0fef
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_1aef48f5f62d
  - scenario: room.set_custom_stacking_height.guard_1
    title: "set custom stacking height: the guard 'item is not IRoomFloorItem floor || floor.Logic is not Fu...' holds"
    given:
      guard: item is not IRoomFloorItem floor || floor.Logic is not FurnitureCustomStackHeightLogic logic
      guard_holds: "true"
    when:
      packet: SetCustomStackingHeight
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SetCustomStackingHeight sent while 'item is not IRoomFloorItem floor || floor.Logic is not FurnitureCustomStackHeightLogic logic' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_5e51a330c801
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_1aef48f5f62d
  - scenario: room.set_custom_stacking_height.guard_2
    title: "set custom stacking height: the guard '!reset && (heightHundredths < 0 || heightHundredths > _ro...' holds"
    given:
      guard: "!reset && (heightHundredths < 0 || heightHundredths > _roomConfig.MaxStackHeight.ToInt())"
      guard_holds: "true"
    when:
      packet: SetCustomStackingHeight
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SetCustomStackingHeight sent while '!reset && (heightHundredths < 0 || heightHundredths > _roomConfig.MaxStackHeight.ToInt())' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_5e51a330c801
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_1aef48f5f62d
  - scenario: room.set_custom_stacking_height.guard_3
    title: "set custom stacking height: the guard '!MapModule.InBounds(tileIdx)' holds"
    given:
      guard: "!MapModule.InBounds(tileIdx)"
      guard_holds: "true"
    when:
      packet: SetCustomStackingHeight
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SetCustomStackingHeight sent while '!MapModule.InBounds(tileIdx)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_5e51a330c801
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_1aef48f5f62d
  - scenario: room.set_custom_stacking_height.guard_4
    title: "set custom stacking height: the guard '!InBounds(tileIdx)' holds"
    given:
      guard: "!InBounds(tileIdx)"
      guard_holds: "true"
    when:
      packet: SetCustomStackingHeight
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SetCustomStackingHeight sent while '!InBounds(tileIdx)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: rejected
      emits: []
      evidence: ev_407ccfd7d8f8
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_1aef48f5f62d
  - scenario: room.set_custom_stacking_height.guard_5
    title: "set custom stacking height: the guard '!InBounds(nTileIdx)' holds"
    given:
      guard: "!InBounds(nTileIdx)"
      guard_holds: "true"
    when:
      packet: SetCustomStackingHeight
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SetCustomStackingHeight sent while '!InBounds(nTileIdx)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: rejected
      emits: []
      evidence: ev_407ccfd7d8f8
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_1aef48f5f62d
  - scenario: room.set_custom_stacking_height.success
    title: "set custom stacking height: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: SetCustomStackingHeight
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of SetCustomStackingHeight succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_2bcdafed0fef
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_1aef48f5f62d
  evidence:
  - id: ev_1aef48f5f62d
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/items/SetCustomStackingHeightEvent.java
    symbol: SetCustomStackingHeightEvent
    line: 15
    note: third-party server implementation; evidence, not authority
  - id: ev_2bcdafed0fef
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Furniture/SetCustomStackingHeightMessageHandler.cs
    symbol: SetCustomStackingHeightMessageHandler
    line: 24
  - id: ev_407ccfd7d8f8
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomMapModule.Floor.cs
    symbol: RoomMapModule
    line: 125
  - id: ev_5e51a330c801
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Furni.Interactive.cs
    symbol: RoomGrain
    line: 119
verified: {}
manual: {}
