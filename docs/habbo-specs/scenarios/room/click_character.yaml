# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:e1ac54994ad21dfcb47650c4c362e5eddab782102041fa7d1bfccfef54625188"
generated:
  feature: room.click_character
  scenarios:
  - scenario: room.click_character.guard_0
    title: "click character: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: ClickCharacter
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ClickCharacter sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_72921a91afd1
  - scenario: room.click_character.guard_1
    title: "click character: the guard 'ctx.Origin != ActionOrigin.Player || ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.Origin != ActionOrigin.Player || ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: ClickCharacter
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ClickCharacter sent while 'ctx.Origin != ActionOrigin.Player || ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ac641f7283b9
  - scenario: room.click_character.guard_2
    title: "click character: the guard '!_state.AvatarsByObjectId.TryGetValue(targetObjectId, out...' holds"
    given:
      guard: "!_state.AvatarsByObjectId.TryGetValue(targetObjectId, out IRoomAvatar? avatar) || avatar is not IRoomPlayer target"
      guard_holds: "true"
    when:
      packet: ClickCharacter
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ClickCharacter sent while '!_state.AvatarsByObjectId.TryGetValue(targetObjectId, out IRoomAvatar? avatar) || avatar is not IRoomPlayer target' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ac641f7283b9
  - scenario: room.click_character.success
    title: "click character: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ClickCharacter
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ClickCharacter succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_4f7fa7efaeda
  evidence:
  - id: ev_4f7fa7efaeda
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/ClickCharacterMessageHandler.cs
    symbol: ClickCharacterMessageHandler
    line: 11
  - id: ev_72921a91afd1
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/ClickCharacterMessageHandler.cs
    symbol: ClickCharacterMessageHandler
    line: 22
  - id: ev_ac641f7283b9
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Avatar.cs
    symbol: RoomGrain
    line: 81
verified: {}
manual: {}
