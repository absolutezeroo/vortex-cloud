# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:55ddf9d1dc9c77da87ed4a6f2594a9e0633e6d5e8c9230f4e2db759d25174ba4"
generated:
  feature: room.sign
  scenarios:
  - scenario: room.sign.guard_0
    title: "sign: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || me...' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.SignId < 0 || message.SignId > MaxSignId
      guard_holds: "true"
    when:
      packet: Sign
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Sign sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.SignId < 0 || message.SignId > MaxSignId' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_8568204207b7
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_5d0274148f9d
  - scenario: room.sign.guard_1
    title: "sign: the guard '!_state.AvatarsByPlayerId.TryGetValue(ctx.PlayerId, out R...' holds"
    given:
      guard: "!_state.AvatarsByPlayerId.TryGetValue(ctx.PlayerId, out RoomObjectId objectId) || !await AvatarModule.SetAvatarSignAsync(objectId, signId, ct)"
      guard_holds: "true"
    when:
      packet: Sign
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Sign sent while '!_state.AvatarsByPlayerId.TryGetValue(ctx.PlayerId, out RoomObjectId objectId) || !await AvatarModule.SetAvatarSignAsync(objectId, signId, ct)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ac641f7283b9
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_5d0274148f9d
  - scenario: room.sign.guard_2
    title: "sign: the guard 'objectId <= 0 || !_roomGrain._state.AvatarsByObjectId.Try...' holds"
    given:
      guard: objectId <= 0 || !_roomGrain._state.AvatarsByObjectId.TryGetValue( objectId.Value, out IRoomAvatar? avatar )
      guard_holds: "true"
    when:
      packet: Sign
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Sign sent while 'objectId <= 0 || !_roomGrain._state.AvatarsByObjectId.TryGetValue( objectId.Value, out IRoomAvatar? avatar )' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e6fef545a42a
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_5d0274148f9d
  - scenario: room.sign.success
    title: "sign: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: Sign
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of Sign succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - UserUpdate
      evidence: ev_8568204207b7
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_5d0274148f9d
  evidence:
  - id: ev_5d0274148f9d
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/users/SignMessageEvent.java
    symbol: SignMessageEvent
    line: 13
    note: third-party server implementation; evidence, not authority
  - id: ev_8568204207b7
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Avatar/SignMessageHandler.cs
    symbol: SignMessageHandler
    line: 26
  - id: ev_ac641f7283b9
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Avatar.cs
    symbol: RoomGrain
    line: 348
  - id: ev_e6fef545a42a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomAvatarModule.cs
    symbol: RoomAvatarModule
    line: 648
verified: {}
manual: {}
