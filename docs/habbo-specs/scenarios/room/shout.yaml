# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:1c30e003a8aa6e72e6acc0b2feb29d26c01a52da21b386c2950e7de9fe05e7b2"
generated:
  feature: room.shout
  scenarios:
  - scenario: room.shout.guard_0
    title: "shout: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || st...' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || string.IsNullOrWhiteSpace(message.Text)
      guard_holds: "true"
    when:
      packet: Shout
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Shout sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || string.IsNullOrWhiteSpace(message.Text)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_604923b3bfd5
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_a5b9d9854295
  - scenario: room.shout.guard_1
    title: "shout: the guard 'targetPlayerId is not null && !_roomGrain._state.AvatarsB...' holds"
    given:
      guard: targetPlayerId is not null && !_roomGrain._state.AvatarsByPlayerId.ContainsKey(targetPlayerId.Value)
      guard_holds: "true"
    when:
      packet: Shout
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Shout sent while 'targetPlayerId is not null && !_roomGrain._state.AvatarsByPlayerId.ContainsKey(targetPlayerId.Value)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f0fbc7d68cc3
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_a5b9d9854295
  - scenario: room.shout.guard_2
    title: "shout: the guard '!_roomGrain._state.AvatarsByPlayerId.TryGetValue(playerId...' holds"
    given:
      guard: "!_roomGrain._state.AvatarsByPlayerId.TryGetValue(playerId, out RoomObjectId objectId) || !_roomGrain._state.AvatarsByObjectId.TryGetValue(objectId, out IRoomAvatar? avatar)"
      guard_holds: "true"
    when:
      packet: Shout
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Shout sent while '!_roomGrain._state.AvatarsByPlayerId.TryGetValue(playerId, out RoomObjectId objectId) || !_roomGrain._state.AvatarsByObjectId.TryGetValue(objectId, out IRoomAvatar? avatar)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f0fbc7d68cc3
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_a5b9d9854295
  - scenario: room.shout.guard_3
    title: "shout: the guard 'IsUserMuted(playerId, out int secondsRemaining)' holds"
    given:
      guard: IsUserMuted(playerId, out int secondsRemaining)
      guard_holds: "true"
    when:
      packet: Shout
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Shout sent while 'IsUserMuted(playerId, out int secondsRemaining)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: rejected
      emits:
      - FloodControl
      - RemainingMutePeriod
      evidence: ev_f0fbc7d68cc3
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_a5b9d9854295
  - scenario: room.shout.guard_4
    title: "shout: the guard 'IsFloodGated(playerId, avatar, out int floodSecondsRemain...' holds"
    given:
      guard: IsFloodGated(playerId, avatar, out int floodSecondsRemaining)
      guard_holds: "true"
    when:
      packet: Shout
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Shout sent while 'IsFloodGated(playerId, avatar, out int floodSecondsRemaining)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: rejected
      emits:
      - FloodControl
      - RemainingMutePeriod
      evidence: ev_f0fbc7d68cc3
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_a5b9d9854295
  - scenario: room.shout.success
    title: "shout: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: Shout
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of Shout succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - FloodControl
      - RemainingMutePeriod
      evidence: ev_3dfe9ba800df
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_a5b9d9854295
  evidence:
  - id: ev_a5b9d9854295
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/users/ShoutMessageEvent.java
    symbol: ShoutMessageEvent
    line: 16
    note: third-party server implementation; evidence, not authority
  - id: ev_3dfe9ba800df
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomEventModule.cs
    symbol: RoomEventModule
    line: 27
  - id: ev_604923b3bfd5
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Chat/ShoutMessageHandler.cs
    symbol: ShoutMessageHandler
    line: 24
  - id: ev_f0fbc7d68cc3
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomChatSystem.cs
    symbol: RoomChatSystem
    line: 42
verified: {}
manual: {}
