# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:02a10eab66febffb0d22d82205882272fdd83fe22b658ba6d040bb619b9fac89"
generated:
  feature: room.look_to
  scenarios:
  - scenario: room.look_to.guard_0
    title: "look to: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: LookTo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of LookTo sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_99b368bdfe17
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_cbbbf87ab4e3
  - scenario: room.look_to.guard_1
    title: "look to: the guard '!_state.AvatarsByPlayerId.TryGetValue(ctx.PlayerId, out R...' holds"
    given:
      guard: "!_state.AvatarsByPlayerId.TryGetValue(ctx.PlayerId, out RoomObjectId objectId) || !await AvatarModule.LookToAvatarAsync(objectId, targetX, targetY, ct)"
      guard_holds: "true"
    when:
      packet: LookTo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of LookTo sent while '!_state.AvatarsByPlayerId.TryGetValue(ctx.PlayerId, out RoomObjectId objectId) || !await AvatarModule.LookToAvatarAsync(objectId, targetX, targetY, ct)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ac641f7283b9
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_cbbbf87ab4e3
  - scenario: room.look_to.guard_10
    title: "look to: the guard 'dx < 0 && dy == 0' holds"
    given:
      guard: dx < 0 && dy == 0
      guard_holds: "true"
    when:
      packet: LookTo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of LookTo sent while 'dx < 0 && dy == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ec05fa1b0194
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_cbbbf87ab4e3
  - scenario: room.look_to.guard_11
    title: "look to: the guard 'HeadRotation == rot' holds"
    given:
      guard: HeadRotation == rot
      guard_holds: "true"
    when:
      packet: LookTo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of LookTo sent while 'HeadRotation == rot' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_9f45a8c43da8
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_cbbbf87ab4e3
  - scenario: room.look_to.guard_2
    title: "look to: the guard 'objectId <= 0 || !_roomGrain._state.AvatarsByObjectId.Try...' holds"
    given:
      guard: objectId <= 0 || !_roomGrain._state.AvatarsByObjectId.TryGetValue( objectId.Value, out IRoomAvatar? avatar )
      guard_holds: "true"
    when:
      packet: LookTo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of LookTo sent while 'objectId <= 0 || !_roomGrain._state.AvatarsByObjectId.TryGetValue( objectId.Value, out IRoomAvatar? avatar )' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e6fef545a42a
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_cbbbf87ab4e3
  - scenario: room.look_to.guard_3
    title: "look to: the guard 'dx == 0 && dy == 0' holds"
    given:
      guard: dx == 0 && dy == 0
      guard_holds: "true"
    when:
      packet: LookTo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of LookTo sent while 'dx == 0 && dy == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e6fef545a42a
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_cbbbf87ab4e3
  - scenario: room.look_to.guard_4
    title: "look to: the guard 'dx == 0 && dy < 0' holds"
    given:
      guard: dx == 0 && dy < 0
      guard_holds: "true"
    when:
      packet: LookTo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of LookTo sent while 'dx == 0 && dy < 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ec05fa1b0194
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_cbbbf87ab4e3
  - scenario: room.look_to.guard_5
    title: "look to: the guard 'dx > 0 && dy < 0' holds"
    given:
      guard: dx > 0 && dy < 0
      guard_holds: "true"
    when:
      packet: LookTo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of LookTo sent while 'dx > 0 && dy < 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ec05fa1b0194
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_cbbbf87ab4e3
  - scenario: room.look_to.guard_6
    title: "look to: the guard 'dx > 0 && dy == 0' holds"
    given:
      guard: dx > 0 && dy == 0
      guard_holds: "true"
    when:
      packet: LookTo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of LookTo sent while 'dx > 0 && dy == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ec05fa1b0194
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_cbbbf87ab4e3
  - scenario: room.look_to.guard_7
    title: "look to: the guard 'dx > 0 && dy > 0' holds"
    given:
      guard: dx > 0 && dy > 0
      guard_holds: "true"
    when:
      packet: LookTo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of LookTo sent while 'dx > 0 && dy > 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ec05fa1b0194
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_cbbbf87ab4e3
  - scenario: room.look_to.guard_8
    title: "look to: the guard 'dx == 0 && dy > 0' holds"
    given:
      guard: dx == 0 && dy > 0
      guard_holds: "true"
    when:
      packet: LookTo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of LookTo sent while 'dx == 0 && dy > 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ec05fa1b0194
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_cbbbf87ab4e3
  - scenario: room.look_to.guard_9
    title: "look to: the guard 'dx < 0 && dy > 0' holds"
    given:
      guard: dx < 0 && dy > 0
      guard_holds: "true"
    when:
      packet: LookTo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of LookTo sent while 'dx < 0 && dy > 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ec05fa1b0194
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_cbbbf87ab4e3
  - scenario: room.look_to.success
    title: "look to: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: LookTo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of LookTo succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - UserUpdate
      evidence: ev_99b368bdfe17
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_cbbbf87ab4e3
  evidence:
  - id: ev_cbbbf87ab4e3
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/users/LookToMessageEvent.java
    symbol: LookToMessageEvent
    line: 12
    note: third-party server implementation; evidence, not authority
  - id: ev_99b368bdfe17
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Avatar/LookToMessageHandler.cs
    symbol: LookToMessageHandler
    line: 21
  - id: ev_9f45a8c43da8
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Object/Avatars/RoomAvatar.cs
    symbol: RoomAvatar
    line: 122
  - id: ev_ac641f7283b9
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Avatar.cs
    symbol: RoomGrain
    line: 405
  - id: ev_e6fef545a42a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomAvatarModule.cs
    symbol: RoomAvatarModule
    line: 770
  - id: ev_ec05fa1b0194
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Primitives/Rooms/Enums/Rotation.cs
    symbol: RotationExtensions
    line: 68
verified: {}
manual: {}
