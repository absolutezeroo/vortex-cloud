# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:4fbf80b68981616545a7d82f142f8cbf8da1e6e3b4bd8d78099e262dbb81273f"
generated:
  feature: room.get_pet_info
  scenarios:
  - scenario: room.get_pet_info.guard_0
    title: "get pet info: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0
      guard_holds: "true"
    when:
      packet: GetPetInfo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetPetInfo sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_08242e595499
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - PetInfo
      evidence: ev_eff3eb411455
  - scenario: room.get_pet_info.guard_1
    title: "get pet info: the guard 'pet is null' holds"
    given:
      guard: pet is null
      guard_holds: "true"
    when:
      packet: GetPetInfo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetPetInfo sent while 'pet is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_08242e595499
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - PetInfo
      evidence: ev_eff3eb411455
  - scenario: room.get_pet_info.guard_2
    title: "get pet info: the guard '_idToName.TryGetValue(playerId, out string? x)' holds"
    given:
      guard: _idToName.TryGetValue(playerId, out string? x)
      guard_holds: "true"
    when:
      packet: GetPetInfo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetPetInfo sent while '_idToName.TryGetValue(playerId, out string? x)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7f41f2bdde62
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - PetInfo
      evidence: ev_eff3eb411455
  - scenario: room.get_pet_info.guard_3
    title: "get pet info: the guard 'string.IsNullOrWhiteSpace(dbName)' holds"
    given:
      guard: string.IsNullOrWhiteSpace(dbName)
      guard_holds: "true"
    when:
      packet: GetPetInfo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetPetInfo sent while 'string.IsNullOrWhiteSpace(dbName)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7f41f2bdde62
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - PetInfo
      evidence: ev_eff3eb411455
  - scenario: room.get_pet_info.success
    title: "get pet info: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetPetInfo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetPetInfo succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - PetInfo
      evidence: ev_08242e595499
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - PetInfo
      evidence: ev_eff3eb411455
  evidence:
  - id: ev_eff3eb411455
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/pets/GetPetInfoMessageEvent.java
    symbol: GetPetInfoMessageEvent
    line: 10
    note: third-party server implementation; evidence, not authority
  - id: ev_08242e595499
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Pets/GetPetInfoMessageHandler.cs
    symbol: GetPetInfoMessageHandler
    line: 26
  - id: ev_7f41f2bdde62
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerDirectoryGrain.cs
    symbol: PlayerDirectoryGrain
    line: 31
verified: {}
manual: {}
