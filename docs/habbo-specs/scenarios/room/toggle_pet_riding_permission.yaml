# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:5eb3eddd16615a876a45bd4a7d28fd7fc93ae42e66683208614cd43a0abc659b"
generated:
  feature: room.toggle_pet_riding_permission
  scenarios:
  - scenario: room.toggle_pet_riding_permission.guard_0
    title: "toggle pet riding permission: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0
      guard_holds: "true"
    when:
      packet: TogglePetRidingPermission
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of TogglePetRidingPermission sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ce158854e54c
  - scenario: room.toggle_pet_riding_permission.guard_1
    title: "toggle pet riding permission: the guard '!_roomGrain._state.PetsById.TryGetValue(petId, out PetSna...' holds"
    given:
      guard: "!_roomGrain._state.PetsById.TryGetValue(petId, out PetSnapshot? pet) || pet.OwnerId != ctx.PlayerId"
      guard_holds: "true"
    when:
      packet: TogglePetRidingPermission
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of TogglePetRidingPermission sent while '!_roomGrain._state.PetsById.TryGetValue(petId, out PetSnapshot? pet) || pet.OwnerId != ctx.PlayerId' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_acc1f9bce7eb
  - scenario: room.toggle_pet_riding_permission.success
    title: "toggle pet riding permission: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: TogglePetRidingPermission
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of TogglePetRidingPermission succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_03fc9d83360f
  evidence:
  - id: ev_03fc9d83360f
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/TogglePetRidingPermissionMessageHandler.cs
    symbol: TogglePetRidingPermissionMessageHandler
    line: 11
  - id: ev_acc1f9bce7eb
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomPetSystem.Riding.cs
    symbol: RoomPetSystem
    line: 83
  - id: ev_ce158854e54c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/TogglePetRidingPermissionMessageHandler.cs
    symbol: TogglePetRidingPermissionMessageHandler
    line: 22
verified: {}
manual: {}
