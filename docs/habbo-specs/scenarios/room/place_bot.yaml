# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:521487ba6203f10823181e097dd8251ce62f1ec08facfd4f49bdf0c1e5735663"
generated:
  feature: room.place_bot
  scenarios:
  - scenario: room.place_bot.guard_0
    title: "place bot: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.BotId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.BotId <= 0
      guard_holds: "true"
    when:
      packet: PlaceBot
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of PlaceBot sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.BotId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_dfc4de7ba812
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_65af4b15c668
  - scenario: room.place_bot.success
    title: "place bot: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: PlaceBot
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of PlaceBot succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_dfc4de7ba812
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_65af4b15c668
  evidence:
  - id: ev_65af4b15c668
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/bots/PlaceBotMessageEvent.java
    symbol: PlaceBotMessageEvent
    line: 10
    note: third-party server implementation; evidence, not authority
  - id: ev_dfc4de7ba812
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/PlaceBotMessageHandler.cs
    symbol: PlaceBotMessageHandler
    line: 18
verified: {}
manual: {}
