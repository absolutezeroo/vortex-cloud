# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:32967e2a98748291b79d6981b61c39f88ede42533232a1a8e207c615e9c21fcd"
generated:
  feature: room.move_pet_in_room
  scenarios:
  - scenario: room.move_pet_in_room.guard_0
    title: "move pet in room: the guard 'ctx.Origin != ActionOrigin.Player || ctx.PlayerId <= 0 ||...' holds"
    given:
      guard: ctx.Origin != ActionOrigin.Player || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || petId <= 0
      guard_holds: "true"
    when:
      packet: MovePet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MovePet sent while 'ctx.Origin != ActionOrigin.Player || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || petId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_5d4c75baab22
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - UserUpdate
      evidence: ev_b8601b1ad57c
  - scenario: room.move_pet_in_room.guard_1
    title: "move pet in room: the guard 'pet is null' holds"
    given:
      guard: pet is null
      guard_holds: "true"
    when:
      packet: MovePet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MovePet sent while 'pet is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7327c7799f5f
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - UserUpdate
      evidence: ev_b8601b1ad57c
  - scenario: room.move_pet_in_room.success
    title: "move pet in room: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: MovePet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of MovePet succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1e4f030a2935
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - UserUpdate
      evidence: ev_b8601b1ad57c
  evidence:
  - id: ev_b8601b1ad57c
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/pets/MovePetMessageEvent.java
    symbol: MovePetMessageEvent
    line: 12
    note: third-party server implementation; evidence, not authority
  - id: ev_1e4f030a2935
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/MovePetMessageHandler.cs
    symbol: MovePetMessageHandler
    line: 9
  - id: ev_5d4c75baab22
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/RoomService.Pets.cs
    symbol: RoomService
    line: 40
  - id: ev_7327c7799f5f
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomPetSystem.Placement.cs
    symbol: RoomPetSystem
    line: 154
verified: {}
manual: {}
