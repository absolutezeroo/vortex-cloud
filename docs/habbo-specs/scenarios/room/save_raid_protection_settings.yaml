# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:122326203fcd487fd0324548bab6cec9c6909560fba9d33024ee0f40064e98f5"
generated:
  feature: room.save_raid_protection_settings
  scenarios:
  - scenario: room.save_raid_protection_settings.guard_0
    title: "save raid protection settings: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.RoomId !=...' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.RoomId != ctx.RoomId
      guard_holds: "true"
    when:
      packet: SaveRaidProtectionSettings
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SaveRaidProtectionSettings sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.RoomId != ctx.RoomId' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_27b0a0ae2bbf
  - scenario: room.save_raid_protection_settings.success
    title: "save raid protection settings: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: SaveRaidProtectionSettings
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of SaveRaidProtectionSettings succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - RaidProtectionSettingsResult
      evidence: ev_15d1e33a59c7
  evidence:
  - id: ev_15d1e33a59c7
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/RaidProtection/SaveRaidProtectionSettingsMessageHandler.cs
    symbol: SaveRaidProtectionSettingsMessageHandler
    line: 19
  - id: ev_27b0a0ae2bbf
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/RaidProtection/SaveRaidProtectionSettingsMessageHandler.cs
    symbol: SaveRaidProtectionSettingsMessageHandler
    line: 30
verified: {}
manual: {}
