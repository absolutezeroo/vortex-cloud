# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:600be8e0b04d8f08cd3263a9a6583d86211b062af0f2f937213f3b416d158dd2"
generated:
  feature: room.remove_rights
  scenarios:
  - scenario: room.remove_rights.guard_0
    title: "remove rights: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.TargetUse...' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.TargetUserIds.IsEmpty
      guard_holds: "true"
    when:
      packet: RemoveRights
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RemoveRights sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.TargetUserIds.IsEmpty' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_b0c6819b754b
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_82bae9d71653
  - scenario: room.remove_rights.guard_1
    title: "remove rights: the guard 'targets.IsEmpty || !await IsRoomOwnerAsync(actor).Configu...' holds"
    given:
      guard: targets.IsEmpty || !await IsRoomOwnerAsync(actor).ConfigureAwait(true)
      guard_holds: "true"
    when:
      packet: RemoveRights
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RemoveRights sent while 'targets.IsEmpty || !await IsRoomOwnerAsync(actor).ConfigureAwait(true)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_329537530b13
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_82bae9d71653
  - scenario: room.remove_rights.guard_2
    title: "remove rights: the guard '!_roomGrain._state.AvatarsByPlayerId.TryGetValue(playerId...' holds"
    given:
      guard: "!_roomGrain._state.AvatarsByPlayerId.TryGetValue(playerId, out RoomObjectId objectId) || !_roomGrain._state.AvatarsByObjectId.TryGetValue(objectId, out IRoomAvatar? avatar)"
      guard_holds: "true"
    when:
      packet: RemoveRights
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RemoveRights sent while '!_roomGrain._state.AvatarsByPlayerId.TryGetValue(playerId, out RoomObjectId objectId) || !_roomGrain._state.AvatarsByObjectId.TryGetValue(objectId, out IRoomAvatar? avatar)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_82c2d2746361
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_82bae9d71653
  - scenario: room.remove_rights.guard_3
    title: "remove rights: the guard '_state.ActiveRoomId != roomId' holds"
    given:
      guard: _state.ActiveRoomId != roomId
      guard_holds: "true"
    when:
      packet: RemoveRights
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RemoveRights sent while '_state.ActiveRoomId != roomId' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_b44ef304d36a
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_82bae9d71653
  - scenario: room.remove_rights.guard_4
    title: "remove rights: the guard 'controllerType >= RoomControllerType.Rights' holds"
    given:
      guard: controllerType >= RoomControllerType.Rights
      guard_holds: "true"
    when:
      packet: RemoveRights
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RemoveRights sent while 'controllerType >= RoomControllerType.Rights' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_b44ef304d36a
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_82bae9d71653
  - scenario: room.remove_rights.success
    title: "remove rights: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RemoveRights
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RemoveRights succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - FlatControllersEvent
      - WiredPermissionsEvent
      - YouAreController
      - YouAreNotController
      - YouAreOwner
      evidence: ev_2ab49cd53acd
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_82bae9d71653
  evidence:
  - id: ev_82bae9d71653
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/users/RemoveRightsMessageEvent.java
    symbol: RemoveRightsMessageEvent
    line: 9
    note: third-party server implementation; evidence, not authority
  - id: ev_2ab49cd53acd
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Action/RemoveRightsMessageHandler.cs
    symbol: RemoveRightsMessageHandler
    line: 14
  - id: ev_329537530b13
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Settings.cs
    symbol: RoomGrain
    line: 430
  - id: ev_82c2d2746361
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomSecurityModule.cs
    symbol: RoomSecurityModule
    line: 256
  - id: ev_b0c6819b754b
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Action/RemoveRightsMessageHandler.cs
    symbol: RemoveRightsMessageHandler
    line: 25
  - id: ev_b44ef304d36a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerPresenceGrain.Room.cs
    symbol: PlayerPresenceGrain
    line: 199
verified: {}
manual: {}
