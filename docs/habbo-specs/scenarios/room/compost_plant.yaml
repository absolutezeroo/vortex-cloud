# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:b24ba16422e958701ce0c85d9063510ec34b74290f262836b999474c14e15dac"
generated:
  feature: room.compost_plant
  scenarios:
  - scenario: room.compost_plant.guard_0
    title: "compost plant: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0
      guard_holds: "true"
    when:
      packet: CompostPlant
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of CompostPlant sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3c8a5114b819
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - ObjectAdd
      evidence: ev_b716fdc95c4c
  - scenario: room.compost_plant.guard_1
    title: "compost plant: the guard '!_roomGrain._state.PetsById.TryGetValue(petId, out PetSna...' holds"
    given:
      guard: "!_roomGrain._state.PetsById.TryGetValue(petId, out PetSnapshot? plant) || plant.Type != MonsterplantPetType || plant.Energy > 0"
      guard_holds: "true"
    when:
      packet: CompostPlant
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of CompostPlant sent while '!_roomGrain._state.PetsById.TryGetValue(petId, out PetSnapshot? plant) || plant.Type != MonsterplantPetType || plant.Energy > 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a24ca6ca32d9
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - ObjectAdd
      evidence: ev_b716fdc95c4c
  - scenario: room.compost_plant.guard_2
    title: "compost plant: the guard 'plant.OwnerId != ctx.PlayerId && _roomGrain._state.RoomSn...' holds"
    given:
      guard: plant.OwnerId != ctx.PlayerId && _roomGrain._state.RoomSnapshot.OwnerId != ctx.PlayerId
      guard_holds: "true"
    when:
      packet: CompostPlant
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of CompostPlant sent while 'plant.OwnerId != ctx.PlayerId && _roomGrain._state.RoomSnapshot.OwnerId != ctx.PlayerId' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a24ca6ca32d9
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - ObjectAdd
      evidence: ev_b716fdc95c4c
  - scenario: room.compost_plant.guard_3
    title: "compost plant: the guard 'entity is null' holds"
    given:
      guard: entity is null
      guard_holds: "true"
    when:
      packet: CompostPlant
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of CompostPlant sent while 'entity is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a24ca6ca32d9
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - ObjectAdd
      evidence: ev_b716fdc95c4c
  - scenario: room.compost_plant.success
    title: "compost plant: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: CompostPlant
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of CompostPlant succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - UserRemove
      evidence: ev_3c8a5114b819
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - ObjectAdd
      evidence: ev_b716fdc95c4c
  evidence:
  - id: ev_b716fdc95c4c
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/pets/CompostPlantMessageEvent.java
    symbol: CompostPlantMessageEvent
    line: 22
    note: third-party server implementation; evidence, not authority
  - id: ev_3c8a5114b819
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Pets/CompostPlantMessageHandler.cs
    symbol: CompostPlantMessageHandler
    line: 24
  - id: ev_a24ca6ca32d9
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomPetSystem.Plants.cs
    symbol: RoomPetSystem
    line: 221
verified: {}
manual: {}
