# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:a48da911ec829fe953a94da0425eea404ae1243eabce2b5e27b3f713c038f1b3"
generated:
  feature: room.pass_carry_item
  scenarios:
  - scenario: room.pass_carry_item.guard_0
    title: "pass carry item: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.TargetPla...' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.TargetPlayerId <= 0
      guard_holds: "true"
    when:
      packet: PassCarryItem
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of PassCarryItem sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.TargetPlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3ba41275e632
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_7cc758960dce
  - scenario: room.pass_carry_item.guard_1
    title: "pass carry item: the guard 'fromPlayerId == toPlayerId || !TryFindAvatar(fromPlayerId...' holds"
    given:
      guard: fromPlayerId == toPlayerId || !TryFindAvatar(fromPlayerId, out IRoomAvatar? giver) || !TryFindAvatar(toPlayerId, out IRoomAvatar? taker) || giver.CarryItemId == 0 || !IsWithinReach(giver, taker)
      guard_holds: "true"
    when:
      packet: PassCarryItem
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of PassCarryItem sent while 'fromPlayerId == toPlayerId || !TryFindAvatar(fromPlayerId, out IRoomAvatar? giver) || !TryFindAvatar(toPlayerId, out IRoomAvatar? taker) || giver.CarryItemId == 0 || !IsWithinReach(giver, taker)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_315e04717bdc
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_7cc758960dce
  - scenario: room.pass_carry_item.guard_2
    title: "pass carry item: the guard 'CarryItemId == itemId' holds"
    given:
      guard: CarryItemId == itemId
      guard_holds: "true"
    when:
      packet: PassCarryItem
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of PassCarryItem sent while 'CarryItemId == itemId' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_9f45a8c43da8
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_7cc758960dce
  - scenario: room.pass_carry_item.success
    title: "pass carry item: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: PassCarryItem
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of PassCarryItem succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_315e04717bdc
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_7cc758960dce
  evidence:
  - id: ev_7cc758960dce
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/users/PassCarryItemMessageEvent.java
    symbol: PassCarryItemMessageEvent
    line: 15
    note: third-party server implementation; evidence, not authority
  - id: ev_315e04717bdc
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomHandItemModule.cs
    symbol: RoomHandItemModule
    line: 71
  - id: ev_3ba41275e632
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Avatar/PassCarryItemMessageHandler.cs
    symbol: PassCarryItemMessageHandler
    line: 21
  - id: ev_9f45a8c43da8
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Object/Avatars/RoomAvatar.cs
    symbol: RoomAvatar
    line: 221
verified: {}
manual: {}
