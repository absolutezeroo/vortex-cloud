# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:1183abc867a3de09d34bde5aed2c8e295dd9de4e842f9ce990c40314c27c355c"
generated:
  feature: room.get_custom_room_filter
  scenarios:
  - scenario: room.get_custom_room_filter.success
    title: "get custom room filter: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetCustomRoomFilter
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetCustomRoomFilter succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_5fbb63890d81
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - RoomFilterSettings
      evidence: ev_e222f50acdfc
  evidence:
  - id: ev_e222f50acdfc
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/GetCustomRoomFilterMessageEvent.java
    symbol: GetCustomRoomFilterMessageEvent
    line: 11
    note: third-party server implementation; evidence, not authority
  - id: ev_5fbb63890d81
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/RoomSettings/GetCustomRoomFilterMessageHandler.cs
    symbol: GetCustomRoomFilterMessageHandler
    line: 8
verified: {}
manual: {}
