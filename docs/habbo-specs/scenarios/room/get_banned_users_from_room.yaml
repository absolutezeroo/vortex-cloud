# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:1df440dd6fedf406eb94f69654fb76eae3cd2c534b5d5665543724dcfd00dbcc"
generated:
  feature: room.get_banned_users_from_room
  scenarios:
  - scenario: room.get_banned_users_from_room.guard_0
    title: "get banned users from room: the guard 'ctx.PlayerId <= 0 || message.RoomId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.RoomId <= 0
      guard_holds: "true"
    when:
      packet: GetBannedUsersFromRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetBannedUsersFromRoom sent while 'ctx.PlayerId <= 0 || message.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_170d536d3818
  - scenario: room.get_banned_users_from_room.success
    title: "get banned users from room: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetBannedUsersFromRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetBannedUsersFromRoom succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - BannedUsersFromRoomEvent
      evidence: ev_170d536d3818
  evidence:
  - id: ev_170d536d3818
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/RoomSettings/GetBannedUsersFromRoomMessageHandler.cs
    symbol: GetBannedUsersFromRoomMessageHandler
    line: 23
verified: {}
manual: {}
