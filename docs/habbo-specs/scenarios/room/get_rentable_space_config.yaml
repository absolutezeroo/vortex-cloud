# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:f39924a14e364bc9f314b974911f33549de1c612ca1a12159b707a5243f63c4c"
generated:
  feature: room.get_rentable_space_config
  scenarios:
  - scenario: room.get_rentable_space_config.guard_0
    title: "get rentable space config: the guard 'ctx.PlayerId <= 0 || message.FurnitureId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.FurnitureId <= 0
      guard_holds: "true"
    when:
      packet: RentableSpaceGetConfig
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RentableSpaceGetConfig sent while 'ctx.PlayerId <= 0 || message.FurnitureId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7cc987ce7b01
  - scenario: room.get_rentable_space_config.success
    title: "get rentable space config: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RentableSpaceGetConfig
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RentableSpaceGetConfig succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - RentableSpaceConfig
      evidence: ev_3bfcf2d3587b
  evidence:
  - id: ev_3bfcf2d3587b
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/RentableSpaceGrain.cs
    symbol: RentableSpaceGrain
    line: 389
  - id: ev_7cc987ce7b01
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Furniture/GetRentableSpaceConfigMessageHandler.cs
    symbol: GetRentableSpaceConfigMessageHandler
    line: 23
verified: {}
manual: {}
