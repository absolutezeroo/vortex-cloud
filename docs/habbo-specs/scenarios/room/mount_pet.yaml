# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:59d8fbfeab99068076cfa189bd3de16a13686059ba52ea84d5509bf8716a72bd"
generated:
  feature: room.mount_pet
  scenarios:
  - scenario: room.mount_pet.guard_0
    title: "mount pet: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0
      guard_holds: "true"
    when:
      packet: MountPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MountPet sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_cd560852d7b5
  - scenario: room.mount_pet.guard_1
    title: "mount pet: the guard '!_roomGrain._state.PetsById.TryGetValue(petId, out PetSna...' holds"
    given:
      guard: "!_roomGrain._state.PetsById.TryGetValue(petId, out PetSnapshot? pet)"
      guard_holds: "true"
    when:
      packet: MountPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MountPet sent while '!_roomGrain._state.PetsById.TryGetValue(petId, out PetSnapshot? pet)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_acc1f9bce7eb
  - scenario: room.mount_pet.guard_2
    title: "mount pet: the guard 'mount' holds"
    given:
      guard: mount
      guard_holds: "true"
    when:
      packet: MountPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MountPet sent while 'mount' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_acc1f9bce7eb
  - scenario: room.mount_pet.guard_3
    title: "mount pet: the guard '!allowed' holds"
    given:
      guard: "!allowed"
      guard_holds: "true"
    when:
      packet: MountPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MountPet sent while '!allowed' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_acc1f9bce7eb
  - scenario: room.mount_pet.guard_4
    title: "mount pet: the guard 'pet.RiderId != ctx.PlayerId' holds"
    given:
      guard: pet.RiderId != ctx.PlayerId
      guard_holds: "true"
    when:
      packet: MountPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MountPet sent while 'pet.RiderId != ctx.PlayerId' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_acc1f9bce7eb
  - scenario: room.mount_pet.success
    title: "mount pet: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: MountPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of MountPet succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3f0163668026
  evidence:
  - id: ev_3f0163668026
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/MountPetMessageHandler.cs
    symbol: MountPetMessageHandler
    line: 11
  - id: ev_acc1f9bce7eb
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomPetSystem.Riding.cs
    symbol: RoomPetSystem
    line: 25
  - id: ev_cd560852d7b5
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/MountPetMessageHandler.cs
    symbol: MountPetMessageHandler
    line: 21
verified: {}
manual: {}
