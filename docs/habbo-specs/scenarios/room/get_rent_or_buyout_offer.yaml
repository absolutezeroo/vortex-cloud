# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:c66b9b3d3a7363aefb6f5023bf58613448edf5228a77617c33d60ada6703afad"
generated:
  feature: room.get_rent_or_buyout_offer
  scenarios:
  - scenario: room.get_rent_or_buyout_offer.success
    title: "get rent or buyout offer: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetRentOrBuyoutOffer
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetRentOrBuyoutOffer succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_2a100b787aa6
  evidence:
  - id: ev_2a100b787aa6
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Furniture/GetRentOrBuyoutOfferMessageHandler.cs
    symbol: GetRentOrBuyoutOfferMessageHandler
    line: 8
verified: {}
manual: {}
