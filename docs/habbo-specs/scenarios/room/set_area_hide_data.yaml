# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:a3d55414bec1642fdb17dffaddbd93c4e1a63c6c6e3b19d1d177f6433d0305f7"
generated:
  feature: room.set_area_hide_data
  scenarios:
  - scenario: room.set_area_hide_data.success
    title: "set area hide data: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: SetAreaHideData
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of SetAreaHideData succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0ca0249efff5
  evidence:
  - id: ev_0ca0249efff5
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Furniture/SetAreaHideDataMessageHandler.cs
    symbol: SetAreaHideDataMessageHandler
    line: 8
verified: {}
manual: {}
