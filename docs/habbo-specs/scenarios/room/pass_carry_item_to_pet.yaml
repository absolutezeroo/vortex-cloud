# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:3f748db4bc82598b319634bcba5a80e475bfddc7fabc822da1d7b8acc201d7ee"
generated:
  feature: room.pass_carry_item_to_pet
  scenarios:
  - scenario: room.pass_carry_item_to_pet.guard_0
    title: "pass carry item to pet: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0
      guard_holds: "true"
    when:
      packet: PassCarryItemToPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of PassCarryItemToPet sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_b835c9e727ff
  - scenario: room.pass_carry_item_to_pet.guard_1
    title: "pass carry item to pet: the guard 'handItemId == 0 || !_roomGrain._state.PetsById.TryGetValu...' holds"
    given:
      guard: handItemId == 0 || !_roomGrain._state.PetsById.TryGetValue(petId, out PetSnapshot? pet)
      guard_holds: "true"
    when:
      packet: PassCarryItemToPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of PassCarryItemToPet sent while 'handItemId == 0 || !_roomGrain._state.PetsById.TryGetValue(petId, out PetSnapshot? pet)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_21367fcb5d6f
  - scenario: room.pass_carry_item_to_pet.guard_2
    title: "pass carry item to pet: the guard '!IsActorWithinReachOfPet(ctx, pet)' holds"
    given:
      guard: "!IsActorWithinReachOfPet(ctx, pet)"
      guard_holds: "true"
    when:
      packet: PassCarryItemToPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of PassCarryItemToPet sent while '!IsActorWithinReachOfPet(ctx, pet)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_21367fcb5d6f
  - scenario: room.pass_carry_item_to_pet.guard_3
    title: "pass carry item to pet: the guard 'handItem is null || (handItem.Nutrition <= 0 && handItem....' holds"
    given:
      guard: handItem is null || (handItem.Nutrition <= 0 && handItem.Thirst <= 0)
      guard_holds: "true"
    when:
      packet: PassCarryItemToPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of PassCarryItemToPet sent while 'handItem is null || (handItem.Nutrition <= 0 && handItem.Thirst <= 0)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_21367fcb5d6f
  - scenario: room.pass_carry_item_to_pet.guard_4
    title: "pass carry item to pet: the guard '!TryFindAvatar(playerId, out IRoomAvatar? avatar) || avat...' holds"
    given:
      guard: "!TryFindAvatar(playerId, out IRoomAvatar? avatar) || avatar.CarryItemId == 0"
      guard_holds: "true"
    when:
      packet: PassCarryItemToPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of PassCarryItemToPet sent while '!TryFindAvatar(playerId, out IRoomAvatar? avatar) || avatar.CarryItemId == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_315e04717bdc
  - scenario: room.pass_carry_item_to_pet.guard_5
    title: "pass carry item to pet: the guard 'CarryItemId == itemId' holds"
    given:
      guard: CarryItemId == itemId
      guard_holds: "true"
    when:
      packet: PassCarryItemToPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of PassCarryItemToPet sent while 'CarryItemId == itemId' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_9f45a8c43da8
  - scenario: room.pass_carry_item_to_pet.success
    title: "pass carry item to pet: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: PassCarryItemToPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of PassCarryItemToPet succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_21367fcb5d6f
  evidence:
  - id: ev_21367fcb5d6f
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomPetSystem.HandItem.cs
    symbol: RoomPetSystem
    line: 31
  - id: ev_315e04717bdc
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomHandItemModule.cs
    symbol: RoomHandItemModule
    line: 52
  - id: ev_9f45a8c43da8
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Object/Avatars/RoomAvatar.cs
    symbol: RoomAvatar
    line: 221
  - id: ev_b835c9e727ff
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Avatar/PassCarryItemToPetMessageHandler.cs
    symbol: PassCarryItemToPetMessageHandler
    line: 20
verified: {}
manual: {}
