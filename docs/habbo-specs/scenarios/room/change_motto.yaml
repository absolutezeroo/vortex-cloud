# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:a9a7a190cfa626c2d399fa3b385f158b643e349b38000e875f2d14c99214b21f"
generated:
  feature: room.change_motto
  scenarios:
  - scenario: room.change_motto.guard_0
    title: "change motto: the guard 'ctx.PlayerId < 0' holds"
    given:
      guard: ctx.PlayerId < 0
      guard_holds: "true"
    when:
      packet: ChangeMotto
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ChangeMotto sent while 'ctx.PlayerId < 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_2aec8f9f5aa3
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - UserChange
      evidence: ev_2407931e65ff
  - scenario: room.change_motto.guard_1
    title: "change motto: the guard 'snapshot.PlayerId <= 0 || !_roomGrain._state.AvatarsByPla...' holds"
    given:
      guard: snapshot.PlayerId <= 0 || !_roomGrain._state.AvatarsByPlayerId.TryGetValue( snapshot.PlayerId, out RoomObjectId objectId ) || !_roomGrain._state.AvatarsByObjectId.TryGetValue(objectId, out IRoomAvatar? avatar) || avatar is not IRoomPlayer avatarPlayer || !avatarPlayer.UpdateWithPlayer(snapshot)
      guard_holds: "true"
    when:
      packet: ChangeMotto
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ChangeMotto sent while 'snapshot.PlayerId <= 0 || !_roomGrain._state.AvatarsByPlayerId.TryGetValue( snapshot.PlayerId, out RoomObjectId objectId ) || !_roomGrain._state.AvatarsByObjectId.TryGetValue(objectId, out IRoomAvatar? avatar) || avatar is not IRoomPlayer avatarPlayer || !avatarPlayer.UpdateWithPlayer(snapshot)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e6fef545a42a
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - UserChange
      evidence: ev_2407931e65ff
  - scenario: room.change_motto.guard_2
    title: "change motto: the guard '_registry is null' holds"
    given:
      guard: _registry is null
      guard_holds: "true"
    when:
      packet: ChangeMotto
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ChangeMotto sent while '_registry is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_34bf3ac088e2
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - UserChange
      evidence: ev_2407931e65ff
  - scenario: room.change_motto.success
    title: "change motto: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ChangeMotto
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ChangeMotto succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - UserChange
      evidence: ev_1f83e61934ca
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - UserChange
      evidence: ev_2407931e65ff
  evidence:
  - id: ev_2407931e65ff
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/users/ChangeMottoMessageEvent.java
    symbol: ChangeMottoMessageEvent
    line: 11
    note: third-party server implementation; evidence, not authority
  - id: ev_1f83e61934ca
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerGrain.cs
    symbol: PlayerGrain
    line: 182
  - id: ev_2aec8f9f5aa3
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Avatar/ChangeMottoMessageHandler.cs
    symbol: ChangeMottoMessageHandler
    line: 22
  - id: ev_34bf3ac088e2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Events/EventSystem.cs
    symbol: EventSystem
    line: 16
  - id: ev_e6fef545a42a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomAvatarModule.cs
    symbol: RoomAvatarModule
    line: 514
verified: {}
manual: {}
