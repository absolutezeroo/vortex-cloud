# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:1dba3a930dbe9ed80cdc6917e92a2f7910772c9c4e141a73bf293a972ae6aed2"
generated:
  feature: room.remove_saddle_from_pet
  scenarios:
  - scenario: room.remove_saddle_from_pet.guard_0
    title: "remove saddle from pet: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0
      guard_holds: "true"
    when:
      packet: RemoveSaddleFromPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RemoveSaddleFromPet sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_85b544fbe508
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - UnseenItems
      - FurniListInvalidate
      evidence: ev_52eb3665510b
  - scenario: room.remove_saddle_from_pet.guard_1
    title: "remove saddle from pet: the guard '!_roomGrain._state.PetsById.TryGetValue(petId, out PetSna...' holds"
    given:
      guard: "!_roomGrain._state.PetsById.TryGetValue(petId, out PetSnapshot? pet) || pet.OwnerId != ctx.PlayerId"
      guard_holds: "true"
    when:
      packet: RemoveSaddleFromPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RemoveSaddleFromPet sent while '!_roomGrain._state.PetsById.TryGetValue(petId, out PetSnapshot? pet) || pet.OwnerId != ctx.PlayerId' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_acc1f9bce7eb
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - UnseenItems
      - FurniListInvalidate
      evidence: ev_52eb3665510b
  - scenario: room.remove_saddle_from_pet.success
    title: "remove saddle from pet: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RemoveSaddleFromPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RemoveSaddleFromPet succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_472f140d6fa1
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - UnseenItems
      - FurniListInvalidate
      evidence: ev_52eb3665510b
  evidence:
  - id: ev_52eb3665510b
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/pets/RemoveSaddleFromPetMessageEvent.java
    symbol: RemoveSaddleFromPetMessageEvent
    line: 25
    note: third-party server implementation; evidence, not authority
  - id: ev_472f140d6fa1
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/RemoveSaddleFromPetMessageHandler.cs
    symbol: RemoveSaddleFromPetMessageHandler
    line: 11
  - id: ev_85b544fbe508
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/RemoveSaddleFromPetMessageHandler.cs
    symbol: RemoveSaddleFromPetMessageHandler
    line: 22
  - id: ev_acc1f9bce7eb
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomPetSystem.Riding.cs
    symbol: RoomPetSystem
    line: 63
verified: {}
manual: {}
