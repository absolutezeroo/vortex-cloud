# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:d434918d7442522e2b1b942a53b2d2146f594bbc62d33f51f8e084cda757e09c"
generated:
  feature: room.command_bot
  scenarios:
  - scenario: room.command_bot.guard_0
    title: "command bot: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.BotId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.BotId <= 0
      guard_holds: "true"
    when:
      packet: CommandBot
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of CommandBot sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.BotId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_27143b751641
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - Dance
      - UserNameChanged
      - BotError
      - Users
      evidence: ev_aba982f1be68
  - scenario: room.command_bot.success
    title: "command bot: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: CommandBot
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of CommandBot succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_27143b751641
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - Dance
      - UserNameChanged
      - BotError
      - Users
      evidence: ev_aba982f1be68
  evidence:
  - id: ev_aba982f1be68
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/bots/CommandBotEvent.java
    symbol: CommandBotEvent
    line: 23
    note: third-party server implementation; evidence, not authority
  - id: ev_27143b751641
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Bots/CommandBotMessageHandler.cs
    symbol: CommandBotMessageHandler
    line: 19
verified: {}
manual: {}
