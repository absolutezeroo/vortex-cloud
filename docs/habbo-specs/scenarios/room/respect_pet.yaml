# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:c70641ff19f72d598d4fa3b4cbb42b6ca19c93f807d8d3421b438d529af94149"
generated:
  feature: room.respect_pet
  scenarios:
  - scenario: room.respect_pet.guard_0
    title: "respect pet: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0
      guard_holds: "true"
    when:
      packet: RespectPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RespectPet sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1557b71ceec2
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_8b5b3e27d8c5
  - scenario: room.respect_pet.guard_1
    title: "respect pet: the guard '!_roomGrain._state.PetsById.TryGetValue(petId, out PetSna...' holds"
    given:
      guard: "!_roomGrain._state.PetsById.TryGetValue(petId, out PetSnapshot? pet)"
      guard_holds: "true"
    when:
      packet: RespectPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RespectPet sent while '!_roomGrain._state.PetsById.TryGetValue(petId, out PetSnapshot? pet)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3b983736fec5
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_8b5b3e27d8c5
  - scenario: room.respect_pet.guard_2
    title: "respect pet: the guard 'RoomPetRuntime.IsPlant(pet)' holds"
    given:
      guard: RoomPetRuntime.IsPlant(pet)
      guard_holds: "true"
    when:
      packet: RespectPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RespectPet sent while 'RoomPetRuntime.IsPlant(pet)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3b983736fec5
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_8b5b3e27d8c5
  - scenario: room.respect_pet.guard_3
    title: "respect pet: the guard 'ageDays < minimumAgeDays' holds"
    given:
      guard: ageDays < minimumAgeDays
      guard_holds: "true"
    when:
      packet: RespectPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RespectPet sent while 'ageDays < minimumAgeDays' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: rejected
      emits:
      - PetRespectFailed
      evidence: ev_3b983736fec5
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_8b5b3e27d8c5
  - scenario: room.respect_pet.guard_4
    title: "respect pet: the guard 'pet.RespectTodayCount >= dailyCap' holds"
    given:
      guard: pet.RespectTodayCount >= dailyCap
      guard_holds: "true"
    when:
      packet: RespectPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RespectPet sent while 'pet.RespectTodayCount >= dailyCap' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3b983736fec5
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_8b5b3e27d8c5
  - scenario: room.respect_pet.success
    title: "respect pet: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RespectPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RespectPet succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - PetRespectFailed
      evidence: ev_1557b71ceec2
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_8b5b3e27d8c5
  evidence:
  - id: ev_8b5b3e27d8c5
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/pets/RespectPetMessageEvent.java
    symbol: RespectPetMessageEvent
    line: 19
    note: third-party server implementation; evidence, not authority
  - id: ev_1557b71ceec2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Pets/RespectPetMessageHandler.cs
    symbol: RespectPetMessageHandler
    line: 22
  - id: ev_3b983736fec5
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomPetSystem.Care.cs
    symbol: RoomPetSystem
    line: 42
verified: {}
manual: {}
