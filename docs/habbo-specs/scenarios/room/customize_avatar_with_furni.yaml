# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:609efc5ae4d07aeb6041249b5ee6804e7a505b4b34c9e309ea4e5e90e3907ae2"
generated:
  feature: room.customize_avatar_with_furni
  scenarios:
  - scenario: room.customize_avatar_with_furni.success
    title: "customize avatar with furni: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: CustomizeAvatarWithFurni
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of CustomizeAvatarWithFurni succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e73401e732a3
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - FigureSetIds
      - NotificationDialog
      - NotificationDialog
      evidence: ev_533f6f514f5a
  evidence:
  - id: ev_533f6f514f5a
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/items/CustomizeAvatarWithFurniMessageEvent.java
    symbol: CustomizeAvatarWithFurniMessageEvent
    line: 26
    note: third-party server implementation; evidence, not authority
  - id: ev_e73401e732a3
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Avatar/CustomizeAvatarWithFurniMessageHandler.cs
    symbol: CustomizeAvatarWithFurniMessageHandler
    line: 8
verified: {}
manual: {}
