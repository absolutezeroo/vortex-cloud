# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:2ad44f5078a3d4dca916310a47fc80a1564355f3ab8d4a7b1f45564b17f1e270"
generated:
  feature: room.set_object_data
  scenarios:
  - scenario: room.set_object_data.guard_0
    title: "set object data: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.Pairs.IsD...' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.Pairs.IsDefaultOrEmpty
      guard_holds: "true"
    when:
      packet: SetObjectData
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SetObjectData sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.Pairs.IsDefaultOrEmpty' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ead081b2ea17
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_92eca25ad89b
  - scenario: room.set_object_data.guard_1
    title: "set object data: the guard 'item?.Logic.StuffData is not IMapStuffData map' holds"
    given:
      guard: item?.Logic.StuffData is not IMapStuffData map
      guard_holds: "true"
    when:
      packet: SetObjectData
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SetObjectData sent while 'item?.Logic.StuffData is not IMapStuffData map' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_5e51a330c801
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_92eca25ad89b
  - scenario: room.set_object_data.success
    title: "set object data: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: SetObjectData
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of SetObjectData succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_5e51a330c801
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_92eca25ad89b
  evidence:
  - id: ev_92eca25ad89b
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/items/SetObjectDataMessageEvent.java
    symbol: SetObjectDataMessageEvent
    line: 13
    note: third-party server implementation; evidence, not authority
  - id: ev_5e51a330c801
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Furni.Interactive.cs
    symbol: RoomGrain
    line: 675
  - id: ev_ead081b2ea17
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/SetObjectDataMessageHandler.cs
    symbol: SetObjectDataMessageHandler
    line: 23
verified: {}
manual: {}
