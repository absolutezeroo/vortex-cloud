# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:9c6b3f2950f24dd142719db2ee7b6089f301eacd019a403dd6fc149887531d93"
generated:
  feature: room.click_item_in_room
  scenarios:
  - scenario: room.click_item_in_room.guard_0
    title: "click item in room: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || itemId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || itemId <= 0
      guard_holds: "true"
    when:
      packet: ClickFurni
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ClickFurni sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || itemId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7c6cea3bc17b
  - scenario: room.click_item_in_room.guard_1
    title: "click item in room: the guard '!await ActionModule.ClickItemByIdAsync(ctx, itemId, ct, p...' holds"
    given:
      guard: "!await ActionModule.ClickItemByIdAsync(ctx, itemId, ct, param)"
      guard_holds: "true"
    when:
      packet: ClickFurni
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ClickFurni sent while '!await ActionModule.ClickItemByIdAsync(ctx, itemId, ct, param)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_2298dc2dabc2
  - scenario: room.click_item_in_room.guard_2
    title: "click item in room: the guard '!_roomGrain._state.ItemsById.TryGetValue(itemId, out IRoo...' holds"
    given:
      guard: "!_roomGrain._state.ItemsById.TryGetValue(itemId, out IRoomItem? item)"
      guard_holds: "true"
    when:
      packet: ClickFurni
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ClickFurni sent while '!_roomGrain._state.ItemsById.TryGetValue(itemId, out IRoomItem? item)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: rejected
      emits: []
      evidence: ev_e4165c982e0e
  - scenario: room.click_item_in_room.success
    title: "click item in room: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ClickFurni
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ClickFurni succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_2298dc2dabc2
  evidence:
  - id: ev_2298dc2dabc2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Furni.cs
    symbol: RoomGrain
    line: 109
  - id: ev_7c6cea3bc17b
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/RoomService.cs
    symbol: RoomService
    line: 370
  - id: ev_e4165c982e0e
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomFurniModule.cs
    symbol: RoomFurniModule
    line: 108
verified: {}
manual: {}
