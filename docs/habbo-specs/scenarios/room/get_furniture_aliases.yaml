# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:681548b92686ea0f3e0d1ffb4363d3e25502b40898a80c07af3043b91c05a7a2"
generated:
  feature: room.get_furniture_aliases
  scenarios:
  - scenario: room.get_furniture_aliases.success
    title: "get furniture aliases: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetFurnitureAliases
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetFurnitureAliases succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - FurnitureAliases
      evidence: ev_366a7a0309d8
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_c7bfd6b37eb1
  evidence:
  - id: ev_c7bfd6b37eb1
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/GetFurnitureAliasesMessageEvent.java
    symbol: GetFurnitureAliasesMessageEvent
    line: 9
    note: third-party server implementation; evidence, not authority
  - id: ev_366a7a0309d8
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/GetFurnitureAliasesMessageHandler.cs
    symbol: GetFurnitureAliasesMessageHandler
    line: 9
verified: {}
manual: {}
