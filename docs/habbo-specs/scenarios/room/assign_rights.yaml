# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:f4b2b17f7444629169568c497b8771f692e1078849445497b57ba8b1819543a7"
generated:
  feature: room.assign_rights
  scenarios:
  - scenario: room.assign_rights.guard_0
    title: "assign rights: the guard 'ctx.PlayerId <= 0 || message.TargetUserId <= 0 || ctx.Roo...' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.TargetUserId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: AssignRights
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AssignRights sent while 'ctx.PlayerId <= 0 || message.TargetUserId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3fffbe68a58d
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_805b538d3195
  - scenario: room.assign_rights.guard_1
    title: "assign rights: the guard 'target <= 0 || actor == target || !await IsRoomOwnerAsync...' holds"
    given:
      guard: target <= 0 || actor == target || !await IsRoomOwnerAsync(actor).ConfigureAwait(true)
      guard_holds: "true"
    when:
      packet: AssignRights
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AssignRights sent while 'target <= 0 || actor == target || !await IsRoomOwnerAsync(actor).ConfigureAwait(true)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_329537530b13
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_805b538d3195
  - scenario: room.assign_rights.guard_2
    title: "assign rights: the guard 'alreadyHasRights' holds"
    given:
      guard: alreadyHasRights
      guard_holds: "true"
    when:
      packet: AssignRights
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AssignRights sent while 'alreadyHasRights' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_329537530b13
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_805b538d3195
  - scenario: room.assign_rights.guard_3
    title: "assign rights: the guard '!_roomGrain._state.AvatarsByPlayerId.TryGetValue(playerId...' holds"
    given:
      guard: "!_roomGrain._state.AvatarsByPlayerId.TryGetValue(playerId, out RoomObjectId objectId) || !_roomGrain._state.AvatarsByObjectId.TryGetValue(objectId, out IRoomAvatar? avatar)"
      guard_holds: "true"
    when:
      packet: AssignRights
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AssignRights sent while '!_roomGrain._state.AvatarsByPlayerId.TryGetValue(playerId, out RoomObjectId objectId) || !_roomGrain._state.AvatarsByObjectId.TryGetValue(objectId, out IRoomAvatar? avatar)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_82c2d2746361
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_805b538d3195
  - scenario: room.assign_rights.guard_4
    title: "assign rights: the guard '_state.ActiveRoomId != roomId' holds"
    given:
      guard: _state.ActiveRoomId != roomId
      guard_holds: "true"
    when:
      packet: AssignRights
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AssignRights sent while '_state.ActiveRoomId != roomId' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_b44ef304d36a
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_805b538d3195
  - scenario: room.assign_rights.guard_5
    title: "assign rights: the guard 'controllerType >= RoomControllerType.Rights' holds"
    given:
      guard: controllerType >= RoomControllerType.Rights
      guard_holds: "true"
    when:
      packet: AssignRights
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AssignRights sent while 'controllerType >= RoomControllerType.Rights' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_b44ef304d36a
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_805b538d3195
  - scenario: room.assign_rights.guard_6
    title: "assign rights: the guard '_registry is null' holds"
    given:
      guard: _registry is null
      guard_holds: "true"
    when:
      packet: AssignRights
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AssignRights sent while '_registry is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_34bf3ac088e2
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_805b538d3195
  - scenario: room.assign_rights.success
    title: "assign rights: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: AssignRights
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of AssignRights succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - FlatControllersEvent
      - WiredPermissionsEvent
      - YouAreController
      - YouAreNotController
      - YouAreOwner
      evidence: ev_329537530b13
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_805b538d3195
  evidence:
  - id: ev_805b538d3195
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/users/AssignRightsMessageEvent.java
    symbol: AssignRightsMessageEvent
    line: 13
    note: third-party server implementation; evidence, not authority
  - id: ev_329537530b13
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Settings.cs
    symbol: RoomGrain
    line: 389
  - id: ev_34bf3ac088e2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Events/EventSystem.cs
    symbol: EventSystem
    line: 16
  - id: ev_3fffbe68a58d
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Action/AssignRightsMessageHandler.cs
    symbol: AssignRightsMessageHandler
    line: 22
  - id: ev_82c2d2746361
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomSecurityModule.cs
    symbol: RoomSecurityModule
    line: 256
  - id: ev_b44ef304d36a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerPresenceGrain.Room.cs
    symbol: PlayerPresenceGrain
    line: 199
verified: {}
manual: {}
