# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:bacfa40293c9eff48977255716b9261edd5718e0b17cdca198dfc51eefd3ca79"
generated:
  feature: room.get_raid_protection_settings
  scenarios:
  - scenario: room.get_raid_protection_settings.guard_0
    title: "get raid protection settings: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.RoomId !=...' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.RoomId != ctx.RoomId
      guard_holds: "true"
    when:
      packet: GetRaidProtectionSettings
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetRaidProtectionSettings sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.RoomId != ctx.RoomId' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_c7f37b1df18c
  - scenario: room.get_raid_protection_settings.guard_1
    title: "get raid protection settings: the guard 'settings is null' holds"
    given:
      guard: settings is null
      guard_holds: "true"
    when:
      packet: GetRaidProtectionSettings
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetRaidProtectionSettings sent while 'settings is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_c7f37b1df18c
  - scenario: room.get_raid_protection_settings.success
    title: "get raid protection settings: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetRaidProtectionSettings
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetRaidProtectionSettings succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - RaidProtectionSettings
      evidence: ev_c7f37b1df18c
  evidence:
  - id: ev_c7f37b1df18c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/RaidProtection/GetRaidProtectionSettingsMessageHandler.cs
    symbol: GetRaidProtectionSettingsMessageHandler
    line: 35
verified: {}
manual: {}
