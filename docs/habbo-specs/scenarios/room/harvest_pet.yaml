# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:d1132d8962ecd44679d0536fd4786390027584420b75a6c5a827ab6739985699"
generated:
  feature: room.harvest_pet
  scenarios:
  - scenario: room.harvest_pet.guard_0
    title: "harvest pet: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0
      guard_holds: "true"
    when:
      packet: HarvestPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of HarvestPet sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0c7e31931576
  - scenario: room.harvest_pet.guard_1
    title: "harvest pet: the guard '!_roomGrain._state.PetsById.TryGetValue(petId, out PetSna...' holds"
    given:
      guard: "!_roomGrain._state.PetsById.TryGetValue(petId, out PetSnapshot? plant) || plant.Type != MonsterplantPetType || plant.OwnerId != ctx.PlayerId || plant.Level < PlantMaxLevel || !plant.CanBreed || plant.Energy <= 0"
      guard_holds: "true"
    when:
      packet: HarvestPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of HarvestPet sent while '!_roomGrain._state.PetsById.TryGetValue(petId, out PetSnapshot? plant) || plant.Type != MonsterplantPetType || plant.OwnerId != ctx.PlayerId || plant.Level < PlantMaxLevel || !plant.CanBreed || plant.Energy <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a24ca6ca32d9
  - scenario: room.harvest_pet.guard_2
    title: "harvest pet: the guard 'seedDefinitionId is null' holds"
    given:
      guard: seedDefinitionId is null
      guard_holds: "true"
    when:
      packet: HarvestPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of HarvestPet sent while 'seedDefinitionId is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a24ca6ca32d9
  - scenario: room.harvest_pet.guard_3
    title: "harvest pet: the guard '!string.IsNullOrEmpty(jsonData)' holds"
    given:
      guard: "!string.IsNullOrEmpty(jsonData)"
      guard_holds: "true"
    when:
      packet: HarvestPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of HarvestPet sent while '!string.IsNullOrEmpty(jsonData)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7a660921f135
  - scenario: room.harvest_pet.success
    title: "harvest pet: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: HarvestPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of HarvestPet succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0c7e31931576
  evidence:
  - id: ev_0c7e31931576
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Pets/HarvestPetMessageHandler.cs
    symbol: HarvestPetMessageHandler
    line: 24
  - id: ev_7a660921f135
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Furniture/Providers/StuffDataFactory.cs
    symbol: StuffDataFactory
    line: 46
  - id: ev_a24ca6ca32d9
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomPetSystem.Plants.cs
    symbol: RoomPetSystem
    line: 168
verified: {}
manual: {}
