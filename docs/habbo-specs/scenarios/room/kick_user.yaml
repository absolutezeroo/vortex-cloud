# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:4c5e43c4a41c1d932c1d451a76b6c8e62fea03f3925f26889c4b4079ab4187e6"
generated:
  feature: room.kick_user
  scenarios:
  - scenario: room.kick_user.guard_0
    title: "kick user: the guard 'ctx.PlayerId <= 0 || message.UserId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.UserId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: KickUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of KickUser sent while 'ctx.PlayerId <= 0 || message.UserId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_fa1bb9b48a63
  - scenario: room.kick_user.guard_1
    title: "kick user: the guard '!await RoomModerationGuard .CanActOnTargetAsync( permissi...' holds"
    given:
      guard: "!await RoomModerationGuard .CanActOnTargetAsync( permissionService, events, ctx, ctx.RoomId, message.UserId, ModerationAction.Kick, ct ) .ConfigureAwait(false)"
      guard_holds: "true"
    when:
      packet: KickUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of KickUser sent while '!await RoomModerationGuard .CanActOnTargetAsync( permissionService, events, ctx, ctx.RoomId, message.UserId, ModerationAction.Kick, ct ) .ConfigureAwait(false)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_fa1bb9b48a63
  - scenario: room.kick_user.guard_2
    title: "kick user: the guard 'ModerationPolicy.CanActOnTarget(actorPermissions, targetP...' holds"
    given:
      guard: ModerationPolicy.CanActOnTarget(actorPermissions, targetPermissions)
      guard_holds: "true"
    when:
      packet: KickUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of KickUser sent while 'ModerationPolicy.CanActOnTarget(actorPermissions, targetPermissions)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ede1461ee378
  - scenario: room.kick_user.guard_3
    title: "kick user: the guard 'playerId <= 0' holds"
    given:
      guard: playerId <= 0
      guard_holds: "true"
    when:
      packet: KickUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of KickUser sent while 'playerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_743940bb69f7
  - scenario: room.kick_user.guard_4
    title: "kick user: the guard 'actorCtx.PlayerId <= 0 || actorCtx.RoomId != _roomGrain._...' holds"
    given:
      guard: actorCtx.PlayerId <= 0 || actorCtx.RoomId != _roomGrain._state.RoomId || actorCtx.PlayerId == targetPlayerId
      guard_holds: "true"
    when:
      packet: KickUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of KickUser sent while 'actorCtx.PlayerId <= 0 || actorCtx.RoomId != _roomGrain._state.RoomId || actorCtx.PlayerId == targetPlayerId' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_9eb84a2a2088
  - scenario: room.kick_user.guard_5
    title: "kick user: the guard '_registry is null' holds"
    given:
      guard: _registry is null
      guard_holds: "true"
    when:
      packet: KickUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of KickUser sent while '_registry is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_34bf3ac088e2
  - scenario: room.kick_user.success
    title: "kick user: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: KickUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of KickUser succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_34bf3ac088e2
  evidence:
  - id: ev_34bf3ac088e2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Events/EventSystem.cs
    symbol: EventSystem
    line: 16
  - id: ev_743940bb69f7
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Authentication/Permissions/PermissionService.cs
    symbol: PermissionService
    line: 62
  - id: ev_9eb84a2a2088
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomModerationSystem.cs
    symbol: RoomModerationSystem
    line: 33
  - id: ev_ede1461ee378
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Action/RoomModerationGuard.cs
    symbol: RoomModerationGuard
    line: 48
  - id: ev_fa1bb9b48a63
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Action/KickUserMessageHandler.cs
    symbol: KickUserMessageHandler
    line: 26
verified: {}
manual: {}
