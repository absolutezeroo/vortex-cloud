# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:2cf3c7b023f9ccc3d403ad5d19629dc638e95c65bad9b3013a7aede209db1c2d"
generated:
  feature: room.remove_item
  scenarios:
  - scenario: room.remove_item.guard_0
    title: "remove item: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: RemoveItem
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RemoveItem sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a311be697b0f
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - ItemRemove
      evidence: ev_005598a2f1e5
  - scenario: room.remove_item.guard_1
    title: "remove item: the guard 'item is not IRoomWallItem' holds"
    given:
      guard: item is not IRoomWallItem
      guard_holds: "true"
    when:
      packet: RemoveItem
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RemoveItem sent while 'item is not IRoomWallItem' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_5e51a330c801
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - ItemRemove
      evidence: ev_005598a2f1e5
  - scenario: room.remove_item.guard_2
    title: "remove item: the guard 'Array.IndexOf(DisposableWallLogics, item.Definition.Logic...' holds"
    given:
      guard: Array.IndexOf(DisposableWallLogics, item.Definition.LogicName) < 0
      guard_holds: "true"
    when:
      packet: RemoveItem
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RemoveItem sent while 'Array.IndexOf(DisposableWallLogics, item.Definition.LogicName) < 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_5e51a330c801
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - ItemRemove
      evidence: ev_005598a2f1e5
  - scenario: room.remove_item.success
    title: "remove item: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RemoveItem
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RemoveItem succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_5e51a330c801
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - ItemRemove
      evidence: ev_005598a2f1e5
  evidence:
  - id: ev_005598a2f1e5
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/items/RemoveItemMessageEvent.java
    symbol: RemoveItemMessageEvent
    line: 15
    note: third-party server implementation; evidence, not authority
  - id: ev_5e51a330c801
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Furni.Interactive.cs
    symbol: RoomGrain
    line: 263
  - id: ev_a311be697b0f
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/RemoveItemMessageHandler.cs
    symbol: RemoveItemMessageHandler
    line: 23
verified: {}
manual: {}
