# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:1ca1c5875840f32d402d47d219654b8493af8fdfcbf549ea36bba72cbd2bc8f6"
generated:
  feature: room.click_tile
  scenarios:
  - scenario: room.click_tile.guard_0
    title: "click tile: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: MoveAvatar
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MoveAvatar sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7c6cea3bc17b
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_f034e257c0f5
  - scenario: room.click_tile.guard_1
    title: "click tile: the guard '!tile.Has(RoomTileFlags.TileClickListener)' holds"
    given:
      guard: "!tile.Has(RoomTileFlags.TileClickListener)"
      guard_holds: "true"
    when:
      packet: MoveAvatar
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MoveAvatar sent while '!tile.Has(RoomTileFlags.TileClickListener)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_c26714967e6b
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_f034e257c0f5
  - scenario: room.click_tile.guard_2
    title: "click tile: the guard 'ctx.PlayerId <= 0 || !_roomGrain._state.AvatarsByPlayerId...' holds"
    given:
      guard: ctx.PlayerId <= 0 || !_roomGrain._state.AvatarsByPlayerId.TryGetValue( ctx.PlayerId, out RoomObjectId objectIdValue ) || !_roomGrain._state.AvatarsByObjectId.TryGetValue( objectIdValue, out IRoomAvatar? avatar ) || !await WalkAvatarToAsync(avatar, targetX, targetY, ct)
      guard_holds: "true"
    when:
      packet: MoveAvatar
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MoveAvatar sent while 'ctx.PlayerId <= 0 || !_roomGrain._state.AvatarsByPlayerId.TryGetValue( ctx.PlayerId, out RoomObjectId objectIdValue ) || !_roomGrain._state.AvatarsByObjectId.TryGetValue( objectIdValue, out IRoomAvatar? avatar ) || !await WalkAvatarToAsync(avatar, targetX, targetY, ct)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e6fef545a42a
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_f034e257c0f5
  - scenario: room.click_tile.success
    title: "click tile: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: MoveAvatar
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of MoveAvatar succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3dfe9ba800df
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_f034e257c0f5
  evidence:
  - id: ev_f034e257c0f5
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/users/MoveAvatarMessageEvent.java
    symbol: MoveAvatarMessageEvent
    line: 30
    note: third-party server implementation; evidence, not authority
  - id: ev_3dfe9ba800df
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomEventModule.cs
    symbol: RoomEventModule
    line: 27
  - id: ev_7c6cea3bc17b
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/RoomService.cs
    symbol: RoomService
    line: 314
  - id: ev_c26714967e6b
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomMapModule.cs
    symbol: RoomMapModule
    line: 292
  - id: ev_e6fef545a42a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomAvatarModule.cs
    symbol: RoomAvatarModule
    line: 207
verified: {}
manual: {}
