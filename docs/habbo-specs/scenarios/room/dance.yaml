# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:05da3d7b9c8f6bccb0d201603b16095bda74ef9936409356f24d6bdf8b36db23"
generated:
  feature: room.dance
  scenarios:
  - scenario: room.dance.guard_0
    title: "dance: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: Dance
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Dance sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e202f0e0c1a0
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_d980d0ba0a93
  - scenario: room.dance.guard_1
    title: "dance: the guard '!await _grainFactory .GetRoomAvatars(ctx.RoomId) .SetAvat...' holds"
    given:
      guard: "!await _grainFactory .GetRoomAvatars(ctx.RoomId) .SetAvatarDanceAsync(ctx.AsActionContext(), (AvatarDanceType)message.DanceId, ct) .ConfigureAwait(false)"
      guard_holds: "true"
    when:
      packet: Dance
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Dance sent while '!await _grainFactory .GetRoomAvatars(ctx.RoomId) .SetAvatarDanceAsync(ctx.AsActionContext(), (AvatarDanceType)message.DanceId, ct) .ConfigureAwait(false)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e202f0e0c1a0
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_d980d0ba0a93
  - scenario: room.dance.guard_2
    title: "dance: the guard '!_state.AvatarsByPlayerId.TryGetValue(ctx.PlayerId, out R...' holds"
    given:
      guard: "!_state.AvatarsByPlayerId.TryGetValue(ctx.PlayerId, out RoomObjectId objectId) || !await AvatarModule.SetAvatarDanceAsync(objectId, danceType, ct)"
      guard_holds: "true"
    when:
      packet: Dance
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Dance sent while '!_state.AvatarsByPlayerId.TryGetValue(ctx.PlayerId, out RoomObjectId objectId) || !await AvatarModule.SetAvatarDanceAsync(objectId, danceType, ct)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ac641f7283b9
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_d980d0ba0a93
  - scenario: room.dance.guard_3
    title: "dance: the guard 'objectId <= 0 || !_roomGrain._state.AvatarsByObjectId.Try...' holds"
    given:
      guard: objectId <= 0 || !_roomGrain._state.AvatarsByObjectId.TryGetValue( objectId.Value, out IRoomAvatar? avatar ) || avatar is not IRoomPlayer player || !player.SetDance(danceType)
      guard_holds: "true"
    when:
      packet: Dance
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Dance sent while 'objectId <= 0 || !_roomGrain._state.AvatarsByObjectId.TryGetValue( objectId.Value, out IRoomAvatar? avatar ) || avatar is not IRoomPlayer player || !player.SetDance(danceType)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e6fef545a42a
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_d980d0ba0a93
  - scenario: room.dance.guard_4
    title: "dance: the guard 'DanceType == danceType' holds"
    given:
      guard: DanceType == danceType
      guard_holds: "true"
    when:
      packet: Dance
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Dance sent while 'DanceType == danceType' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7fd0730f7cca
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_d980d0ba0a93
  - scenario: room.dance.guard_5
    title: "dance: the guard 'HasStatus(AvatarStatusType.Sit, AvatarStatusType.Lay)' holds"
    given:
      guard: HasStatus(AvatarStatusType.Sit, AvatarStatusType.Lay)
      guard_holds: "true"
    when:
      packet: Dance
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Dance sent while 'HasStatus(AvatarStatusType.Sit, AvatarStatusType.Lay)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7fd0730f7cca
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_d980d0ba0a93
  - scenario: room.dance.success
    title: "dance: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: Dance
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of Dance succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - Dance
      evidence: ev_274cd76a0729
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_d980d0ba0a93
  evidence:
  - id: ev_d980d0ba0a93
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/users/DanceMessageEvent.java
    symbol: DanceMessageEvent
    line: 13
    note: third-party server implementation; evidence, not authority
  - id: ev_274cd76a0729
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Avatar/DanceMessageHandler.cs
    symbol: DanceMessageHandler
    line: 12
  - id: ev_7fd0730f7cca
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Object/Avatars/Player/RoomPlayerAvatar.cs
    symbol: RoomPlayerAvatar
    line: 93
  - id: ev_ac641f7283b9
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Avatar.cs
    symbol: RoomGrain
    line: 193
  - id: ev_e202f0e0c1a0
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Avatar/DanceMessageHandler.cs
    symbol: DanceMessageHandler
    line: 22
  - id: ev_e6fef545a42a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomAvatarModule.cs
    symbol: RoomAvatarModule
    line: 465
verified: {}
manual: {}
