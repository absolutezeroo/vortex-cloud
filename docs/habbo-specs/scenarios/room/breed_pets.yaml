# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:3a3927d9db9ea3747e74422989e43b46db918ee3bd04ff8076225a351834c547"
generated:
  feature: room.breed_pets
  scenarios:
  - scenario: room.breed_pets.guard_0
    title: "breed pets: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetOneId ...' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetOneId <= 0 || message.PetTwoId <= 0
      guard_holds: "true"
    when:
      packet: BreedPets
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of BreedPets sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetOneId <= 0 || message.PetTwoId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_01eee9e5476a
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_a78a45b7847f
  - scenario: room.breed_pets.guard_1
    title: "breed pets: the guard '!_roomGrain._state.PetsById.TryGetValue(petOneId, out Pet...' holds"
    given:
      guard: "!_roomGrain._state.PetsById.TryGetValue(petOneId, out PetSnapshot? petOne) || !_roomGrain._state.PetsById.TryGetValue(petTwoId, out PetSnapshot? petTwo)"
      guard_holds: "true"
    when:
      packet: BreedPets
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of BreedPets sent while '!_roomGrain._state.PetsById.TryGetValue(petOneId, out PetSnapshot? petOne) || !_roomGrain._state.PetsById.TryGetValue(petTwoId, out PetSnapshot? petTwo)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e3aeea05728f
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_a78a45b7847f
  - scenario: room.breed_pets.guard_2
    title: "breed pets: the guard 'petOne.Type != petTwo.Type' holds"
    given:
      guard: petOne.Type != petTwo.Type
      guard_holds: "true"
    when:
      packet: BreedPets
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of BreedPets sent while 'petOne.Type != petTwo.Type' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e3aeea05728f
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_a78a45b7847f
  - scenario: room.breed_pets.guard_3
    title: "breed pets: the guard '!petTwo.CanBreed' holds"
    given:
      guard: "!petTwo.CanBreed"
      guard_holds: "true"
    when:
      packet: BreedPets
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of BreedPets sent while '!petTwo.CanBreed' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e3aeea05728f
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_a78a45b7847f
  - scenario: room.breed_pets.guard_4
    title: "breed pets: the guard 'effectIds.Count == 0' holds"
    given:
      guard: effectIds.Count == 0
      guard_holds: "true"
    when:
      packet: BreedPets
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of BreedPets sent while 'effectIds.Count == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_fe1afccfe33a
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_a78a45b7847f
  - scenario: room.breed_pets.success
    title: "breed pets: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: BreedPets
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of BreedPets succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - ConfirmBreedingRequestEvent
      - PetBreedingEvent
      evidence: ev_01eee9e5476a
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_a78a45b7847f
  evidence:
  - id: ev_a78a45b7847f
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/pets/BreedPetsMessageEvent.java
    symbol: BreedPetsMessageEvent
    line: 10
    note: third-party server implementation; evidence, not authority
  - id: ev_01eee9e5476a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Pets/BreedPetsMessageHandler.cs
    symbol: BreedPetsMessageHandler
    line: 21
  - id: ev_e3aeea05728f
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomPetSystem.Breeding.cs
    symbol: RoomPetSystem
    line: 81
  - id: ev_fe1afccfe33a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Wired/WiredUnseenCycle.cs
    symbol: WiredUnseenCycle
    line: 23
verified: {}
manual: {}
