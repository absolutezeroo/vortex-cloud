# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:2612a004e8d07ac4752bad44585b5167663ebe20ad585045eecacfaac7d62d07"
generated:
  feature: room.rentable_space_status
  scenarios:
  - scenario: room.rentable_space_status.guard_0
    title: "rentable space status: the guard 'ctx.PlayerId <= 0 || message.FurnitureId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.FurnitureId <= 0
      guard_holds: "true"
    when:
      packet: RentableSpaceStatus
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RentableSpaceStatus sent while 'ctx.PlayerId <= 0 || message.FurnitureId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ebd9fc902b67
  - scenario: room.rentable_space_status.success
    title: "rentable space status: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RentableSpaceStatus
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RentableSpaceStatus succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - RentableSpaceStatus
      evidence: ev_3bfcf2d3587b
  evidence:
  - id: ev_3bfcf2d3587b
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/RentableSpaceGrain.cs
    symbol: RentableSpaceGrain
    line: 56
  - id: ev_ebd9fc902b67
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Furniture/RentableSpaceStatusMessageHandler.cs
    symbol: RentableSpaceStatusMessageHandler
    line: 23
verified: {}
manual: {}
