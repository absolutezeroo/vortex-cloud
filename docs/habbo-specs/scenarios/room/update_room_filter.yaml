# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:f18ff42ee03dc0a86672897520d562c2490beab6b66e0ac5d65471dce08da34c"
generated:
  feature: room.update_room_filter
  scenarios:
  - scenario: room.update_room_filter.success
    title: "update room filter: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: UpdateRoomFilter
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of UpdateRoomFilter succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_b0bc6af39047
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_0b5b1ff6fd4e
  evidence:
  - id: ev_0b5b1ff6fd4e
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/UpdateRoomFilterMessageEvent.java
    symbol: UpdateRoomFilterMessageEvent
    line: 9
    note: third-party server implementation; evidence, not authority
  - id: ev_b0bc6af39047
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/RoomSettings/UpdateRoomFilterMessageHandler.cs
    symbol: UpdateRoomFilterMessageHandler
    line: 8
verified: {}
manual: {}
