# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:e16f5f9b3d545d260dc36166096855f5d70b23a9a6b5f855e6b239a6cccbff0d"
generated:
  feature: room.get_room_settings
  scenarios:
  - scenario: room.get_room_settings.guard_0
    title: "get room settings: the guard 'ctx.PlayerId <= 0 || message.RoomId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.RoomId <= 0
      guard_holds: "true"
    when:
      packet: GetRoomSettings
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetRoomSettings sent while 'ctx.PlayerId <= 0 || message.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_2f074d4ea877
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - RoomSettingsData
      evidence: ev_f07514c3244b
  - scenario: room.get_room_settings.guard_1
    title: "get room settings: the guard 'snapshot is null' holds"
    given:
      guard: snapshot is null
      guard_holds: "true"
    when:
      packet: GetRoomSettings
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetRoomSettings sent while 'snapshot is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_2f074d4ea877
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - RoomSettingsData
      evidence: ev_f07514c3244b
  - scenario: room.get_room_settings.guard_2
    title: "get room settings: the guard '!await IsRoomOwnerAsync(actor).ConfigureAwait(true)' holds"
    given:
      guard: "!await IsRoomOwnerAsync(actor).ConfigureAwait(true)"
      guard_holds: "true"
    when:
      packet: GetRoomSettings
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetRoomSettings sent while '!await IsRoomOwnerAsync(actor).ConfigureAwait(true)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_329537530b13
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - RoomSettingsData
      evidence: ev_f07514c3244b
  - scenario: room.get_room_settings.success
    title: "get room settings: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetRoomSettings
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetRoomSettings succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - RoomSettingsDataEvent
      evidence: ev_2f074d4ea877
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - RoomSettingsData
      evidence: ev_f07514c3244b
  evidence:
  - id: ev_f07514c3244b
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/GetRoomSettingsMessageEvent.java
    symbol: GetRoomSettingsMessageEvent
    line: 10
    note: third-party server implementation; evidence, not authority
  - id: ev_2f074d4ea877
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/RoomSettings/GetRoomSettingsMessageHandler.cs
    symbol: GetRoomSettingsMessageHandler
    line: 22
  - id: ev_329537530b13
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Settings.cs
    symbol: RoomGrain
    line: 40
verified: {}
manual: {}
