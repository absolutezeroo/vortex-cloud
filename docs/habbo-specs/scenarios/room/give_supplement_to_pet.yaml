# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:648422c1923b92aeecd6ee35da34c5e37cb019b01adf15305ce84d7511983e5d"
generated:
  feature: room.give_supplement_to_pet
  scenarios:
  - scenario: room.give_supplement_to_pet.guard_0
    title: "give supplement to pet: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0
      guard_holds: "true"
    when:
      packet: GiveSupplementToPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GiveSupplementToPet sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_babcd35870ab
  - scenario: room.give_supplement_to_pet.guard_1
    title: "give supplement to pet: the guard '!_roomGrain._state.PetsById.TryGetValue(petId, out PetSna...' holds"
    given:
      guard: "!_roomGrain._state.PetsById.TryGetValue(petId, out PetSnapshot? pet)"
      guard_holds: "true"
    when:
      packet: GiveSupplementToPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GiveSupplementToPet sent while '!_roomGrain._state.PetsById.TryGetValue(petId, out PetSnapshot? pet)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3b983736fec5
  - scenario: room.give_supplement_to_pet.success
    title: "give supplement to pet: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GiveSupplementToPet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GiveSupplementToPet succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3b983736fec5
  evidence:
  - id: ev_3b983736fec5
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomPetSystem.Care.cs
    symbol: RoomPetSystem
    line: 155
  - id: ev_babcd35870ab
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Engine/GiveSupplementToPetMessageHandler.cs
    symbol: GiveSupplementToPetMessageHandler
    line: 22
verified: {}
manual: {}
