# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:5e7e2428b0bf2a486f34f9f2e4730465453e21ba8905e0c03a051fc030ba0c14"
generated:
  feature: room.get_flat_controllers
  scenarios:
  - scenario: room.get_flat_controllers.guard_0
    title: "get flat controllers: the guard 'ctx.PlayerId <= 0 || message.RoomId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.RoomId <= 0
      guard_holds: "true"
    when:
      packet: GetFlatControllers
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetFlatControllers sent while 'ctx.PlayerId <= 0 || message.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_392abf549ae0
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - FlatControllers
      evidence: ev_45d7f642be3c
  - scenario: room.get_flat_controllers.success
    title: "get flat controllers: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetFlatControllers
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetFlatControllers succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - FlatControllersEvent
      evidence: ev_329537530b13
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - FlatControllers
      evidence: ev_45d7f642be3c
  evidence:
  - id: ev_45d7f642be3c
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/GetFlatControllersMessageEvent.java
    symbol: GetFlatControllersMessageEvent
    line: 10
    note: third-party server implementation; evidence, not authority
  - id: ev_329537530b13
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Settings.cs
    symbol: RoomGrain
    line: 266
  - id: ev_392abf549ae0
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/RoomSettings/GetFlatControllersMessageHandler.cs
    symbol: GetFlatControllersMessageHandler
    line: 23
verified: {}
manual: {}
