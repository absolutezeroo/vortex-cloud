# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:cadf8ecec9f0f14e4da053365f54d826c2d682da641ea8553bc810f9803529cb"
generated:
  feature: room.mute_user
  scenarios:
  - scenario: room.mute_user.guard_0
    title: "mute user: the guard 'ctx.PlayerId <= 0 || message.UserId <= 0 || message.Minut...' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.UserId <= 0 || message.Minutes <= 0
      guard_holds: "true"
    when:
      packet: MuteUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MuteUser sent while 'ctx.PlayerId <= 0 || message.UserId <= 0 || message.Minutes <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3341ead08ab4
  - scenario: room.mute_user.guard_1
    title: "mute user: the guard 'actorRoomId <= 0' holds"
    given:
      guard: actorRoomId <= 0
      guard_holds: "true"
    when:
      packet: MuteUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MuteUser sent while 'actorRoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3341ead08ab4
  - scenario: room.mute_user.guard_2
    title: "mute user: the guard '!await RoomModerationGuard .CanActOnTargetAsync( permissi...' holds"
    given:
      guard: "!await RoomModerationGuard .CanActOnTargetAsync( permissionService, events, ctx, actorRoomId, message.UserId, ModerationAction.Mute, ct ) .ConfigureAwait(false)"
      guard_holds: "true"
    when:
      packet: MuteUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MuteUser sent while '!await RoomModerationGuard .CanActOnTargetAsync( permissionService, events, ctx, actorRoomId, message.UserId, ModerationAction.Mute, ct ) .ConfigureAwait(false)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3341ead08ab4
  - scenario: room.mute_user.guard_3
    title: "mute user: the guard 'ModerationPolicy.CanActOnTarget(actorPermissions, targetP...' holds"
    given:
      guard: ModerationPolicy.CanActOnTarget(actorPermissions, targetPermissions)
      guard_holds: "true"
    when:
      packet: MuteUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MuteUser sent while 'ModerationPolicy.CanActOnTarget(actorPermissions, targetPermissions)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ede1461ee378
  - scenario: room.mute_user.guard_4
    title: "mute user: the guard 'playerId <= 0' holds"
    given:
      guard: playerId <= 0
      guard_holds: "true"
    when:
      packet: MuteUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MuteUser sent while 'playerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_743940bb69f7
  - scenario: room.mute_user.guard_5
    title: "mute user: the guard 'actorCtx.PlayerId <= 0 || targetPlayerId <= 0 || actorCtx...' holds"
    given:
      guard: actorCtx.PlayerId <= 0 || targetPlayerId <= 0 || actorCtx.RoomId != _roomGrain._state.RoomId
      guard_holds: "true"
    when:
      packet: MuteUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MuteUser sent while 'actorCtx.PlayerId <= 0 || targetPlayerId <= 0 || actorCtx.RoomId != _roomGrain._state.RoomId' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_9eb84a2a2088
  - scenario: room.mute_user.guard_6
    title: "mute user: the guard 'durationSeconds <= 0 || actorCtx.PlayerId == targetPlayerId' holds"
    given:
      guard: durationSeconds <= 0 || actorCtx.PlayerId == targetPlayerId
      guard_holds: "true"
    when:
      packet: MuteUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MuteUser sent while 'durationSeconds <= 0 || actorCtx.PlayerId == targetPlayerId' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_9eb84a2a2088
  - scenario: room.mute_user.guard_7
    title: "mute user: the guard '!await CanModerateAsync( actorCtx, _roomGrain._state.Room...' holds"
    given:
      guard: "!await CanModerateAsync( actorCtx, _roomGrain._state.RoomSnapshot.ModSettings.WhoCanMute, ModerationAction.Mute )"
      guard_holds: "true"
    when:
      packet: MuteUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MuteUser sent while '!await CanModerateAsync( actorCtx, _roomGrain._state.RoomSnapshot.ModSettings.WhoCanMute, ModerationAction.Mute )' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_9eb84a2a2088
  - scenario: room.mute_user.guard_8
    title: "mute user: the guard '_registry is null' holds"
    given:
      guard: _registry is null
      guard_holds: "true"
    when:
      packet: MuteUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MuteUser sent while '_registry is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_34bf3ac088e2
  - scenario: room.mute_user.success
    title: "mute user: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: MuteUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of MuteUser succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3341ead08ab4
  evidence:
  - id: ev_3341ead08ab4
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Action/MuteUserMessageHandler.cs
    symbol: MuteUserMessageHandler
    line: 28
  - id: ev_34bf3ac088e2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Events/EventSystem.cs
    symbol: EventSystem
    line: 16
  - id: ev_743940bb69f7
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Authentication/Permissions/PermissionService.cs
    symbol: PermissionService
    line: 62
  - id: ev_9eb84a2a2088
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomModerationSystem.cs
    symbol: RoomModerationSystem
    line: 162
  - id: ev_ede1461ee378
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Action/RoomModerationGuard.cs
    symbol: RoomModerationGuard
    line: 48
verified: {}
manual: {}
