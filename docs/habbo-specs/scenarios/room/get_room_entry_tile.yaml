# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:cc69c9847e9dd8fe5747f3a0841904c013fcaf2e711a20925cfdf0fac1ac9259"
generated:
  feature: room.get_room_entry_tile
  scenarios:
  - scenario: room.get_room_entry_tile.guard_0
    title: "get room entry tile: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: GetRoomEntryTile
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetRoomEntryTile sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a41a5b6a9d25
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - RoomEntryTile
      - RoomFloorThicknessUpdated
      evidence: ev_800bb7ca9a46
  - scenario: room.get_room_entry_tile.success
    title: "get room entry tile: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetRoomEntryTile
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetRoomEntryTile succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - RoomEntryTile
      evidence: ev_5c4457759c24
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - RoomEntryTile
      - RoomFloorThicknessUpdated
      evidence: ev_800bb7ca9a46
  evidence:
  - id: ev_800bb7ca9a46
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/floorplaneditor/GetRoomEntryTileMessageEvent.java
    symbol: GetRoomEntryTileMessageEvent
    line: 9
    note: third-party server implementation; evidence, not authority
  - id: ev_5c4457759c24
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Layout/GetRoomEntryTileMessageHandler.cs
    symbol: GetRoomEntryTileMessageHandler
    line: 13
  - id: ev_a41a5b6a9d25
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Layout/GetRoomEntryTileMessageHandler.cs
    symbol: GetRoomEntryTileMessageHandler
    line: 24
verified: {}
manual: {}
