# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:5f0e76810611bfd77efebf5b23cc1b7d30c1f02beb44cee5d14e7645448ed072"
generated:
  feature: room.ambassador_alert
  scenarios:
  - scenario: room.ambassador_alert.success
    title: "ambassador alert: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: AmbassadorAlert
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of AmbassadorAlert succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_d1cdb7a00304
  evidence:
  - id: ev_d1cdb7a00304
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Room/Action/AmbassadorAlertMessageHandler.cs
    symbol: AmbassadorAlertMessageHandler
    line: 8
verified: {}
manual: {}
