# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:069400d5297291c17ca698d50da3fc05c5c51129a2a6fba3c3ee50e9d469d77d"
generated:
  feature: quest.get_daily_quest
  scenarios:
  - scenario: quest.get_daily_quest.guard_0
    title: "get daily quest: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetDailyQuest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetDailyQuest sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1f56ebff3117
  - scenario: quest.get_daily_quest.success
    title: "get daily quest: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetDailyQuest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetDailyQuest succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - QuestDaily
      evidence: ev_1f56ebff3117
  evidence:
  - id: ev_1f56ebff3117
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Quest/GetDailyQuestMessageHandler.cs
    symbol: GetDailyQuestMessageHandler
    line: 21
verified: {}
manual: {}
