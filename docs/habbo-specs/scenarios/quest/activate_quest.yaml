# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:6cb7b26dcad481b14006e74a3f9fc73edbf6cad30e392bcac3a63ba9149e602c"
generated:
  feature: quest.activate_quest
  scenarios:
  - scenario: quest.activate_quest.guard_0
    title: "activate quest: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: ActivateQuest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ActivateQuest sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_56c775d65c26
  - scenario: quest.activate_quest.guard_1
    title: "activate quest: the guard 'row is { Completed: true }' holds"
    given:
      guard: "row is { Completed: true }"
      guard_holds: "true"
    when:
      packet: ActivateQuest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ActivateQuest sent while 'row is { Completed: true }' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f7df1e322a73
  - scenario: quest.activate_quest.success
    title: "activate quest: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ActivateQuest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ActivateQuest succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - Quest
      - QuestCancelled
      - Quests
      evidence: ev_373e76aa8a80
  evidence:
  - id: ev_373e76aa8a80
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerPresenceGrain.cs
    symbol: PlayerPresenceGrain
    line: 112
  - id: ev_56c775d65c26
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Quest/ActivateQuestMessageHandler.cs
    symbol: ActivateQuestMessageHandler
    line: 19
  - id: ev_f7df1e322a73
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerQuestGrain.cs
    symbol: PlayerQuestGrain
    line: 264
verified: {}
manual: {}
