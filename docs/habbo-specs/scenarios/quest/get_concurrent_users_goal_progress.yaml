# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:a5730c1dedfe37db021618b13e14a9e7f1f16afcf6f12a421726d98c1e7c3d8b"
generated:
  feature: quest.get_concurrent_users_goal_progress
  scenarios:
  - scenario: quest.get_concurrent_users_goal_progress.guard_0
    title: "get concurrent users goal progress: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetConcurrentUsersGoalProgress
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetConcurrentUsersGoalProgress sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_51419bb75e94
  - scenario: quest.get_concurrent_users_goal_progress.success
    title: "get concurrent users goal progress: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetConcurrentUsersGoalProgress
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetConcurrentUsersGoalProgress succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_51419bb75e94
  evidence:
  - id: ev_51419bb75e94
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Quest/GetConcurrentUsersGoalProgressMessageHandler.cs
    symbol: GetConcurrentUsersGoalProgressMessageHandler
    line: 27
verified: {}
manual: {}
