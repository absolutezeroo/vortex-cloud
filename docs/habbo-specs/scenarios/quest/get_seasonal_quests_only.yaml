# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:721155e57411e9e29310ff2e643033bc8c775bb89f5a6790a57728824dc72585"
generated:
  feature: quest.get_seasonal_quests_only
  scenarios:
  - scenario: quest.get_seasonal_quests_only.guard_0
    title: "get seasonal quests only: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetSeasonalQuestsOnly
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetSeasonalQuestsOnly sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_cfce1049cc44
  - scenario: quest.get_seasonal_quests_only.success
    title: "get seasonal quests only: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetSeasonalQuestsOnly
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetSeasonalQuestsOnly succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - SeasonalQuests
      evidence: ev_3e19f38401db
  evidence:
  - id: ev_3e19f38401db
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Progression/Grains/PlayerQuestGrain.cs
    symbol: PlayerQuestGrain
    line: 204
  - id: ev_cfce1049cc44
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Quest/GetSeasonalQuestsOnlyMessageHandler.cs
    symbol: GetSeasonalQuestsOnlyMessageHandler
    line: 21
verified: {}
manual: {}
