# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:feb86365c5f26124217828f10f0d43f4efd19df256261e22d5c9fb1a3dbacfab"
generated:
  feature: quest.start_campaign
  scenarios:
  - scenario: quest.start_campaign.guard_0
    title: "start campaign: the guard 'ctx.PlayerId <= 0 || string.IsNullOrWhiteSpace(message.Ca...' holds"
    given:
      guard: ctx.PlayerId <= 0 || string.IsNullOrWhiteSpace(message.CampaignCode)
      guard_holds: "true"
    when:
      packet: StartCampaign
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of StartCampaign sent while 'ctx.PlayerId <= 0 || string.IsNullOrWhiteSpace(message.CampaignCode)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e80416fbe3e6
  - scenario: quest.start_campaign.guard_1
    title: "start campaign: the guard 'campaign.Count == 0' holds"
    given:
      guard: campaign.Count == 0
      guard_holds: "true"
    when:
      packet: StartCampaign
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of StartCampaign sent while 'campaign.Count == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3e19f38401db
  - scenario: quest.start_campaign.success
    title: "start campaign: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: StartCampaign
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of StartCampaign succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - Quest
      - Quests
      evidence: ev_373e76aa8a80
  evidence:
  - id: ev_373e76aa8a80
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerPresenceGrain.cs
    symbol: PlayerPresenceGrain
    line: 112
  - id: ev_3e19f38401db
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Progression/Grains/PlayerQuestGrain.cs
    symbol: PlayerQuestGrain
    line: 180
  - id: ev_e80416fbe3e6
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Quest/StartCampaignMessageHandler.cs
    symbol: StartCampaignMessageHandler
    line: 23
verified: {}
manual: {}
