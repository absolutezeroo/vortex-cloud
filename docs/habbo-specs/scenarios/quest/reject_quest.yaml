# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:e217b78bce7ea25cc79a5e161cb31b8f41fbbddb9e890bc0d36fe562eb1619c8"
generated:
  feature: quest.reject_quest
  scenarios:
  - scenario: quest.reject_quest.guard_0
    title: "reject quest: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: RejectQuest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RejectQuest sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_cc9c5d1f80d8
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - Quests
      - QuestCancelled
      evidence: ev_0f8b46cdb458
  - scenario: quest.reject_quest.success
    title: "reject quest: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RejectQuest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RejectQuest succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_c3da1eeb3a8d
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - Quests
      - QuestCancelled
      evidence: ev_0f8b46cdb458
  evidence:
  - id: ev_0f8b46cdb458
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/quests/RejectQuestEvent.java
    symbol: RejectQuestEvent
    line: 16
    note: third-party server implementation; evidence, not authority
  - id: ev_c3da1eeb3a8d
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Quest/RejectQuestMessageHandler.cs
    symbol: RejectQuestMessageHandler
    line: 10
  - id: ev_cc9c5d1f80d8
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Quest/RejectQuestMessageHandler.cs
    symbol: RejectQuestMessageHandler
    line: 19
verified: {}
manual: {}
