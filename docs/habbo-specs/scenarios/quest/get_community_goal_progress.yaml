# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:0f87b6c5f7f80360a269218ed74e8b83802b5bfefa7a1c965ab65ecb0ecc8747"
generated:
  feature: quest.get_community_goal_progress
  scenarios:
  - scenario: quest.get_community_goal_progress.guard_0
    title: "get community goal progress: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetCommunityGoalProgress
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetCommunityGoalProgress sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_003066bbe9f3
  - scenario: quest.get_community_goal_progress.guard_1
    title: "get community goal progress: the guard '_goal is null' holds"
    given:
      guard: _goal is null
      guard_holds: "true"
    when:
      packet: GetCommunityGoalProgress
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetCommunityGoalProgress sent while '_goal is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_4875f459ad01
  - scenario: quest.get_community_goal_progress.success
    title: "get community goal progress: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetCommunityGoalProgress
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetCommunityGoalProgress succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - CommunityGoalProgress
      evidence: ev_003066bbe9f3
  evidence:
  - id: ev_003066bbe9f3
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Quest/GetCommunityGoalProgressMessageHandler.cs
    symbol: GetCommunityGoalProgressMessageHandler
    line: 23
  - id: ev_4875f459ad01
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/CommunityGoalGrain.cs
    symbol: CommunityGoalGrain
    line: 50
verified: {}
manual: {}
