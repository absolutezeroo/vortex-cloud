# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:36f7a0970b34f7160bcc359099afed7934cb39ebb94d4dac9e29b9a4935e6c09"
generated:
  feature: quest.get_daily_tasks
  scenarios:
  - scenario: quest.get_daily_tasks.guard_0
    title: "get daily tasks: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetDailyTasks
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetDailyTasks sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a434a66e0e14
  - scenario: quest.get_daily_tasks.success
    title: "get daily tasks: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetDailyTasks
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetDailyTasks succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - DailyTasksActiveList
      evidence: ev_373e76aa8a80
  evidence:
  - id: ev_373e76aa8a80
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerPresenceGrain.cs
    symbol: PlayerPresenceGrain
    line: 112
  - id: ev_a434a66e0e14
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Quest/GetDailyTasksMessageHandler.cs
    symbol: GetDailyTasksMessageHandler
    line: 27
verified: {}
manual: {}
