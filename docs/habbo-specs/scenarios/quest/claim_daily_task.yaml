# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:9b4f3f5a341cffe0234973922bf068d36ca455eb822692bfa872ab54d6d906ee"
generated:
  feature: quest.claim_daily_task
  scenarios:
  - scenario: quest.claim_daily_task.guard_0
    title: "claim daily task: the guard 'ctx.PlayerId <= 0 || message.TaskId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.TaskId <= 0
      guard_holds: "true"
    when:
      packet: ClaimDailyTask
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ClaimDailyTask sent while 'ctx.PlayerId <= 0 || message.TaskId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_25e3c0ce70c6
  - scenario: quest.claim_daily_task.guard_1
    title: "claim daily task: the guard 'assignment is null || assignment.Status != DailyTaskStatu...' holds"
    given:
      guard: assignment is null || assignment.Status != DailyTaskStatus.Completed
      guard_holds: "true"
    when:
      packet: ClaimDailyTask
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ClaimDailyTask sent while 'assignment is null || assignment.Status != DailyTaskStatus.Completed' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_2abb3cf8a990
  - scenario: quest.claim_daily_task.guard_2
    title: "claim daily task: the guard '_registry is null' holds"
    given:
      guard: _registry is null
      guard_holds: "true"
    when:
      packet: ClaimDailyTask
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ClaimDailyTask sent while '_registry is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_34bf3ac088e2
  - scenario: quest.claim_daily_task.success
    title: "claim daily task: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ClaimDailyTask
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ClaimDailyTask succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - DailyTasksTaskUpdate
      evidence: ev_25e3c0ce70c6
  evidence:
  - id: ev_25e3c0ce70c6
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Quest/ClaimDailyTaskMessageHandler.cs
    symbol: ClaimDailyTaskMessageHandler
    line: 23
  - id: ev_2abb3cf8a990
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Progression/Grains/PlayerDailyTaskGrain.cs
    symbol: PlayerDailyTaskGrain
    line: 172
  - id: ev_34bf3ac088e2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Events/EventSystem.cs
    symbol: EventSystem
    line: 16
verified: {}
manual: {}
