# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:5c444d2b62ef3b4d1371603be86c2fcc61a822b279bc040527eb18db7e321ea6"
generated:
  feature: quest.cancel_quest
  scenarios:
  - scenario: quest.cancel_quest.guard_0
    title: "cancel quest: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: CancelQuest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of CancelQuest sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_325247bf0257
  - scenario: quest.cancel_quest.success
    title: "cancel quest: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: CancelQuest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of CancelQuest succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_325247bf0257
  evidence:
  - id: ev_325247bf0257
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Quest/CancelQuestMessageHandler.cs
    symbol: CancelQuestMessageHandler
    line: 19
verified: {}
manual: {}
