# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:9c651308bfff13f7750442c9074b0041fcf152a8e8dd3411f097098ce37a2d83"
generated:
  feature: quest.open_quest_tracker
  scenarios:
  - scenario: quest.open_quest_tracker.guard_0
    title: "open quest tracker: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: OpenQuestTracker
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of OpenQuestTracker sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7dca240b5281
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - Quest
      - Quests
      evidence: ev_de4656c2f892
  - scenario: quest.open_quest_tracker.guard_1
    title: "open quest tracker: the guard 'tracked is null' holds"
    given:
      guard: tracked is null
      guard_holds: "true"
    when:
      packet: OpenQuestTracker
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of OpenQuestTracker sent while 'tracked is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7dca240b5281
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - Quest
      - Quests
      evidence: ev_de4656c2f892
  - scenario: quest.open_quest_tracker.success
    title: "open quest tracker: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: OpenQuestTracker
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of OpenQuestTracker succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - Quest
      evidence: ev_3e19f38401db
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - Quest
      - Quests
      evidence: ev_de4656c2f892
  evidence:
  - id: ev_de4656c2f892
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/quests/OpenQuestTrackerEvent.java
    symbol: OpenQuestTrackerEvent
    line: 15
    note: third-party server implementation; evidence, not authority
  - id: ev_3e19f38401db
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Progression/Grains/PlayerQuestGrain.cs
    symbol: PlayerQuestGrain
    line: 238
  - id: ev_7dca240b5281
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Quest/OpenQuestTrackerMessageHandler.cs
    symbol: OpenQuestTrackerMessageHandler
    line: 21
verified: {}
manual: {}
