# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:c5139eb8a02d4f6ed2c907c7574b89ac76308328bd3df54b8bef11cdffd7d489"
generated:
  feature: quest.get_quests
  scenarios:
  - scenario: quest.get_quests.guard_0
    title: "get quests: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetQuests
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetQuests sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_154995b507c3
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - Quests
      evidence: ev_ab59d553aa3d
  - scenario: quest.get_quests.success
    title: "get quests: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetQuests
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetQuests succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - Quests
      evidence: ev_154995b507c3
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - Quests
      evidence: ev_ab59d553aa3d
  evidence:
  - id: ev_ab59d553aa3d
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/quests/GetQuestsEvent.java
    symbol: GetQuestsEvent
    line: 13
    note: third-party server implementation; evidence, not authority
  - id: ev_154995b507c3
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Quest/GetQuestsMessageHandler.cs
    symbol: GetQuestsMessageHandler
    line: 20
verified: {}
manual: {}
