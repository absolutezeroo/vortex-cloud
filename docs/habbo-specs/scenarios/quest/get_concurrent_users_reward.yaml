# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:2ab2c97e8d0e1c0ce2c89f165b5dcf4a5c0adbb8405aed77fad0e6bcf2636e95"
generated:
  feature: quest.get_concurrent_users_reward
  scenarios:
  - scenario: quest.get_concurrent_users_reward.guard_0
    title: "get concurrent users reward: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetConcurrentUsersReward
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetConcurrentUsersReward sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1156f62d5639
  - scenario: quest.get_concurrent_users_reward.guard_1
    title: "get concurrent users reward: the guard 'string.IsNullOrWhiteSpace(badgeCode)' holds"
    given:
      guard: string.IsNullOrWhiteSpace(badgeCode)
      guard_holds: "true"
    when:
      packet: GetConcurrentUsersReward
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetConcurrentUsersReward sent while 'string.IsNullOrWhiteSpace(badgeCode)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_c6396662000a
  - scenario: quest.get_concurrent_users_reward.guard_2
    title: "get concurrent users reward: the guard 'alreadyOwned' holds"
    given:
      guard: alreadyOwned
      guard_holds: "true"
    when:
      packet: GetConcurrentUsersReward
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetConcurrentUsersReward sent while 'alreadyOwned' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_c6396662000a
  - scenario: quest.get_concurrent_users_reward.guard_3
    title: "get concurrent users reward: the guard '_registry is null' holds"
    given:
      guard: _registry is null
      guard_holds: "true"
    when:
      packet: GetConcurrentUsersReward
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetConcurrentUsersReward sent while '_registry is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_34bf3ac088e2
  - scenario: quest.get_concurrent_users_reward.success
    title: "get concurrent users reward: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetConcurrentUsersReward
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetConcurrentUsersReward succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - BadgeReceived
      evidence: ev_1156f62d5639
  evidence:
  - id: ev_1156f62d5639
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Quest/GetConcurrentUsersRewardMessageHandler.cs
    symbol: GetConcurrentUsersRewardMessageHandler
    line: 27
  - id: ev_34bf3ac088e2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Events/EventSystem.cs
    symbol: EventSystem
    line: 16
  - id: ev_c6396662000a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Inventory/Grains/InventoryGrain.Furni.cs
    symbol: InventoryGrain
    line: 459
verified: {}
manual: {}
