# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:a4ef1148e107768b4b4b7c9ce98a779f6ea9ff8e9b748247ef86c693159475db"
generated:
  feature: quest.friend_request_quest_complete
  scenarios:
  - scenario: quest.friend_request_quest_complete.guard_0
    title: "friend request quest complete: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: FriendRequestQuestComplete
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of FriendRequestQuestComplete sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_02470a6f7ab3
  - scenario: quest.friend_request_quest_complete.success
    title: "friend request quest complete: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: FriendRequestQuestComplete
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of FriendRequestQuestComplete succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_02470a6f7ab3
  evidence:
  - id: ev_02470a6f7ab3
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Quest/FriendRequestQuestCompleteMessageHandler.cs
    symbol: FriendRequestQuestCompleteMessageHandler
    line: 26
verified: {}
manual: {}
