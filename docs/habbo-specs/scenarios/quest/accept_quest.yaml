# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:d6aa6404f635b9b02e6fbb217f023af8fc67032552ea0809179d8afae22d218a"
generated:
  feature: quest.accept_quest
  scenarios:
  - scenario: quest.accept_quest.guard_0
    title: "accept quest: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: AcceptQuest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AcceptQuest sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_15c350b48faf
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - Quest
      evidence: ev_2eb053d11547
  - scenario: quest.accept_quest.success
    title: "accept quest: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: AcceptQuest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of AcceptQuest succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_15c350b48faf
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - Quest
      evidence: ev_2eb053d11547
  evidence:
  - id: ev_2eb053d11547
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/quests/AcceptQuestEvent.java
    symbol: AcceptQuestEvent
    line: 12
    note: third-party server implementation; evidence, not authority
  - id: ev_15c350b48faf
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Quest/AcceptQuestMessageHandler.cs
    symbol: AcceptQuestMessageHandler
    line: 19
verified: {}
manual: {}
