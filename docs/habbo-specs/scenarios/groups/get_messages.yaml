# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:2284ecad30822ea5d4f06a78a978fec6746e1c4f9f9fa7bf8acbd1bbd7d45e10"
generated:
  feature: groups.get_messages
  scenarios:
  - scenario: groups.get_messages.guard_0
    title: "get messages: the guard 'ctx.PlayerId <= 0 || message.GroupId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.GroupId <= 0
      guard_holds: "true"
    when:
      packet: GetMessages
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetMessages sent while 'ctx.PlayerId <= 0 || message.GroupId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_521b40de641f
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - ErrorReport
      - ThreadMessages
      - ForumData
      - NotificationDialog
      evidence: ev_b6409304f400
  - scenario: groups.get_messages.guard_1
    title: "get messages: the guard 'page is null' holds"
    given:
      guard: page is null
      guard_holds: "true"
    when:
      packet: GetMessages
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetMessages sent while 'page is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_521b40de641f
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - ErrorReport
      - ThreadMessages
      - ForumData
      - NotificationDialog
      evidence: ev_b6409304f400
  - scenario: groups.get_messages.guard_2
    title: "get messages: the guard 'group is null || group.ForumSettings is null' holds"
    given:
      guard: group is null || group.ForumSettings is null
      guard_holds: "true"
    when:
      packet: GetMessages
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetMessages sent while 'group is null || group.ForumSettings is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_76106ea21309
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - ErrorReport
      - ThreadMessages
      - ForumData
      - NotificationDialog
      evidence: ev_b6409304f400
  - scenario: groups.get_messages.guard_3
    title: "get messages: the guard '!CanRead(group.ForumSettings, role)' holds"
    given:
      guard: "!CanRead(group.ForumSettings, role)"
      guard_holds: "true"
    when:
      packet: GetMessages
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetMessages sent while '!CanRead(group.ForumSettings, role)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_76106ea21309
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - ErrorReport
      - ThreadMessages
      - ForumData
      - NotificationDialog
      evidence: ev_b6409304f400
  - scenario: groups.get_messages.success
    title: "get messages: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetMessages
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetMessages succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - ThreadMessages
      evidence: ev_09932710687b
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - ErrorReport
      - ThreadMessages
      - ForumData
      - NotificationDialog
      evidence: ev_b6409304f400
  evidence:
  - id: ev_b6409304f400
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/guilds/forums/GetMessagesMessageEvent.java
    symbol: GetMessagesMessageEvent
    line: 21
    note: third-party server implementation; evidence, not authority
  - id: ev_09932710687b
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Primitives/Orleans/GrainFactoryExtensions.cs
    symbol: GrainFactoryExtensions
    line: 217
  - id: ev_521b40de641f
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/GroupForums/GetMessagesMessageHandler.cs
    symbol: GetMessagesMessageHandler
    line: 23
  - id: ev_76106ea21309
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/GroupForumGrain.cs
    symbol: GroupForumGrain
    line: 206
verified: {}
manual: {}
