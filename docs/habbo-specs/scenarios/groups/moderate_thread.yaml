# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:41d096d62317744d18dcf654456aa1dbf2d976afdaadc2b7ae753c13d41255d6"
generated:
  feature: groups.moderate_thread
  scenarios:
  - scenario: groups.moderate_thread.guard_0
    title: "moderate thread: the guard 'ctx.PlayerId <= 0 || message.GroupId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.GroupId <= 0
      guard_holds: "true"
    when:
      packet: ModerateThread
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ModerateThread sent while 'ctx.PlayerId <= 0 || message.GroupId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f9fea23f57c8
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - ErrorReport
      - ErrorReport
      - ErrorReport
      - NotificationDialog
      - NotificationDialog
      - PostThreadMessage
      - GuildForumThreads
      evidence: ev_3dc5d38843f6
  - scenario: groups.moderate_thread.guard_1
    title: "moderate thread: the guard 'thread is null' holds"
    given:
      guard: thread is null
      guard_holds: "true"
    when:
      packet: ModerateThread
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ModerateThread sent while 'thread is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f9fea23f57c8
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - ErrorReport
      - ErrorReport
      - ErrorReport
      - NotificationDialog
      - NotificationDialog
      - PostThreadMessage
      - GuildForumThreads
      evidence: ev_3dc5d38843f6
  - scenario: groups.moderate_thread.guard_2
    title: "moderate thread: the guard 'group is null' holds"
    given:
      guard: group is null
      guard_holds: "true"
    when:
      packet: ModerateThread
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ModerateThread sent while 'group is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_da3ad2e8d895
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - ErrorReport
      - ErrorReport
      - ErrorReport
      - NotificationDialog
      - NotificationDialog
      - PostThreadMessage
      - GuildForumThreads
      evidence: ev_3dc5d38843f6
  - scenario: groups.moderate_thread.guard_3
    title: "moderate thread: the guard '_registry is null' holds"
    given:
      guard: _registry is null
      guard_holds: "true"
    when:
      packet: ModerateThread
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ModerateThread sent while '_registry is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_34bf3ac088e2
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - ErrorReport
      - ErrorReport
      - ErrorReport
      - NotificationDialog
      - NotificationDialog
      - PostThreadMessage
      - GuildForumThreads
      evidence: ev_3dc5d38843f6
  - scenario: groups.moderate_thread.success
    title: "moderate thread: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ModerateThread
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ModerateThread succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - UpdateThread
      evidence: ev_34bf3ac088e2
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - ErrorReport
      - ErrorReport
      - ErrorReport
      - NotificationDialog
      - NotificationDialog
      - PostThreadMessage
      - GuildForumThreads
      evidence: ev_3dc5d38843f6
  evidence:
  - id: ev_3dc5d38843f6
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/guilds/forums/ModerateThreadMessageEvent.java
    symbol: ModerateThreadMessageEvent
    line: 20
    note: third-party server implementation; evidence, not authority
  - id: ev_34bf3ac088e2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Events/EventSystem.cs
    symbol: EventSystem
    line: 16
  - id: ev_da3ad2e8d895
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Social/Grains/GroupForumGrain.cs
    symbol: GroupForumGrain
    line: 486
  - id: ev_f9fea23f57c8
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/GroupForums/ModerateThreadMessageHandler.cs
    symbol: ModerateThreadMessageHandler
    line: 23
verified: {}
manual: {}
