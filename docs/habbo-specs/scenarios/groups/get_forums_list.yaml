# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:8fb75c185387b4b3b9a92f19963a1a9da47a6469897322beaed6994eb344ccc6"
generated:
  feature: groups.get_forums_list
  scenarios:
  - scenario: groups.get_forums_list.guard_0
    title: "get forums list: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetForumsList
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetForumsList sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_40292ff8590b
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - ForumsList
      evidence: ev_8408121caa5a
  - scenario: groups.get_forums_list.guard_1
    title: "get forums list: the guard 'groups.Count == 0' holds"
    given:
      guard: groups.Count == 0
      guard_holds: "true"
    when:
      packet: GetForumsList
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetForumsList sent while 'groups.Count == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_d53218d99bdd
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - ForumsList
      evidence: ev_8408121caa5a
  - scenario: groups.get_forums_list.success
    title: "get forums list: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetForumsList
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetForumsList succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - ForumsList
      evidence: ev_40292ff8590b
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - ForumsList
      evidence: ev_8408121caa5a
  evidence:
  - id: ev_8408121caa5a
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/guilds/forums/GetForumsListMessageEvent.java
    symbol: GetForumsListMessageEvent
    line: 22
    note: third-party server implementation; evidence, not authority
  - id: ev_40292ff8590b
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/GroupForums/GetForumsListMessageHandler.cs
    symbol: GetForumsListMessageHandler
    line: 23
  - id: ev_d53218d99bdd
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Social/Grains/GroupDirectoryGrain.cs
    symbol: GroupDirectoryGrain
    line: 495
verified: {}
manual: {}
