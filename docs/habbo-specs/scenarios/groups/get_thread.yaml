# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:2b65d7c9e3b01670cf5c64789effe8e8e214c9731988ca33aaa1d3d48dd1bbb3"
generated:
  feature: groups.get_thread
  scenarios:
  - scenario: groups.get_thread.guard_0
    title: "get thread: the guard 'ctx.PlayerId <= 0 || message.GroupId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.GroupId <= 0
      guard_holds: "true"
    when:
      packet: GetThread
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetThread sent while 'ctx.PlayerId <= 0 || message.GroupId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_4a9cf1fedd4f
  - scenario: groups.get_thread.guard_1
    title: "get thread: the guard 'page is null' holds"
    given:
      guard: page is null
      guard_holds: "true"
    when:
      packet: GetThread
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetThread sent while 'page is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_4a9cf1fedd4f
  - scenario: groups.get_thread.guard_2
    title: "get thread: the guard 'group is null || group.ForumSettings is null' holds"
    given:
      guard: group is null || group.ForumSettings is null
      guard_holds: "true"
    when:
      packet: GetThread
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetThread sent while 'group is null || group.ForumSettings is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_da3ad2e8d895
  - scenario: groups.get_thread.guard_3
    title: "get thread: the guard '!CanRead(group.ForumSettings, role)' holds"
    given:
      guard: "!CanRead(group.ForumSettings, role)"
      guard_holds: "true"
    when:
      packet: GetThread
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetThread sent while '!CanRead(group.ForumSettings, role)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_da3ad2e8d895
  - scenario: groups.get_thread.success
    title: "get thread: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetThread
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetThread succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - ThreadMessages
      evidence: ev_09932710687b
  evidence:
  - id: ev_09932710687b
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Primitives/Orleans/GrainFactoryExtensions.cs
    symbol: GrainFactoryExtensions
    line: 223
  - id: ev_4a9cf1fedd4f
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/GroupForums/GetThreadMessageHandler.cs
    symbol: GetThreadMessageHandler
    line: 23
  - id: ev_da3ad2e8d895
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Social/Grains/GroupForumGrain.cs
    symbol: GroupForumGrain
    line: 206
verified: {}
manual: {}
