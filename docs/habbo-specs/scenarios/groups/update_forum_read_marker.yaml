# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:32d75f78f24298b661bd09627b6bf888072c02f3f5b0745eea8246727e2c4392"
generated:
  feature: groups.update_forum_read_marker
  scenarios:
  - scenario: groups.update_forum_read_marker.guard_0
    title: "update forum read marker: the guard 'ctx.PlayerId <= 0 || message.Markers.Length == 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.Markers.Length == 0
      guard_holds: "true"
    when:
      packet: UpdateForumReadMarker
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateForumReadMarker sent while 'ctx.PlayerId <= 0 || message.Markers.Length == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0f1bf7f61b42
  - scenario: groups.update_forum_read_marker.guard_1
    title: "update forum read marker: the guard 'markers.Count == 0' holds"
    given:
      guard: markers.Count == 0
      guard_holds: "true"
    when:
      packet: UpdateForumReadMarker
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateForumReadMarker sent while 'markers.Count == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_678209870593
  - scenario: groups.update_forum_read_marker.success
    title: "update forum read marker: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: UpdateForumReadMarker
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of UpdateForumReadMarker succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0f1bf7f61b42
  evidence:
  - id: ev_0f1bf7f61b42
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/GroupForums/UpdateForumReadMarkerMessageHandler.cs
    symbol: UpdateForumReadMarkerMessageHandler
    line: 26
  - id: ev_678209870593
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/GroupDirectoryGrain.cs
    symbol: GroupDirectoryGrain
    line: 687
verified: {}
manual: {}
