# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:d9955d9e182be1298a3bd5139248886fcdbdbf3533b732bbdd8809a8acb4d181"
generated:
  feature: groups.get_threads
  scenarios:
  - scenario: groups.get_threads.guard_0
    title: "get threads: the guard 'ctx.PlayerId <= 0 || message.GroupId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.GroupId <= 0
      guard_holds: "true"
    when:
      packet: GetThreads
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetThreads sent while 'ctx.PlayerId <= 0 || message.GroupId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e7ca800ad2bd
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - ErrorReport
      - ForumData
      - GuildForumThreads
      evidence: ev_8667852b897b
  - scenario: groups.get_threads.guard_1
    title: "get threads: the guard 'page is null' holds"
    given:
      guard: page is null
      guard_holds: "true"
    when:
      packet: GetThreads
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetThreads sent while 'page is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e7ca800ad2bd
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - ErrorReport
      - ForumData
      - GuildForumThreads
      evidence: ev_8667852b897b
  - scenario: groups.get_threads.guard_2
    title: "get threads: the guard 'group is null || group.ForumSettings is null' holds"
    given:
      guard: group is null || group.ForumSettings is null
      guard_holds: "true"
    when:
      packet: GetThreads
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetThreads sent while 'group is null || group.ForumSettings is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_da3ad2e8d895
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - ErrorReport
      - ForumData
      - GuildForumThreads
      evidence: ev_8667852b897b
  - scenario: groups.get_threads.guard_3
    title: "get threads: the guard '!CanRead(group.ForumSettings, role)' holds"
    given:
      guard: "!CanRead(group.ForumSettings, role)"
      guard_holds: "true"
    when:
      packet: GetThreads
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetThreads sent while '!CanRead(group.ForumSettings, role)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_da3ad2e8d895
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - ErrorReport
      - ForumData
      - GuildForumThreads
      evidence: ev_8667852b897b
  - scenario: groups.get_threads.success
    title: "get threads: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetThreads
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetThreads succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - ForumThreads
      evidence: ev_09932710687b
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - ErrorReport
      - ForumData
      - GuildForumThreads
      evidence: ev_8667852b897b
  evidence:
  - id: ev_8667852b897b
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/guilds/forums/GetThreadsMessageEvent.java
    symbol: GetThreadsMessageEvent
    line: 12
    note: third-party server implementation; evidence, not authority
  - id: ev_09932710687b
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Primitives/Orleans/GrainFactoryExtensions.cs
    symbol: GrainFactoryExtensions
    line: 217
  - id: ev_da3ad2e8d895
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Social/Grains/GroupForumGrain.cs
    symbol: GroupForumGrain
    line: 141
  - id: ev_e7ca800ad2bd
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/GroupForums/GetThreadsMessageHandler.cs
    symbol: GetThreadsMessageHandler
    line: 23
verified: {}
manual: {}
