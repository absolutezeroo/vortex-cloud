# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:72ecea1c87dd364e14d694b441e04490ce7d18f39ece83e0678cfcb1a3f73a2b"
generated:
  feature: groups.update_thread
  scenarios:
  - scenario: groups.update_thread.guard_0
    title: "update thread: the guard 'ctx.PlayerId <= 0 || message.GroupId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.GroupId <= 0
      guard_holds: "true"
    when:
      packet: UpdateThread
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateThread sent while 'ctx.PlayerId <= 0 || message.GroupId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_66c9c1a8d7ea
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - ErrorReport
      - ErrorReport
      - ErrorReport
      - NotificationDialog
      - NotificationDialog
      - ThreadUpdated
      - GuildForumThreads
      evidence: ev_f463089e15e8
  - scenario: groups.update_thread.guard_1
    title: "update thread: the guard 'thread is null' holds"
    given:
      guard: thread is null
      guard_holds: "true"
    when:
      packet: UpdateThread
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateThread sent while 'thread is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_66c9c1a8d7ea
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - ErrorReport
      - ErrorReport
      - ErrorReport
      - NotificationDialog
      - NotificationDialog
      - ThreadUpdated
      - GuildForumThreads
      evidence: ev_f463089e15e8
  - scenario: groups.update_thread.guard_2
    title: "update thread: the guard 'group is null' holds"
    given:
      guard: group is null
      guard_holds: "true"
    when:
      packet: UpdateThread
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateThread sent while 'group is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_da3ad2e8d895
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - ErrorReport
      - ErrorReport
      - ErrorReport
      - NotificationDialog
      - NotificationDialog
      - ThreadUpdated
      - GuildForumThreads
      evidence: ev_f463089e15e8
  - scenario: groups.update_thread.guard_3
    title: "update thread: the guard '_registry is null' holds"
    given:
      guard: _registry is null
      guard_holds: "true"
    when:
      packet: UpdateThread
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateThread sent while '_registry is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_34bf3ac088e2
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - ErrorReport
      - ErrorReport
      - ErrorReport
      - NotificationDialog
      - NotificationDialog
      - ThreadUpdated
      - GuildForumThreads
      evidence: ev_f463089e15e8
  - scenario: groups.update_thread.success
    title: "update thread: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: UpdateThread
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of UpdateThread succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - UpdateThread
      evidence: ev_34bf3ac088e2
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - ErrorReport
      - ErrorReport
      - ErrorReport
      - NotificationDialog
      - NotificationDialog
      - ThreadUpdated
      - GuildForumThreads
      evidence: ev_f463089e15e8
  evidence:
  - id: ev_f463089e15e8
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/guilds/forums/UpdateThreadMessageEvent.java
    symbol: UpdateThreadMessageEvent
    line: 19
    note: third-party server implementation; evidence, not authority
  - id: ev_34bf3ac088e2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Events/EventSystem.cs
    symbol: EventSystem
    line: 16
  - id: ev_66c9c1a8d7ea
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/GroupForums/UpdateThreadMessageHandler.cs
    symbol: UpdateThreadMessageHandler
    line: 23
  - id: ev_da3ad2e8d895
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Social/Grains/GroupForumGrain.cs
    symbol: GroupForumGrain
    line: 439
verified: {}
manual: {}
