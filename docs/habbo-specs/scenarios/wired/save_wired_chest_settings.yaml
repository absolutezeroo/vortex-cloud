# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:905c2fa8fe98133a9606390a222420bd0a6eea07b1eca69c5dd23b79d3d5f447"
generated:
  feature: wired.save_wired_chest_settings
  scenarios:
  - scenario: wired.save_wired_chest_settings.guard_0
    title: "save wired chest settings: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: SaveWiredChestSettings
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SaveWiredChestSettings sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_c6552b9a3adb
  - scenario: wired.save_wired_chest_settings.guard_1
    title: "save wired chest settings: the guard '!await CanUseChestAsync(ctx, chestId).ConfigureAwait(true)' holds"
    given:
      guard: "!await CanUseChestAsync(ctx, chestId).ConfigureAwait(true)"
      guard_holds: "true"
    when:
      packet: SaveWiredChestSettings
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SaveWiredChestSettings sent while '!await CanUseChestAsync(ctx, chestId).ConfigureAwait(true)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_d3a71568179c
  - scenario: wired.save_wired_chest_settings.guard_2
    title: "save wired chest settings: the guard 'chest is not null' holds"
    given:
      guard: chest is not null
      guard_holds: "true"
    when:
      packet: SaveWiredChestSettings
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SaveWiredChestSettings sent while 'chest is not null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_d3a71568179c
  - scenario: wired.save_wired_chest_settings.success
    title: "save wired chest settings: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: SaveWiredChestSettings
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of SaveWiredChestSettings succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - WiredChestUpdateSuccess
      evidence: ev_071f4c8b1064
  evidence:
  - id: ev_071f4c8b1064
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserDefinedRoomEvents/Wiredtrading/SaveWiredChestSettingsMessageHandler.cs
    symbol: SaveWiredChestSettingsMessageHandler
    line: 15
  - id: ev_c6552b9a3adb
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserDefinedRoomEvents/Wiredtrading/SaveWiredChestSettingsMessageHandler.cs
    symbol: SaveWiredChestSettingsMessageHandler
    line: 26
  - id: ev_d3a71568179c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/WiredTrading/WiredChestStore.cs
    symbol: WiredChestStore
    line: 370
verified: {}
manual: {}
