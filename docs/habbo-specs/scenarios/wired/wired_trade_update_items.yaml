# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:3e0067fd96ef77dfb5b67747fe0ecce792587ffe66c82d7803d81c9f274d9ded"
generated:
  feature: wired.wired_trade_update_items
  scenarios:
  - scenario: wired.wired_trade_update_items.guard_0
    title: "wired trade update items: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: WiredTradeUpdateItems
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of WiredTradeUpdateItems sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3819e935e77f
  - scenario: wired.wired_trade_update_items.guard_1
    title: "wired trade update items: the guard 'deposit is null' holds"
    given:
      guard: deposit is null
      guard_holds: "true"
    when:
      packet: WiredTradeUpdateItems
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of WiredTradeUpdateItems sent while 'deposit is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3819e935e77f
  - scenario: wired.wired_trade_update_items.guard_2
    title: "wired trade update items: the guard '!_sessions.TryGetValue(ctx.PlayerId, out WiredTradeSessio...' holds"
    given:
      guard: "!_sessions.TryGetValue(ctx.PlayerId, out WiredTradeSession? deposit)"
      guard_holds: "true"
    when:
      packet: WiredTradeUpdateItems
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of WiredTradeUpdateItems sent while '!_sessions.TryGetValue(ctx.PlayerId, out WiredTradeSession? deposit)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_50116836f822
  - scenario: wired.wired_trade_update_items.guard_3
    title: "wired trade update items: the guard 'itemIds.IsDefaultOrEmpty' holds"
    given:
      guard: itemIds.IsDefaultOrEmpty
      guard_holds: "true"
    when:
      packet: WiredTradeUpdateItems
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of WiredTradeUpdateItems sent while 'itemIds.IsDefaultOrEmpty' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_d3a71568179c
  - scenario: wired.wired_trade_update_items.guard_4
    title: "wired trade update items: the guard 'contract.YouGiveRules is not { } rules || rules.IsEmpty' holds"
    given:
      guard: contract.YouGiveRules is not { } rules || rules.IsEmpty
      guard_holds: "true"
    when:
      packet: WiredTradeUpdateItems
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of WiredTradeUpdateItems sent while 'contract.YouGiveRules is not { } rules || rules.IsEmpty' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f7dd5a8d0419
  - scenario: wired.wired_trade_update_items.guard_5
    title: "wired trade update items: the guard 'TryTake(rule, multiplier, staked, out ContractCharge? cha...' holds"
    given:
      guard: TryTake(rule, multiplier, staked, out ContractCharge? charge)
      guard_holds: "true"
    when:
      packet: WiredTradeUpdateItems
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of WiredTradeUpdateItems sent while 'TryTake(rule, multiplier, staked, out ContractCharge? charge)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f7dd5a8d0419
  - scenario: wired.wired_trade_update_items.guard_6
    title: "wired trade update items: the guard 'contract.YouGetRule is not { } rule' holds"
    given:
      guard: contract.YouGetRule is not { } rule
      guard_holds: "true"
    when:
      packet: WiredTradeUpdateItems
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of WiredTradeUpdateItems sent while 'contract.YouGetRule is not { } rule' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f7dd5a8d0419
  - scenario: wired.wired_trade_update_items.guard_7
    title: "wired trade update items: the guard '!TryTake(rule, multiplier, stock, out ContractCharge? owed)' holds"
    given:
      guard: "!TryTake(rule, multiplier, stock, out ContractCharge? owed)"
      guard_holds: "true"
    when:
      packet: WiredTradeUpdateItems
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of WiredTradeUpdateItems sent while '!TryTake(rule, multiplier, stock, out ContractCharge? owed)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f7dd5a8d0419
  - scenario: wired.wired_trade_update_items.guard_8
    title: "wired trade update items: the guard 'owed!.Coins > chestCredits' holds"
    given:
      guard: owed!.Coins > chestCredits
      guard_holds: "true"
    when:
      packet: WiredTradeUpdateItems
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of WiredTradeUpdateItems sent while 'owed!.Coins > chestCredits' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f7dd5a8d0419
  - scenario: wired.wired_trade_update_items.success
    title: "wired trade update items: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: WiredTradeUpdateItems
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of WiredTradeUpdateItems succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - WiredTradeItemsUpdate
      evidence: ev_2ec03446aa2a
  evidence:
  - id: ev_2ec03446aa2a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Furniture/Providers/FurnitureDefinitionProvider.cs
    symbol: FurnitureDefinitionProvider
    line: 41
  - id: ev_3819e935e77f
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserDefinedRoomEvents/Wiredtrading/WiredTradeUpdateItemsMessageHandler.cs
    symbol: WiredTradeUpdateItemsMessageHandler
    line: 33
  - id: ev_50116836f822
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/WiredTrading/RoomWiredTradingSystem.cs
    symbol: RoomWiredTradingSystem
    line: 223
  - id: ev_d3a71568179c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/WiredTrading/WiredChestStore.cs
    symbol: WiredChestStore
    line: 409
  - id: ev_f7dd5a8d0419
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Wired/WiredContractSettlement.cs
    symbol: WiredContractSettlement
    line: 65
verified: {}
manual: {}
