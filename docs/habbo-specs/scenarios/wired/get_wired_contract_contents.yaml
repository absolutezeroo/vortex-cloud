# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:0a7b32ad17394f4279e382e6b2acf2e487a75833b502e2da8e8300aceb5ab7a3"
generated:
  feature: wired.get_wired_contract_contents
  scenarios:
  - scenario: wired.get_wired_contract_contents.guard_0
    title: "get wired contract contents: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: GetWiredContractContents
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetWiredContractContents sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e88bb6d8f548
  - scenario: wired.get_wired_contract_contents.guard_1
    title: "get wired contract contents: the guard 'contract is null' holds"
    given:
      guard: contract is null
      guard_holds: "true"
    when:
      packet: GetWiredContractContents
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetWiredContractContents sent while 'contract is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e88bb6d8f548
  - scenario: wired.get_wired_contract_contents.guard_2
    title: "get wired contract contents: the guard '!await CanEditContractAsync(ctx, contractId).ConfigureAwa...' holds"
    given:
      guard: "!await CanEditContractAsync(ctx, contractId).ConfigureAwait(true)"
      guard_holds: "true"
    when:
      packet: GetWiredContractContents
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetWiredContractContents sent while '!await CanEditContractAsync(ctx, contractId).ConfigureAwait(true)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_62e15b6f2f52
  - scenario: wired.get_wired_contract_contents.success
    title: "get wired contract contents: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetWiredContractContents
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetWiredContractContents succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - WiredContractContents
      evidence: ev_62e15b6f2f52
  evidence:
  - id: ev_62e15b6f2f52
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Wired.ContractStore.cs
    symbol: RoomGrain
    line: 87
  - id: ev_e88bb6d8f548
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserDefinedRoomEvents/Wiredtrading/GetWiredContractContentsMessageHandler.cs
    symbol: GetWiredContractContentsMessageHandler
    line: 31
verified: {}
manual: {}
