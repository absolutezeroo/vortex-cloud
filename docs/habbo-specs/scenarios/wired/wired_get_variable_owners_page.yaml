# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:5195c5d2cb4060c5e2a18219dc78550d5244b78234b8096e407ab92ed67093d5"
generated:
  feature: wired.wired_get_variable_owners_page
  scenarios:
  - scenario: wired.wired_get_variable_owners_page.guard_0
    title: "wired get variable owners page: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: WiredGetVariableOwnersPage
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of WiredGetVariableOwnersPage sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_8125aab164ea
  - scenario: wired.wired_get_variable_owners_page.success
    title: "wired get variable owners page: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: WiredGetVariableOwnersPage
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of WiredGetVariableOwnersPage succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - WiredUserVariablesList
      evidence: ev_4b42983a07ff
  evidence:
  - id: ev_4b42983a07ff
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Wired.PermanentVariables.cs
    symbol: RoomGrain
    line: 169
  - id: ev_8125aab164ea
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserDefinedRoomEvents/Wiredmenu/WiredGetVariableOwnersPageMessageHandler.cs
    symbol: WiredGetVariableOwnersPageMessageHandler
    line: 23
verified: {}
manual: {}
