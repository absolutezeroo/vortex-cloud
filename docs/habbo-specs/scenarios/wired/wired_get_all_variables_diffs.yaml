# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:92bf050639c7d285de722bfee421c2fd12eebc1c59f6ebe07f6cb1c299e007fb"
generated:
  feature: wired.wired_get_all_variables_diffs
  scenarios:
  - scenario: wired.wired_get_all_variables_diffs.guard_0
    title: "wired get all variables diffs: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: WiredGetAllVariablesDiffs
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of WiredGetAllVariablesDiffs sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_eb70f454ffab
  - scenario: wired.wired_get_all_variables_diffs.success
    title: "wired get all variables diffs: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: WiredGetAllVariablesDiffs
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of WiredGetAllVariablesDiffs succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - WiredAllVariablesDiffs
      evidence: ev_0080929ab318
  evidence:
  - id: ev_0080929ab318
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Furni.Floor.cs
    symbol: RoomGrain
    line: 155
  - id: ev_eb70f454ffab
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserDefinedRoomEvents/Wiredmenu/WiredGetAllVariablesDiffsMessageHandler.cs
    symbol: WiredGetAllVariablesDiffsMessageHandler
    line: 26
verified: {}
manual: {}
