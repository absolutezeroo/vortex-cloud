# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:b155a63fb6084b339e414cd902fabca0bc7a8657ed879203eb48e15d28ed613b"
generated:
  feature: wired.wired_get_user_permanent_variables
  scenarios:
  - scenario: wired.wired_get_user_permanent_variables.guard_0
    title: "wired get user permanent variables: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: WiredGetUserPermanentVariables
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of WiredGetUserPermanentVariables sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_c37fb632fa77
  - scenario: wired.wired_get_user_permanent_variables.success
    title: "wired get user permanent variables: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: WiredGetUserPermanentVariables
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of WiredGetUserPermanentVariables succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - WiredUserPermanentVariables
      evidence: ev_4b42983a07ff
  evidence:
  - id: ev_4b42983a07ff
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Wired.PermanentVariables.cs
    symbol: RoomGrain
    line: 28
  - id: ev_c37fb632fa77
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserDefinedRoomEvents/Wiredmenu/WiredGetUserPermanentVariablesMessageHandler.cs
    symbol: WiredGetUserPermanentVariablesMessageHandler
    line: 24
verified: {}
manual: {}
