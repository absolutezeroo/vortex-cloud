# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:d5dd5227fb6857b6db2b506fe6bc376348dd8075cac1e48ee786e34ed3b4b3da"
generated:
  feature: wired.wired_get_room_logs
  scenarios:
  - scenario: wired.wired_get_room_logs.guard_0
    title: "wired get room logs: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: WiredGetRoomLogs
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of WiredGetRoomLogs sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_97aa8d4ed3db
  - scenario: wired.wired_get_room_logs.success
    title: "wired get room logs: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: WiredGetRoomLogs
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of WiredGetRoomLogs succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - WiredRoomLogs
      evidence: ev_266d52e7bb83
  evidence:
  - id: ev_266d52e7bb83
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Wired.Logs.cs
    symbol: RoomGrain
    line: 16
  - id: ev_97aa8d4ed3db
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserDefinedRoomEvents/Wiredmenu/WiredGetRoomLogsMessageHandler.cs
    symbol: WiredGetRoomLogsMessageHandler
    line: 22
verified: {}
manual: {}
