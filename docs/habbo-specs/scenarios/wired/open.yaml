# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:248efcb49a4809fd45d44f274082174e001c278656dd267399d3e8d74b974b08"
generated:
  feature: wired.open
  scenarios:
  - scenario: wired.open.guard_0
    title: "open: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || me...' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.Id <= 0
      guard_holds: "true"
    when:
      packet: Open
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Open sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.Id <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_b1a051803832
  - scenario: wired.open.guard_1
    title: "open: the guard 'wiredData is null' holds"
    given:
      guard: wiredData is null
      guard_holds: "true"
    when:
      packet: Open
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Open sent while 'wiredData is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_b1a051803832
  - scenario: wired.open.guard_2
    title: "open: the guard 'composer is null' holds"
    given:
      guard: composer is null
      guard_holds: "true"
    when:
      packet: Open
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Open sent while 'composer is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_b1a051803832
  - scenario: wired.open.guard_3
    title: "open: the guard '!_state.ItemsById.TryGetValue(itemId, out IRoomItem? item...' holds"
    given:
      guard: "!_state.ItemsById.TryGetValue(itemId, out IRoomItem? item) || item.Logic is not FurnitureWiredLogic wiredLogic"
      guard_holds: "true"
    when:
      packet: Open
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Open sent while '!_state.ItemsById.TryGetValue(itemId, out IRoomItem? item) || item.Logic is not FurnitureWiredLogic wiredLogic' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0080929ab318
  - scenario: wired.open.guard_4
    title: "open: the guard 'wiredLogic is not WiredVariableReference' holds"
    given:
      guard: wiredLogic is not WiredVariableReference
      guard_holds: "true"
    when:
      packet: Open
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Open sent while 'wiredLogic is not WiredVariableReference' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0080929ab318
  - scenario: wired.open.guard_5
    title: "open: the guard '_root.ValueKind != JsonValueKind.Object' holds"
    given:
      guard: _root.ValueKind != JsonValueKind.Object
      guard_holds: "true"
    when:
      packet: Open
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Open sent while '_root.ValueKind != JsonValueKind.Object' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0f9ffda870b1
  - scenario: wired.open.guard_6
    title: "open: the guard '!_root.TryGetProperty(name, out element)' holds"
    given:
      guard: "!_root.TryGetProperty(name, out element)"
      guard_holds: "true"
    when:
      packet: Open
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Open sent while '!_root.TryGetProperty(name, out element)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0f9ffda870b1
  - scenario: wired.open.guard_7
    title: "open: the guard 'element.ValueKind == JsonValueKind.Null' holds"
    given:
      guard: element.ValueKind == JsonValueKind.Null
      guard_holds: "true"
    when:
      packet: Open
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of Open sent while 'element.ValueKind == JsonValueKind.Null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0f9ffda870b1
  - scenario: wired.open.success
    title: "open: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: Open
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of Open succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - WiredFurniAction
      - WiredFurniAddon
      - WiredFurniCondition
      - WiredFurniSelector
      - WiredFurniTrigger
      - WiredFurniVariable
      - unresolved
      evidence: ev_0080929ab318
  evidence:
  - id: ev_0080929ab318
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Furni.Floor.cs
    symbol: RoomGrain
    line: 145
  - id: ev_0f9ffda870b1
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Furniture/ExtraDataReader.cs
    symbol: ExtraDataReader
    line: 35
  - id: ev_b1a051803832
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserDefinedRoomEvents/OpenMessageHandler.cs
    symbol: OpenMessageHandler
    line: 24
verified: {}
manual: {}
