# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:d0b0bfa6bbd4a1a9579527c29e565422e295c3aa18dfbb852d856cba4feee660"
generated:
  feature: wired.save_wired_contract
  scenarios:
  - scenario: wired.save_wired_contract.guard_0
    title: "save wired contract: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: SaveWiredContract
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SaveWiredContract sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_355d6b6207bd
  - scenario: wired.save_wired_contract.guard_1
    title: "save wired contract: the guard 'saved is null' holds"
    given:
      guard: saved is null
      guard_holds: "true"
    when:
      packet: SaveWiredContract
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SaveWiredContract sent while 'saved is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: rejected
      emits:
      - WiredContractContents
      - WiredContractUpdateResult
      evidence: ev_355d6b6207bd
  - scenario: wired.save_wired_contract.guard_2
    title: "save wired contract: the guard '!await CanEditContractAsync(ctx, contract.ContractId).Con...' holds"
    given:
      guard: "!await CanEditContractAsync(ctx, contract.ContractId).ConfigureAwait(true)"
      guard_holds: "true"
    when:
      packet: SaveWiredContract
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SaveWiredContract sent while '!await CanEditContractAsync(ctx, contract.ContractId).ConfigureAwait(true)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_62e15b6f2f52
  - scenario: wired.save_wired_contract.success
    title: "save wired contract: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: SaveWiredContract
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of SaveWiredContract succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - WiredContractContents
      - WiredContractUpdateResult
      evidence: ev_355d6b6207bd
  evidence:
  - id: ev_355d6b6207bd
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserDefinedRoomEvents/Wiredtrading/SaveWiredContractMessageHandler.cs
    symbol: SaveWiredContractMessageHandler
    line: 32
  - id: ev_62e15b6f2f52
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Wired.ContractStore.cs
    symbol: RoomGrain
    line: 135
verified: {}
manual: {}
