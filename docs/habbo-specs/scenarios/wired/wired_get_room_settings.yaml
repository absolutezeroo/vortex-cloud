# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:139a5d538968182d17df69e521b124726e767f64a06bed28b1285b05c0e89d73"
generated:
  feature: wired.wired_get_room_settings
  scenarios:
  - scenario: wired.wired_get_room_settings.guard_0
    title: "wired get room settings: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: WiredGetRoomSettings
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of WiredGetRoomSettings sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_fa900c7ca2af
  - scenario: wired.wired_get_room_settings.success
    title: "wired get room settings: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: WiredGetRoomSettings
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of WiredGetRoomSettings succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - WiredRoomSettingsEvent
      evidence: ev_16b476be0ed1
  evidence:
  - id: ev_16b476be0ed1
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Wired.RoomSettings.cs
    symbol: RoomGrain
    line: 13
  - id: ev_fa900c7ca2af
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserDefinedRoomEvents/Wiredmenu/WiredGetRoomSettingsMessageHandler.cs
    symbol: WiredGetRoomSettingsMessageHandler
    line: 24
verified: {}
manual: {}
