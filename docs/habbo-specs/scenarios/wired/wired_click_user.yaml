# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:429f5e429766af50d1be0d1e2b637b90491ea429a3ece676b8edf9b97ff2171d"
generated:
  feature: wired.wired_click_user
  scenarios:
  - scenario: wired.wired_click_user.guard_0
    title: "wired click user: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: WiredClickUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of WiredClickUser sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0a311b92eb6e
  - scenario: wired.wired_click_user.success
    title: "wired click user: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: WiredClickUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of WiredClickUser succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - WiredClickUserResponse
      evidence: ev_058d83af74c8
  evidence:
  - id: ev_058d83af74c8
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Wired.ClickUser.cs
    symbol: RoomGrain
    line: 9
  - id: ev_0a311b92eb6e
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserDefinedRoomEvents/WiredClickUserMessageHandler.cs
    symbol: WiredClickUserMessageHandler
    line: 33
verified: {}
manual: {}
