# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:71113016aad2d436d8f36103dc7090d825919315e26669423847f7b2cd77e31d"
generated:
  feature: wired.wired_trade_cancel
  scenarios:
  - scenario: wired.wired_trade_cancel.guard_0
    title: "wired trade cancel: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: WiredTradeCancel
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of WiredTradeCancel sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0a128bef11b0
  - scenario: wired.wired_trade_cancel.guard_1
    title: "wired trade cancel: the guard '!cancelled' holds"
    given:
      guard: "!cancelled"
      guard_holds: "true"
    when:
      packet: WiredTradeCancel
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of WiredTradeCancel sent while '!cancelled' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0a128bef11b0
  - scenario: wired.wired_trade_cancel.guard_2
    title: "wired trade cancel: the guard '!_sessions.Remove(ctx.PlayerId, out WiredTradeSession? ca...' holds"
    given:
      guard: "!_sessions.Remove(ctx.PlayerId, out WiredTradeSession? cancelled)"
      guard_holds: "true"
    when:
      packet: WiredTradeCancel
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of WiredTradeCancel sent while '!_sessions.Remove(ctx.PlayerId, out WiredTradeSession? cancelled)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_50116836f822
  - scenario: wired.wired_trade_cancel.success
    title: "wired trade cancel: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: WiredTradeCancel
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of WiredTradeCancel succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - WiredTradeCancelled
      evidence: ev_055f671e672b
  evidence:
  - id: ev_055f671e672b
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserDefinedRoomEvents/Wiredtrading/WiredTradeCancelMessageHandler.cs
    symbol: WiredTradeCancelMessageHandler
    line: 20
  - id: ev_0a128bef11b0
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserDefinedRoomEvents/Wiredtrading/WiredTradeCancelMessageHandler.cs
    symbol: WiredTradeCancelMessageHandler
    line: 33
  - id: ev_50116836f822
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/WiredTrading/RoomWiredTradingSystem.cs
    symbol: RoomWiredTradingSystem
    line: 423
verified: {}
manual: {}
