# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:cccbf481f0c8ea27a822d9c9ff2ff92033dbc9e8d06ab75915f8b3baff602cc0"
generated:
  feature: wired.set_wired_chest_lock
  scenarios:
  - scenario: wired.set_wired_chest_lock.guard_0
    title: "set wired chest lock: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: SetWiredChestLock
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SetWiredChestLock sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f064c2317678
  - scenario: wired.set_wired_chest_lock.guard_1
    title: "set wired chest lock: the guard '!await CanUseChestAsync(ctx, chestId).ConfigureAwait(true)' holds"
    given:
      guard: "!await CanUseChestAsync(ctx, chestId).ConfigureAwait(true)"
      guard_holds: "true"
    when:
      packet: SetWiredChestLock
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SetWiredChestLock sent while '!await CanUseChestAsync(ctx, chestId).ConfigureAwait(true)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_d3a71568179c
  - scenario: wired.set_wired_chest_lock.guard_2
    title: "set wired chest lock: the guard 'chest is not null' holds"
    given:
      guard: chest is not null
      guard_holds: "true"
    when:
      packet: SetWiredChestLock
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SetWiredChestLock sent while 'chest is not null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_d3a71568179c
  - scenario: wired.set_wired_chest_lock.success
    title: "set wired chest lock: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: SetWiredChestLock
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of SetWiredChestLock succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_84ee82d9d8e2
  evidence:
  - id: ev_84ee82d9d8e2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/WiredTrading/RoomWiredTradingSystem.Chests.cs
    symbol: RoomWiredTradingSystem
    line: 389
  - id: ev_d3a71568179c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/WiredTrading/WiredChestStore.cs
    symbol: WiredChestStore
    line: 370
  - id: ev_f064c2317678
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserDefinedRoomEvents/Wiredtrading/SetWiredChestLockMessageHandler.cs
    symbol: SetWiredChestLockMessageHandler
    line: 29
verified: {}
manual: {}
