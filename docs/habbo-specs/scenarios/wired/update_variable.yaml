# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:4d83f4664a9dbfc9d71348041fcc1f7ad852c4dcdb92a59223bd5a167be207d9"
generated:
  feature: wired.update_variable
  scenarios:
  - scenario: wired.update_variable.guard_0
    title: "update variable: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || me...' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.Id <= 0
      guard_holds: "true"
    when:
      packet: UpdateVariable
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateVariable sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.Id <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e87b09ae933e
  - scenario: wired.update_variable.guard_1
    title: "update variable: the guard '!await _grainFactory .GetRoomFurni(ctx.RoomId) .ApplyWire...' holds"
    given:
      guard: "!await _grainFactory .GetRoomFurni(ctx.RoomId) .ApplyWiredUpdateAsync(ctx.AsActionContext(), message.Id, message.ToRequest(), ct) .ConfigureAwait(false)"
      guard_holds: "true"
    when:
      packet: UpdateVariable
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateVariable sent while '!await _grainFactory .GetRoomFurni(ctx.RoomId) .ApplyWiredUpdateAsync(ctx.AsActionContext(), message.Id, message.ToRequest(), ct) .ConfigureAwait(false)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e87b09ae933e
  - scenario: wired.update_variable.guard_2
    title: "update variable: the guard '!await ActionModule.ApplyWiredUpdateAsync(ctx, itemId, up...' holds"
    given:
      guard: "!await ActionModule.ApplyWiredUpdateAsync(ctx, itemId, update, ct)"
      guard_holds: "true"
    when:
      packet: UpdateVariable
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateVariable sent while '!await ActionModule.ApplyWiredUpdateAsync(ctx, itemId, update, ct)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0080929ab318
  - scenario: wired.update_variable.guard_3
    title: "update variable: the guard '!_roomGrain._state.ItemsById.TryGetValue(itemId, out IRoo...' holds"
    given:
      guard: "!_roomGrain._state.ItemsById.TryGetValue(itemId, out IRoomItem? item)"
      guard_holds: "true"
    when:
      packet: UpdateVariable
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateVariable sent while '!_roomGrain._state.ItemsById.TryGetValue(itemId, out IRoomItem? item)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: rejected
      emits: []
      evidence: ev_e1599f76ae95
  - scenario: wired.update_variable.guard_4
    title: "update variable: the guard 'item.Logic is not FurnitureWiredLogic wiredLogic' holds"
    given:
      guard: item.Logic is not FurnitureWiredLogic wiredLogic
      guard_holds: "true"
    when:
      packet: UpdateVariable
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateVariable sent while 'item.Logic is not FurnitureWiredLogic wiredLogic' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: rejected
      emits: []
      evidence: ev_e1599f76ae95
  - scenario: wired.update_variable.guard_5
    title: "update variable: the guard '!await wiredLogic.ApplyWiredUpdateAsync(ctx, update, ct)' holds"
    given:
      guard: "!await wiredLogic.ApplyWiredUpdateAsync(ctx, update, ct)"
      guard_holds: "true"
    when:
      packet: UpdateVariable
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateVariable sent while '!await wiredLogic.ApplyWiredUpdateAsync(ctx, update, ct)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e1599f76ae95
  - scenario: wired.update_variable.success
    title: "update variable: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: UpdateVariable
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of UpdateVariable succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - WiredSaveSuccessEvent
      evidence: ev_0080929ab318
  evidence:
  - id: ev_0080929ab318
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Furni.Floor.cs
    symbol: RoomGrain
    line: 99
  - id: ev_e1599f76ae95
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomActionModule.Floor.cs
    symbol: RoomActionModule
    line: 232
  - id: ev_e87b09ae933e
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserDefinedRoomEvents/UpdateVariableMessageHandler.cs
    symbol: UpdateVariableMessageHandler
    line: 23
verified: {}
manual: {}
