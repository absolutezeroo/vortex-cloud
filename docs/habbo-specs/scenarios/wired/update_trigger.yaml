# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:c120b558e83a9d819e6044e4a753ab9c4bd7ecea020551287cf86446df3f939a"
generated:
  feature: wired.update_trigger
  scenarios:
  - scenario: wired.update_trigger.guard_0
    title: "update trigger: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || me...' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.Id <= 0
      guard_holds: "true"
    when:
      packet: UpdateTrigger
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateTrigger sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.Id <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_b0dd7db943f4
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - WiredSaved
      - WiredValidationError
      - WiredSaved
      - WiredValidationError
      - WiredValidationError
      evidence: ev_9a9156c07ff2
  - scenario: wired.update_trigger.guard_1
    title: "update trigger: the guard '!await _grainFactory .GetRoomFurni(ctx.RoomId) .ApplyWire...' holds"
    given:
      guard: "!await _grainFactory .GetRoomFurni(ctx.RoomId) .ApplyWiredUpdateAsync(ctx.AsActionContext(), message.Id, message.ToRequest(), ct) .ConfigureAwait(false)"
      guard_holds: "true"
    when:
      packet: UpdateTrigger
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateTrigger sent while '!await _grainFactory .GetRoomFurni(ctx.RoomId) .ApplyWiredUpdateAsync(ctx.AsActionContext(), message.Id, message.ToRequest(), ct) .ConfigureAwait(false)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: rejected
      emits:
      - WiredSaveSuccessEvent
      - WiredValidationErrorEvent
      evidence: ev_b0dd7db943f4
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - WiredSaved
      - WiredValidationError
      - WiredSaved
      - WiredValidationError
      - WiredValidationError
      evidence: ev_9a9156c07ff2
  - scenario: wired.update_trigger.guard_2
    title: "update trigger: the guard '!await ActionModule.ApplyWiredUpdateAsync(ctx, itemId, up...' holds"
    given:
      guard: "!await ActionModule.ApplyWiredUpdateAsync(ctx, itemId, update, ct)"
      guard_holds: "true"
    when:
      packet: UpdateTrigger
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateTrigger sent while '!await ActionModule.ApplyWiredUpdateAsync(ctx, itemId, update, ct)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0080929ab318
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - WiredSaved
      - WiredValidationError
      - WiredSaved
      - WiredValidationError
      - WiredValidationError
      evidence: ev_9a9156c07ff2
  - scenario: wired.update_trigger.guard_3
    title: "update trigger: the guard '!_roomGrain._state.ItemsById.TryGetValue(itemId, out IRoo...' holds"
    given:
      guard: "!_roomGrain._state.ItemsById.TryGetValue(itemId, out IRoomItem? item)"
      guard_holds: "true"
    when:
      packet: UpdateTrigger
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateTrigger sent while '!_roomGrain._state.ItemsById.TryGetValue(itemId, out IRoomItem? item)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: rejected
      emits: []
      evidence: ev_e1599f76ae95
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - WiredSaved
      - WiredValidationError
      - WiredSaved
      - WiredValidationError
      - WiredValidationError
      evidence: ev_9a9156c07ff2
  - scenario: wired.update_trigger.guard_4
    title: "update trigger: the guard 'item.Logic is not FurnitureWiredLogic wiredLogic' holds"
    given:
      guard: item.Logic is not FurnitureWiredLogic wiredLogic
      guard_holds: "true"
    when:
      packet: UpdateTrigger
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateTrigger sent while 'item.Logic is not FurnitureWiredLogic wiredLogic' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: rejected
      emits: []
      evidence: ev_e1599f76ae95
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - WiredSaved
      - WiredValidationError
      - WiredSaved
      - WiredValidationError
      - WiredValidationError
      evidence: ev_9a9156c07ff2
  - scenario: wired.update_trigger.guard_5
    title: "update trigger: the guard '!await wiredLogic.ApplyWiredUpdateAsync(ctx, update, ct)' holds"
    given:
      guard: "!await wiredLogic.ApplyWiredUpdateAsync(ctx, update, ct)"
      guard_holds: "true"
    when:
      packet: UpdateTrigger
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateTrigger sent while '!await wiredLogic.ApplyWiredUpdateAsync(ctx, update, ct)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e1599f76ae95
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - WiredSaved
      - WiredValidationError
      - WiredSaved
      - WiredValidationError
      - WiredValidationError
      evidence: ev_9a9156c07ff2
  - scenario: wired.update_trigger.success
    title: "update trigger: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: UpdateTrigger
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of UpdateTrigger succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - WiredSaveSuccessEvent
      - WiredValidationErrorEvent
      evidence: ev_0080929ab318
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - WiredSaved
      - WiredValidationError
      - WiredSaved
      - WiredValidationError
      - WiredValidationError
      evidence: ev_9a9156c07ff2
  evidence:
  - id: ev_9a9156c07ff2
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/wired/UpdateTriggerMessageEvent.java
    symbol: UpdateTriggerMessageEvent
    line: 20
    note: third-party server implementation; evidence, not authority
  - id: ev_0080929ab318
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Furni.Floor.cs
    symbol: RoomGrain
    line: 99
  - id: ev_b0dd7db943f4
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserDefinedRoomEvents/UpdateTriggerMessageHandler.cs
    symbol: UpdateTriggerMessageHandler
    line: 23
  - id: ev_e1599f76ae95
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomActionModule.Floor.cs
    symbol: RoomActionModule
    line: 232
verified: {}
manual: {}
