# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:1880e5cfa6c0e17fbae03b5834d182af084bb742235a07ba9eb3c4a2bb5e0d0f"
generated:
  feature: wired.wired_set_preferences
  scenarios:
  - scenario: wired.wired_set_preferences.guard_0
    title: "wired set preferences: the guard 'ctx is null || ctx.PlayerId <= 0' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: WiredSetPreferences
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of WiredSetPreferences sent while 'ctx is null || ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_000d97b69eaf
  - scenario: wired.wired_set_preferences.success
    title: "wired set preferences: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: WiredSetPreferences
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of WiredSetPreferences succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_000d97b69eaf
  evidence:
  - id: ev_000d97b69eaf
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserDefinedRoomEvents/Wiredmenu/WiredSetPreferencesMessageHandler.cs
    symbol: WiredSetPreferencesMessageHandler
    line: 22
verified: {}
manual: {}
