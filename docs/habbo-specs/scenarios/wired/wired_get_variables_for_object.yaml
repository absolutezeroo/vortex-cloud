# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:fff6c65169ca90fbf8a6e19f376a0ecd97f7c5b34771828dac3075e6b956f6a1"
generated:
  feature: wired.wired_get_variables_for_object
  scenarios:
  - scenario: wired.wired_get_variables_for_object.guard_0
    title: "wired get variables for object: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: WiredGetVariablesForObject
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of WiredGetVariablesForObject sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_aed50979a0d3
  - scenario: wired.wired_get_variables_for_object.success
    title: "wired get variables for object: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: WiredGetVariablesForObject
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of WiredGetVariablesForObject succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - WiredVariablesForObjectEvent
      evidence: ev_0080929ab318
  evidence:
  - id: ev_0080929ab318
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Furni.Floor.cs
    symbol: RoomGrain
    line: 158
  - id: ev_aed50979a0d3
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserDefinedRoomEvents/Wiredmenu/WiredGetVariablesForObjectMessageHandler.cs
    symbol: WiredGetVariablesForObjectMessageHandler
    line: 26
verified: {}
manual: {}
