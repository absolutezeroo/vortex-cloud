# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:1b21e7cb3959e9707a8b5dd9e504b0b0c1e1da26ecaf5c47f196686502c39698"
generated:
  feature: wired.upgrade_wired_chest
  scenarios:
  - scenario: wired.upgrade_wired_chest.guard_0
    title: "upgrade wired chest: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: UpgradeWiredChest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpgradeWiredChest sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_abddc8b6a6a6
  - scenario: wired.upgrade_wired_chest.success
    title: "upgrade wired chest: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: UpgradeWiredChest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of UpgradeWiredChest succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - WiredChestUpgradeResult
      evidence: ev_62e15b6f2f52
  evidence:
  - id: ev_62e15b6f2f52
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Wired.ContractStore.cs
    symbol: RoomGrain
    line: 387
  - id: ev_abddc8b6a6a6
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserDefinedRoomEvents/Wiredtrading/UpgradeWiredChestMessageHandler.cs
    symbol: UpgradeWiredChestMessageHandler
    line: 31
verified: {}
manual: {}
