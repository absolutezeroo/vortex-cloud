# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:34ae5f99ea9e5bb0c91c45b188a908d7c755aed6d15dfe8e93f1e8cc796d26cb"
generated:
  feature: wired.close_wired_chest
  scenarios:
  - scenario: wired.close_wired_chest.guard_0
    title: "close wired chest: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: CloseWiredChest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of CloseWiredChest sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a26c2de0b21a
  - scenario: wired.close_wired_chest.guard_1
    title: "close wired chest: the guard '!_chestViewers.TryGetValue(chestId, out HashSet<PlayerId>...' holds"
    given:
      guard: "!_chestViewers.TryGetValue(chestId, out HashSet<PlayerId>? viewers) || !viewers.Remove(ctx.PlayerId)"
      guard_holds: "true"
    when:
      packet: CloseWiredChest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of CloseWiredChest sent while '!_chestViewers.TryGetValue(chestId, out HashSet<PlayerId>? viewers) || !viewers.Remove(ctx.PlayerId)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_84ee82d9d8e2
  - scenario: wired.close_wired_chest.guard_2
    title: "close wired chest: the guard 'viewers.Count > 0' holds"
    given:
      guard: viewers.Count > 0
      guard_holds: "true"
    when:
      packet: CloseWiredChest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of CloseWiredChest sent while 'viewers.Count > 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_84ee82d9d8e2
  - scenario: wired.close_wired_chest.guard_3
    title: "close wired chest: the guard '!_roomGrain._state.ItemsById.TryGetValue(chestId, out IRo...' holds"
    given:
      guard: "!_roomGrain._state.ItemsById.TryGetValue(chestId, out IRoomItem? item) || item.Logic.StuffData is not IMapStuffData map"
      guard_holds: "true"
    when:
      packet: CloseWiredChest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of CloseWiredChest sent while '!_roomGrain._state.ItemsById.TryGetValue(chestId, out IRoomItem? item) || item.Logic.StuffData is not IMapStuffData map' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_d3a71568179c
  - scenario: wired.close_wired_chest.success
    title: "close wired chest: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: CloseWiredChest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of CloseWiredChest succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_120b08c78fe9
  evidence:
  - id: ev_120b08c78fe9
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Object/Logic/Furniture/Floor/WiredChestStuffData.cs
    symbol: WiredChestStuffData
    line: 104
  - id: ev_84ee82d9d8e2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/WiredTrading/RoomWiredTradingSystem.Chests.cs
    symbol: RoomWiredTradingSystem
    line: 117
  - id: ev_a26c2de0b21a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserDefinedRoomEvents/Wiredtrading/CloseWiredChestMessageHandler.cs
    symbol: CloseWiredChestMessageHandler
    line: 30
  - id: ev_d3a71568179c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/WiredTrading/WiredChestStore.cs
    symbol: WiredChestStore
    line: 237
verified: {}
manual: {}
