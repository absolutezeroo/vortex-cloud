# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:1f67c4d7f0e3944691e2c068c72daa671f02370c71a7d6d9e5d12f520492633c"
generated:
  feature: wired.update_selector
  scenarios:
  - scenario: wired.update_selector.guard_0
    title: "update selector: the guard 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || me...' holds"
    given:
      guard: ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.Id <= 0
      guard_holds: "true"
    when:
      packet: UpdateSelector
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateSelector sent while 'ctx is null || ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.Id <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f463aa277a16
  - scenario: wired.update_selector.guard_1
    title: "update selector: the guard '!await _grainFactory .GetRoomFurni(ctx.RoomId) .ApplyWire...' holds"
    given:
      guard: "!await _grainFactory .GetRoomFurni(ctx.RoomId) .ApplyWiredUpdateAsync(ctx.AsActionContext(), message.Id, message, ct) .ConfigureAwait(false)"
      guard_holds: "true"
    when:
      packet: UpdateSelector
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateSelector sent while '!await _grainFactory .GetRoomFurni(ctx.RoomId) .ApplyWiredUpdateAsync(ctx.AsActionContext(), message.Id, message, ct) .ConfigureAwait(false)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f463aa277a16
  - scenario: wired.update_selector.guard_2
    title: "update selector: the guard '!await ActionModule.ApplyWiredUpdateAsync(ctx, itemId, up...' holds"
    given:
      guard: "!await ActionModule.ApplyWiredUpdateAsync(ctx, itemId, update, ct)"
      guard_holds: "true"
    when:
      packet: UpdateSelector
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateSelector sent while '!await ActionModule.ApplyWiredUpdateAsync(ctx, itemId, update, ct)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0080929ab318
  - scenario: wired.update_selector.guard_3
    title: "update selector: the guard '!_roomGrain._state.ItemsById.TryGetValue(itemId, out IRoo...' holds"
    given:
      guard: "!_roomGrain._state.ItemsById.TryGetValue(itemId, out IRoomItem? item)"
      guard_holds: "true"
    when:
      packet: UpdateSelector
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateSelector sent while '!_roomGrain._state.ItemsById.TryGetValue(itemId, out IRoomItem? item)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: rejected
      emits: []
      evidence: ev_e1599f76ae95
  - scenario: wired.update_selector.guard_4
    title: "update selector: the guard 'item.Logic is not FurnitureWiredLogic wiredLogic' holds"
    given:
      guard: item.Logic is not FurnitureWiredLogic wiredLogic
      guard_holds: "true"
    when:
      packet: UpdateSelector
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateSelector sent while 'item.Logic is not FurnitureWiredLogic wiredLogic' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: rejected
      emits: []
      evidence: ev_e1599f76ae95
  - scenario: wired.update_selector.guard_5
    title: "update selector: the guard '!await wiredLogic.ApplyWiredUpdateAsync(ctx, update, ct)' holds"
    given:
      guard: "!await wiredLogic.ApplyWiredUpdateAsync(ctx, update, ct)"
      guard_holds: "true"
    when:
      packet: UpdateSelector
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateSelector sent while '!await wiredLogic.ApplyWiredUpdateAsync(ctx, update, ct)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e1599f76ae95
  - scenario: wired.update_selector.success
    title: "update selector: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: UpdateSelector
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of UpdateSelector succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - WiredSaveSuccessEvent
      evidence: ev_0080929ab318
  evidence:
  - id: ev_0080929ab318
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Furni.Floor.cs
    symbol: RoomGrain
    line: 99
  - id: ev_e1599f76ae95
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomActionModule.Floor.cs
    symbol: RoomActionModule
    line: 231
  - id: ev_f463aa277a16
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserDefinedRoomEvents/UpdateSelectorMessageHandler.cs
    symbol: UpdateSelectorMessageHandler
    line: 22
verified: {}
manual: {}
