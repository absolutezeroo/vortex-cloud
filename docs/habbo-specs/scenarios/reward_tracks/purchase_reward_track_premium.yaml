# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:d147ab8d811af998f0b1986fb1c635ede4d82bbffd809f5efc269e0f30159c8f"
generated:
  feature: reward_tracks.purchase_reward_track_premium
  scenarios:
  - scenario: reward_tracks.purchase_reward_track_premium.guard_0
    title: "purchase reward track premium: the guard 'ctx.PlayerId <= 0 || string.IsNullOrWhiteSpace(message.Tr...' holds"
    given:
      guard: ctx.PlayerId <= 0 || string.IsNullOrWhiteSpace(message.TrackId)
      guard_holds: "true"
    when:
      packet: PurchaseRewardTrackPremium
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of PurchaseRewardTrackPremium sent while 'ctx.PlayerId <= 0 || string.IsNullOrWhiteSpace(message.TrackId)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_8d5a658eb63e
  - scenario: reward_tracks.purchase_reward_track_premium.success
    title: "purchase reward track premium: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: PurchaseRewardTrackPremium
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of PurchaseRewardTrackPremium succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_8d5a658eb63e
  evidence:
  - id: ev_8d5a658eb63e
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/RewardTracks/PurchaseRewardTrackPremiumMessageHandler.cs
    symbol: PurchaseRewardTrackPremiumMessageHandler
    line: 23
verified: {}
manual: {}
