# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:996250ce204582ce248db1e322a098868851d184f11b67b204c2503a4108e286"
generated:
  feature: reward_tracks.claim_reward_track_prize
  scenarios:
  - scenario: reward_tracks.claim_reward_track_prize.guard_0
    title: "claim reward track prize: the guard 'ctx.PlayerId <= 0 || string.IsNullOrWhiteSpace(message.Tr...' holds"
    given:
      guard: ctx.PlayerId <= 0 || string.IsNullOrWhiteSpace(message.TrackId) || string.IsNullOrWhiteSpace(message.PrizeId)
      guard_holds: "true"
    when:
      packet: ClaimRewardTrackPrize
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ClaimRewardTrackPrize sent while 'ctx.PlayerId <= 0 || string.IsNullOrWhiteSpace(message.TrackId) || string.IsNullOrWhiteSpace(message.PrizeId)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_537eed55903a
  - scenario: reward_tracks.claim_reward_track_prize.success
    title: "claim reward track prize: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ClaimRewardTrackPrize
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ClaimRewardTrackPrize succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_537eed55903a
  evidence:
  - id: ev_537eed55903a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/RewardTracks/ClaimRewardTrackPrizeMessageHandler.cs
    symbol: ClaimRewardTrackPrizeMessageHandler
    line: 23
verified: {}
manual: {}
