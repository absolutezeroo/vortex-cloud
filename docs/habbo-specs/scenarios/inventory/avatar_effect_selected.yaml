# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:394c57f713584159aa078e575d2794a612a6ec4307795b9f0320b87fb6d61094"
generated:
  feature: inventory.avatar_effect_selected
  scenarios:
  - scenario: inventory.avatar_effect_selected.guard_0
    title: "avatar effect selected: the guard 'ctx.PlayerId <= 0 || message.EffectType < 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.EffectType < 0
      guard_holds: "true"
    when:
      packet: AvatarEffectSelected
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AvatarEffectSelected sent while 'ctx.PlayerId <= 0 || message.EffectType < 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7f3075d7b8ac
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_1a867fdc8190
  - scenario: inventory.avatar_effect_selected.guard_1
    title: "avatar effect selected: the guard 'wearable is null' holds"
    given:
      guard: wearable is null
      guard_holds: "true"
    when:
      packet: AvatarEffectSelected
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AvatarEffectSelected sent while 'wearable is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7ef4c91324dc
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_1a867fdc8190
  - scenario: inventory.avatar_effect_selected.guard_2
    title: "avatar effect selected: the guard '!_state.AvatarsByPlayerId.TryGetValue(ctx.PlayerId, out R...' holds"
    given:
      guard: "!_state.AvatarsByPlayerId.TryGetValue(ctx.PlayerId, out RoomObjectId objectId) || !await AvatarModule.SetAvatarEffectAsync(objectId, effectId, ct)"
      guard_holds: "true"
    when:
      packet: AvatarEffectSelected
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AvatarEffectSelected sent while '!_state.AvatarsByPlayerId.TryGetValue(ctx.PlayerId, out RoomObjectId objectId) || !await AvatarModule.SetAvatarEffectAsync(objectId, effectId, ct)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ac641f7283b9
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_1a867fdc8190
  - scenario: inventory.avatar_effect_selected.guard_3
    title: "avatar effect selected: the guard 'objectId <= 0 || !_roomGrain._state.AvatarsByObjectId.Try...' holds"
    given:
      guard: objectId <= 0 || !_roomGrain._state.AvatarsByObjectId.TryGetValue( objectId.Value, out IRoomAvatar? avatar ) || !avatar.SetEffect(effectId)
      guard_holds: "true"
    when:
      packet: AvatarEffectSelected
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AvatarEffectSelected sent while 'objectId <= 0 || !_roomGrain._state.AvatarsByObjectId.TryGetValue( objectId.Value, out IRoomAvatar? avatar ) || !avatar.SetEffect(effectId)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e6fef545a42a
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_1a867fdc8190
  - scenario: inventory.avatar_effect_selected.guard_4
    title: "avatar effect selected: the guard 'CurrentEffectId == effectId' holds"
    given:
      guard: CurrentEffectId == effectId
      guard_holds: "true"
    when:
      packet: AvatarEffectSelected
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AvatarEffectSelected sent while 'CurrentEffectId == effectId' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_9f45a8c43da8
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_1a867fdc8190
  - scenario: inventory.avatar_effect_selected.success
    title: "avatar effect selected: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: AvatarEffectSelected
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of AvatarEffectSelected succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - AvatarEffect
      - AvatarEffectExpired
      - AvatarEffectSelected
      evidence: ev_04e643c029b0
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_1a867fdc8190
  evidence:
  - id: ev_1a867fdc8190
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/users/AvatarEffectSelectedEvent.java
    symbol: AvatarEffectSelectedEvent
    line: 7
    note: third-party server implementation; evidence, not authority
  - id: ev_04e643c029b0
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Inventory/Avatareffect/AvatarEffectSelectedMessageHandler.cs
    symbol: AvatarEffectSelectedMessageHandler
    line: 10
  - id: ev_7ef4c91324dc
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerEffectGrain.cs
    symbol: PlayerEffectGrain
    line: 235
  - id: ev_7f3075d7b8ac
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Inventory/Avatareffect/AvatarEffectSelectedMessageHandler.cs
    symbol: AvatarEffectSelectedMessageHandler
    line: 22
  - id: ev_9f45a8c43da8
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Object/Avatars/RoomAvatar.cs
    symbol: RoomAvatar
    line: 243
  - id: ev_ac641f7283b9
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Avatar.cs
    symbol: RoomGrain
    line: 237
  - id: ev_e6fef545a42a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Modules/RoomAvatarModule.cs
    symbol: RoomAvatarModule
    line: 533
verified: {}
manual: {}
