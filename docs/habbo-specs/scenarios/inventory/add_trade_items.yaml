# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:2f39ad509eb2306d1b30ffed6d8dddbafa5b251fc73fa6b1d5b755c947196fd5"
generated:
  feature: inventory.add_trade_items
  scenarios:
  - scenario: inventory.add_trade_items.guard_0
    title: "add trade items: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || itemIds.Count == 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || itemIds.Count == 0
      guard_holds: "true"
    when:
      packet: AddItemToTrade
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AddItemToTrade sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || itemIds.Count == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a8707f5e7b6c
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_27898759c449
  - scenario: inventory.add_trade_items.guard_1
    title: "add trade items: the guard '!TryGetSession(requesterId, out RoomTradeSession? session)' holds"
    given:
      guard: "!TryGetSession(requesterId, out RoomTradeSession? session)"
      guard_holds: "true"
    when:
      packet: AddItemToTrade
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AddItemToTrade sent while '!TryGetSession(requesterId, out RoomTradeSession? session)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1d91c4a1b72c
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_27898759c449
  - scenario: inventory.add_trade_items.guard_2
    title: "add trade items: the guard 'session.Phase != TradePhase.Building' holds"
    given:
      guard: session.Phase != TradePhase.Building
      guard_holds: "true"
    when:
      packet: AddItemToTrade
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AddItemToTrade sent while 'session.Phase != TradePhase.Building' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1d91c4a1b72c
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_27898759c449
  - scenario: inventory.add_trade_items.success
    title: "add trade items: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: AddItemToTrade
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of AddItemToTrade succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1d91c4a1b72c
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_27898759c449
  evidence:
  - id: ev_27898759c449
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/trading/AddItemToTradeEvent.java
    symbol: AddItemToTradeEvent
    line: 9
    note: third-party server implementation; evidence, not authority
  - id: ev_1d91c4a1b72c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomTradingSystem.cs
    symbol: RoomTradingSystem
    line: 130
  - id: ev_a8707f5e7b6c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/RoomService.Trading.cs
    symbol: RoomService
    line: 50
verified: {}
manual: {}
