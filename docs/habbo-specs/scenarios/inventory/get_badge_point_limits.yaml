# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:df0c67b617f52b428fb65e128e1162fdbe75f7cea571b5a259bef33a7a09c786"
generated:
  feature: inventory.get_badge_point_limits
  scenarios:
  - scenario: inventory.get_badge_point_limits.guard_0
    title: "get badge point limits: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetBadgePointLimits
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetBadgePointLimits sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_18c3506bd65f
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - BadgePointLimits
      evidence: ev_f5ffe63178bd
  - scenario: inventory.get_badge_point_limits.success
    title: "get badge point limits: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetBadgePointLimits
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetBadgePointLimits succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - BadgePointLimitsEvent
      evidence: ev_18c3506bd65f
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - BadgePointLimits
      evidence: ev_f5ffe63178bd
  evidence:
  - id: ev_f5ffe63178bd
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/unknown/GetBadgePointLimitsEvent.java
    symbol: GetBadgePointLimitsEvent
    line: 8
    note: third-party server implementation; evidence, not authority
  - id: ev_18c3506bd65f
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Inventory/Badges/GetBadgePointLimitsMessageHandler.cs
    symbol: GetBadgePointLimitsMessageHandler
    line: 29
verified: {}
manual: {}
