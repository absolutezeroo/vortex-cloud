# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:034795f2a1a4eda80a8c8de51abeccf522e6cc6ed1004655a18925454bed892b"
generated:
  feature: inventory.get_bot_inventory
  scenarios:
  - scenario: inventory.get_bot_inventory.guard_0
    title: "get bot inventory: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetBotInventory
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetBotInventory sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_edb01640bae6
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - BotInventory
      evidence: ev_bbff9e09af23
  - scenario: inventory.get_bot_inventory.success
    title: "get bot inventory: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetBotInventory
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetBotInventory succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - BotInventoryEvent
      evidence: ev_90ba9eeb98e7
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - BotInventory
      evidence: ev_bbff9e09af23
  evidence:
  - id: ev_bbff9e09af23
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/inventory/GetBotInventoryEvent.java
    symbol: GetBotInventoryEvent
    line: 8
    note: third-party server implementation; evidence, not authority
  - id: ev_90ba9eeb98e7
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Inventory/Bots/GetBotInventoryMessageHandler.cs
    symbol: GetBotInventoryMessageHandler
    line: 13
  - id: ev_edb01640bae6
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Inventory/Bots/GetBotInventoryMessageHandler.cs
    symbol: GetBotInventoryMessageHandler
    line: 22
verified: {}
manual: {}
