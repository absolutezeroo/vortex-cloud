# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:56f719fc27d816d1f19a1aba52907201918f431fab5cf580ea174291f15e89d2"
generated:
  feature: inventory.get_achievements
  scenarios:
  - scenario: inventory.get_achievements.guard_0
    title: "get achievements: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetAchievements
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetAchievements sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a7798658d9db
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - Achievements
      evidence: ev_77faa2ed35e1
  - scenario: inventory.get_achievements.guard_1
    title: "get achievements: the guard 'definitions.IsEmpty' holds"
    given:
      guard: definitions.IsEmpty
      guard_holds: "true"
    when:
      packet: GetAchievements
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetAchievements sent while 'definitions.IsEmpty' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_b6254b352078
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - Achievements
      evidence: ev_77faa2ed35e1
  - scenario: inventory.get_achievements.success
    title: "get achievements: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetAchievements
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetAchievements succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - AchievementsEvent
      evidence: ev_6a825bb2f759
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - Achievements
      evidence: ev_77faa2ed35e1
  evidence:
  - id: ev_77faa2ed35e1
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/achievements/GetAchievementsEvent.java
    symbol: GetAchievementsEvent
    line: 8
    note: third-party server implementation; evidence, not authority
  - id: ev_6a825bb2f759
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Progression/Grains/AchievementManagerGrain.cs
    symbol: AchievementManagerGrain
    line: 45
  - id: ev_a7798658d9db
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Inventory/Achievements/GetAchievementsMessageHandler.cs
    symbol: GetAchievementsMessageHandler
    line: 21
  - id: ev_b6254b352078
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Progression/Grains/PlayerAchievementGrain.cs
    symbol: PlayerAchievementGrain
    line: 106
verified: {}
manual: {}
