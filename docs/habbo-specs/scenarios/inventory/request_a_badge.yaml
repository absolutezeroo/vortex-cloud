# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:0b93ed609653e07ad0b9c3a21a1c4dd97ed436f2c6b3ce0dbc25053dfdeb585b"
generated:
  feature: inventory.request_a_badge
  scenarios:
  - scenario: inventory.request_a_badge.guard_0
    title: "request a badge: the guard 'ctx.PlayerId <= 0 || string.IsNullOrEmpty(message.Request...' holds"
    given:
      guard: ctx.PlayerId <= 0 || string.IsNullOrEmpty(message.RequestCode)
      guard_holds: "true"
    when:
      packet: RequestABadge
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RequestABadge sent while 'ctx.PlayerId <= 0 || string.IsNullOrEmpty(message.RequestCode)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f4e9b4a63c7f
  - scenario: inventory.request_a_badge.success
    title: "request a badge: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RequestABadge
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RequestABadge succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - IsBadgeRequestFulfilledEvent
      evidence: ev_cb07d3019d09
  evidence:
  - id: ev_cb07d3019d09
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Inventory/Badges/RequestABadgeMessageHandler.cs
    symbol: RequestABadgeMessageHandler
    line: 16
  - id: ev_f4e9b4a63c7f
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Inventory/Badges/RequestABadgeMessageHandler.cs
    symbol: RequestABadgeMessageHandler
    line: 25
verified: {}
manual: {}
