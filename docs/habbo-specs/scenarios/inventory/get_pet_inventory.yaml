# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:3ce35e88c7daa189d40bf04e2d9947711bdc4d5571d26a530854860f554438f7"
generated:
  feature: inventory.get_pet_inventory
  scenarios:
  - scenario: inventory.get_pet_inventory.guard_0
    title: "get pet inventory: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetPetInventory
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetPetInventory sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_fdfd185d7468
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - PetInventory
      evidence: ev_ccaab92cba21
  - scenario: inventory.get_pet_inventory.success
    title: "get pet inventory: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetPetInventory
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetPetInventory succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - PetInventoryEvent
      evidence: ev_55d55f5c1d10
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - PetInventory
      evidence: ev_ccaab92cba21
  evidence:
  - id: ev_ccaab92cba21
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/inventory/GetPetInventoryEvent.java
    symbol: GetPetInventoryEvent
    line: 8
    note: third-party server implementation; evidence, not authority
  - id: ev_55d55f5c1d10
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Inventory/Grains/InventoryGrain.Pets.cs
    symbol: InventoryGrain
    line: 131
  - id: ev_fdfd185d7468
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Inventory/Pets/GetPetInventoryMessageHandler.cs
    symbol: GetPetInventoryMessageHandler
    line: 25
verified: {}
manual: {}
