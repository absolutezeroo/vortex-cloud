# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:120f9bc050adae4789ac0d3ec0ca39b71525dd3cbb19ea685ca5dea408c0ee68"
generated:
  feature: inventory.get_credits_info
  scenarios:
  - scenario: inventory.get_credits_info.guard_0
    title: "get credits info: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetCreditsInfo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetCreditsInfo sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_45104da5e6fe
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - CreditBalance
      - ActivityPoints
      evidence: ev_720a5bfe8c9f
  - scenario: inventory.get_credits_info.success
    title: "get credits info: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetCreditsInfo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetCreditsInfo succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - ActivityPoints
      - CreditBalanceEvent
      evidence: ev_0a5d3f47e95a
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - CreditBalance
      - ActivityPoints
      evidence: ev_720a5bfe8c9f
  evidence:
  - id: ev_720a5bfe8c9f
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/users/GetCreditsInfoEvent.java
    symbol: GetCreditsInfoEvent
    line: 9
    note: third-party server implementation; evidence, not authority
  - id: ev_0a5d3f47e95a
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Inventory/Purse/GetCreditsInfoMessageHandler.cs
    symbol: GetCreditsInfoMessageHandler
    line: 17
  - id: ev_45104da5e6fe
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Inventory/Purse/GetCreditsInfoMessageHandler.cs
    symbol: GetCreditsInfoMessageHandler
    line: 28
verified: {}
manual: {}
