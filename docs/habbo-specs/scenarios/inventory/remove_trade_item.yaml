# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:08ec3b1fa54465f6143473a163b4b3c227465b856bc62f1e679a5ff1cbed3ef8"
generated:
  feature: inventory.remove_trade_item
  scenarios:
  - scenario: inventory.remove_trade_item.guard_0
    title: "remove trade item: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || itemId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || itemId <= 0
      guard_holds: "true"
    when:
      packet: RemoveItemFromTrade
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RemoveItemFromTrade sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || itemId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a8707f5e7b6c
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_395a0abadc24
  - scenario: inventory.remove_trade_item.guard_1
    title: "remove trade item: the guard '!TryGetSession(requesterId, out RoomTradeSession? session)' holds"
    given:
      guard: "!TryGetSession(requesterId, out RoomTradeSession? session)"
      guard_holds: "true"
    when:
      packet: RemoveItemFromTrade
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RemoveItemFromTrade sent while '!TryGetSession(requesterId, out RoomTradeSession? session)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1d91c4a1b72c
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_395a0abadc24
  - scenario: inventory.remove_trade_item.guard_2
    title: "remove trade item: the guard 'session.Phase != TradePhase.Building' holds"
    given:
      guard: session.Phase != TradePhase.Building
      guard_holds: "true"
    when:
      packet: RemoveItemFromTrade
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RemoveItemFromTrade sent while 'session.Phase != TradePhase.Building' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1d91c4a1b72c
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_395a0abadc24
  - scenario: inventory.remove_trade_item.guard_3
    title: "remove trade item: the guard '!session.ItemsOf(requesterId).Remove(itemId)' holds"
    given:
      guard: "!session.ItemsOf(requesterId).Remove(itemId)"
      guard_holds: "true"
    when:
      packet: RemoveItemFromTrade
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RemoveItemFromTrade sent while '!session.ItemsOf(requesterId).Remove(itemId)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1d91c4a1b72c
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_395a0abadc24
  - scenario: inventory.remove_trade_item.success
    title: "remove trade item: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RemoveItemFromTrade
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RemoveItemFromTrade succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1d91c4a1b72c
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_395a0abadc24
  evidence:
  - id: ev_395a0abadc24
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/trading/RemoveItemFromTradeEvent.java
    symbol: RemoveItemFromTradeEvent
    line: 9
    note: third-party server implementation; evidence, not authority
  - id: ev_1d91c4a1b72c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomTradingSystem.cs
    symbol: RoomTradingSystem
    line: 245
  - id: ev_a8707f5e7b6c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/RoomService.Trading.cs
    symbol: RoomService
    line: 63
verified: {}
manual: {}
