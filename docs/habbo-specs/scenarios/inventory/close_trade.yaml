# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:b11b84e5bd893444791f3c32a04e6bd9bdb3f150ccdc35524b530aaa20fc0b6e"
generated:
  feature: inventory.close_trade
  scenarios:
  - scenario: inventory.close_trade.guard_0
    title: "close trade: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: CloseTrading
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of CloseTrading sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a8707f5e7b6c
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_164b2efc3bf4
  - scenario: inventory.close_trade.guard_1
    title: "close trade: the guard '!TryGetSession(requesterId, out RoomTradeSession? session)' holds"
    given:
      guard: "!TryGetSession(requesterId, out RoomTradeSession? session)"
      guard_holds: "true"
    when:
      packet: CloseTrading
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of CloseTrading sent while '!TryGetSession(requesterId, out RoomTradeSession? session)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1d91c4a1b72c
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_164b2efc3bf4
  - scenario: inventory.close_trade.success
    title: "close trade: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: CloseTrading
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of CloseTrading succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1d91c4a1b72c
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_164b2efc3bf4
  evidence:
  - id: ev_164b2efc3bf4
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/trading/CloseTradingEvent.java
    symbol: CloseTradingEvent
    line: 10
    note: third-party server implementation; evidence, not authority
  - id: ev_1d91c4a1b72c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomTradingSystem.cs
    symbol: RoomTradingSystem
    line: 333
  - id: ev_a8707f5e7b6c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/RoomService.Trading.cs
    symbol: RoomService
    line: 102
verified: {}
manual: {}
