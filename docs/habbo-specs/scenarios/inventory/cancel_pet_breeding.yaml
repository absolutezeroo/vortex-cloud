# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:8ea7f328a14a53cf7f256835a60ad4113a24a41394497b1bd9a579eb20e278f3"
generated:
  feature: inventory.cancel_pet_breeding
  scenarios:
  - scenario: inventory.cancel_pet_breeding.guard_0
    title: "cancel pet breeding: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0
      guard_holds: "true"
    when:
      packet: CancelPetBreeding
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of CancelPetBreeding sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.PetId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_baa9ea544388
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_f93390318b62
  - scenario: inventory.cancel_pet_breeding.success
    title: "cancel pet breeding: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: CancelPetBreeding
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of CancelPetBreeding succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7252ff05fcad
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_f93390318b62
  evidence:
  - id: ev_f93390318b62
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/pets/CancelPetBreedingEvent.java
    symbol: CancelPetBreedingEvent
    line: 9
    note: third-party server implementation; evidence, not authority
  - id: ev_7252ff05fcad
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Inventory/Pets/CancelPetBreedingMessageHandler.cs
    symbol: CancelPetBreedingMessageHandler
    line: 11
  - id: ev_baa9ea544388
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Inventory/Pets/CancelPetBreedingMessageHandler.cs
    symbol: CancelPetBreedingMessageHandler
    line: 22
verified: {}
manual: {}
