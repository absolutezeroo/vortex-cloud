# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:0798bc84e4588a251d602a6cdbfb0111d9b9da8ffa9b288246978b203d5b626e"
generated:
  feature: inventory.set_trade_accept
  scenarios:
  - scenario: inventory.set_trade_accept.guard_0
    title: "set trade accept: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: AcceptTrading
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AcceptTrading sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a8707f5e7b6c
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_79762ff5fabd
  - scenario: inventory.set_trade_accept.guard_1
    title: "set trade accept: the guard '!TryGetSession(requesterId, out RoomTradeSession? session)' holds"
    given:
      guard: "!TryGetSession(requesterId, out RoomTradeSession? session)"
      guard_holds: "true"
    when:
      packet: AcceptTrading
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AcceptTrading sent while '!TryGetSession(requesterId, out RoomTradeSession? session)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1d91c4a1b72c
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_79762ff5fabd
  - scenario: inventory.set_trade_accept.guard_2
    title: "set trade accept: the guard 'session.Phase != TradePhase.Building || session.AcceptedO...' holds"
    given:
      guard: session.Phase != TradePhase.Building || session.AcceptedOf(requesterId) == accepted
      guard_holds: "true"
    when:
      packet: AcceptTrading
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AcceptTrading sent while 'session.Phase != TradePhase.Building || session.AcceptedOf(requesterId) == accepted' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1d91c4a1b72c
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_79762ff5fabd
  - scenario: inventory.set_trade_accept.success
    title: "set trade accept: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: AcceptTrading
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of AcceptTrading succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - TradingAcceptEvent
      - TradingConfirmationEvent
      evidence: ev_17349bcc2f7a
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_79762ff5fabd
  evidence:
  - id: ev_79762ff5fabd
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/trading/AcceptTradingEvent.java
    symbol: AcceptTradingEvent
    line: 9
    note: third-party server implementation; evidence, not authority
  - id: ev_17349bcc2f7a
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Inventory/Trading/UnacceptTradingMessageHandler.cs
    symbol: UnacceptTradingMessageHandler
    line: 9
  - id: ev_1d91c4a1b72c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomTradingSystem.cs
    symbol: RoomTradingSystem
    line: 297
  - id: ev_a8707f5e7b6c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/RoomService.Trading.cs
    symbol: RoomService
    line: 89
verified: {}
manual: {}
