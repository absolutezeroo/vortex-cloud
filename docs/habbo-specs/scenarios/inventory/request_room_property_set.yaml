# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:f4e0fa0e5df01972d9727b5de837090c9b716b71449aaf786fb251660220f993"
generated:
  feature: inventory.request_room_property_set
  scenarios:
  - scenario: inventory.request_room_property_set.guard_0
    title: "request room property set: the guard 'ctx.PlayerId <= 0 || message.RoomId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.RoomId <= 0
      guard_holds: "true"
    when:
      packet: RequestRoomPropertySet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RequestRoomPropertySet sent while 'ctx.PlayerId <= 0 || message.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_fc75e6123bc2
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - FurniListRemove
      - RoomProperty
      - FurniListRemove
      evidence: ev_5d5af748423d
  - scenario: inventory.request_room_property_set.success
    title: "request room property set: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RequestRoomPropertySet
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RequestRoomPropertySet succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - RoomProperty
      evidence: ev_373e76aa8a80
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - FurniListRemove
      - RoomProperty
      - FurniListRemove
      evidence: ev_5d5af748423d
  evidence:
  - id: ev_5d5af748423d
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/RequestRoomPropertySetEvent.java
    symbol: RequestRoomPropertySetEvent
    line: 14
    note: third-party server implementation; evidence, not authority
  - id: ev_373e76aa8a80
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerPresenceGrain.cs
    symbol: PlayerPresenceGrain
    line: 112
  - id: ev_fc75e6123bc2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Inventory/Furni/RequestRoomPropertySetMessageHandler.cs
    symbol: RequestRoomPropertySetMessageHandler
    line: 23
verified: {}
manual: {}
