# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:099ca79ace1902fd5883cba9fc8e9093a86b4948511ee51be3af7c55cc2149c4"
generated:
  feature: inventory.get_is_badge_request_fulfilled
  scenarios:
  - scenario: inventory.get_is_badge_request_fulfilled.guard_0
    title: "get is badge request fulfilled: the guard 'ctx.PlayerId <= 0 || string.IsNullOrEmpty(message.Request...' holds"
    given:
      guard: ctx.PlayerId <= 0 || string.IsNullOrEmpty(message.RequestCode)
      guard_holds: "true"
    when:
      packet: GetIsBadgeRequestFulfilled
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetIsBadgeRequestFulfilled sent while 'ctx.PlayerId <= 0 || string.IsNullOrEmpty(message.RequestCode)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7f08fc156914
  - scenario: inventory.get_is_badge_request_fulfilled.success
    title: "get is badge request fulfilled: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetIsBadgeRequestFulfilled
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetIsBadgeRequestFulfilled succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - IsBadgeRequestFulfilled
      evidence: ev_5c18cdfb768a
  evidence:
  - id: ev_5c18cdfb768a
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Inventory/Badges/GetIsBadgeRequestFulfilledMessageHandler.cs
    symbol: GetIsBadgeRequestFulfilledMessageHandler
    line: 13
  - id: ev_7f08fc156914
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Inventory/Badges/GetIsBadgeRequestFulfilledMessageHandler.cs
    symbol: GetIsBadgeRequestFulfilledMessageHandler
    line: 22
verified: {}
manual: {}
