# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:e5765bee32cd6f7cb55afd1f9d5c34751e442cc2669b84ed0f621ba69fbe2bd2"
generated:
  feature: inventory.request_furni_inventory_when_not_in_room
  scenarios:
  - scenario: inventory.request_furni_inventory_when_not_in_room.guard_0
    title: "request furni inventory when not in room: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: RequestFurniInventoryWhenNotInRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RequestFurniInventoryWhenNotInRoom sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a2f5f7a4e8c7
  - scenario: inventory.request_furni_inventory_when_not_in_room.success
    title: "request furni inventory when not in room: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RequestFurniInventoryWhenNotInRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RequestFurniInventoryWhenNotInRoom succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - FurniListEvent
      evidence: ev_0c8ec94a79b8
  evidence:
  - id: ev_0c8ec94a79b8
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Inventory/Furni/RequestFurniInventoryWhenNotInRoomMessageHandler.cs
    symbol: RequestFurniInventoryWhenNotInRoomMessageHandler
    line: 11
  - id: ev_a2f5f7a4e8c7
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Inventory/Furni/RequestFurniInventoryWhenNotInRoomMessageHandler.cs
    symbol: RequestFurniInventoryWhenNotInRoomMessageHandler
    line: 22
verified: {}
manual: {}
