# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:f8eb3edacec01aafb04de75db885b2bcef5579ef6e978a9ce8a555ee3310b30a"
generated:
  feature: inventory.request_furni_inventory
  scenarios:
  - scenario: inventory.request_furni_inventory.guard_0
    title: "request furni inventory: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: RequestFurniInventory
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RequestFurniInventory sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e3cca687e112
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - FurniList
      - FurniList
      - FurniList
      evidence: ev_d4d1bf248390
  - scenario: inventory.request_furni_inventory.success
    title: "request furni inventory: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RequestFurniInventory
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RequestFurniInventory succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - FurniListEvent
      evidence: ev_373e76aa8a80
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - FurniList
      - FurniList
      - FurniList
      evidence: ev_d4d1bf248390
  evidence:
  - id: ev_d4d1bf248390
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/inventory/RequestFurniInventoryEvent.java
    symbol: RequestFurniInventoryEvent
    line: 18
    note: third-party server implementation; evidence, not authority
  - id: ev_373e76aa8a80
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerPresenceGrain.cs
    symbol: PlayerPresenceGrain
    line: 112
  - id: ev_e3cca687e112
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Inventory/Furni/RequestFurniInventoryMessageHandler.cs
    symbol: RequestFurniInventoryMessageHandler
    line: 22
verified: {}
manual: {}
