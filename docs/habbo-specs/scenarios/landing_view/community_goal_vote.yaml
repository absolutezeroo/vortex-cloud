# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:e37073d28bc7521999ecdaf21931bf06e74f7834474a1a4fd53385affe30324e"
generated:
  feature: landing_view.community_goal_vote
  scenarios:
  - scenario: landing_view.community_goal_vote.success
    title: "community goal vote: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: CommunityGoalVote
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of CommunityGoalVote succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f8b62da04a50
  evidence:
  - id: ev_f8b62da04a50
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Landingview/Votes/CommunityGoalVoteMessageHandler.cs
    symbol: CommunityGoalVoteMessageHandler
    line: 8
verified: {}
manual: {}
