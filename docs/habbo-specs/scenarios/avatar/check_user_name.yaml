# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:ed492843eb7b0dbb6c997c6898aa762b690f654727884e02dbaf2d0a8c50cb7d"
generated:
  feature: avatar.check_user_name
  scenarios:
  - scenario: avatar.check_user_name.guard_0
    title: "check user name: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: CheckUserName
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of CheckUserName sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_82bff245d7eb
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - ChangeNameCheckResult
      evidence: ev_a6736c1a918f
  - scenario: avatar.check_user_name.guard_1
    title: "check user name: the guard 'string.IsNullOrWhiteSpace(name)' holds"
    given:
      guard: string.IsNullOrWhiteSpace(name)
      guard_holds: "true"
    when:
      packet: CheckUserName
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of CheckUserName sent while 'string.IsNullOrWhiteSpace(name)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7f41f2bdde62
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - ChangeNameCheckResult
      evidence: ev_a6736c1a918f
  - scenario: avatar.check_user_name.guard_2
    title: "check user name: the guard '_nameToId.TryGetValue(name, out PlayerId playerId)' holds"
    given:
      guard: _nameToId.TryGetValue(name, out PlayerId playerId)
      guard_holds: "true"
    when:
      packet: CheckUserName
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of CheckUserName sent while '_nameToId.TryGetValue(name, out PlayerId playerId)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7f41f2bdde62
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - ChangeNameCheckResult
      evidence: ev_a6736c1a918f
  - scenario: avatar.check_user_name.guard_3
    title: "check user name: the guard 'foundId is null' holds"
    given:
      guard: foundId is null
      guard_holds: "true"
    when:
      packet: CheckUserName
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of CheckUserName sent while 'foundId is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7f41f2bdde62
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - ChangeNameCheckResult
      evidence: ev_a6736c1a918f
  - scenario: avatar.check_user_name.guard_4
    title: "check user name: the guard 'string.IsNullOrWhiteSpace(name) || count <= 0' holds"
    given:
      guard: string.IsNullOrWhiteSpace(name) || count <= 0
      guard_holds: "true"
    when:
      packet: CheckUserName
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of CheckUserName sent while 'string.IsNullOrWhiteSpace(name) || count <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ede91c591c8c
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - ChangeNameCheckResult
      evidence: ev_a6736c1a918f
  - scenario: avatar.check_user_name.success
    title: "check user name: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: CheckUserName
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of CheckUserName succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - CheckUserNameResult
      evidence: ev_233f63fc1946
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - ChangeNameCheckResult
      evidence: ev_a6736c1a918f
  evidence:
  - id: ev_a6736c1a918f
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/users/CheckUserNameMessageEvent.java
    symbol: CheckUserNameMessageEvent
    line: 15
    note: third-party server implementation; evidence, not authority
  - id: ev_233f63fc1946
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Avatar/CheckUserNameMessageHandler.cs
    symbol: CheckUserNameMessageHandler
    line: 23
  - id: ev_7f41f2bdde62
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerDirectoryGrain.cs
    symbol: PlayerDirectoryGrain
    line: 146
  - id: ev_82bff245d7eb
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Avatar/CheckUserNameMessageHandler.cs
    symbol: CheckUserNameMessageHandler
    line: 37
  - id: ev_ede91c591c8c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Primitives/Players/NameChangePolicy.cs
    symbol: NameChangePolicy
    line: 67
verified: {}
manual: {}
