# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:fbcb5803bcfc612c5f82234bebdfe534170ba9b27e96d25846b32678853057a9"
generated:
  feature: tracking.latency_ping_request
  scenarios:
  - scenario: tracking.latency_ping_request.success
    title: "latency ping request: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: LatencyPingRequest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of LatencyPingRequest succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_dc405f0ecf0a
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - LatencyPingResponse
      evidence: ev_0b6f013626b5
  evidence:
  - id: ev_0b6f013626b5
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/handshake/LatencyPingRequestMessageEvent.java
    symbol: LatencyPingRequestMessageEvent
    line: 8
    note: third-party server implementation; evidence, not authority
  - id: ev_dc405f0ecf0a
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Tracking/LatencyPingRequestMessageHandler.cs
    symbol: LatencyPingRequestMessageHandler
    line: 9
verified: {}
manual: {}
