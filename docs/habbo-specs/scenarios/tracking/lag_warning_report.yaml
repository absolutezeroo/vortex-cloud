# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:3433c710ebaf7dbacfb2444c8957fdd00f785822790b2646a0c79fe48e3ab580"
generated:
  feature: tracking.lag_warning_report
  scenarios:
  - scenario: tracking.lag_warning_report.success
    title: "lag warning report: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: LagWarningReport
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of LagWarningReport succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_50ed9b71a6b0
  evidence:
  - id: ev_50ed9b71a6b0
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Tracking/LagWarningReportMessageHandler.cs
    symbol: LagWarningReportMessageHandler
    line: 8
verified: {}
manual: {}
