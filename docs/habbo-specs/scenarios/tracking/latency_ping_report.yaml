# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:df99af28adb351e268f5b6bdf6c773451a82e0ccd7ed024561b78b5a3deda0d9"
generated:
  feature: tracking.latency_ping_report
  scenarios:
  - scenario: tracking.latency_ping_report.success
    title: "latency ping report: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: LatencyPingReport
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of LatencyPingReport succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a019e2c83f57
  evidence:
  - id: ev_a019e2c83f57
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Tracking/LatencyPingReportMessageHandler.cs
    symbol: LatencyPingReportMessageHandler
    line: 8
verified: {}
manual: {}
