# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:5a4a343ff0ab6dc656c36a24931763b7bf3b2c19cd7ad8bc24dec74ec3bbda29"
generated:
  feature: tracking.performance_log
  scenarios:
  - scenario: tracking.performance_log.success
    title: "performance log: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: PerformanceLog
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of PerformanceLog succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_eead9f902c38
  evidence:
  - id: ev_eead9f902c38
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Tracking/PerformanceLogMessageHandler.cs
    symbol: PerformanceLogMessageHandler
    line: 10
verified: {}
manual: {}
