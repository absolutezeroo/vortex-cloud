# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:56f0291d4b78e66a42e53b7e542e701b83b3955dd2d34170cca3c36e6c29aded"
generated:
  feature: gifts.try_phone_number
  scenarios:
  - scenario: gifts.try_phone_number.success
    title: "try phone number: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: TryPhoneNumber
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of TryPhoneNumber succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_eb4e9f4ac629
  evidence:
  - id: ev_eb4e9f4ac629
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Gifts/TryPhoneNumberMessageHandler.cs
    symbol: TryPhoneNumberMessageHandler
    line: 8
verified: {}
manual: {}
