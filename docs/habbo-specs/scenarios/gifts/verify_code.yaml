# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:1b415555922aa7f9bdafd9c82434e1a3527573f1aebe7f24fdb943d7f6070a06"
generated:
  feature: gifts.verify_code
  scenarios:
  - scenario: gifts.verify_code.success
    title: "verify code: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: VerifyCode
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of VerifyCode succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_d25abcd16704
  evidence:
  - id: ev_d25abcd16704
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Gifts/VerifyCodeMessageHandler.cs
    symbol: VerifyCodeMessageHandler
    line: 8
verified: {}
manual: {}
