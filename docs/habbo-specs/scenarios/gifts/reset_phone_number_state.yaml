# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:cfa15f6b9e0f9ebf5dd46baf1942487f19a9c66b473504f407996fe0c3c24522"
generated:
  feature: gifts.reset_phone_number_state
  scenarios:
  - scenario: gifts.reset_phone_number_state.success
    title: "reset phone number state: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ResetPhoneNumberState
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ResetPhoneNumberState succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_598df4ca8ab7
  evidence:
  - id: ev_598df4ca8ab7
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Gifts/ResetPhoneNumberStateMessageHandler.cs
    symbol: ResetPhoneNumberStateMessageHandler
    line: 8
verified: {}
manual: {}
