# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:8abee78d1c1a60bcfbcdc3ead656cc9850872246ae38d527a254894d4aa6228d"
generated:
  feature: gifts.set_phone_number_verification_status
  scenarios:
  - scenario: gifts.set_phone_number_verification_status.success
    title: "set phone number verification status: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: SetPhoneNumberVerificationStatus
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of SetPhoneNumberVerificationStatus succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ba13459de577
  evidence:
  - id: ev_ba13459de577
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Gifts/SetPhoneNumberVerificationStatusMessageHandler.cs
    symbol: SetPhoneNumberVerificationStatusMessageHandler
    line: 8
verified: {}
manual: {}
