# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:c96834cb1cfe3765ba872229a08f6c087d3b0cebafdc6652f53f76b3582c996f"
generated:
  feature: marketplace.get_settings
  scenarios:
  - scenario: marketplace.get_settings.success
    title: "get settings: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetMarketplaceConfiguration
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetMarketplaceConfiguration succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - MarketplaceConfiguration
      evidence: ev_3269baae7de7
  evidence:
  - id: ev_3269baae7de7
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Marketplace/GetMarketplaceConfigurationMessageHandler.cs
    symbol: GetMarketplaceConfigurationMessageHandler
    line: 26
verified: {}
manual: {}
