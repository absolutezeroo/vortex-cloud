# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:6bcce13fd86122fe45634894a6af878219046a815cfd10b0fb6dfbf670a697f6"
generated:
  feature: marketplace.get_marketplace_can_make_offer
  scenarios:
  - scenario: marketplace.get_marketplace_can_make_offer.success
    title: "get marketplace can make offer: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetMarketplaceCanMakeOffer
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetMarketplaceCanMakeOffer succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - MarketplaceCanMakeOfferResult
      evidence: ev_1e27fed8f4ec
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - MarketplaceCanMakeOfferResult
      - MarketplaceCanMakeOfferResult
      evidence: ev_ca04c3a609d1
  evidence:
  - id: ev_ca04c3a609d1
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/catalog/marketplace/GetMarketplaceCanMakeOfferEvent.java
    symbol: GetMarketplaceCanMakeOfferEvent
    line: 9
    note: third-party server implementation; evidence, not authority
  - id: ev_1e27fed8f4ec
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Marketplace/GetMarketplaceCanMakeOfferMessageHandler.cs
    symbol: GetMarketplaceCanMakeOfferMessageHandler
    line: 21
verified: {}
manual: {}
