# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:138e8aa4ca78e7b9401069ff0fa074166ecd852e1ac7daa1c4ebf499701714f1"
generated:
  feature: marketplace.buy_marketplace_tokens
  scenarios:
  - scenario: marketplace.buy_marketplace_tokens.success
    title: "buy marketplace tokens: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: BuyMarketplaceTokens
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of BuyMarketplaceTokens succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_556c10a999a7
  evidence:
  - id: ev_556c10a999a7
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Marketplace/BuyMarketplaceTokensMessageHandler.cs
    symbol: BuyMarketplaceTokensMessageHandler
    line: 8
verified: {}
manual: {}
