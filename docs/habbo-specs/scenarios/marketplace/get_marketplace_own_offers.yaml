# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:128029fc75b5d415387722f7f4badf982ef18c1b54a1d38c688cd961164fa78f"
generated:
  feature: marketplace.get_marketplace_own_offers
  scenarios:
  - scenario: marketplace.get_marketplace_own_offers.guard_0
    title: "get marketplace own offers: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetMarketplaceOwnOffers
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetMarketplaceOwnOffers sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_d271c933118b
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - MarketPlaceOwnOffers
      evidence: ev_acbc3c0bca7a
  - scenario: marketplace.get_marketplace_own_offers.success
    title: "get marketplace own offers: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetMarketplaceOwnOffers
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetMarketplaceOwnOffers succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - MarketPlaceOwnOffersEvent
      evidence: ev_0e6cd9dbf830
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - MarketPlaceOwnOffers
      evidence: ev_acbc3c0bca7a
  evidence:
  - id: ev_acbc3c0bca7a
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/catalog/marketplace/GetMarketplaceOwnOffersMessageEvent.java
    symbol: GetMarketplaceOwnOffersMessageEvent
    line: 8
    note: third-party server implementation; evidence, not authority
  - id: ev_0e6cd9dbf830
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Marketplace/GetMarketplaceOwnOffersMessageHandler.cs
    symbol: GetMarketplaceOwnOffersMessageHandler
    line: 13
  - id: ev_d271c933118b
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Marketplace/GetMarketplaceOwnOffersMessageHandler.cs
    symbol: GetMarketplaceOwnOffersMessageHandler
    line: 24
verified: {}
manual: {}
