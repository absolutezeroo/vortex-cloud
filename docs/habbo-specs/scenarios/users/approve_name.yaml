# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:9f8871dbf328e4ce4bbe32e03162f871c22328fa7cee95b2e33481f94879e28d"
generated:
  feature: users.approve_name
  scenarios:
  - scenario: users.approve_name.success
    title: "approve name: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ApproveName
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ApproveName succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - ApproveName
      evidence: ev_8296a694354f
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - ApproveName
      - ApproveName
      - ApproveName
      - ApproveName
      evidence: ev_a5d940f956ad
  evidence:
  - id: ev_a5d940f956ad
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/catalog/ApproveNameMessageEvent.java
    symbol: ApproveNameMessageEvent
    line: 13
    note: third-party server implementation; evidence, not authority
  - id: ev_8296a694354f
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Users/ApproveNameMessageHandler.cs
    symbol: ApproveNameMessageHandler
    line: 24
verified: {}
manual: {}
