# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:d93c8d3904af29edc6ad014fbc177345a2631401fd59f9c2248e6fbb16da31c7"
generated:
  feature: users.change_email
  scenarios:
  - scenario: users.change_email.success
    title: "change email: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ChangeEmail
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ChangeEmail succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_142679a18321
  evidence:
  - id: ev_142679a18321
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Users/ChangeEmailMessageHandler.cs
    symbol: ChangeEmailMessageHandler
    line: 8
verified: {}
manual: {}
