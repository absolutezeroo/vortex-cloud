# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:56a88cbc2ebdb07e9efb36c435786e58477e3b62232f235e3fc2d7bae8686275"
generated:
  feature: users.update_guild_settings
  scenarios:
  - scenario: users.update_guild_settings.guard_0
    title: "update guild settings: the guard 'ctx.PlayerId <= 0 || message.GroupId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.GroupId <= 0
      guard_holds: "true"
    when:
      packet: UpdateGuildSettings
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateGuildSettings sent while 'ctx.PlayerId <= 0 || message.GroupId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_d81f2a614408
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_b02339341b3a
  - scenario: users.update_guild_settings.success
    title: "update guild settings: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: UpdateGuildSettings
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of UpdateGuildSettings succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - GroupDetailsChanged
      - GuildEditFailed
      evidence: ev_257d20d10bb7
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_b02339341b3a
  evidence:
  - id: ev_b02339341b3a
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/guilds/UpdateGuildSettingsMessageEvent.java
    symbol: UpdateGuildSettingsMessageEvent
    line: 13
    note: third-party server implementation; evidence, not authority
  - id: ev_257d20d10bb7
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/GroupGrain.Settings.cs
    symbol: GroupGrain
    line: 95
  - id: ev_d81f2a614408
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Users/UpdateGuildSettingsMessageHandler.cs
    symbol: UpdateGuildSettingsMessageHandler
    line: 22
verified: {}
manual: {}
