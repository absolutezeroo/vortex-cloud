# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:1bf64888b258924be4a5ec1950327f413a53f1f65875d9f2f7164b2fb9337d13"
generated:
  feature: users.approve_all_membership_requests
  scenarios:
  - scenario: users.approve_all_membership_requests.guard_0
    title: "approve all membership requests: the guard 'ctx.PlayerId <= 0 || message.GroupId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.GroupId <= 0
      guard_holds: "true"
    when:
      packet: ApproveAllMembershipRequests
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ApproveAllMembershipRequests sent while 'ctx.PlayerId <= 0 || message.GroupId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_4bca94047361
  - scenario: users.approve_all_membership_requests.guard_1
    title: "approve all membership requests: the guard 'group is null' holds"
    given:
      guard: group is null
      guard_holds: "true"
    when:
      packet: ApproveAllMembershipRequests
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ApproveAllMembershipRequests sent while 'group is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_b1c46fe16e7f
  - scenario: users.approve_all_membership_requests.guard_2
    title: "approve all membership requests: the guard 'requests.Count == 0' holds"
    given:
      guard: requests.Count == 0
      guard_holds: "true"
    when:
      packet: ApproveAllMembershipRequests
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ApproveAllMembershipRequests sent while 'requests.Count == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_b1c46fe16e7f
  - scenario: users.approve_all_membership_requests.guard_3
    title: "approve all membership requests: the guard '_registry is null' holds"
    given:
      guard: _registry is null
      guard_holds: "true"
    when:
      packet: ApproveAllMembershipRequests
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ApproveAllMembershipRequests sent while '_registry is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_34bf3ac088e2
  - scenario: users.approve_all_membership_requests.success
    title: "approve all membership requests: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ApproveAllMembershipRequests
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ApproveAllMembershipRequests succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - GuildMembershipUpdated
      evidence: ev_34bf3ac088e2
  evidence:
  - id: ev_34bf3ac088e2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Events/EventSystem.cs
    symbol: EventSystem
    line: 16
  - id: ev_4bca94047361
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Users/ApproveAllMembershipRequestsMessageHandler.cs
    symbol: ApproveAllMembershipRequestsMessageHandler
    line: 24
  - id: ev_b1c46fe16e7f
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Social/Grains/GroupGrain.Membership.cs
    symbol: GroupGrain
    line: 223
verified: {}
manual: {}
