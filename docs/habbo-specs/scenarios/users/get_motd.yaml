# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:b026db4e284116ec68af4e4606d212fa96c764f84ee8c6586add557475d099d5"
generated:
  feature: users.get_motd
  scenarios:
  - scenario: users.get_motd.guard_0
    title: "get motd: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetMOTD
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetMOTD sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_c140f572d164
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_9cb025d331d5
  - scenario: users.get_motd.guard_1
    title: "get motd: the guard '!_cache.TryGetValue(MotdKey, out string? json) || string....' holds"
    given:
      guard: "!_cache.TryGetValue(MotdKey, out string? json) || string.IsNullOrWhiteSpace(json)"
      guard_holds: "true"
    when:
      packet: GetMOTD
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetMOTD sent while '!_cache.TryGetValue(MotdKey, out string? json) || string.IsNullOrWhiteSpace(json)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_86f81f4f919c
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_9cb025d331d5
  - scenario: users.get_motd.success
    title: "get motd: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetMOTD
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetMOTD succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - MOTDNotificationEvent
      evidence: ev_86f81f4f919c
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_9cb025d331d5
  evidence:
  - id: ev_9cb025d331d5
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/friends/GetMOTDMessageEvent.java
    symbol: GetMOTDMessageEvent
    line: 7
    note: third-party server implementation; evidence, not authority
  - id: ev_86f81f4f919c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/ServerConfigGrain.cs
    symbol: ServerConfigGrain
    line: 131
  - id: ev_c140f572d164
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Users/GetMOTDMessageHandler.cs
    symbol: GetMOTDMessageHandler
    line: 23
verified: {}
manual: {}
