# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:2292d34da1c4c34597537cf9c0a2e2a102ec766ca99b2e700a14d4f54b28516a"
generated:
  feature: users.get_email_status
  scenarios:
  - scenario: users.get_email_status.success
    title: "get email status: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetEmailStatus
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetEmailStatus succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_5be6897911f5
  evidence:
  - id: ev_5be6897911f5
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Users/GetEmailStatusMessageHandler.cs
    symbol: GetEmailStatusMessageHandler
    line: 8
verified: {}
manual: {}
