# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:38ecc827d89f3a1af0f456f248d5491382f117b366b1e07492b943483f97af76"
generated:
  feature: users.get_guild_editor_data
  scenarios:
  - scenario: users.get_guild_editor_data.guard_0
    title: "get guild editor data: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetGuildEditorData
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetGuildEditorData sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_08bd509e14e9
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - GuildEditorData
      evidence: ev_a4ea70433f10
  - scenario: users.get_guild_editor_data.success
    title: "get guild editor data: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetGuildEditorData
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetGuildEditorData succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - GuildEditorData
      evidence: ev_08bd509e14e9
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - GuildEditorData
      evidence: ev_a4ea70433f10
  evidence:
  - id: ev_a4ea70433f10
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/guilds/GetGuildEditorDataMessageEvent.java
    symbol: GetGuildEditorDataMessageEvent
    line: 8
    note: third-party server implementation; evidence, not authority
  - id: ev_08bd509e14e9
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Users/GetGuildEditorDataMessageHandler.cs
    symbol: GetGuildEditorDataMessageHandler
    line: 23
verified: {}
manual: {}
