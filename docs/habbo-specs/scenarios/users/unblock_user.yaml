# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:8f39c5dda94176895630447277aff454d8075e83dee7ad539dea742f45cd5d74"
generated:
  feature: users.unblock_user
  scenarios:
  - scenario: users.unblock_user.guard_0
    title: "unblock user: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: UnblockUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UnblockUser sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_570be9e02797
  - scenario: users.unblock_user.guard_1
    title: "unblock user: the guard '!_blockedIds.Remove(targetId.Value)' holds"
    given:
      guard: "!_blockedIds.Remove(targetId.Value)"
      guard_holds: "true"
    when:
      packet: UnblockUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UnblockUser sent while '!_blockedIds.Remove(targetId.Value)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e805723a88fe
  - scenario: users.unblock_user.guard_2
    title: "unblock user: the guard '_registry is null' holds"
    given:
      guard: _registry is null
      guard_holds: "true"
    when:
      packet: UnblockUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UnblockUser sent while '_registry is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_34bf3ac088e2
  - scenario: users.unblock_user.success
    title: "unblock user: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: UnblockUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of UnblockUser succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - BlockUserUpdate
      evidence: ev_34bf3ac088e2
  evidence:
  - id: ev_34bf3ac088e2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Events/EventSystem.cs
    symbol: EventSystem
    line: 16
  - id: ev_570be9e02797
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Users/UnblockUserMessageHandler.cs
    symbol: UnblockUserMessageHandler
    line: 24
  - id: ev_e805723a88fe
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Social/Grains/MessengerGrain.Friends.cs
    symbol: MessengerGrain
    line: 527
verified: {}
manual: {}
