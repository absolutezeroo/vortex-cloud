# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:317f7c9afdf1edb1917e16daf5373595cac8e5d23d614fd3096f357d1bac549f"
generated:
  feature: users.give_star_gem_to_user
  scenarios:
  - scenario: users.give_star_gem_to_user.success
    title: "give star gem to user: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GiveStarGemToUser
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GiveStarGemToUser succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_c80e09a72447
  evidence:
  - id: ev_c80e09a72447
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Users/GiveStarGemToUserMessageHandler.cs
    symbol: GiveStarGemToUserMessageHandler
    line: 8
verified: {}
manual: {}
