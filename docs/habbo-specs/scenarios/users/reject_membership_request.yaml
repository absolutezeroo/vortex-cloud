# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:533d4a7069514d8ecb4d5366f78da31a25639138de43c4eeb11a341854185dd7"
generated:
  feature: users.reject_membership_request
  scenarios:
  - scenario: users.reject_membership_request.guard_0
    title: "reject membership request: the guard 'ctx.PlayerId <= 0 || message.GroupId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.GroupId <= 0
      guard_holds: "true"
    when:
      packet: RejectMembershipRequest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RejectMembershipRequest sent while 'ctx.PlayerId <= 0 || message.GroupId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_9471d6183101
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - GuildMembers
      - GuildMembershipRejected
      evidence: ev_8d021e9852d7
  - scenario: users.reject_membership_request.guard_1
    title: "reject membership request: the guard 'await LoadIfAdminAsync(dbCtx, actor, ct) is null' holds"
    given:
      guard: await LoadIfAdminAsync(dbCtx, actor, ct) is null
      guard_holds: "true"
    when:
      packet: RejectMembershipRequest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RejectMembershipRequest sent while 'await LoadIfAdminAsync(dbCtx, actor, ct) is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_c4e53aab9ae9
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - GuildMembers
      - GuildMembershipRejected
      evidence: ev_8d021e9852d7
  - scenario: users.reject_membership_request.guard_2
    title: "reject membership request: the guard 'deleted == 0' holds"
    given:
      guard: deleted == 0
      guard_holds: "true"
    when:
      packet: RejectMembershipRequest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RejectMembershipRequest sent while 'deleted == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_c4e53aab9ae9
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - GuildMembers
      - GuildMembershipRejected
      evidence: ev_8d021e9852d7
  - scenario: users.reject_membership_request.guard_3
    title: "reject membership request: the guard '_registry is null' holds"
    given:
      guard: _registry is null
      guard_holds: "true"
    when:
      packet: RejectMembershipRequest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RejectMembershipRequest sent while '_registry is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_34bf3ac088e2
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - GuildMembers
      - GuildMembershipRejected
      evidence: ev_8d021e9852d7
  - scenario: users.reject_membership_request.success
    title: "reject membership request: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RejectMembershipRequest
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RejectMembershipRequest succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - GuildMemberMgmtFailed
      - GuildMembershipRejected
      evidence: ev_34bf3ac088e2
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - GuildMembers
      - GuildMembershipRejected
      evidence: ev_8d021e9852d7
  evidence:
  - id: ev_8d021e9852d7
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/guilds/RejectMembershipRequestMessageEvent.java
    symbol: RejectMembershipRequestMessageEvent
    line: 18
    note: third-party server implementation; evidence, not authority
  - id: ev_34bf3ac088e2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Events/EventSystem.cs
    symbol: EventSystem
    line: 16
  - id: ev_9471d6183101
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Users/RejectMembershipRequestMessageHandler.cs
    symbol: RejectMembershipRequestMessageHandler
    line: 22
  - id: ev_c4e53aab9ae9
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/GroupGrain.Membership.cs
    symbol: GroupGrain
    line: 186
verified: {}
manual: {}
