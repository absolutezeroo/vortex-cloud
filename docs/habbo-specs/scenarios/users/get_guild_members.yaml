# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:3cbc7b9d7165968b1bc93324ed249efb157cd4e7f534a3a6bbf256ec3589b7f5"
generated:
  feature: users.get_guild_members
  scenarios:
  - scenario: users.get_guild_members.guard_0
    title: "get guild members: the guard 'ctx.PlayerId <= 0 || message.GroupId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.GroupId <= 0
      guard_holds: "true"
    when:
      packet: GetGuildMembers
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetGuildMembers sent while 'ctx.PlayerId <= 0 || message.GroupId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_548ff518d789
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - GuildMembers
      evidence: ev_3b1eff7761be
  - scenario: users.get_guild_members.guard_1
    title: "get guild members: the guard 'page is null' holds"
    given:
      guard: page is null
      guard_holds: "true"
    when:
      packet: GetGuildMembers
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetGuildMembers sent while 'page is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_548ff518d789
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - GuildMembers
      evidence: ev_3b1eff7761be
  - scenario: users.get_guild_members.guard_2
    title: "get guild members: the guard 'group is null' holds"
    given:
      guard: group is null
      guard_holds: "true"
    when:
      packet: GetGuildMembers
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetGuildMembers sent while 'group is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_25e7df53e794
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - GuildMembers
      evidence: ev_3b1eff7761be
  - scenario: users.get_guild_members.guard_3
    title: "get guild members: the guard 'wantsPendingRequests && !allowedToManage' holds"
    given:
      guard: wantsPendingRequests && !allowedToManage
      guard_holds: "true"
    when:
      packet: GetGuildMembers
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetGuildMembers sent while 'wantsPendingRequests && !allowedToManage' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_25e7df53e794
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - GuildMembers
      evidence: ev_3b1eff7761be
  - scenario: users.get_guild_members.success
    title: "get guild members: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetGuildMembers
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetGuildMembers succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - GuildMembers
      evidence: ev_09932710687b
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - GuildMembers
      evidence: ev_3b1eff7761be
  evidence:
  - id: ev_3b1eff7761be
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/guilds/GetGuildMembersMessageEvent.java
    symbol: GetGuildMembersMessageEvent
    line: 18
    note: third-party server implementation; evidence, not authority
  - id: ev_09932710687b
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Primitives/Orleans/GrainFactoryExtensions.cs
    symbol: GrainFactoryExtensions
    line: 223
  - id: ev_25e7df53e794
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Social/Grains/GroupGrain.Read.cs
    symbol: GroupGrain
    line: 147
  - id: ev_548ff518d789
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Users/GetGuildMembersMessageHandler.cs
    symbol: GetGuildMembersMessageHandler
    line: 23
verified: {}
manual: {}
