# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:60fa65b26d45d8ee3d09337e92650619abe7d8f6b41518d240c93a7133f600d9"
generated:
  feature: users.get_ignored_users
  scenarios:
  - scenario: users.get_ignored_users.guard_0
    title: "get ignored users: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetIgnoredUsers
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetIgnoredUsers sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_13ade511745d
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - IgnoredUsers
      evidence: ev_bb79c2bde95f
  - scenario: users.get_ignored_users.success
    title: "get ignored users: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetIgnoredUsers
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetIgnoredUsers succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - IgnoredUsers
      evidence: ev_13ade511745d
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - IgnoredUsers
      evidence: ev_bb79c2bde95f
  evidence:
  - id: ev_bb79c2bde95f
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/handshake/GetIgnoredUsersMessageEvent.java
    symbol: GetIgnoredUsersMessageEvent
    line: 8
    note: third-party server implementation; evidence, not authority
  - id: ev_13ade511745d
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Users/GetIgnoredUsersMessageHandler.cs
    symbol: GetIgnoredUsersMessageHandler
    line: 24
verified: {}
manual: {}
