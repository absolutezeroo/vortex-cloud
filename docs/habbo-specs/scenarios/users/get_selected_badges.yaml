# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:c5f6d262c282f6ee57cba0abefcefa461762bbb8ae3b2edc75cf03cc2d06ca23"
generated:
  feature: users.get_selected_badges
  scenarios:
  - scenario: users.get_selected_badges.guard_0
    title: "get selected badges: the guard 'ctx.PlayerId <= 0 || message.UserId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.UserId <= 0
      guard_holds: "true"
    when:
      packet: GetSelectedBadges
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetSelectedBadges sent while 'ctx.PlayerId <= 0 || message.UserId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0213676ee15c
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - UserBadges
      - UserBadges
      evidence: ev_40595359bd6b
  - scenario: users.get_selected_badges.success
    title: "get selected badges: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetSelectedBadges
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetSelectedBadges succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - HabboUserBadges
      evidence: ev_0213676ee15c
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - UserBadges
      - UserBadges
      evidence: ev_40595359bd6b
  evidence:
  - id: ev_40595359bd6b
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/users/GetSelectedBadgesMessageEvent.java
    symbol: GetSelectedBadgesMessageEvent
    line: 11
    note: third-party server implementation; evidence, not authority
  - id: ev_0213676ee15c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Users/GetSelectedBadgesMessageHandler.cs
    symbol: GetSelectedBadgesMessageHandler
    line: 25
verified: {}
manual: {}
