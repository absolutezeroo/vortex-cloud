# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:68c24d534fbdf6548ed40d6305236909da294cca986267cac50f6372d4eae62a"
generated:
  feature: users.kick_member
  scenarios:
  - scenario: users.kick_member.guard_0
    title: "kick member: the guard 'ctx.PlayerId <= 0 || message.GroupId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.GroupId <= 0
      guard_holds: "true"
    when:
      packet: KickMember
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of KickMember sent while 'ctx.PlayerId <= 0 || message.GroupId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1b6983ac9d0d
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - GuildMembershipRejected
      evidence: ev_5a67319ee97f
  - scenario: users.kick_member.success
    title: "kick member: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: KickMember
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of KickMember succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - GuildMemberMgmtFailed
      evidence: ev_1b6983ac9d0d
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - GuildMembershipRejected
      evidence: ev_5a67319ee97f
  evidence:
  - id: ev_5a67319ee97f
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/guilds/KickMemberMessageEvent.java
    symbol: KickMemberMessageEvent
    line: 18
    note: third-party server implementation; evidence, not authority
  - id: ev_1b6983ac9d0d
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Users/KickMemberMessageHandler.cs
    symbol: KickMemberMessageHandler
    line: 22
verified: {}
manual: {}
