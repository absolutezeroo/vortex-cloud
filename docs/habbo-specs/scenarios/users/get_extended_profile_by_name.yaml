# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:e790fb67b764c3b72053faf2ba87fdf17ac7007882b7d859f8ddb9e176f3ade5"
generated:
  feature: users.get_extended_profile_by_name
  scenarios:
  - scenario: users.get_extended_profile_by_name.guard_0
    title: "get extended profile by name: the guard 'playerId is null' holds"
    given:
      guard: playerId is null
      guard_holds: "true"
    when:
      packet: GetExtendedProfileByName
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetExtendedProfileByName sent while 'playerId is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3bd2b1e19fbd
  - scenario: users.get_extended_profile_by_name.guard_1
    title: "get extended profile by name: the guard 'string.IsNullOrWhiteSpace(name)' holds"
    given:
      guard: string.IsNullOrWhiteSpace(name)
      guard_holds: "true"
    when:
      packet: GetExtendedProfileByName
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetExtendedProfileByName sent while 'string.IsNullOrWhiteSpace(name)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7f41f2bdde62
  - scenario: users.get_extended_profile_by_name.guard_2
    title: "get extended profile by name: the guard '_nameToId.TryGetValue(name, out PlayerId playerId)' holds"
    given:
      guard: _nameToId.TryGetValue(name, out PlayerId playerId)
      guard_holds: "true"
    when:
      packet: GetExtendedProfileByName
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetExtendedProfileByName sent while '_nameToId.TryGetValue(name, out PlayerId playerId)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7f41f2bdde62
  - scenario: users.get_extended_profile_by_name.guard_3
    title: "get extended profile by name: the guard 'foundId is null' holds"
    given:
      guard: foundId is null
      guard_holds: "true"
    when:
      packet: GetExtendedProfileByName
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetExtendedProfileByName sent while 'foundId is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7f41f2bdde62
  - scenario: users.get_extended_profile_by_name.guard_4
    title: "get extended profile by name: the guard 'groupIds.Count == 0' holds"
    given:
      guard: groupIds.Count == 0
      guard_holds: "true"
    when:
      packet: GetExtendedProfileByName
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetExtendedProfileByName sent while 'groupIds.Count == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_678209870593
  - scenario: users.get_extended_profile_by_name.guard_5
    title: "get extended profile by name: the guard 'match is not null' holds"
    given:
      guard: match is not null
      guard_holds: "true"
    when:
      packet: GetExtendedProfileByName
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetExtendedProfileByName sent while 'match is not null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_5e99184ee3a3
  - scenario: users.get_extended_profile_by_name.success
    title: "get extended profile by name: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetExtendedProfileByName
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetExtendedProfileByName succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - ExtendedProfile
      evidence: ev_1f83e61934ca
  evidence:
  - id: ev_1f83e61934ca
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerGrain.cs
    symbol: PlayerGrain
    line: 286
  - id: ev_3bd2b1e19fbd
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Users/GetExtendedProfileByNameMessageHandler.cs
    symbol: GetExtendedProfileByNameMessageHandler
    line: 37
  - id: ev_5e99184ee3a3
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Providers/GroupBadgePartProvider.cs
    symbol: GroupBadgePartProvider
    line: 43
  - id: ev_678209870593
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/GroupDirectoryGrain.cs
    symbol: GroupDirectoryGrain
    line: 314
  - id: ev_7f41f2bdde62
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerDirectoryGrain.cs
    symbol: PlayerDirectoryGrain
    line: 146
verified: {}
manual: {}
