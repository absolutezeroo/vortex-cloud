# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:5661d63e3fd70f5a2acab3d888be4694bd3924a39b803f625b2e3b05f4e65fb4"
generated:
  feature: users.block_list_init
  scenarios:
  - scenario: users.block_list_init.guard_0
    title: "block list init: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: BlockListInit
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of BlockListInit sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_48ce07eab040
  - scenario: users.block_list_init.success
    title: "block list init: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: BlockListInit
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of BlockListInit succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - BlockList
      evidence: ev_48ce07eab040
  evidence:
  - id: ev_48ce07eab040
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Users/BlockListInitMessageHandler.cs
    symbol: BlockListInitMessageHandler
    line: 24
verified: {}
manual: {}
