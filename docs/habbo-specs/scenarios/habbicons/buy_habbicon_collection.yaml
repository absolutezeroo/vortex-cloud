# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:903d289284d08e1970b20631f8bfc2cb650ba2752d3b6086c4aade9b3e617901"
generated:
  feature: habbicons.buy_habbicon_collection
  scenarios:
  - scenario: habbicons.buy_habbicon_collection.guard_0
    title: "buy habbicon collection: the guard 'ctx.PlayerId <= 0 || message.CollectionId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.CollectionId <= 0
      guard_holds: "true"
    when:
      packet: BuyHabbiconCollection
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of BuyHabbiconCollection sent while 'ctx.PlayerId <= 0 || message.CollectionId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_6c02e7f22f74
  - scenario: habbicons.buy_habbicon_collection.success
    title: "buy habbicon collection: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: BuyHabbiconCollection
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of BuyHabbiconCollection succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_6c02e7f22f74
  evidence:
  - id: ev_6c02e7f22f74
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Habbicons/BuyHabbiconCollectionMessageHandler.cs
    symbol: BuyHabbiconCollectionMessageHandler
    line: 20
verified: {}
manual: {}
