# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:79918fda0552898ee494fce3cd41b0e55cc9394f94e10f1bfcddaa527dce5efe"
generated:
  feature: habbicons.send_habbicon
  scenarios:
  - scenario: habbicons.send_habbicon.guard_0
    title: "send habbicon: the guard 'ctx.PlayerId <= 0 || message.ChatId <= 0 || message.Habbi...' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.ChatId <= 0 || message.HabbiconId <= 0
      guard_holds: "true"
    when:
      packet: SendHabbicon
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SendHabbicon sent while 'ctx.PlayerId <= 0 || message.ChatId <= 0 || message.HabbiconId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_6413edc5e503
  - scenario: habbicons.send_habbicon.success
    title: "send habbicon: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: SendHabbicon
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of SendHabbicon succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_6413edc5e503
  evidence:
  - id: ev_6413edc5e503
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Habbicons/SendHabbiconMessageHandler.cs
    symbol: SendHabbiconMessageHandler
    line: 23
verified: {}
manual: {}
