# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:b39d61705cf96eba3759ecbd384e7bfae6c5b7e7b6210cf1d66e234eccd1b745"
generated:
  feature: habbicons.get_habbicon_info
  scenarios:
  - scenario: habbicons.get_habbicon_info.guard_0
    title: "get habbicon info: the guard 'ctx.PlayerId <= 0 || message.HabbiconId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.HabbiconId <= 0
      guard_holds: "true"
    when:
      packet: GetHabbiconInfo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetHabbiconInfo sent while 'ctx.PlayerId <= 0 || message.HabbiconId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_6bce9accbbae
  - scenario: habbicons.get_habbicon_info.success
    title: "get habbicon info: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetHabbiconInfo
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetHabbiconInfo succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_6bce9accbbae
  evidence:
  - id: ev_6bce9accbbae
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Habbicons/GetHabbiconInfoMessageHandler.cs
    symbol: GetHabbiconInfoMessageHandler
    line: 19
verified: {}
manual: {}
