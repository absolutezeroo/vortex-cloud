# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:f44f7c4db2752b48ba8d90c30350ecf7c4542891a44ef3748cfe1f8457656f32"
generated:
  feature: habbicons.buy_habbicon
  scenarios:
  - scenario: habbicons.buy_habbicon.guard_0
    title: "buy habbicon: the guard 'ctx.PlayerId <= 0 || message.HabbiconId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.HabbiconId <= 0
      guard_holds: "true"
    when:
      packet: BuyHabbicon
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of BuyHabbicon sent while 'ctx.PlayerId <= 0 || message.HabbiconId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a95ac2e8a9f5
  - scenario: habbicons.buy_habbicon.success
    title: "buy habbicon: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: BuyHabbicon
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of BuyHabbicon succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_756cbccedf6b
  evidence:
  - id: ev_756cbccedf6b
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Habbicons/BuyHabbiconMessageHandler.cs
    symbol: BuyHabbiconMessageHandler
    line: 14
  - id: ev_a95ac2e8a9f5
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Habbicons/BuyHabbiconMessageHandler.cs
    symbol: BuyHabbiconMessageHandler
    line: 23
verified: {}
manual: {}
