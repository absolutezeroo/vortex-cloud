# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:3592cc8380841ad032a46387732956095ffd2f2caae851f0a6e849fabd3fbc15"
generated:
  feature: habbicons.trigger_habbicon
  scenarios:
  - scenario: habbicons.trigger_habbicon.guard_0
    title: "trigger habbicon: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.HabbiconI...' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.HabbiconId <= 0
      guard_holds: "true"
    when:
      packet: TriggerHabbicon
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of TriggerHabbicon sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || message.HabbiconId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_73d39eb594b9
  - scenario: habbicons.trigger_habbicon.success
    title: "trigger habbicon: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: TriggerHabbicon
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of TriggerHabbicon succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_73d39eb594b9
  evidence:
  - id: ev_73d39eb594b9
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Habbicons/TriggerHabbiconMessageHandler.cs
    symbol: TriggerHabbiconMessageHandler
    line: 23
verified: {}
manual: {}
