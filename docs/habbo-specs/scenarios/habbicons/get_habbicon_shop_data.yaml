# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:cf87f3ae8281ff80a1e361b64c895c7b3a9056ff697196403cd005c4949bc132"
generated:
  feature: habbicons.get_habbicon_shop_data
  scenarios:
  - scenario: habbicons.get_habbicon_shop_data.guard_0
    title: "get habbicon shop data: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetHabbiconShopData
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetHabbiconShopData sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_aed5f2876566
  - scenario: habbicons.get_habbicon_shop_data.success
    title: "get habbicon shop data: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetHabbiconShopData
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetHabbiconShopData succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_aed5f2876566
  evidence:
  - id: ev_aed5f2876566
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Habbicons/GetHabbiconShopDataMessageHandler.cs
    symbol: GetHabbiconShopDataMessageHandler
    line: 20
verified: {}
manual: {}
