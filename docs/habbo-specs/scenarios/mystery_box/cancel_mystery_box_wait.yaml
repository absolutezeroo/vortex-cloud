# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:c6d8ce9f42093617d2610a4b4af593841b01d83122319e2eb270c3c5aa4c47ba"
generated:
  feature: mystery_box.cancel_mystery_box_wait
  scenarios:
  - scenario: mystery_box.cancel_mystery_box_wait.guard_0
    title: "cancel mystery box wait: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || boxOwnerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || boxOwnerId <= 0
      guard_holds: "true"
    when:
      packet: MysteryBoxWaitingCanceled
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MysteryBoxWaitingCanceled sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || boxOwnerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_8f10ab449a9e
  - scenario: mystery_box.cancel_mystery_box_wait.guard_1
    title: "cancel mystery box wait: the guard '!_roomGrain._state.MysteryBoxSessionsByOwnerId.TryGetValu...' holds"
    given:
      guard: "!_roomGrain._state.MysteryBoxSessionsByOwnerId.TryGetValue( boxOwnerId, out RoomMysteryBoxSession? session )"
      guard_holds: "true"
    when:
      packet: MysteryBoxWaitingCanceled
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MysteryBoxWaitingCanceled sent while '!_roomGrain._state.MysteryBoxSessionsByOwnerId.TryGetValue( boxOwnerId, out RoomMysteryBoxSession? session )' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_b2d21836a70a
  - scenario: mystery_box.cancel_mystery_box_wait.guard_2
    title: "cancel mystery box wait: the guard '!session.IsParticipant(actorId)' holds"
    given:
      guard: "!session.IsParticipant(actorId)"
      guard_holds: "true"
    when:
      packet: MysteryBoxWaitingCanceled
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MysteryBoxWaitingCanceled sent while '!session.IsParticipant(actorId)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_b2d21836a70a
  - scenario: mystery_box.cancel_mystery_box_wait.guard_3
    title: "cancel mystery box wait: the guard 'actorId == session.BoxOwnerId' holds"
    given:
      guard: actorId == session.BoxOwnerId
      guard_holds: "true"
    when:
      packet: MysteryBoxWaitingCanceled
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MysteryBoxWaitingCanceled sent while 'actorId == session.BoxOwnerId' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_b2d21836a70a
  - scenario: mystery_box.cancel_mystery_box_wait.success
    title: "cancel mystery box wait: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: MysteryBoxWaitingCanceled
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of MysteryBoxWaitingCanceled succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - CancelMysteryBoxWait
      evidence: ev_688de033687f
  evidence:
  - id: ev_688de033687f
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/MysteryBox/MysteryBoxWaitingCanceledMessageHandler.cs
    symbol: MysteryBoxWaitingCanceledMessageHandler
    line: 9
  - id: ev_8f10ab449a9e
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/RoomService.MysteryBox.cs
    symbol: RoomService
    line: 21
  - id: ev_b2d21836a70a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomMysteryBoxSystem.cs
    symbol: RoomMysteryBoxSystem
    line: 142
verified: {}
manual: {}
