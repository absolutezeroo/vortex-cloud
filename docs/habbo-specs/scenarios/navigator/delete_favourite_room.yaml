# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:a0cd832e1d88ebe465d9c404ce8087b20e3b6a7c4b1233f49fccd2c08072e79e"
generated:
  feature: navigator.delete_favourite_room
  scenarios:
  - scenario: navigator.delete_favourite_room.guard_0
    title: "delete favourite room: the guard 'ctx.PlayerId <= 0 || message.RoomId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.RoomId <= 0
      guard_holds: "true"
    when:
      packet: DeleteFavouriteRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of DeleteFavouriteRoom sent while 'ctx.PlayerId <= 0 || message.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_17d7e7a65163
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - FavouriteChanged
      evidence: ev_9d1b57f076ea
  - scenario: navigator.delete_favourite_room.success
    title: "delete favourite room: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: DeleteFavouriteRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of DeleteFavouriteRoom succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_17d7e7a65163
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - FavouriteChanged
      evidence: ev_9d1b57f076ea
  evidence:
  - id: ev_9d1b57f076ea
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/DeleteFavouriteRoomMessageEvent.java
    symbol: DeleteFavouriteRoomMessageEvent
    line: 10
    note: third-party server implementation; evidence, not authority
  - id: ev_17d7e7a65163
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Navigator/DeleteFavouriteRoomMessageHandler.cs
    symbol: DeleteFavouriteRoomMessageHandler
    line: 21
verified: {}
manual: {}
