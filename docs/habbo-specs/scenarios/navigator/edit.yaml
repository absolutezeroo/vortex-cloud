# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:1a1fabd25028c7c2eb7b36eba5712eac7e4fa0153d4dcbf86ce5ca9a57545658"
generated:
  feature: navigator.edit
  scenarios:
  - scenario: navigator.edit.guard_0
    title: "edit: the guard 'ctx.PlayerId <= 0 || message.Id <= 0 || string.IsNullOrWh...' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.Id <= 0 || string.IsNullOrWhiteSpace(message.Name)
      guard_holds: "true"
    when:
      packet: EditEvent
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of EditEvent sent while 'ctx.PlayerId <= 0 || message.Id <= 0 || string.IsNullOrWhiteSpace(message.Name)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_065fa12e16db
  - scenario: navigator.edit.guard_1
    title: "edit: the guard 'updated is null' holds"
    given:
      guard: updated is null
      guard_holds: "true"
    when:
      packet: EditEvent
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of EditEvent sent while 'updated is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_065fa12e16db
  - scenario: navigator.edit.guard_2
    title: "edit: the guard 'entity is null || entity.RoomEntity?.PlayerEntityId != ac...' holds"
    given:
      guard: entity is null || entity.RoomEntity?.PlayerEntityId != actorPlayerId
      guard_holds: "true"
    when:
      packet: EditEvent
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of EditEvent sent while 'entity is null || entity.RoomEntity?.PlayerEntityId != actorPlayerId' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_89f5ad7deadb
  - scenario: navigator.edit.success
    title: "edit: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: EditEvent
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of EditEvent succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - RoomEvent
      evidence: ev_065fa12e16db
  evidence:
  - id: ev_065fa12e16db
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Navigator/EditEventMessageHandler.cs
    symbol: EditEventMessageHandler
    line: 22
  - id: ev_89f5ad7deadb
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/RoomAdvertisementService.cs
    symbol: RoomAdvertisementService
    line: 38
verified: {}
manual: {}
