# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:87d9635b9e26e1e8b86f013b2ba40199b38d58c28dc5ff38bfb4b05e9b1f306c"
generated:
  feature: navigator.add_favourite_room
  scenarios:
  - scenario: navigator.add_favourite_room.guard_0
    title: "add favourite room: the guard 'ctx.PlayerId <= 0 || message.RoomId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.RoomId <= 0
      guard_holds: "true"
    when:
      packet: AddFavouriteRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AddFavouriteRoom sent while 'ctx.PlayerId <= 0 || message.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_06536aa69567
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - FavouriteChanged
      evidence: ev_d94716018abf
  - scenario: navigator.add_favourite_room.guard_1
    title: "add favourite room: the guard 'alreadyFavourited' holds"
    given:
      guard: alreadyFavourited
      guard_holds: "true"
    when:
      packet: AddFavouriteRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AddFavouriteRoom sent while 'alreadyFavourited' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_d429657b2673
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - FavouriteChanged
      evidence: ev_d94716018abf
  - scenario: navigator.add_favourite_room.success
    title: "add favourite room: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: AddFavouriteRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of AddFavouriteRoom succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_06536aa69567
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - FavouriteChanged
      evidence: ev_d94716018abf
  evidence:
  - id: ev_d94716018abf
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/AddFavouriteRoomMessageEvent.java
    symbol: AddFavouriteRoomMessageEvent
    line: 10
    note: third-party server implementation; evidence, not authority
  - id: ev_06536aa69567
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Navigator/AddFavouriteRoomMessageHandler.cs
    symbol: AddFavouriteRoomMessageHandler
    line: 21
  - id: ev_d429657b2673
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerNavigatorGrain.cs
    symbol: PlayerNavigatorGrain
    line: 254
verified: {}
manual: {}
