# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:38b893b00f68473c86e20a38aa5c219df200ddc2d99745f7763ddfd3b476ea9d"
generated:
  feature: navigator.get_popular_tags
  scenarios:
  - scenario: navigator.get_popular_tags.success
    title: "get popular tags: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetPopularRoomTags
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetPopularRoomTags succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - PopularRoomTagsResult
      evidence: ev_53096f2bd97d
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - PopularRoomTagsResult
      evidence: ev_7da6beaedcb9
  evidence:
  - id: ev_7da6beaedcb9
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/navigator/GetPopularRoomTagsMessageEvent.java
    symbol: GetPopularRoomTagsMessageEvent
    line: 9
    note: third-party server implementation; evidence, not authority
  - id: ev_53096f2bd97d
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Navigator/GetPopularRoomTagsMessageHandler.cs
    symbol: GetPopularRoomTagsMessageHandler
    line: 11
verified: {}
manual: {}
