# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:a9b6d04ceea60f3412953b621f5d7df62fbc858ee4fc3290a9ecc1a1f908c3b6"
generated:
  feature: navigator.get_search_results
  scenarios:
  - scenario: navigator.get_search_results.guard_0
    title: "get search results: the guard 'rooms.Count == 0' holds"
    given:
      guard: rooms.Count == 0
      guard_holds: "true"
    when:
      packet: CompetitionRoomsSearch
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of CompetitionRoomsSearch sent while 'rooms.Count == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ca6db2d966f5
  - scenario: navigator.get_search_results.success
    title: "get search results: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: CompetitionRoomsSearch
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of CompetitionRoomsSearch succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - NavigatorSearchResultBlocks
      evidence: ev_00779a5388dd
  evidence:
  - id: ev_00779a5388dd
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Navigator/MyGuildBasesSearchMessageHandler.cs
    symbol: MyGuildBasesSearchMessageHandler
    line: 47
  - id: ev_ca6db2d966f5
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Navigator/NavigatorService.cs
    symbol: NavigatorService
    line: 120
verified: {}
manual: {}
