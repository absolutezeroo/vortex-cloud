# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:125eaf056018d35ae8e5569f2486d40e118bdff0298d1cfe9b2b9e902f7c3ab2"
generated:
  feature: navigator.get_event_categories
  scenarios:
  - scenario: navigator.get_event_categories.guard_0
    title: "get event categories: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetUserEventCats
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetUserEventCats sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ccbd19c08c4a
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - UserEventCats
      evidence: ev_940f8a9b5ec1
  - scenario: navigator.get_event_categories.success
    title: "get event categories: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetUserEventCats
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetUserEventCats succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - CanCreateRoomEvent
      - UserEventCats
      evidence: ev_b46938ec853d
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - UserEventCats
      evidence: ev_940f8a9b5ec1
  evidence:
  - id: ev_940f8a9b5ec1
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/navigator/GetUserEventCatsMessageEvent.java
    symbol: GetUserEventCatsMessageEvent
    line: 8
    note: third-party server implementation; evidence, not authority
  - id: ev_b46938ec853d
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.cs
    symbol: RoomGrain
    line: 247
  - id: ev_ccbd19c08c4a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Navigator/GetUserEventCatsMessageHandler.cs
    symbol: GetUserEventCatsMessageHandler
    line: 30
verified: {}
manual: {}
