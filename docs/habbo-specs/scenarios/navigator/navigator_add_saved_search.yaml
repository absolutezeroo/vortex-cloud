# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:01c07c1b823c70000b34c238a4adeb744f415d025b69483c902bd8fe34c2818c"
generated:
  feature: navigator.navigator_add_saved_search
  scenarios:
  - scenario: navigator.navigator_add_saved_search.guard_0
    title: "navigator add saved search: the guard 'string.IsNullOrWhiteSpace(message.SearchCode)' holds"
    given:
      guard: string.IsNullOrWhiteSpace(message.SearchCode)
      guard_holds: "true"
    when:
      packet: NavigatorAddSavedSearch
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of NavigatorAddSavedSearch sent while 'string.IsNullOrWhiteSpace(message.SearchCode)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_bc4740ce9db2
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - NavigatorSavedSearches
      evidence: ev_9d27f1b9d7eb
  - scenario: navigator.navigator_add_saved_search.success
    title: "navigator add saved search: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: NavigatorAddSavedSearch
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of NavigatorAddSavedSearch succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_bc4740ce9db2
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - NavigatorSavedSearches
      evidence: ev_9d27f1b9d7eb
  evidence:
  - id: ev_9d27f1b9d7eb
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/navigator/NavigatorAddSavedSearchEvent.java
    symbol: NavigatorAddSavedSearchEvent
    line: 9
    note: third-party server implementation; evidence, not authority
  - id: ev_bc4740ce9db2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/NewNavigator/NavigatorAddSavedSearchMessageHandler.cs
    symbol: NavigatorAddSavedSearchMessageHandler
    line: 21
verified: {}
manual: {}
