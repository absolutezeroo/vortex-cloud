# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:032fdddcb73a3155a7a2addaf914945724d4d9095a8134cc68446c598c95b58a"
generated:
  feature: navigator.cancel
  scenarios:
  - scenario: navigator.cancel.guard_0
    title: "cancel: the guard 'ctx.PlayerId <= 0 || message.AdvertisementId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.AdvertisementId <= 0
      guard_holds: "true"
    when:
      packet: CancelEvent
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of CancelEvent sent while 'ctx.PlayerId <= 0 || message.AdvertisementId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_cc2f079ff2d9
  - scenario: navigator.cancel.guard_1
    title: "cancel: the guard 'roomId is null' holds"
    given:
      guard: roomId is null
      guard_holds: "true"
    when:
      packet: CancelEvent
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of CancelEvent sent while 'roomId is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_cc2f079ff2d9
  - scenario: navigator.cancel.guard_2
    title: "cancel: the guard 'entity is null || entity.RoomEntity?.PlayerEntityId != ac...' holds"
    given:
      guard: entity is null || entity.RoomEntity?.PlayerEntityId != actorPlayerId
      guard_holds: "true"
    when:
      packet: CancelEvent
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of CancelEvent sent while 'entity is null || entity.RoomEntity?.PlayerEntityId != actorPlayerId' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_89f5ad7deadb
  - scenario: navigator.cancel.success
    title: "cancel: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: CancelEvent
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of CancelEvent succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - RoomEventCancel
      evidence: ev_89f5ad7deadb
  evidence:
  - id: ev_89f5ad7deadb
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/RoomAdvertisementService.cs
    symbol: RoomAdvertisementService
    line: 79
  - id: ev_cc2f079ff2d9
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Navigator/CancelEventMessageHandler.cs
    symbol: CancelEventMessageHandler
    line: 22
verified: {}
manual: {}
