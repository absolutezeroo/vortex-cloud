# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:f537fdf87eb6d994e8e4773bc1ef760c90d08a95ee1009dad311ac79827b9530"
generated:
  feature: navigator.get_flat_categories
  scenarios:
  - scenario: navigator.get_flat_categories.success
    title: "get flat categories: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetUserFlatCats
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetUserFlatCats succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - UserFlatCats
      evidence: ev_0fcea1d32705
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - UserFlatCats
      evidence: ev_eaaa99b72197
  evidence:
  - id: ev_eaaa99b72197
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/navigator/GetUserFlatCatsMessageEvent.java
    symbol: GetUserFlatCatsMessageEvent
    line: 12
    note: third-party server implementation; evidence, not authority
  - id: ev_0fcea1d32705
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Navigator/GetUserFlatCatsMessageHandler.cs
    symbol: GetUserFlatCatsMessageHandler
    line: 12
verified: {}
manual: {}
