# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:450ec8e34a4428bfba08245dcb1fbcd8d60941e182854ec05e690bc56c41c60b"
generated:
  feature: navigator.get_room_count_by_owner
  scenarios:
  - scenario: navigator.get_room_count_by_owner.success
    title: "get room count by owner: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: CanCreateRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of CanCreateRoom succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - CanCreateRoom
      evidence: ev_8527e203d9ca
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - CanCreateRoom
      evidence: ev_98808f6aa35f
  evidence:
  - id: ev_98808f6aa35f
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/navigator/CanCreateRoomMessageEvent.java
    symbol: CanCreateRoomMessageEvent
    line: 10
    note: third-party server implementation; evidence, not authority
  - id: ev_8527e203d9ca
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Navigator/CanCreateRoomMessageHandler.cs
    symbol: CanCreateRoomMessageHandler
    line: 14
verified: {}
manual: {}
