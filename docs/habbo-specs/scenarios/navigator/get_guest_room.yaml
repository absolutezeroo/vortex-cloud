# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:45d83c23e5065064051a591ed78164804a829b640ebe06745e592d43cfb22703"
generated:
  feature: navigator.get_guest_room
  scenarios:
  - scenario: navigator.get_guest_room.success
    title: "get guest room: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetGuestRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetGuestRoom succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - GetGuestRoomResult
      - unresolved
      evidence: ev_25e7df53e794
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - GetGuestRoomResult
      evidence: ev_3c5f7d7443f6
  evidence:
  - id: ev_3c5f7d7443f6
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/GetGuestRoomMessageEvent.java
    symbol: GetGuestRoomMessageEvent
    line: 10
    note: third-party server implementation; evidence, not authority
  - id: ev_25e7df53e794
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Social/Grains/GroupGrain.Read.cs
    symbol: GroupGrain
    line: 118
verified: {}
manual: {}
