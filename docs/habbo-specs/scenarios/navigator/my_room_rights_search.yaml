# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:edcf33f29413eb7afc8c547e7b9617ee980d35db1c6c9c1df67a357324760214"
generated:
  feature: navigator.my_room_rights_search
  scenarios:
  - scenario: navigator.my_room_rights_search.guard_0
    title: "my room rights search: the guard 'rooms.Count == 0' holds"
    given:
      guard: rooms.Count == 0
      guard_holds: "true"
    when:
      packet: MyRoomRightsSearch
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MyRoomRightsSearch sent while 'rooms.Count == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ca6db2d966f5
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_ae54a368d013
  - scenario: navigator.my_room_rights_search.success
    title: "my room rights search: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: MyRoomRightsSearch
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of MyRoomRightsSearch succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - NavigatorSearchResultBlocks
      - unresolved
      evidence: ev_0cdd228c4850
    - origin: arcturus
      authority: reference_emulator
      outcome: ignored
      emits: []
      evidence: ev_ae54a368d013
  evidence:
  - id: ev_ae54a368d013
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/navigator/MyRoomRightsSearchMessageEvent.java
    symbol: MyRoomRightsSearchMessageEvent
    line: 8
    note: third-party server implementation; evidence, not authority
  - id: ev_0cdd228c4850
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Navigator/NavigatorSearchHandlerHelper.cs
    symbol: NavigatorSearchHandlerHelper
    line: 21
  - id: ev_ca6db2d966f5
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Navigator/NavigatorService.cs
    symbol: NavigatorService
    line: 120
verified: {}
manual: {}
