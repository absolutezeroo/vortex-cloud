# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:a468aa0bb52d8e8ad1c564e1c926478f4404bb9f200c645ca267c8ee353766a0"
generated:
  feature: navigator.get_category_visitor_counts
  scenarios:
  - scenario: navigator.get_category_visitor_counts.success
    title: "get category visitor counts: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: NewNavigatorInit
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of NewNavigatorInit succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - CategoriesWithVisitorCount
      - NewNavigatorPreferences
      evidence: ev_8862c7e0fdc9
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - NewNavigatorPreferences
      - NavigatorMetaData
      - NavigatorLiftedRooms
      - CollapsedCategories
      - NavigatorSavedSearches
      - UserEventCats
      evidence: ev_b4979eab29b5
  evidence:
  - id: ev_b4979eab29b5
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/navigator/NewNavigatorInitEvent.java
    symbol: NewNavigatorInitEvent
    line: 8
    note: third-party server implementation; evidence, not authority
  - id: ev_8862c7e0fdc9
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/NewNavigator/NewNavigatorInitMessageHandler.cs
    symbol: NewNavigatorInitMessageHandler
    line: 18
verified: {}
manual: {}
