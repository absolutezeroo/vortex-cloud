# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:caea125b34c0c314610728933addcbc29e0c1988fb0150e57c6d98b348deca8e"
generated:
  feature: navigator.room_ad_event_tab_viewed
  scenarios:
  - scenario: navigator.room_ad_event_tab_viewed.success
    title: "room ad event tab viewed: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RoomAdEventTabViewed
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RoomAdEventTabViewed succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1de993c9dc75
  evidence:
  - id: ev_1de993c9dc75
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Navigator/RoomAdEventTabViewedMessageHandler.cs
    symbol: RoomAdEventTabViewedMessageHandler
    line: 11
verified: {}
manual: {}
