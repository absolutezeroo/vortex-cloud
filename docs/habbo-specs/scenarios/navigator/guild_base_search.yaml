# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:9d4b5b204abbf0cbd4af0aa2cbe94baabdb492438d1d89534d597c83a8aee6c3"
generated:
  feature: navigator.guild_base_search
  scenarios:
  - scenario: navigator.guild_base_search.guard_0
    title: "guild base search: the guard 'rooms.Count == 0' holds"
    given:
      guard: rooms.Count == 0
      guard_holds: "true"
    when:
      packet: GuildBaseSearch
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GuildBaseSearch sent while 'rooms.Count == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ca6db2d966f5
  - scenario: navigator.guild_base_search.success
    title: "guild base search: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GuildBaseSearch
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GuildBaseSearch succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - NavigatorSearchResultBlocks
      - unresolved
      evidence: ev_0cdd228c4850
  evidence:
  - id: ev_0cdd228c4850
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Navigator/NavigatorSearchHandlerHelper.cs
    symbol: NavigatorSearchHandlerHelper
    line: 21
  - id: ev_ca6db2d966f5
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Navigator/NavigatorService.cs
    symbol: NavigatorService
    line: 120
verified: {}
manual: {}
