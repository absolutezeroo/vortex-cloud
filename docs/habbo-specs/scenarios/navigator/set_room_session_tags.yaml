# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:b3a9ee6037820ce69416ca5b99cc1e5c8fcd02d5ec6af3e14ed23f2306370c9d"
generated:
  feature: navigator.set_room_session_tags
  scenarios:
  - scenario: navigator.set_room_session_tags.guard_0
    title: "set room session tags: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: SetRoomSessionTags
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SetRoomSessionTags sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1a2cbd2070a9
  - scenario: navigator.set_room_session_tags.guard_1
    title: "set room session tags: the guard '!await IsRoomOwnerAsync(actor).ConfigureAwait(true)' holds"
    given:
      guard: "!await IsRoomOwnerAsync(actor).ConfigureAwait(true)"
      guard_holds: "true"
    when:
      packet: SetRoomSessionTags
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SetRoomSessionTags sent while '!await IsRoomOwnerAsync(actor).ConfigureAwait(true)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_329537530b13
  - scenario: navigator.set_room_session_tags.guard_2
    title: "set room session tags: the guard 'entity is null' holds"
    given:
      guard: entity is null
      guard_holds: "true"
    when:
      packet: SetRoomSessionTags
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SetRoomSessionTags sent while 'entity is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_329537530b13
  - scenario: navigator.set_room_session_tags.guard_3
    title: "set room session tags: the guard 'string.IsNullOrWhiteSpace(tag)' holds"
    given:
      guard: string.IsNullOrWhiteSpace(tag)
      guard_holds: "true"
    when:
      packet: SetRoomSessionTags
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SetRoomSessionTags sent while 'string.IsNullOrWhiteSpace(tag)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_329537530b13
  - scenario: navigator.set_room_session_tags.success
    title: "set room session tags: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: SetRoomSessionTags
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of SetRoomSessionTags succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1a2cbd2070a9
  evidence:
  - id: ev_1a2cbd2070a9
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Navigator/SetRoomSessionTagsMessageHandler.cs
    symbol: SetRoomSessionTagsMessageHandler
    line: 20
  - id: ev_329537530b13
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/RoomGrain.Settings.cs
    symbol: RoomGrain
    line: 554
verified: {}
manual: {}
