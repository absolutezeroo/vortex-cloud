# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:9ba6b9b53a8c7de14d35cedea2e9ef3edd8841c2b9b07d5a17137bc255c8cee8"
generated:
  feature: navigator.convert_global_room_id
  scenarios:
  - scenario: navigator.convert_global_room_id.guard_0
    title: "convert global room id: the guard 'ctx.PlayerId <= 0 || !int.TryParse(message.FlatId, out in...' holds"
    given:
      guard: ctx.PlayerId <= 0 || !int.TryParse(message.FlatId, out int roomId) || roomId <= 0
      guard_holds: "true"
    when:
      packet: ConvertGlobalRoomId
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ConvertGlobalRoomId sent while 'ctx.PlayerId <= 0 || !int.TryParse(message.FlatId, out int roomId) || roomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3a3f04f4fd85
  - scenario: navigator.convert_global_room_id.success
    title: "convert global room id: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ConvertGlobalRoomId
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ConvertGlobalRoomId succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - GetGuestRoomResult
      - unresolved
      evidence: ev_25e7df53e794
  evidence:
  - id: ev_25e7df53e794
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Social/Grains/GroupGrain.Read.cs
    symbol: GroupGrain
    line: 118
  - id: ev_3a3f04f4fd85
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Navigator/ConvertGlobalRoomIdMessageHandler.cs
    symbol: ConvertGlobalRoomIdMessageHandler
    line: 26
verified: {}
manual: {}
