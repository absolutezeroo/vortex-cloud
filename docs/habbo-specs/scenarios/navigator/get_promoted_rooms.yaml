# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:9118e41e1e6a131379bcc7dad71211728b814627c5dfdf1454c5d85fb361483c"
generated:
  feature: navigator.get_promoted_rooms
  scenarios:
  - scenario: navigator.get_promoted_rooms.guard_0
    title: "get promoted rooms: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: ForwardToARandomPromotedRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ForwardToARandomPromotedRoom sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_5ef8d489ea02
  - scenario: navigator.get_promoted_rooms.guard_1
    title: "get promoted rooms: the guard 'promoted.Count == 0' holds"
    given:
      guard: promoted.Count == 0
      guard_holds: "true"
    when:
      packet: ForwardToARandomPromotedRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ForwardToARandomPromotedRoom sent while 'promoted.Count == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_5ef8d489ea02
  - scenario: navigator.get_promoted_rooms.guard_2
    title: "get promoted rooms: the guard 'effectIds.Count == 0' holds"
    given:
      guard: effectIds.Count == 0
      guard_holds: "true"
    when:
      packet: ForwardToARandomPromotedRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ForwardToARandomPromotedRoom sent while 'effectIds.Count == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_fe1afccfe33a
  - scenario: navigator.get_promoted_rooms.success
    title: "get promoted rooms: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ForwardToARandomPromotedRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ForwardToARandomPromotedRoom succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - GetGuestRoomResult
      - unresolved
      evidence: ev_25e7df53e794
  evidence:
  - id: ev_25e7df53e794
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Social/Grains/GroupGrain.Read.cs
    symbol: GroupGrain
    line: 118
  - id: ev_5ef8d489ea02
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Navigator/ForwardToARandomPromotedRoomMessageHandler.cs
    symbol: ForwardToARandomPromotedRoomMessageHandler
    line: 25
  - id: ev_fe1afccfe33a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Wired/WiredUnseenCycle.cs
    symbol: WiredUnseenCycle
    line: 23
verified: {}
manual: {}
