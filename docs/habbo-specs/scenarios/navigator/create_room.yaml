# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:2ca38b86dff99b53a5183b34e4c99cfb237aadf29ec3474bef6d58224b3de113"
generated:
  feature: navigator.create_room
  scenarios:
  - scenario: navigator.create_room.success
    title: "create room: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: CreateFlat
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of CreateFlat succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - FlatCreated
      - RoomForward
      evidence: ev_2fda1ce6d091
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - CanCreateRoom
      - FlatCreated
      evidence: ev_ad5fd845af29
  evidence:
  - id: ev_ad5fd845af29
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/navigator/CreateFlatMessageEvent.java
    symbol: CreateFlatMessageEvent
    line: 18
    note: third-party server implementation; evidence, not authority
  - id: ev_2fda1ce6d091
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Navigator/CreateFlatMessageHandler.cs
    symbol: CreateFlatMessageHandler
    line: 11
verified: {}
manual: {}
