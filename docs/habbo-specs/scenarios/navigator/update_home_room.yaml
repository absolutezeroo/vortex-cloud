# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:805ecac8ec85ddd0ce12b83b68a3d33befa5e658de18af2879c9f37cee4b15a3"
generated:
  feature: navigator.update_home_room
  scenarios:
  - scenario: navigator.update_home_room.guard_0
    title: "update home room: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: UpdateHomeRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of UpdateHomeRoom sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_13f104799f2b
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - NavigatorSettings
      evidence: ev_ae62feb93e3c
  - scenario: navigator.update_home_room.success
    title: "update home room: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: UpdateHomeRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of UpdateHomeRoom succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_13f104799f2b
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - NavigatorSettings
      evidence: ev_ae62feb93e3c
  evidence:
  - id: ev_ae62feb93e3c
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/rooms/UpdateHomeRoomMessageEvent.java
    symbol: UpdateHomeRoomMessageEvent
    line: 8
    note: third-party server implementation; evidence, not authority
  - id: ev_13f104799f2b
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Navigator/UpdateHomeRoomMessageHandler.cs
    symbol: UpdateHomeRoomMessageHandler
    line: 19
verified: {}
manual: {}
