# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:509b24eb392cccab9bccfccd4aaa49bb2817f13bcc7c4c1f69d1ebcdf5638e17"
generated:
  feature: nux.create_room
  scenarios:
  - scenario: nux.create_room.guard_0
    title: "create room: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: SelectInitialRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SelectInitialRoom sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_aa7fca1e0066
  - scenario: nux.create_room.guard_1
    title: "create room: the guard '_idToName.TryGetValue(playerId, out string? x)' holds"
    given:
      guard: _idToName.TryGetValue(playerId, out string? x)
      guard_holds: "true"
    when:
      packet: SelectInitialRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SelectInitialRoom sent while '_idToName.TryGetValue(playerId, out string? x)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7f41f2bdde62
  - scenario: nux.create_room.guard_2
    title: "create room: the guard 'string.IsNullOrWhiteSpace(dbName)' holds"
    given:
      guard: string.IsNullOrWhiteSpace(dbName)
      guard_holds: "true"
    when:
      packet: SelectInitialRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SelectInitialRoom sent while 'string.IsNullOrWhiteSpace(dbName)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7f41f2bdde62
  - scenario: nux.create_room.guard_3
    title: "create room: the guard '_registry is null' holds"
    given:
      guard: _registry is null
      guard_holds: "true"
    when:
      packet: SelectInitialRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of SelectInitialRoom sent while '_registry is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_34bf3ac088e2
  - scenario: nux.create_room.success
    title: "create room: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: SelectInitialRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of SelectInitialRoom succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - SelectInitialRoomEvent
      evidence: ev_34bf3ac088e2
  evidence:
  - id: ev_34bf3ac088e2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Events/EventSystem.cs
    symbol: EventSystem
    line: 16
  - id: ev_7f41f2bdde62
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerDirectoryGrain.cs
    symbol: PlayerDirectoryGrain
    line: 31
  - id: ev_aa7fca1e0066
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Nux/SelectInitialRoomMessageHandler.cs
    symbol: SelectInitialRoomMessageHandler
    line: 49
verified: {}
manual: {}
