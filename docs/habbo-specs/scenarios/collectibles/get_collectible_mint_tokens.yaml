# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:7098fa41897b8b0b339034529fd503c6d312fde5391e15b853f003c461b6c9b2"
generated:
  feature: collectibles.get_collectible_mint_tokens
  scenarios:
  - scenario: collectibles.get_collectible_mint_tokens.guard_0
    title: "get collectible mint tokens: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetCollectibleMintTokens
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetCollectibleMintTokens sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1188c2e2ba2a
  - scenario: collectibles.get_collectible_mint_tokens.success
    title: "get collectible mint tokens: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetCollectibleMintTokens
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetCollectibleMintTokens succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - CollectibleMintTokenCount
      evidence: ev_1188c2e2ba2a
  evidence:
  - id: ev_1188c2e2ba2a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Collectibles/GetCollectibleMintTokensMessageHandler.cs
    symbol: GetCollectibleMintTokensMessageHandler
    line: 30
verified: {}
manual: {}
