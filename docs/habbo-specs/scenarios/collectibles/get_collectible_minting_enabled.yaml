# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:d93f7547a3ea35e1a2b8dae7481912ba8673b769eaef76cf45a3a048ed0d8d04"
generated:
  feature: collectibles.get_collectible_minting_enabled
  scenarios:
  - scenario: collectibles.get_collectible_minting_enabled.success
    title: "get collectible minting enabled: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetCollectibleMintingEnabled
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetCollectibleMintingEnabled succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - CollectibleMintingEnabled
      evidence: ev_61b974b83353
  evidence:
  - id: ev_61b974b83353
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Collectibles/GetCollectibleMintingEnabledMessageHandler.cs
    symbol: GetCollectibleMintingEnabledMessageHandler
    line: 41
verified: {}
manual: {}
