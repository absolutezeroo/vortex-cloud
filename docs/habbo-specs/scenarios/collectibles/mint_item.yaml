# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:f3bf6d1ea54f71e27018b59c959c9f4d3345a7fe437c77d348f986442c90dfe6"
generated:
  feature: collectibles.mint_item
  scenarios:
  - scenario: collectibles.mint_item.guard_0
    title: "mint item: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: MintItem
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MintItem sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_65f8c2fb0e45
  - scenario: collectibles.mint_item.guard_1
    title: "mint item: the guard '!await minting.IsMintingEnabledAsync(ct).ConfigureAwait(t...' holds"
    given:
      guard: "!await minting.IsMintingEnabledAsync(ct).ConfigureAwait(true)"
      guard_holds: "true"
    when:
      packet: MintItem
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MintItem sent while '!await minting.IsMintingEnabledAsync(ct).ConfigureAwait(true)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a376b354a3ae
  - scenario: collectibles.mint_item.guard_2
    title: "mint item: the guard 'item is null' holds"
    given:
      guard: item is null
      guard_holds: "true"
    when:
      packet: MintItem
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MintItem sent while 'item is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a376b354a3ae
  - scenario: collectibles.mint_item.guard_3
    title: "mint item: the guard 'terms is null' holds"
    given:
      guard: terms is null
      guard_holds: "true"
    when:
      packet: MintItem
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MintItem sent while 'terms is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a376b354a3ae
  - scenario: collectibles.mint_item.guard_4
    title: "mint item: the guard 'balance < terms.StampPrice' holds"
    given:
      guard: balance < terms.StampPrice
      guard_holds: "true"
    when:
      packet: MintItem
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MintItem sent while 'balance < terms.StampPrice' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a376b354a3ae
  - scenario: collectibles.mint_item.guard_5
    title: "mint item: the guard 'terms.EditionSize > 0 && minted >= terms.EditionSize' holds"
    given:
      guard: terms.EditionSize > 0 && minted >= terms.EditionSize
      guard_holds: "true"
    when:
      packet: MintItem
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MintItem sent while 'terms.EditionSize > 0 && minted >= terms.EditionSize' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a376b354a3ae
  - scenario: collectibles.mint_item.guard_6
    title: "mint item: the guard 'deleted == 0' holds"
    given:
      guard: deleted == 0
      guard_holds: "true"
    when:
      packet: MintItem
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MintItem sent while 'deleted == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a376b354a3ae
  - scenario: collectibles.mint_item.guard_7
    title: "mint item: the guard '!string.IsNullOrEmpty(jsonData)' holds"
    given:
      guard: "!string.IsNullOrEmpty(jsonData)"
      guard_holds: "true"
    when:
      packet: MintItem
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MintItem sent while '!string.IsNullOrEmpty(jsonData)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7a660921f135
  - scenario: collectibles.mint_item.guard_8
    title: "mint item: the guard '!await _furniModule.RemoveFurnitureAsync(itemId, ct)' holds"
    given:
      guard: "!await _furniModule.RemoveFurnitureAsync(itemId, ct)"
      guard_holds: "true"
    when:
      packet: MintItem
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MintItem sent while '!await _furniModule.RemoveFurnitureAsync(itemId, ct)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_c6396662000a
  - scenario: collectibles.mint_item.guard_9
    title: "mint item: the guard '!_state.FurnitureById.Remove(itemId, out IFurnitureItem? ...' holds"
    given:
      guard: "!_state.FurnitureById.Remove(itemId, out IFurnitureItem? item)"
      guard_holds: "true"
    when:
      packet: MintItem
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of MintItem sent while '!_state.FurnitureById.Remove(itemId, out IFurnitureItem? item)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_48fa24e8d4a2
  - scenario: collectibles.mint_item.success
    title: "mint item: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: MintItem
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of MintItem succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - CollectibleMintTokenCount
      - CollectibleMintableItemResult
      - FurniListRemoveEvent
      evidence: ev_2ec03446aa2a
  evidence:
  - id: ev_2ec03446aa2a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Furniture/Providers/FurnitureDefinitionProvider.cs
    symbol: FurnitureDefinitionProvider
    line: 41
  - id: ev_48fa24e8d4a2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Inventory/Grains/Modules/InventoryFurniModule.cs
    symbol: InventoryFurniModule
    line: 61
  - id: ev_65f8c2fb0e45
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Collectibles/MintItemMessageHandler.cs
    symbol: MintItemMessageHandler
    line: 43
  - id: ev_7a660921f135
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Furniture/Providers/StuffDataFactory.cs
    symbol: StuffDataFactory
    line: 46
  - id: ev_a376b354a3ae
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Collectibles/Grains/PlayerMintGrain.cs
    symbol: PlayerMintGrain
    line: 144
  - id: ev_c6396662000a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Inventory/Grains/InventoryGrain.Furni.cs
    symbol: InventoryGrain
    line: 108
verified: {}
manual: {}
