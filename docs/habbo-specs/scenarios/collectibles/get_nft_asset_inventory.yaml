# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:7dbb2e11c0cc0d44d6b5e4cfef7732b6594d6f34fef3dd5f1fae18d324c3b41d"
generated:
  feature: collectibles.get_nft_asset_inventory
  scenarios:
  - scenario: collectibles.get_nft_asset_inventory.guard_0
    title: "get nft asset inventory: the guard 'assets.Length == 0' holds"
    given:
      guard: assets.Length == 0
      guard_holds: "true"
    when:
      packet: GetNftAssetInventory
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetNftAssetInventory sent while 'assets.Length == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a376b354a3ae
  - scenario: collectibles.get_nft_asset_inventory.guard_1
    title: "get nft asset inventory: the guard 'codes.Length == 0' holds"
    given:
      guard: codes.Length == 0
      guard_holds: "true"
    when:
      packet: GetNftAssetInventory
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetNftAssetInventory sent while 'codes.Length == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_963651d08ab1
  - scenario: collectibles.get_nft_asset_inventory.success
    title: "get nft asset inventory: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetNftAssetInventory
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetNftAssetInventory succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - TradeNftAssetInventory
      evidence: ev_3bca62ae6f6e
  evidence:
  - id: ev_3bca62ae6f6e
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Primitives/Collectibles/CollectibleProductIdentity.cs
    symbol: CollectibleProductIdentity
    line: 38
  - id: ev_963651d08ab1
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Collectibles/Grains/FurnitureDefinitionLookup.cs
    symbol: FurnitureDefinitionLookup
    line: 57
  - id: ev_a376b354a3ae
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Collectibles/Grains/PlayerMintGrain.cs
    symbol: PlayerMintGrain
    line: 334
verified: {}
manual: {}
