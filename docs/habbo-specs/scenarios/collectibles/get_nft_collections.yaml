# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:9477955df118ca25159a626315ae04615c757e9123aebf9f289f78237a63ae7d"
generated:
  feature: collectibles.get_nft_collections
  scenarios:
  - scenario: collectibles.get_nft_collections.guard_0
    title: "get nft collections: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetNftCollections
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetNftCollections sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_73561da4a983
  - scenario: collectibles.get_nft_collections.guard_1
    title: "get nft collections: the guard '_collections.Length == 0' holds"
    given:
      guard: _collections.Length == 0
      guard_holds: "true"
    when:
      packet: GetNftCollections
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetNftCollections sent while '_collections.Length == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_8e6fa88abbd1
  - scenario: collectibles.get_nft_collections.success
    title: "get nft collections: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetNftCollections
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetNftCollections succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - NftCollections
      evidence: ev_051589fc45e5
  evidence:
  - id: ev_051589fc45e5
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Collectibles/GetNftCollectionsMessageHandler.cs
    symbol: GetNftCollectionsMessageHandler
    line: 14
  - id: ev_73561da4a983
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Collectibles/GetNftCollectionsMessageHandler.cs
    symbol: GetNftCollectionsMessageHandler
    line: 23
  - id: ev_8e6fa88abbd1
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/NftCollectionsGrain.cs
    symbol: NftCollectionsGrain
    line: 57
verified: {}
manual: {}
