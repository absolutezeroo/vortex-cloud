# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:0aabfe8e64b8a41587e7e2504b1ed757d1801148b5081ec9dcb0f6facaca431e"
generated:
  feature: collectibles.purchase_mint_token
  scenarios:
  - scenario: collectibles.purchase_mint_token.guard_0
    title: "purchase mint token: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: PurchaseMintToken
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of PurchaseMintToken sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ee343dece6c3
  - scenario: collectibles.purchase_mint_token.guard_1
    title: "purchase mint token: the guard 'offer is null || offer.AmountTokens <= 0' holds"
    given:
      guard: offer is null || offer.AmountTokens <= 0
      guard_holds: "true"
    when:
      packet: PurchaseMintToken
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of PurchaseMintToken sent while 'offer is null || offer.AmountTokens <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a376b354a3ae
  - scenario: collectibles.purchase_mint_token.guard_2
    title: "purchase mint token: the guard '!result.Succeeded' holds"
    given:
      guard: "!result.Succeeded"
      guard_holds: "true"
    when:
      packet: PurchaseMintToken
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of PurchaseMintToken sent while '!result.Succeeded' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a376b354a3ae
  - scenario: collectibles.purchase_mint_token.guard_3
    title: "purchase mint token: the guard '_registry is null' holds"
    given:
      guard: _registry is null
      guard_holds: "true"
    when:
      packet: PurchaseMintToken
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of PurchaseMintToken sent while '_registry is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_34bf3ac088e2
  - scenario: collectibles.purchase_mint_token.success
    title: "purchase mint token: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: PurchaseMintToken
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of PurchaseMintToken succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - CollectibleMintTokenCount
      evidence: ev_34bf3ac088e2
  evidence:
  - id: ev_34bf3ac088e2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Events/EventSystem.cs
    symbol: EventSystem
    line: 16
  - id: ev_a376b354a3ae
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Collectibles/Grains/PlayerMintGrain.cs
    symbol: PlayerMintGrain
    line: 79
  - id: ev_ee343dece6c3
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Collectibles/PurchaseMintTokenMessageHandler.cs
    symbol: PurchaseMintTokenMessageHandler
    line: 37
verified: {}
manual: {}
