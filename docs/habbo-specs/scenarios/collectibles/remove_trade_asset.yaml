# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:c83057e903b3ecd6f2595328f049a6e7427a233f837b349acd27dc77c211fb22"
generated:
  feature: collectibles.remove_trade_asset
  scenarios:
  - scenario: collectibles.remove_trade_asset.guard_0
    title: "remove trade asset: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || assetId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || assetId <= 0
      guard_holds: "true"
    when:
      packet: RemoveNftFromTrade
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RemoveNftFromTrade sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || assetId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a8707f5e7b6c
  - scenario: collectibles.remove_trade_asset.guard_1
    title: "remove trade asset: the guard '!TryGetSession(requesterId, out RoomTradeSession? session)' holds"
    given:
      guard: "!TryGetSession(requesterId, out RoomTradeSession? session)"
      guard_holds: "true"
    when:
      packet: RemoveNftFromTrade
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RemoveNftFromTrade sent while '!TryGetSession(requesterId, out RoomTradeSession? session)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1d91c4a1b72c
  - scenario: collectibles.remove_trade_asset.guard_2
    title: "remove trade asset: the guard 'session.Phase != TradePhase.Building' holds"
    given:
      guard: session.Phase != TradePhase.Building
      guard_holds: "true"
    when:
      packet: RemoveNftFromTrade
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RemoveNftFromTrade sent while 'session.Phase != TradePhase.Building' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1d91c4a1b72c
  - scenario: collectibles.remove_trade_asset.guard_3
    title: "remove trade asset: the guard '!session.AssetsOf(requesterId).Remove(assetId)' holds"
    given:
      guard: "!session.AssetsOf(requesterId).Remove(assetId)"
      guard_holds: "true"
    when:
      packet: RemoveNftFromTrade
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RemoveNftFromTrade sent while '!session.AssetsOf(requesterId).Remove(assetId)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1d91c4a1b72c
  - scenario: collectibles.remove_trade_asset.success
    title: "remove trade asset: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RemoveNftFromTrade
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RemoveNftFromTrade succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1d91c4a1b72c
  evidence:
  - id: ev_1d91c4a1b72c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomTradingSystem.cs
    symbol: RoomTradingSystem
    line: 275
  - id: ev_a8707f5e7b6c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/RoomService.Trading.cs
    symbol: RoomService
    line: 76
verified: {}
manual: {}
