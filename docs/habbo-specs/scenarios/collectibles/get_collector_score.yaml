# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:12ea787c6646aaa39c89c10f26e6a5d58fa3d51c1e5c232b5e3ee9cf7fdc12fd"
generated:
  feature: collectibles.get_collector_score
  scenarios:
  - scenario: collectibles.get_collector_score.guard_0
    title: "get collector score: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetCollectorScore
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetCollectorScore sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_80ac316ce2d7
  - scenario: collectibles.get_collector_score.success
    title: "get collector score: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetCollectorScore
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetCollectorScore succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - NftCollectionsScore
      evidence: ev_03de12ea0513
  evidence:
  - id: ev_03de12ea0513
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Collectibles/Grains/NftCollectionsGrain.cs
    symbol: NftCollectionsGrain
    line: 67
  - id: ev_80ac316ce2d7
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Collectibles/GetCollectorScoreMessageHandler.cs
    symbol: GetCollectorScoreMessageHandler
    line: 22
verified: {}
manual: {}
