# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:b5070661eb46558743e93526205017d81bd35b4fb7db637947ab7be017213ecb"
generated:
  feature: collectibles.get_nft_claims
  scenarios:
  - scenario: collectibles.get_nft_claims.guard_0
    title: "get nft claims: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetNftClaims
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetNftClaims sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_67625979fb59
  - scenario: collectibles.get_nft_claims.guard_1
    title: "get nft claims: the guard 'claims.Length == 0' holds"
    given:
      guard: claims.Length == 0
      guard_holds: "true"
    when:
      packet: GetNftClaims
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetNftClaims sent while 'claims.Length == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_d72434a4527c
  - scenario: collectibles.get_nft_claims.success
    title: "get nft claims: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetNftClaims
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetNftClaims succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - NftClaims
      evidence: ev_67625979fb59
  evidence:
  - id: ev_67625979fb59
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Collectibles/GetNftClaimsMessageHandler.cs
    symbol: GetNftClaimsMessageHandler
    line: 34
  - id: ev_d72434a4527c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Collectibles/Grains/PlayerNftClaimsGrain.cs
    symbol: PlayerNftClaimsGrain
    line: 53
verified: {}
manual: {}
