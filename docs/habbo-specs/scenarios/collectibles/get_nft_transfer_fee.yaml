# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:ce2c91684226ca37e8c868a1a2238eb91f20c7cde44e221a26460c4849ba649e"
generated:
  feature: collectibles.get_nft_transfer_fee
  scenarios:
  - scenario: collectibles.get_nft_transfer_fee.success
    title: "get nft transfer fee: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetNftTransferFee
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetNftTransferFee succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - NftTransferFee
      evidence: ev_a8e17d490a06
  evidence:
  - id: ev_a8e17d490a06
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Collectibles/GetNftTransferFeeMessageHandler.cs
    symbol: GetNftTransferFeeMessageHandler
    line: 19
verified: {}
manual: {}
