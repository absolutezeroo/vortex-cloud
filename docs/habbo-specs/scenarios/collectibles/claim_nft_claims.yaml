# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:97d32e21f0440d793fd345cfe5966522010ded29705692a9eb51b91a3ea4b809"
generated:
  feature: collectibles.claim_nft_claims
  scenarios:
  - scenario: collectibles.claim_nft_claims.guard_0
    title: "claim nft claims: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: ClaimNftClaims
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ClaimNftClaims sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_b5ea56e36399
  - scenario: collectibles.claim_nft_claims.guard_1
    title: "claim nft claims: the guard 'claims.Count == 0' holds"
    given:
      guard: claims.Count == 0
      guard_holds: "true"
    when:
      packet: ClaimNftClaims
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ClaimNftClaims sent while 'claims.Count == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_66b61a78f518
  - scenario: collectibles.claim_nft_claims.guard_2
    title: "claim nft claims: the guard '!string.IsNullOrEmpty(jsonData)' holds"
    given:
      guard: "!string.IsNullOrEmpty(jsonData)"
      guard_holds: "true"
    when:
      packet: ClaimNftClaims
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ClaimNftClaims sent while '!string.IsNullOrEmpty(jsonData)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7a660921f135
  - scenario: collectibles.claim_nft_claims.success
    title: "claim nft claims: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ClaimNftClaims
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ClaimNftClaims succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - NftClaimResult
      evidence: ev_2ec03446aa2a
  evidence:
  - id: ev_2ec03446aa2a
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Furniture/Providers/FurnitureDefinitionProvider.cs
    symbol: FurnitureDefinitionProvider
    line: 41
  - id: ev_66b61a78f518
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerNftClaimsGrain.cs
    symbol: PlayerNftClaimsGrain
    line: 77
  - id: ev_7a660921f135
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Furniture/Providers/StuffDataFactory.cs
    symbol: StuffDataFactory
    line: 46
  - id: ev_b5ea56e36399
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Collectibles/ClaimNftClaimsMessageHandler.cs
    symbol: ClaimNftClaimsMessageHandler
    line: 35
verified: {}
manual: {}
