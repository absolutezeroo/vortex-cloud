# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:e6eff4853b3164c86c4fa75983ad78933b6fc61f75efc9c32d5967340b81a4e6"
generated:
  feature: collectibles.get_nft_store_offers
  scenarios:
  - scenario: collectibles.get_nft_store_offers.success
    title: "get nft store offers: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetNftStoreOffers
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetNftStoreOffers succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - NftStoreOffers
      evidence: ev_144151606547
  evidence:
  - id: ev_144151606547
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Collectibles/Grains/NftStoreGrain.cs
    symbol: NftStoreGrain
    line: 57
verified: {}
manual: {}
