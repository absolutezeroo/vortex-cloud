# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:4bbc4ba544182107189f71f3c43532a09ef61d7c820284b38bae9251cd6fafdb"
generated:
  feature: collectibles.add_trade_assets
  scenarios:
  - scenario: collectibles.add_trade_assets.guard_0
    title: "add trade assets: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || assetIds.Count == 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0 || assetIds.Count == 0
      guard_holds: "true"
    when:
      packet: AddNftToTrade
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AddNftToTrade sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0 || assetIds.Count == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a8707f5e7b6c
  - scenario: collectibles.add_trade_assets.guard_1
    title: "add trade assets: the guard '!TryGetSession(requesterId, out RoomTradeSession? session)' holds"
    given:
      guard: "!TryGetSession(requesterId, out RoomTradeSession? session)"
      guard_holds: "true"
    when:
      packet: AddNftToTrade
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AddNftToTrade sent while '!TryGetSession(requesterId, out RoomTradeSession? session)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1d91c4a1b72c
  - scenario: collectibles.add_trade_assets.guard_2
    title: "add trade assets: the guard 'session.Phase != TradePhase.Building' holds"
    given:
      guard: session.Phase != TradePhase.Building
      guard_holds: "true"
    when:
      packet: AddNftToTrade
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AddNftToTrade sent while 'session.Phase != TradePhase.Building' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1d91c4a1b72c
  - scenario: collectibles.add_trade_assets.guard_3
    title: "add trade assets: the guard 'assets.Length == 0' holds"
    given:
      guard: assets.Length == 0
      guard_holds: "true"
    when:
      packet: AddNftToTrade
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AddNftToTrade sent while 'assets.Length == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a376b354a3ae
  - scenario: collectibles.add_trade_assets.guard_4
    title: "add trade assets: the guard 'codes.Length == 0' holds"
    given:
      guard: codes.Length == 0
      guard_holds: "true"
    when:
      packet: AddNftToTrade
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AddNftToTrade sent while 'codes.Length == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_963651d08ab1
  - scenario: collectibles.add_trade_assets.success
    title: "add trade assets: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: AddNftToTrade
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of AddNftToTrade succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_133b02a66997
  evidence:
  - id: ev_133b02a66997
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Collectibles/AddNftToTradeMessageHandler.cs
    symbol: AddNftToTradeMessageHandler
    line: 27
  - id: ev_1d91c4a1b72c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomTradingSystem.cs
    symbol: RoomTradingSystem
    line: 197
  - id: ev_963651d08ab1
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Collectibles/Grains/FurnitureDefinitionLookup.cs
    symbol: FurnitureDefinitionLookup
    line: 57
  - id: ev_a376b354a3ae
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Collectibles/Grains/PlayerMintGrain.cs
    symbol: PlayerMintGrain
    line: 402
  - id: ev_a8707f5e7b6c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/RoomService.Trading.cs
    symbol: RoomService
    line: 33
verified: {}
manual: {}
