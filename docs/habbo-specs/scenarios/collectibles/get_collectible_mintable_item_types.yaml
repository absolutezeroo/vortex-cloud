# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:c529774eca289904de8ff24ac749e6a00ce27fead2ad84ac1d3966cb46c5b950"
generated:
  feature: collectibles.get_collectible_mintable_item_types
  scenarios:
  - scenario: collectibles.get_collectible_mintable_item_types.guard_0
    title: "get collectible mintable item types: the guard 'open.Length == 0' holds"
    given:
      guard: open.Length == 0
      guard_holds: "true"
    when:
      packet: GetCollectibleMintableItemTypes
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetCollectibleMintableItemTypes sent while 'open.Length == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_920ca1682c21
  - scenario: collectibles.get_collectible_mintable_item_types.success
    title: "get collectible mintable item types: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetCollectibleMintableItemTypes
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetCollectibleMintableItemTypes succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - CollectableMintableItemTypes
      evidence: ev_4512e162b296
  evidence:
  - id: ev_4512e162b296
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Collectibles/GetCollectibleMintableItemTypesMessageHandler.cs
    symbol: GetCollectibleMintableItemTypesMessageHandler
    line: 22
  - id: ev_920ca1682c21
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Collectibles/Grains/NftMintingGrain.cs
    symbol: NftMintingGrain
    line: 77
verified: {}
manual: {}
