# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:22cde1fc5574337fd59ddf2ad67440a09d2dcaa96618a003bbb3e54b99f3cdea"
generated:
  feature: collectibles.get_collectible_wallet_addresses
  scenarios:
  - scenario: collectibles.get_collectible_wallet_addresses.guard_0
    title: "get collectible wallet addresses: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetCollectibleWalletAddresses
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetCollectibleWalletAddresses sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_655e223fc358
  - scenario: collectibles.get_collectible_wallet_addresses.success
    title: "get collectible wallet addresses: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetCollectibleWalletAddresses
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetCollectibleWalletAddresses succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - CollectibleWalletAddresses
      evidence: ev_655e223fc358
  evidence:
  - id: ev_655e223fc358
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Collectibles/GetCollectibleWalletAddressesMessageHandler.cs
    symbol: GetCollectibleWalletAddressesMessageHandler
    line: 41
verified: {}
manual: {}
