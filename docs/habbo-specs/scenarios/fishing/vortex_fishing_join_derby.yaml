# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:6c58b091dae84110e37d14ac09950a0c391a0abe0bdbe42452865a4e6b3ca059"
generated:
  feature: fishing.vortex_fishing_join_derby
  scenarios:
  - scenario: fishing.vortex_fishing_join_derby.guard_0
    title: "vortex fishing join derby: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: VortexFishingJoinDerby
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of VortexFishingJoinDerby sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_aaa2f14dc1ac
  - scenario: fishing.vortex_fishing_join_derby.success
    title: "vortex fishing join derby: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: VortexFishingJoinDerby
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of VortexFishingJoinDerby succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_617f0a0c8320
  evidence:
  - id: ev_617f0a0c8320
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Fishing/VortexFishingJoinDerbyMessageHandler.cs
    symbol: VortexFishingJoinDerbyMessageHandler
    line: 19
  - id: ev_aaa2f14dc1ac
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Fishing/VortexFishingJoinDerbyMessageHandler.cs
    symbol: VortexFishingJoinDerbyMessageHandler
    line: 28
verified: {}
manual: {}
