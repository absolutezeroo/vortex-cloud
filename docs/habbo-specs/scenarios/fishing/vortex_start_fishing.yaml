# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:951f036661e429b065b772e55bab136debcfc74f649dd3d14f4a35818be19f0d"
generated:
  feature: fishing.vortex_start_fishing
  scenarios:
  - scenario: fishing.vortex_start_fishing.guard_0
    title: "vortex start fishing: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: VortexStartFishing
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of VortexStartFishing sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3d0315c6ef73
  - scenario: fishing.vortex_start_fishing.success
    title: "vortex start fishing: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: VortexStartFishing
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of VortexStartFishing succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3d0315c6ef73
  evidence:
  - id: ev_3d0315c6ef73
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Fishing/VortexStartFishingMessageHandler.cs
    symbol: VortexStartFishingMessageHandler
    line: 31
verified: {}
manual: {}
