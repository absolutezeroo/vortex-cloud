# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:26154251ab047d619bb0d51b6d00062469164d3193bc3b769ce2b1a50b45749f"
generated:
  feature: vault.credit_vault_status
  scenarios:
  - scenario: vault.credit_vault_status.success
    title: "credit vault status: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: CreditVaultStatus
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of CreditVaultStatus succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_27dc5b978b22
  evidence:
  - id: ev_27dc5b978b22
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Vault/CreditVaultStatusMessageHandler.cs
    symbol: CreditVaultStatusMessageHandler
    line: 8
verified: {}
manual: {}
