# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:2ac2c333285cbfa859d16635054e54d027dc40649b01d0b3d920a05f4ec3fea0"
generated:
  feature: vault.withdraw_credit_vault
  scenarios:
  - scenario: vault.withdraw_credit_vault.success
    title: "withdraw credit vault: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: WithdrawCreditVault
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of WithdrawCreditVault succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ea3aff1f5e27
  evidence:
  - id: ev_ea3aff1f5e27
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Vault/WithdrawCreditVaultMessageHandler.cs
    symbol: WithdrawCreditVaultMessageHandler
    line: 8
verified: {}
manual: {}
