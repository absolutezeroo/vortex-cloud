# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:9c47cf50a050020ebfde2d7b51cee9802d239de15fd04d10af67466b00851a3b"
generated:
  feature: vault.income_reward_claim
  scenarios:
  - scenario: vault.income_reward_claim.guard_0
    title: "income reward claim: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: IncomeRewardClaim
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of IncomeRewardClaim sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_d920210215ea
  - scenario: vault.income_reward_claim.guard_1
    title: "income reward claim: the guard 'toGrant.Count == 0' holds"
    given:
      guard: toGrant.Count == 0
      guard_holds: "true"
    when:
      packet: IncomeRewardClaim
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of IncomeRewardClaim sent while 'toGrant.Count == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_64a16635906e
  - scenario: vault.income_reward_claim.success
    title: "income reward claim: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: IncomeRewardClaim
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of IncomeRewardClaim succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - IncomeRewardClaimResponse
      evidence: ev_17ac95f54b71
  evidence:
  - id: ev_17ac95f54b71
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Vault/IncomeRewardClaimMessageHandler.cs
    symbol: IncomeRewardClaimMessageHandler
    line: 11
  - id: ev_64a16635906e
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Collectibles/Grains/PlayerVaultGrain.cs
    symbol: PlayerVaultGrain
    line: 56
  - id: ev_d920210215ea
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Vault/IncomeRewardClaimMessageHandler.cs
    symbol: IncomeRewardClaimMessageHandler
    line: 22
verified: {}
manual: {}
