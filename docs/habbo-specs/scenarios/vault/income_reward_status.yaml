# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:4e70550325272b3cb448f16d8f5b0f141646201336e0ba81bbb336e380264b23"
generated:
  feature: vault.income_reward_status
  scenarios:
  - scenario: vault.income_reward_status.guard_0
    title: "income reward status: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: IncomeRewardStatus
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of IncomeRewardStatus sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_fcb1a9001f34
  - scenario: vault.income_reward_status.success
    title: "income reward status: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: IncomeRewardStatus
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of IncomeRewardStatus succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - IncomeRewardStatus
      evidence: ev_18b2abfb072c
  evidence:
  - id: ev_18b2abfb072c
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerVaultGrain.cs
    symbol: PlayerVaultGrain
    line: 32
  - id: ev_fcb1a9001f34
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Vault/IncomeRewardStatusMessageHandler.cs
    symbol: IncomeRewardStatusMessageHandler
    line: 24
verified: {}
manual: {}
