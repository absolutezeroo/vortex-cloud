# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:12ea9cbadb95df724231519ece60da84c433efb6522b11ae21802f2d94b95e3c"
generated:
  feature: camera.request_camera_configuration
  scenarios:
  - scenario: camera.request_camera_configuration.success
    title: "request camera configuration: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RequestCameraConfiguration
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RequestCameraConfiguration succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_9eb857a07346
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - InitCamera
      evidence: ev_84bbfce0b4f8
  evidence:
  - id: ev_84bbfce0b4f8
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/camera/RequestCameraConfigurationEvent.java
    symbol: RequestCameraConfigurationEvent
    line: 9
    note: third-party server implementation; evidence, not authority
  - id: ev_9eb857a07346
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Camera/RequestCameraConfigurationMessageHandler.cs
    symbol: RequestCameraConfigurationMessageHandler
    line: 8
verified: {}
manual: {}
