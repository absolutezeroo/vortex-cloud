# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:b53c64e953113b82cb63fbf2cb0dedc03e06629679753c9df9b31d6e885a232f"
generated:
  feature: competition.get_current_timing_code
  scenarios:
  - scenario: competition.get_current_timing_code.success
    title: "get current timing code: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetCurrentTimingCode
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetCurrentTimingCode succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - CurrentTimingCode
      evidence: ev_7008cf77cd1b
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - CurrentTimingCode
      - CurrentTimingCode
      - CurrentTimingCode
      evidence: ev_a20ef386df4a
  evidence:
  - id: ev_a20ef386df4a
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/hotelview/GetCurrentTimingCodeMessageEvent.java
    symbol: GetCurrentTimingCodeMessageEvent
    line: 13
    note: third-party server implementation; evidence, not authority
  - id: ev_7008cf77cd1b
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Competition/GetCurrentTimingCodeMessageHandler.cs
    symbol: GetCurrentTimingCodeMessageHandler
    line: 18
verified: {}
manual: {}
