# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:816f1452afecb50624aa05c5542d87f5ade50905f57dceff840436cf4c4893b9"
generated:
  feature: competition.forward_to_random_competition_room
  scenarios:
  - scenario: competition.forward_to_random_competition_room.success
    title: "forward to random competition room: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ForwardToRandomCompetitionRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ForwardToRandomCompetitionRoom succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_991ce9899585
  evidence:
  - id: ev_991ce9899585
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Competition/ForwardToRandomCompetitionRoomMessageHandler.cs
    symbol: ForwardToRandomCompetitionRoomMessageHandler
    line: 8
verified: {}
manual: {}
