# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:ff6fbd9c6f9e3cbbc48637e8fc53db2b9d639cf053291d4d6b6d5e0fef3b3dcc"
generated:
  feature: competition.get_seconds_until
  scenarios:
  - scenario: competition.get_seconds_until.success
    title: "get seconds until: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetSecondsUntil
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetSecondsUntil succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_26a46a9ca486
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - SecondsUntil
      evidence: ev_3021e64545d9
  evidence:
  - id: ev_3021e64545d9
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/hotelview/GetSecondsUntilMessageEvent.java
    symbol: GetSecondsUntilMessageEvent
    line: 14
    note: third-party server implementation; evidence, not authority
  - id: ev_26a46a9ca486
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Competition/GetSecondsUntilMessageHandler.cs
    symbol: GetSecondsUntilMessageHandler
    line: 8
verified: {}
manual: {}
