# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:9510f41b97eaaa77518f9844716fd6c58a4d71087cf79377e1913baa89b271c4"
generated:
  feature: competition.forward_to_a_submittable_room
  scenarios:
  - scenario: competition.forward_to_a_submittable_room.success
    title: "forward to a submittable room: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ForwardToASubmittableRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ForwardToASubmittableRoom succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3bb0ea62b0ea
  evidence:
  - id: ev_3bb0ea62b0ea
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Competition/ForwardToASubmittableRoomMessageHandler.cs
    symbol: ForwardToASubmittableRoomMessageHandler
    line: 8
verified: {}
manual: {}
