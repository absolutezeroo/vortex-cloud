# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:4c02e1ac66d55b446036bed5f0b0bcda1a87547ddf6212f71736023ff8efa98a"
generated:
  feature: competition.room_competition_init
  scenarios:
  - scenario: competition.room_competition_init.success
    title: "room competition init: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RoomCompetitionInit
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RoomCompetitionInit succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1cdd259d9b16
  evidence:
  - id: ev_1cdd259d9b16
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Competition/RoomCompetitionInitMessageHandler.cs
    symbol: RoomCompetitionInitMessageHandler
    line: 8
verified: {}
manual: {}
