# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:b068de2b0fe720ce6a3109fb3c628680b4c807d7c056cbda814afcdd54727f6e"
generated:
  feature: competition.submit_room_to_competition
  scenarios:
  - scenario: competition.submit_room_to_competition.success
    title: "submit room to competition: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: SubmitRoomToCompetition
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of SubmitRoomToCompetition succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_e67974f2e6a8
  evidence:
  - id: ev_e67974f2e6a8
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Competition/SubmitRoomToCompetitionMessageHandler.cs
    symbol: SubmitRoomToCompetitionMessageHandler
    line: 8
verified: {}
manual: {}
