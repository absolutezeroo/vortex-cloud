# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:0f5de56980a686642adc9ae2c1737acb3ee819d59989584aee526d58348b4243"
generated:
  feature: competition.vote_for_room
  scenarios:
  - scenario: competition.vote_for_room.success
    title: "vote for room: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: VoteForRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of VoteForRoom succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_ba69fb3184cd
  evidence:
  - id: ev_ba69fb3184cd
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Competition/VoteForRoomMessageHandler.cs
    symbol: VoteForRoomMessageHandler
    line: 8
verified: {}
manual: {}
