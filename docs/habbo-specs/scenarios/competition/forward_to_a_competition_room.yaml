# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:07b7ae1b55a38051079b5662b8d06e050a6ddfbf0dda3e6425667dd89d720277"
generated:
  feature: competition.forward_to_a_competition_room
  scenarios:
  - scenario: competition.forward_to_a_competition_room.success
    title: "forward to a competition room: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ForwardToACompetitionRoom
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ForwardToACompetitionRoom succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_5448058c3ad5
  evidence:
  - id: ev_5448058c3ad5
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Competition/ForwardToACompetitionRoomMessageHandler.cs
    symbol: ForwardToACompetitionRoomMessageHandler
    line: 8
verified: {}
manual: {}
