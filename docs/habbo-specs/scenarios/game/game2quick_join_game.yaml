# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:cea7b76344020018cd993840bd4c56bf0df565cf96f4b4fdcfd9fc41136c803c"
generated:
  feature: game.game2quick_join_game
  scenarios:
  - scenario: game.game2quick_join_game.success
    title: "game2quick join game: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: Game2QuickJoinGame
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of Game2QuickJoinGame succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_0544004353fc
  evidence:
  - id: ev_0544004353fc
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Game/Directory/Game2QuickJoinGameMessageHandler.cs
    symbol: Game2QuickJoinGameMessageHandler
    line: 8
verified: {}
manual: {}
