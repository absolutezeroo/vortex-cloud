# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:fa6facb4e4463d169a884cf6d6d786b43178405f6595b2c1046c27378959b00a"
generated:
  feature: game.get_user_game_achievements
  scenarios:
  - scenario: game.get_user_game_achievements.success
    title: "get user game achievements: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetUserGameAchievements
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetUserGameAchievements succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_86a2c2819165
  evidence:
  - id: ev_86a2c2819165
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Game/Lobby/GetUserGameAchievementsMessageHandler.cs
    symbol: GetUserGameAchievementsMessageHandler
    line: 8
verified: {}
manual: {}
