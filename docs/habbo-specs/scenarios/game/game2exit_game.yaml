# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:bdaa62c910953ea8d6a44b51309d1550ec2ab61a5261177f9820d13611c01597"
generated:
  feature: game.game2exit_game
  scenarios:
  - scenario: game.game2exit_game.success
    title: "game2exit game: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: Game2ExitGame
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of Game2ExitGame succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_df175444f2a9
  evidence:
  - id: ev_df175444f2a9
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Game/Arena/Game2ExitGameMessageHandler.cs
    symbol: Game2ExitGameMessageHandler
    line: 8
verified: {}
manual: {}
