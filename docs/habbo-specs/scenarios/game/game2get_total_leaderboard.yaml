# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:ed34b78cb43d73cec424f0200ef4ddb73d12c3ef528c0537966db249f0d54321"
generated:
  feature: game.game2get_total_leaderboard
  scenarios:
  - scenario: game.game2get_total_leaderboard.success
    title: "game2get total leaderboard: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: Game2GetTotalLeaderboard
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of Game2GetTotalLeaderboard succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_d520eff95b55
  evidence:
  - id: ev_d520eff95b55
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Game/Score/Game2GetTotalLeaderboardMessageHandler.cs
    symbol: Game2GetTotalLeaderboardMessageHandler
    line: 8
verified: {}
manual: {}
