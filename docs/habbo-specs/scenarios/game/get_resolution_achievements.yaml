# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:cc522f4bfc95af6c4b45c91a85260452b97b7ae0800269056b10a920d956fd2d"
generated:
  feature: game.get_resolution_achievements
  scenarios:
  - scenario: game.get_resolution_achievements.guard_0
    title: "get resolution achievements: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetResolutionAchievements
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetResolutionAchievements sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3f015a72be01
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - AchievementResolutions
      evidence: ev_b35255e6bbbd
  - scenario: game.get_resolution_achievements.guard_1
    title: "get resolution achievements: the guard 'statue is null' holds"
    given:
      guard: statue is null
      guard_holds: "true"
    when:
      packet: GetResolutionAchievements
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetResolutionAchievements sent while 'statue is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_9b89e1ffc2b5
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - AchievementResolutions
      evidence: ev_b35255e6bbbd
  - scenario: game.get_resolution_achievements.success
    title: "get resolution achievements: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetResolutionAchievements
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetResolutionAchievements succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_3f015a72be01
    - origin: arcturus
      authority: reference_emulator
      outcome: success
      emits:
      - AchievementResolutions
      evidence: ev_b35255e6bbbd
  evidence:
  - id: ev_b35255e6bbbd
    kind: reference_handler
    authority: reference_emulator
    origin: arcturus
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/unknown/GetResolutionAchievementsMessageEvent.java
    symbol: GetResolutionAchievementsMessageEvent
    line: 8
    note: third-party server implementation; evidence, not authority
  - id: ev_3f015a72be01
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Game/Lobby/GetResolutionAchievementsMessageHandler.cs
    symbol: GetResolutionAchievementsMessageHandler
    line: 21
  - id: ev_9b89e1ffc2b5
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerAchievementResolutionGrain.cs
    symbol: PlayerAchievementResolutionGrain
    line: 62
verified: {}
manual: {}
