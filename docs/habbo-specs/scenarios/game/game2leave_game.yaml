# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:6689137f55c6d091424078f6b8e913f7471914a49e55cfe07e46b81cdc30302e"
generated:
  feature: game.game2leave_game
  scenarios:
  - scenario: game.game2leave_game.success
    title: "game2leave game: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: Game2LeaveGame
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of Game2LeaveGame succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_aa62d90ffdbc
  evidence:
  - id: ev_aa62d90ffdbc
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Game/Directory/Game2LeaveGameMessageHandler.cs
    symbol: Game2LeaveGameMessageHandler
    line: 8
verified: {}
manual: {}
