# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:99f1d5c53c9cfa79764d989b54d709b3548ad4a064cdce968bddec681ec06e60"
generated:
  feature: game.game2check_game_directory_status
  scenarios:
  - scenario: game.game2check_game_directory_status.success
    title: "game2check game directory status: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: Game2CheckGameDirectoryStatus
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of Game2CheckGameDirectoryStatus succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a81d21025069
  evidence:
  - id: ev_a81d21025069
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Game/Directory/Game2CheckGameDirectoryStatusMessageHandler.cs
    symbol: Game2CheckGameDirectoryStatusMessageHandler
    line: 8
verified: {}
manual: {}
