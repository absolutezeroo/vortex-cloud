# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:8442afc153a3089ccc924ebe4b2f08d40750f6d8a7d2ad6270ac666eec946392"
generated:
  feature: game.reset_resolution_achievement
  scenarios:
  - scenario: game.reset_resolution_achievement.guard_0
    title: "reset resolution achievement: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: ResetResolutionAchievement
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of ResetResolutionAchievement sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_8fbdf1d14c5b
  - scenario: game.reset_resolution_achievement.success
    title: "reset resolution achievement: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ResetResolutionAchievement
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ResetResolutionAchievement succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_8fbdf1d14c5b
  evidence:
  - id: ev_8fbdf1d14c5b
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Game/Lobby/ResetResolutionAchievementMessageHandler.cs
    symbol: ResetResolutionAchievementMessageHandler
    line: 21
verified: {}
manual: {}
