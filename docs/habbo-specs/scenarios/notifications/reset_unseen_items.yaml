# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:a40a115f83d09e2a31236081a0d15905da12df24eb56b6583d0716a495b489d1"
generated:
  feature: notifications.reset_unseen_items
  scenarios:
  - scenario: notifications.reset_unseen_items.success
    title: "reset unseen items: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ResetUnseenItems
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ResetUnseenItems succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_760275575a72
  evidence:
  - id: ev_760275575a72
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Notifications/ResetUnseenItemsMessageHandler.cs
    symbol: ResetUnseenItemsMessageHandler
    line: 8
verified: {}
manual: {}
