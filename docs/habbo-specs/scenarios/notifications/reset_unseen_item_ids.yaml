# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:1b52734427737c6ba481b0fff321bfaa58db90f05e03c891191324b2715f4955"
generated:
  feature: notifications.reset_unseen_item_ids
  scenarios:
  - scenario: notifications.reset_unseen_item_ids.success
    title: "reset unseen item ids: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: ResetUnseenItemIds
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of ResetUnseenItemIds succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_b7e615744b68
  evidence:
  - id: ev_b7e615744b68
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/Notifications/ResetUnseenItemIdsMessageHandler.cs
    symbol: ResetUnseenItemIdsMessageHandler
    line: 8
verified: {}
manual: {}
