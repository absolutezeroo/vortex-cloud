# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:ee416dc726486178c0dcd84ff08108d5b2574125599646fb7f5b3f710b28e1b8"
generated:
  feature: user_classification.room_users_classification
  scenarios:
  - scenario: user_classification.room_users_classification.guard_0
    title: "room users classification: the guard 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || ctx.RoomId <= 0
      guard_holds: "true"
    when:
      packet: RoomUsersClassification
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of RoomUsersClassification sent while 'ctx.PlayerId <= 0 || ctx.RoomId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_eea7d1d057e7
  - scenario: user_classification.room_users_classification.success
    title: "room users classification: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: RoomUsersClassification
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of RoomUsersClassification succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7327c7799f5f
  evidence:
  - id: ev_7327c7799f5f
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Rooms/Grains/Systems/RoomPetSystem.Placement.cs
    symbol: RoomPetSystem
    line: 250
  - id: ev_eea7d1d057e7
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserClassification/RoomUsersClassificationMessageHandler.cs
    symbol: RoomUsersClassificationMessageHandler
    line: 33
verified: {}
manual: {}
