# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:7d3e1d746e74c9658409db2d2e972b7d50a1df60d5d11c58b2e339e7c977b3e2"
generated:
  feature: user_classification.peer_users_classification
  scenarios:
  - scenario: user_classification.peer_users_classification.guard_0
    title: "peer users classification: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: PeerUsersClassification
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of PeerUsersClassification sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_f3185c1cc9e6
  - scenario: user_classification.peer_users_classification.success
    title: "peer users classification: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: PeerUsersClassification
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of PeerUsersClassification succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_63e2409dda9e
  evidence:
  - id: ev_63e2409dda9e
    kind: emulator_handler
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserClassification/PeerUsersClassificationMessageHandler.cs
    symbol: PeerUsersClassificationMessageHandler
    line: 22
  - id: ev_f3185c1cc9e6
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/UserClassification/PeerUsersClassificationMessageHandler.cs
    symbol: PeerUsersClassificationMessageHandler
    line: 35
verified: {}
manual: {}
