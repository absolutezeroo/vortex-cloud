# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:720f1462d5e0f9b4e131009bd0fafce2be9dbc8325730b62187a37b5ac09eb8d"
generated:
  feature: messenger.get_friend_requests
  scenarios:
  - scenario: messenger.get_friend_requests.guard_0
    title: "get friend requests: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetFriendRequests
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetFriendRequests sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_a523caad8115
  - scenario: messenger.get_friend_requests.success
    title: "get friend requests: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetFriendRequests
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetFriendRequests succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - FriendRequests
      evidence: ev_a523caad8115
  evidence:
  - id: ev_a523caad8115
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/FriendList/GetFriendRequestsMessageHandler.cs
    symbol: GetFriendRequestsMessageHandler
    line: 25
verified: {}
manual: {}
