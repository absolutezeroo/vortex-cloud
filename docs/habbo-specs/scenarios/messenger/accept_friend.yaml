# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:95c98518c7e50a159670015f63d6301c1a52ed6de66d9fde7da468127632b653"
generated:
  feature: messenger.accept_friend
  scenarios:
  - scenario: messenger.accept_friend.guard_0
    title: "accept friend: the guard 'ctx.PlayerId <= 0 || message.Friends.Count == 0' holds"
    given:
      guard: ctx.PlayerId <= 0 || message.Friends.Count == 0
      guard_holds: "true"
    when:
      packet: AcceptFriend
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AcceptFriend sent while 'ctx.PlayerId <= 0 || message.Friends.Count == 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1fe1271d1def
  - scenario: messenger.accept_friend.guard_1
    title: "accept friend: the guard '_registry is null' holds"
    given:
      guard: _registry is null
      guard_holds: "true"
    when:
      packet: AcceptFriend
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AcceptFriend sent while '_registry is null' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_34bf3ac088e2
  - scenario: messenger.accept_friend.guard_2
    title: "accept friend: the guard '_idToName.TryGetValue(playerId, out string? x)' holds"
    given:
      guard: _idToName.TryGetValue(playerId, out string? x)
      guard_holds: "true"
    when:
      packet: AcceptFriend
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AcceptFriend sent while '_idToName.TryGetValue(playerId, out string? x)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7f41f2bdde62
  - scenario: messenger.accept_friend.guard_3
    title: "accept friend: the guard 'string.IsNullOrWhiteSpace(dbName)' holds"
    given:
      guard: string.IsNullOrWhiteSpace(dbName)
      guard_holds: "true"
    when:
      packet: AcceptFriend
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of AcceptFriend sent while 'string.IsNullOrWhiteSpace(dbName)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7f41f2bdde62
  - scenario: messenger.accept_friend.success
    title: "accept friend: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: AcceptFriend
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of AcceptFriend succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - AcceptFriendResult
      - FriendListUpdate
      evidence: ev_09932710687b
  evidence:
  - id: ev_09932710687b
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Primitives/Orleans/GrainFactoryExtensions.cs
    symbol: GrainFactoryExtensions
    line: 223
  - id: ev_1fe1271d1def
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/FriendList/AcceptFriendMessageHandler.cs
    symbol: AcceptFriendMessageHandler
    line: 26
  - id: ev_34bf3ac088e2
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Events/EventSystem.cs
    symbol: EventSystem
    line: 16
  - id: ev_7f41f2bdde62
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerDirectoryGrain.cs
    symbol: PlayerDirectoryGrain
    line: 31
verified: {}
manual: {}
