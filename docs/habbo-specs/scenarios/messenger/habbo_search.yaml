# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:491e20ffab79db93c00a5e439f3e227972b7e05597622ebbadc237ed66206346"
generated:
  feature: messenger.habbo_search
  scenarios:
  - scenario: messenger.habbo_search.guard_0
    title: "habbo search: the guard 'ctx.PlayerId <= 0 || string.IsNullOrWhiteSpace(message.Se...' holds"
    given:
      guard: ctx.PlayerId <= 0 || string.IsNullOrWhiteSpace(message.SearchQuery)
      guard_holds: "true"
    when:
      packet: HabboSearch
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of HabboSearch sent while 'ctx.PlayerId <= 0 || string.IsNullOrWhiteSpace(message.SearchQuery)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1158ba6f71e1
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - UserSearchResult
      evidence: ev_28f454490450
  - scenario: messenger.habbo_search.guard_1
    title: "habbo search: the guard 'string.IsNullOrWhiteSpace(query)' holds"
    given:
      guard: string.IsNullOrWhiteSpace(query)
      guard_holds: "true"
    when:
      packet: HabboSearch
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of HabboSearch sent while 'string.IsNullOrWhiteSpace(query)' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_7f41f2bdde62
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - UserSearchResult
      evidence: ev_28f454490450
  - scenario: messenger.habbo_search.success
    title: "habbo search: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: HabboSearch
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of HabboSearch succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - HabboSearchResult
      evidence: ev_1158ba6f71e1
    - origin: habbo-arcturus-daybreak
      authority: reference_emulator
      outcome: success
      emits:
      - UserSearchResult
      evidence: ev_28f454490450
  evidence:
  - id: ev_28f454490450
    kind: reference_handler
    authority: reference_emulator
    origin: habbo-arcturus-daybreak
    source: ../vortex-modern-client/sources/HABBO-ARCTURUS-DAYBREAK/src/main/java/com/eu/habbo/messages/incoming/friends/HabboSearchMessageEvent.java
    symbol: HabboSearchMessageEvent
    line: 15
    note: third-party server implementation; evidence, not authority
  - id: ev_1158ba6f71e1
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/FriendList/HabboSearchMessageHandler.cs
    symbol: HabboSearchMessageHandler
    line: 29
  - id: ev_7f41f2bdde62
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Players/Grains/PlayerDirectoryGrain.cs
    symbol: PlayerDirectoryGrain
    line: 200
verified: {}
manual: {}
