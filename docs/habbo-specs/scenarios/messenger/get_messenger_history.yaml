# Generated by `habbo-spec`. Do not hand-edit anything under `generated:` — it is replaced on
# every scan, and an edit there blocks the next regeneration instead of being silently lost.
# 
# Write corrections under `verified:` (backed by evidence) or `manual:` (notes and context).
# Both are preserved verbatim across regenerations.
# 
# Existing emulator behaviour is evidence, not authority.
# Reference emulator behaviour is evidence, not authority.
# Unknown official behaviour must remain explicitly unknown.

spec: scenarios
spec_version: 1
generated_digest: "sha256:c90f8968573c1bb198398cc85495b5161917a78c035d64c77ca224b087d956d2"
generated:
  feature: messenger.get_messenger_history
  scenarios:
  - scenario: messenger.get_messenger_history.guard_0
    title: "get messenger history: the guard 'ctx.PlayerId <= 0' holds"
    given:
      guard: ctx.PlayerId <= 0
      guard_holds: "true"
    when:
      packet: GetMessengerHistory
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: "an official capture of GetMessengerHistory sent while 'ctx.PlayerId <= 0' holds: does the server reply, stay silent, or disconnect?"
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: ignored
      emits: []
      evidence: ev_1ba6b8990a1e
  - scenario: messenger.get_messenger_history.success
    title: "get messenger history: the request is accepted"
    given:
      preconditions: all guards satisfied
    when:
      packet: GetMessengerHistory
    expected:
      status: unknown
      confidence: unknown
    needs_evidence: an official capture of GetMessengerHistory succeeding, with every packet the server sends back and in what order
    executable: false
    claims:
    - origin: vortex
      authority: vortex_emulator
      outcome: success
      emits:
      - ConsoleMessageHistory
      evidence: ev_09932710687b
  evidence:
  - id: ev_09932710687b
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.Primitives/Orleans/GrainFactoryExtensions.cs
    symbol: GrainFactoryExtensions
    line: 217
  - id: ev_1ba6b8990a1e
    kind: emulator_flow
    authority: vortex_emulator
    origin: vortex
    source: Vortex.PacketHandlers/FriendList/GetMessengerHistoryMessageHandler.cs
    symbol: GetMessengerHistoryMessageHandler
    line: 26
verified: {}
manual: {}
