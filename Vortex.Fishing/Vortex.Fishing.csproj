<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <!--
      Fishing: the definition tables, the sessions, and the progression behind them.

      Reconstructed from Habbo Hotel: Origins, which has no client dump available. The behaviour
      here comes from community documentation, not from a captured protocol — see the client's
      docs/vortex-original/fishing.md, which states the evidence level for every part of it.

      Its own project rather than a corner of Vortex.Progression: fishing is a room activity with a
      session lifecycle, not a counter, and the two share nothing but the word "XP".
    -->
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Hosting" />
    <PackageReference Include="Microsoft.Orleans.Sdk" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\Vortex.Database\Vortex.Database.csproj" />
    <ProjectReference Include="..\Vortex.Events\Vortex.Events.csproj" />
    <ProjectReference Include="..\Vortex.Logging\Vortex.Logging.csproj" />
    <ProjectReference Include="..\Vortex.Primitives\Vortex.Primitives.csproj" />
    <ProjectReference Include="..\Vortex.Protocol\Vortex.Protocol.csproj" />
  </ItemGroup>
</Project>
