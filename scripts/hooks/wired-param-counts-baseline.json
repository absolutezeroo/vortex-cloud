{
  "known": [
    "addon/wf_xtra_mov_physics:client=4:vortex=0",
    "selector/wf_slc_remote:client=2:vortex=0",
    "trigger/wf_trg_user_performs_action:client=1:vortex=0"
  ],
  "notes": {
    "_": "Every entry here is an OPEN BUG, not accepted debt: a box whose entire configuration update the server refuses, silently, on every save. The baseline exists so a NEW one blocks -- it is not permission to leave these.",
    "addon/wf_xtra_mov_physics": "Client MovePhysics.ts sends four booleans (keep altitude, move through furni, move through users, block by furni); the box declares no rules. It also has no behaviour at all beyond WiredCode, so fixing the count alone changes nothing a player can see.",
    "selector/wf_slc_remote": "Client RemoteSelector.ts sends [type, count]; the box declares no rules and has no behaviour beyond WiredCode.",
    "trigger/wf_trg_user_performs_action": "Client UserPerformsAction.ts sends one int, the action code; the box declares no rules.",
    "_fixed": "wf_act_freeze_habbo (client=2 vortex=0) and wf_slc_users_with_var (client=5 vortex=1) were fixed rather than baselined. The three left all need behaviour, not just rules: declaring them alone would change nothing a player can see."
  }
}
