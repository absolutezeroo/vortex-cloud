{
  "known": [
    "wf_cnd_actor_in_group:GetIntParamRules",
    "wf_cnd_habbo_owns_badge:GetIntParamRules",
    "wf_cnd_match_snapshot_new:Evaluate",
    "wf_cnd_stuff_is:GetIntParamRules",
    "wf_cnd_trggrer_on_frn:GetIntParamRules",
    "wf_slc_remote:SelectAsync",
    "wf_trg_transaction_complete:typeof",
    "wf_trg_user_performs_action:CanTriggerAsync,typeof"
  ],
  "notes": {
    "_": "An outlier here is a SUGGESTION, not a defect. Each entry must say why the box differs — and two of them say 'this is a bug', which is the point of writing them down rather than accepting them silently.",
    "wf_cnd_actor_in_group:GetIntParamRules": "Correct. The client's ActorIsGroupMember inherits DefaultElement.readIntParamsFromForm, which returns [] — it sends no ints at all. check-wired-param-counts confirms it at client=0 vortex=0.",
    "wf_cnd_habbo_owns_badge:GetIntParamRules": "Correct, same reason: ActorIsWearingBadge sends no ints (the badge code travels in the string param).",
    "wf_cnd_stuff_is:GetIntParamRules": "Correct, same reason: StuffTypeMatches sends no ints.",
    "wf_cnd_trggrer_on_frn:GetIntParamRules": "Correct, same reason: TriggererIsOnFurni sends no ints.",
    "wf_cnd_match_snapshot_new:Evaluate": "Deliberately incomplete, and the class says so: 'the comparison itself needs the furni-snapshot subsystem, which does not exist yet, so the condition currently never passes'. The rules are declared so the box can at least be saved. This is honest inertness — leave it until the subsystem lands.",
    "wf_trg_transaction_complete:typeof": "The family declares its event type as an expression-bodied member; this one does not. Worth a look, not yet examined.",
    "wf_slc_remote:SelectAsync": "OPEN BUG, not accepted debt. Eighteen selectors implement SelectAsync; this one does not, so it selects nothing. It is also one of the five boxes whose param count disagrees with the client (SEC/wired: client=2 vortex=0), meaning it cannot even be configured. Found here WITHOUT the client, which is what this check is for.",
    "wf_trg_user_performs_action:CanTriggerAsync,typeof": "OPEN BUG, not accepted debt. Deliberately inert on a premise that is false: its comment claims the client ships no configuration class for code 16, but triggerconfs/UserPerformsAction.ts exists and sends one int. Its condition and selector twins are both implemented. See docs/audits/wired-silent-gaps-audit.md §5.3."
  }
}
